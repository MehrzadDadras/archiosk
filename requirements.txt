Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.32
gunicorn==22.0.0
python-dotenv==1.0.1
anthropic==0.34.2
httpx==0.27.2
python-docx==1.1.2
pypdf==4.3.1
openpyxl==3.1.5
Werkzeug==3.0.3
Pillow==12.3.0
Flask-Limiter==4.1.1
Flask-WTF==1.3.0
Flask-Migrate==4.1.0
# CLAUDE-GO-DOCUMENT-EXPORT-01: composes NEW PDFs. pypdf (above) reads and
# manipulates existing ones and cannot author a laid-out report. Checked with
# tools/dependency_fit.py before proposal - PASS on all six constraints.
reportlab==4.2.5
# CLAUDE-DRAWING-REFS-01: reads a PDF drawing as vector geometry plus
# positioned text spans, which is what binding a tag on a plan to a row in a
# schedule needs. pypdf (above) extracts text but not the coordinates, and the
# coordinates are the whole point. Imported as `pymupdf`, not the deprecated
# `fitz` alias. Checked with tools/dependency_fit.py - PASS on all six
# constraints.
pymupdf==1.28.2

# CLAUDE-QR-PASS-01: pure-Python QR encoder for Building Box field passes.
# Zero dependencies of its own and makes NO network call - the QR payload is a
# live bearer token, so rendering it locally is a security property, not a
# preference. dependency_fit.py returns PASS.
# NOTE FOR DEPLOYS: this is a NEW pin. deploy/DEPLOYMENT.md step 7 applies -
# install it explicitly as the venv owner before restarting, or the app will
# fail to import services/qr_pass.py.
segno==1.6.6

# CLAUDE-GEMINI-VISION-01: the SECOND external AI provider, for the
# governed sheet-vision path (services/sheet_vision.py). Product Owner
# authorized 2026-08-29 - transmitting rendered drawing content to
# Google - which is what lifts services/drawing_intake.py's own
# 2026-08-04 recorded block on external-AI vision over uploaded
# drawings.
#
# tools/dependency_fit.py: PASS on five constraints, WARN on one.
# Run honestly WITH --cloud-only, since Gemini is a hosted service:
#     python tools/dependency_fit.py --name "google-genai" --cloud-only
# -> WARN no-new-cloud-dependency, whose own text asks for "the same
# kind of graceful-degradation story, not just an API key check."
# That story, concretely: services/sheet_vision.py ALWAYS completes its
# local PyMuPDF vector/text extraction first and returns it whether or
# not this package is installed, whether or not GEMINI_API_KEY is set,
# and whether or not the gate allows transmission. An absent
# google-genai is an honest skipped_reason on an otherwise-successful
# local read - never an import error, never a failed sheet read. The
# app runs fully offline without this, which is the condition the rule
# actually protects.
#
# NOTE FOR DEPLOYS: a NEW pin. deploy/DEPLOYMENT.md step 7 applies -
# install it explicitly as the venv owner before restarting.
# Pinned at the current release (2.20.0), verified against the index
# rather than assumed. The SDK surface this repo uses - genai.Client,
# client.models.generate_content, types.GenerateContentConfig,
# types.Part.from_bytes, types.HttpOptions - is the documented public
# interface and is unchanged across the 1.x/2.x boundary.
google-genai==2.20.0
