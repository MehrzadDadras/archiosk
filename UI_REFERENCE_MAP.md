# UI Reference Map

**CLAUDE-P40-VW7A**, updated by **CLAUDE-P40-VW7B** and **CLAUDE-P40-VW9**. A stable-ID
registry over the application's Menu, Lists, Display, Toolbox, and
Chat surfaces — the traceability layer this and future stages use to
know what a control currently is, means, and does before renaming,
reparenting, or retiring it. This document is a durable record,
updated alongside the code, the same way `MANIFEST.md` and
`CONTINUATION_CHECKPOINT.md` are.

**VW7B in one line:** the active-Display-targeting mechanism (until
now, Documents only — a real file, embedded via a plain `<iframe src=
file_url>`/`<img>`) was generalized to also cover Investigations and
Overview, via a new `?panel=1` query flag on the existing
`workspace.show_workspace` route (no new route) and
`templates/panel_shell.html` (a minimal standalone document
`case_workspace.html` extends instead of `base.html` when that flag is
set) — see the Display section below and routes/workspace.py's own
`panel_only` comment. `lists.project.tools.data-management` was
retired and relocated (see "Retired references") after VW7B's own
inspection found it resets **every** Project in the deployment, not
the active one — nesting it under one Project's own tools misrepresented
its real scope.

**VW9 in one line:** Files became the second real entry in the
`STABLE_DIRECTORY_KINDS`/`PANEL_KINDS` stable-surface extension point
VW8-QA1 built — one new Lists leaf (`lists.project.files`) and one new
Display surface (`display.files`, the two governed sibling roots below
it) rendered through the exact same registry-driven mechanism Overview
already used, proving that extension point genuinely generalizes rather
than being a comment promising it would. No existing reference was
retired or reparented by this stage; every new reference is additive.
A real, latent bug in the multi-Display click-interceptor (`templates/
base.html`) was found and fixed during this stage's own real-browser
verification: it used to hardcode `kind='overview'` for ANY `data-view`
link, correct only by coincidence when Overview was the only one that
existed — fixed to read the attribute's own value.

## What this is, and isn't

VW7A was purely additive and instrumentation-only: every control
already existed before it; nothing was renamed, moved, or behaviorally
changed to build this registry — `git diff` against the VW7A commit
shows only `data-ui-ref="..."` insertions (plus the new UI Reference Mode
toggle itself and its CSS/JS).

VW7B is a real (if bounded) behavior/structure stage, exactly what
this registry exists to make traceable: it generalized the active-
Display-targeting mechanism to Investigations/Overview, relocated one
misplaced admin control, and added coherent empty states to three
families that were missing them. Every row below states current
behavior as of VW7B, not VW7A — where VW7B changed something, the row
says so; where it didn't, the row is unchanged from VW7A.

**Turn on UI Reference Mode** (Account menu, top-right, "UI Reference
Mode" checkbox) to see every instrumented control's own `data-ui-ref`
value rendered as a small badge directly on the page — the fastest way
to cross-check this document against the live app. Off by default; a
reviewer/device preference (`localStorage`), never a Project record.

## ID scheme

Dot-separated, lowercase, kebab-case-within-segment, hierarchical:
`<surface>.<family>[.<subfamily>][.<kind>]`.

Surfaces: `menu` (top bar), `lists` (left panel), `display` (main
projection area), `toolbox` (right contextual panel), `chat` (bottom
conversation dock), `shell` (structural chrome — panel dividers).

**A `data-ui-ref` value identifies a KIND of control, not one instance.**
For a repeating pattern (a Document leaf, a Task row, a Project row),
every rendered instance shares the same `data-ui-ref` — the existing
per-instance attributes (`data-source-id`, `data-task-id`,
`data-tag-occurrence-id`, the leaf's own `href`) still disambiguate
which one, exactly as they did before this stage. This is a query
selector convention, not an HTML `id` — uniqueness is not implied or
required, and none of the code that reads `data-ui-ref` (only the CSS
reference-mode badge and this stage's own consistency tests) assumes
otherwise.

## Status column

- **active** — currently rendered, in real use.
- **retired** — a control this ID used to name was removed; the ID is
  never reused for something else (see "Retired references" below).

## Developer Mode surfaces

| Reference | Element | Label/summary | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `developer.home.composer` | `<section>` | Developer Composer | Application-level Developer Mode Composer workbench | Developer Mode | active |
| `developer.home.composer.form` | `<form>` | Developer Composer input | Canonical application Composer submission form | Developer Mode | active |
| `developer.home.composer.voice` | `<button>` | microphone | Browser voice-input control for the Developer Composer | Developer Mode | active |
| `developer.home.composer.voice.status` | `<span>` | voice status | Accessible voice-input status output | Developer Mode | active |
| `developer.home.composer.send` | `<button>` | Send | Submits the Developer Composer message | Developer Mode | active |
| `developer.home.messages` | message list | conversation | Current Developer conversation messages | Developer Mode | active |
| `developer.chat.history` | history list | Developer conversations | Compact persisted session-scoped Developer chat history | Developer Mode | active |
| `developer.ccn.active` | `<span>` | CCN | Active contemplated-change indicator | Developer Mode + active CCN | active |
| `developer.home.selection` | `<p>` | Selected application object | Displays the current application-level context selection | Developer Mode | active |
| `developer.home.application-context` | `<div>` | Application context | Holds bounded application-context attachment controls | Developer Mode | active |
| `developer.home.context-actions` | forms | context actions | Attaches an application surface as conversational context | Developer Mode | active |
| `developer.home.environment-context` | `<div>` | environment context | Displays the current application environment context | Developer Mode | active |
| `developer.template.identity` | `<span>/<button>` | Template identity | UI Reveal identity for the governed page template | Developer Mode + UI Reveal | active |
| `menu.archiosk.admin.ui-reveal-toggle` | `<button>` | UI Reveal | Toggles developer template identity visibility | Admin only | active |
| `menu.archiosk.admin.developer-tools` | `<a>` | Developer Tools | Opens protected Developer Tools | Admin only | active |

---

## Menu (top bar — `templates/base.html`, present on every authenticated page)

| Reference | Element | Label/summary | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `menu.brand` | — | — | **Retired, CLAUDE-APP-MENU-01** — the icon+enlarged-wordmark single-link treatment (CLAUDE-P40-BRAND1) is replaced by `menu.archiosk`, a plain menu entry styled identically to its File/Edit/View/... neighbors (Product Owner, explicit: "not a separate logo"); the home-navigation behavior itself survives unchanged as `menu.archiosk.home`, below. Nothing renders this ref any more | — | retired |
| `menu.context` | `<span>` | breadcrumb (Project / Investigation / Document / Overview) | Non-interactive container; its own first segment (`menu.context.switch-project`, below) is now interactive | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.context.return-to-conversation` (new, CLAUDE-GO-NAVIGATION-CONTEXT-GAMES-01 pilot) | `<a>` | "← Return to conversation" | A quiet, restrained link back to the Composer message a hotlink click originated from (`#message-<id>`, the same already-existing conversation-message DOM id, pure client-side fragment scroll, no new route). Only rendered when the current page's own `?origin_message_id=` carries a value — `app.py`'s `render_conversation_hotlinks` now appends this to every Source hotlink it builds. A soft display hint, never validated against the conversation server-side (same "never an authorization boundary of its own" treatment `menu.context.switch-project`'s own `?current=` and `?preview_finding_id=` already get) — a stale/foreign id just fails to scroll, the same honest degradation a hotlink already exhibits when its own cited Source no longer resolves. Bounded, reversible pilot — see `governance/specified-unbuilt/navigation-context-operational-map.md`'s own "Active pilot" section | Only rendered when `project_id`/`workspace` are defined and `?origin_message_id=` is present | active |
| `menu.context.switch-project` | `<a>` (new, CLAUDE-P40-VW7B) | the Foreground Project's own display name | Section 5's own required "deliberate access to Switch Project" — navigates to `portal.choose_project?current=<project_id>` (the Project Vestibule, extended for this stage). `aria-label`/`title` carry the real purpose ("Switch Project") since the visible text alone (just the Project's name) would not otherwise communicate that activating it navigates away. A real, already-authorized navigation — no interruption dialog (per-Project workspace state is already safely persisted; see the template's own note on the now-retired `lists.project-switch-dialog`, below) | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.context.side-indicator` (new, CLAUDE-RFP-BOUNDARY-01) | `<span>` | `operating_environment_label` (+ `lifecycle_stage_label` when set) | A restrained, persistent workspace-side indicator — "Client / Owner" or "Design-Builder / Proponent" — plain, no click handler, no href (Section 11's own "not a casual mode-switch button"). State-aware `title`/`aria-label` (`side_indicator_tooltip`, `routes/workspace.py`) explain the operational boundary, not just repeat the label | Only rendered when `operating_environment` is set (legacy/unclassified projects show nothing) | active |
| `menu.bar` (new, CLAUDE-APP-MENU-01) | `<nav>` | — | The application menu bar itself — Archiosk \| File \| Edit \| View \| Document \| Tools \| Window \| Help — very top-left, above the working surfaces. Every command inside either navigates to a real existing route or clicks/mirrors an existing control by id (`data-reuse-control`) or calls a small named action (`data-action`, `static/js/app_menu.js`) — never a second implementation of a command that already exists elsewhere | Every authenticated page | active |
| `menu.mobile-nav-toggle` (CLAUDE-MOBILE-FRAME-02, new) | `<button aria-expanded>` | ☰ | Reveals `menu.bar` as a left drawer below 640px, where its eight top-level menus otherwise wrapped into two or three full-width rows of competing labels and ate the workspace (the Product Owner's own iPhone screenshot). The menus themselves are untouched — no duplicate mobile menu, no second implementation of any command. Deliberately a plain `html.mobile-nav-open` class toggle rather than a `<details>` wrapper: a closed `<details>` would need JS to force it OPEN for desktop, so a JS failure would cost desktop its whole menu bar; this way the menubar is visible by default at every width CSS calls wide and JS is only ever needed to REVEAL something on a phone. Hidden at 641px and above | Every authenticated page | active |
| `menu.archiosk.mark` (new, CLAUDE-ARCHIOSK-IDENTITY-ACTIVITY-INDICATOR-01) | `<span class="workspace-app-mark">` | — | The stationary application identity mark — `macros.archiosk_mark()` reused verbatim (the one and only mark this repository has ever had), sits immediately before `menu.bar`'s own `<nav>`, restrained (`--text-metadata`, not the retired `--brand-gold` treatment). Purely decorative (`aria-hidden="true"`) — never itself a menu item, never a second tab stop; home navigation is `menu.archiosk.home`, below, unchanged | Every authenticated page | active |
| `menu.archiosk.activity` (new, CLAUDE-ARCHIOSK-IDENTITY-ACTIVITY-INDICATOR-01) | `<span role="status">`, id `workspace-app-activity` | — (three decorative dots) | The canonical GO/system working indicator — hidden by default (idle is quiet), revealed and animated (`.working` class, `static/js/case_workspace.js`'s own Composer submit handler — the SAME real signal that already sets `chat.composer.execution-status`'s text, never a second "GO is working" mechanism) only while a real Composer request is in flight. Self-clearing on the next page load, same discipline as that status span — no persisted state, no client-side timer. `title`/`aria-label` read "GO idle"/"GO working" truthfully; no click handler (no truthful high-level work-status surface exists yet for one to open) | Every authenticated page | active |
| `menu.archiosk` (new, CLAUDE-APP-MENU-01) | `<details>` popup | "Archiosk" | The application's own first menu entry — plain `<summary class="workspace-topbar-btn">`, styled identically to its File/Edit/View/... neighbors (Product Owner, explicit: "not a separate logo"). Retires CLAUDE-P40-BRAND1's icon+enlarged-wordmark single-link treatment for this authenticated topbar specifically (`gateway_shell.html`'s own separate pre-authentication brand treatment is untouched) — see `test_p40brand1_brand_mark.py`'s own `HeaderMarkupTests` docstring for the full record | Every authenticated page | active |
| `menu.archiosk.home` (new, CLAUDE-APP-MENU-01) | `<a>` | "Archiosk Home" | The former `menu.brand` link, relocated — same `href`/`aria-label`, unchanged behavior, now the first item inside the Archiosk panel rather than the clickable summary itself | Every authenticated page | active |
| `menu.archiosk.about` (new, CLAUDE-APP-MENU-01) | `<a>` | "About" | Navigates to the new `portal.about` route — honest, static facts already shown elsewhere (`gateway.html`'s own footer: "Flat-JSON registry" / "Static build v…"), no fabricated version/build metadata | Every authenticated page | active |
| `menu.archiosk.admin` (relocated, CLAUDE-APP-MENU-01 — was menu.account.admin) | `<details>` submenu | "Admin" | New Project/Security/Operations/Project Data Management — same routes, same `is_admin` gate, unchanged; only WHERE these render changed (consolidated under Archiosk per this stage's own explicit instruction, out of the Account menu, which now holds only session-identity actions) | **Admin only** — `is_admin` | active |
| `menu.archiosk.admin.new-project`, `menu.archiosk.admin.security`, `menu.archiosk.admin.operations`, `menu.archiosk.admin.project-data-management` (relocated, CLAUDE-APP-MENU-01 — were `menu.account.admin.*`) | `<a>` (4 distinct items) | "+ New Project" / "Security" / "Operations" / "Project Data Management" | Identical targets/behavior to the retired `menu.account.admin.*` rows below — a physical relocation only | **Admin only** — `is_admin` | active |
| `menu.archiosk.admin.developer-mode-toggle` (new, CLAUDE-DEVELOPER-MODE-COCKPIT-01 Addendum E) | `<button>` (form submit) | "Enter Developer Mode" / "Exit Developer Mode" (label states current state) | Real form-POST to `portal.toggle_developer_mode` (`admin_required`) — a plain reviewer-session flag (`session['developer_mode']`), never project-scoped, never a client-only/localStorage toggle a non-admin could spoof. Same Admin group, same `is_admin` gate as every sibling item — no separate authorization surface | **Admin only** — `is_admin` | active |
| `menu.developer-mode-badge` (new, CLAUDE-DEVELOPER-MODE-COCKPIT-01 Addendum E) | `<span>` | "Developer Mode" | Persistent, unmistakable indicator shown on every authenticated page (not only inside an open Project — deliberately outside the project-context breadcrumb, so it never implies Client/Owner/Proponent/project authority) whenever `developer_mode` is true. Plain, no click handler — same "operational boundary indicator, not decoration" idiom as `menu.context.side-indicator` | Every authenticated page except standalone auth pages — `is_admin and developer_mode` | active |
| `menu.verification-session-badge` (new, CLAUDE-LIVE-VERIFICATION-ACCOUNT-MECHANISM-01) | `<span>` | "Live Verification Session" | Persistent, unmistakable indicator shown on every authenticated page whenever the current session IS the dedicated ephemeral verification identity (`services.verification_access.is_verification_session()` — a fixed-username check, never client-settable). Same "operational boundary indicator, not decoration" idiom as `menu.developer-mode-badge`, deliberately a different color (`--attention-amber`, not `--machine-blue`) so the two are never mistakable for one another | Every authenticated page except standalone auth pages — `verification_session` | active |
| `menu.verification-session-end` (new, CLAUDE-LIVE-VERIFICATION-ACCOUNT-MECHANISM-01) | `<button>` (form submit) | "End" | Real form-POST to `portal.verification_access_end` — self-revoke: deletes the verification identity and logs out in one step. Scoped narrowly (403s if the current session isn't actually the verification identity), so a real admin can never accidentally delete someone else's account through this control | Inside `menu.verification-session-badge`, same gate | active |
| `menu.archiosk.exit` (new, CLAUDE-APP-MENU-01) | `<a>` | "Exit" | The closest truthful "close the application" a browser architecture supports — ends the authenticated session via the SAME `portal.logout` route `menu.account.sign-out` already uses, never a second logout implementation. `static/js/app_menu.js`'s `guardDeparture()` guards it with a native `confirm()` only when real, detectable in-progress input exists (non-empty Composer text, an open tab-rename input, a mid-stroke annotation tool) — the SAME guard function `menu.account.sign-out` now also uses (Disposition 4, below) | Every authenticated page | active |
| `menu.display-layout` (relocated into menu.archiosk, CLAUDE-APP-MENU-01) | `<details>` popup | "Display Layout" | Vertical/Horizontal steppers + Apply — sets `#display-divisions`' grid via `window.ArchioskDisplay`-adjacent client JS (`applyLayout` in `case_workspace.js`). Same ids/behavior as before this relocation — genuinely needs an open Workspace (drives THAT project's own Display grid), unlike Appearance below | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.display-layout.vertical-decrement`, `menu.display-layout.vertical-increment`, `menu.display-layout.horizontal-decrement`, `menu.display-layout.horizontal-increment` | `<button>` (4 distinct, named controls) | −/+ steppers | Adjust the PENDING Vertical/Horizontal count (not yet applied) | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.display-layout.apply` | `<button>` | "Apply" | Commits the pending Vertical × Horizontal count to `#display-divisions` | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.appearance` (relocated into menu.archiosk, CLAUDE-APP-MENU-01) | `<details>` popup | "Appearance" | ONE global **Titanium/Black/Midnight Blue/Deep Forest/Deep Ocean** radio choice (CLAUDE-APPEARANCE-SIMPLIFY-01 — retired the prior per-surface All/Menu/Lists/Display/Toolbox/Chat matrix outright: "do not allow panel-by-panel theme mixing"), `localStorage`-persisted under one key (`beehive:appearance`). **CLAUDE-APP-MENU-01 also removed its old `project_id`/`workspace` gate** — Appearance is reviewer/device presentation state only, never a Project-record write, so it never had a real reason to require an open Workspace; it is now reachable on every authenticated page, same as Archiosk itself | Every authenticated page | active |
| `menu.appearance.light`, `menu.appearance.dark`, `menu.appearance.tinted`, `menu.appearance.deep-forest`, `menu.appearance.deep-ocean` (new, CLAUDE-APPEARANCE-SIMPLIFY-01 — converged from the retired `menu.appearance.all.*` equivalents) | `<input type="radio">` (5 distinct values, one `{% for %}` loop) | "Titanium"/"Black"/"Midnight Blue"/"Deep Forest"/"Deep Ocean" | Applies that ONE mode to every surface at once (app-shell/Menu/Lists/Display/Toolbox+Eye/Chat) — the only way to change Appearance now. `dark`/`tinted` ref suffixes retained unchanged through two label revisions (Dark→Graphite→Black, Tinted→Midnight Blue); `deep-ocean` is the one genuinely new suffix this stage adds — palette traced from the landing/sign-in shell's own deep-ocean background (see `tokens.css`'s own comment) | Every authenticated page | active |
| `menu.project-context` (CLAUDE-POSTCAMEL-PROJECT-CONTEXT-01, new) | `<details>` popup | helm/steering-wheel glyph + "Project Context" | Shows the current Project Context (last entry of `project_context_entries`) or an empty state, an "Update Project Context" subsection (append-only — never overwrites), and a "History" subsection listing every prior entry. Sits in the SAME `{% if project_id is defined and workspace is defined %}` gate as `menu.display-layout`/`menu.appearance` above — reachable from every Document/Investigation/Files/Chats/Overview page, not a new conditional | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.project-context.current-text` | `<p>` | current Project Context text | Rendered only when a current entry exists | Same as above | active |
| `menu.project-context.empty` | `<p>` | "No Project Context set yet." | Empty state when `project_context_entries` is empty | Same as above | active |
| `menu.project-context.form` | `<form>` | (update form) | Posts to `workspace.add_project_context_entry_route` — appends a NEW entry, never edits/removes an existing one; open to any authenticated Project member (same authority level as `workspace.edit_operating_instructions`, not owner/admin-locked) | Same as above | active |
| `menu.project-context.textarea` | `<textarea>` | "Record the Project's current direction…" | Required field — an empty submission is rejected by `CaseWorkspaceStore.add_project_context_entry` (flashed error, no entry recorded) | Same as above | active |
| `menu.project-context.submit` | `<button>` | "Save as current direction" | Submits the form | Same as above | active |
| `menu.project-context.history` | `<ul>` | (prior entries) | Every entry except the current one, most-recent-first — rendered only when non-empty | Same as above | active |
| `menu.project-context.history.entry` | `<li>` (pattern) | prior entry text, author, role, timestamp | One per historical entry — never mutated once written | Same as above | active |
| `menu.account` | `<details>` popup | "…" (username) | Contains Sign out and Removed Projects only now — **CLAUDE-APP-MENU-01 relocated the Admin section (New Project/Security/Operations/Project Data Management) into `menu.archiosk.admin`**, out of this menu entirely; this Account menu now holds session-identity actions exclusively, not application/governance administration | Every authenticated page | active |
| `menu.account.sign-out` (CLAUDE-APPEARANCE-SIMPLIFY-01) | `<a>` | "Sign out" | Navigates to `portal.logout` (same route/security behavior as before). Still the first item in this menu. **CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01, Disposition 4**: now shares the SAME `guardDeparture()` unsaved-input `confirm()` in `static/js/app_menu.js` as `menu.archiosk.exit` — one canonical safe-departure mechanism, exposed consistently from both justified menu locations, never two logout implementations with inconsistent protection | Every authenticated page | active |
| `menu.account.admin`, `menu.account.admin.new-project`, `menu.account.admin.security`, `menu.account.admin.operations`, `menu.account.admin.project-data-management` | — | — | **Retired, CLAUDE-APP-MENU-01** — relocated to `menu.archiosk.admin`/`menu.archiosk.admin.*` (see above), same routes/gate, physical location only changed. Nothing renders these refs any more | — | retired |
| `menu.account.removed-projects` (new, CLAUDE-LEFT-RAIL-01) | `<a>` | "Removed Projects" | Navigates to `portal.removed_projects`. Retired from `lists.removed-projects` — see "Retired references" below. **Deliberately NOT under `menu.account.admin`** — `portal.removed_projects`/`restore_project_route` are `@login_required` only (P32-access-filtered, not admin-only); a separate ref namespace keeps that real authorization difference honest rather than implying admin-only merely by visual proximity | Every authenticated page (P32-filtered at the route level) | active |
| `menu.status` (CLAUDE-CA1D-INSTRUMENT-RAIL-01, new) | `<a>` | "AI calls disabled" | The one quiet global machine fact proven this tranche — links to `operations.department_home` for detail. Deliberately renders NOTHING in the ordinary case (`AI_CALLS_DISABLED` unset/false); only an admin-relevant anomaly is worth ambient visibility in the top bar | **Admin only** — `is_admin`, and only when `AI_CALLS_DISABLED` is set | active |
| `menu.file`, `menu.edit`, `menu.view`, `menu.document`, `menu.tools`, `menu.window`, `menu.help` (new, CLAUDE-APP-MENU-01) | `<details>` popup (7 distinct menus) | "File" / "Edit" / "View" / "Document" / "Tools" / "Window" / "Help" | The remaining top-level menu bar entries. "Batch" deliberately omitted from the top level per this stage's own bounded-inventive-authority judgment call — no genuine multi-document action exists on an already-registered project today (`ingest_folder_upload` is creation-time only); see the completion report's own "Inventive improvements" section | Every authenticated page | active |
| `menu.file.new-project` (new, CLAUDE-FILE-MENU-CANONICAL-COMMANDS-01) | `<a>` | "New Project…" | Navigates to `portal.upload` — the SAME route `menu.archiosk.admin.new-project` and the Gateway New Project card already use, never a second implementation. Admin only (the route itself is `@admin_required`) — a real, already-accepted duplication of global-command access (File) vs. an administrative surface (Archiosk > Admin) and an onboarding shortcut (Gateway), the same duality this session's own redundant-UI audit already found justified for the other two | Only rendered when `is_admin` | active |
| `menu.file.new-project.none` (new, CLAUDE-FILE-MENU-CANONICAL-COMMANDS-01) | `<span>` (disabled placeholder) | "New Project (Admin only)" | Honest disabled placeholder for a non-admin viewer | Rendered in place of `menu.file.new-project` for a non-admin | active |
| `menu.file.open-project` (CLAUDE-FILE-MENU-CANONICAL-COMMANDS-01; changed to a `<details>` direct chooser by CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01 Addendum G) | `<details>` submenu (was a plain `<a>` to `portal.choose_project`) | "Open Project…" | A lightweight direct chooser over the SAME already-access-scoped project list `portal.choose_project`/`menu.context.switch-project` use (app.py's context processor builds `menu_open_project_choices`/`menu_open_project_environment` once per request, never a second query) — no branding/heading/onboarding copy/Removed Projects, each row directly clickable, filtered to the caller's currently-established operating environment when one is resolved. `portal.choose_project`/`project_chooser.html` themselves are UNCHANGED and remain reachable — "Switch Project" (`menu.context.switch-project`) and a direct `/projects` visit both still use the fuller Vestibule page | Every authenticated page | active |
| `menu.file.open-project.search` (new, Addendum G) | `<input type="search">` | "Search your projects" | Plain client-side text filter (`static/js/app_menu.js`) over the already-rendered `menu.file.open-project.item` rows only — never a second fetch/authorization check. Hidden when fewer than 6 choices exist (nothing to usefully filter) | Rendered inside `menu.file.open-project` whenever at least one project choice exists | active |
| `menu.file.open-project.list` / `menu.file.open-project.item` (new, Addendum G) | `<ul>` / `<a>` (repeated) | project display name | Each row a real, directly-clickable link straight to `workspace.show_workspace` — no radio, no second submit button | Rendered inside `menu.file.open-project` whenever at least one project choice exists | active |
| `menu.file.open-project.empty` (new, Addendum G) | `<span>` (disabled placeholder) | "No projects available to open here yet." | Honest empty state — no projects accessible in the resolved operating environment (or at all) | Rendered inside `menu.file.open-project` when the (environment-filtered) choice list is empty | active |
| `menu.file.open-project.new-project` (new, Addendum G) | `<a>` | "New Project…" | Navigates to `portal.upload`, carrying the resolved environment forward — the canonical next action from a genuine zero-project empty state | Rendered alongside `menu.file.open-project.empty` only when `is_admin` | active |
| `menu.file.add-document` | `<a>` | "Add Document…" | Navigates to `portal.reset_project_data` for the OPEN Project — the SAME Project Data Management "Add Documents" form/route (`workspace.add_document_source`) reachable from `menu.archiosk.admin.project-data-management`, a second contextual entry point into that one page, not a second registration path. Adds a Source to the already-open Project — deliberately NOT `portal.upload` (that always CREATES a new Project; duplicating it here both regressed a pre-existing "no duplicate New Project entry point" rule and pointed File's own Add-Document command at the wrong action). Admin only (the route itself is) | Only rendered when `project_id`/`workspace` are defined and `is_admin` | active |
| `menu.file.add-document.none` | `<span>` (disabled placeholder, 2 distinct reasons) | "Add Document (Admin only)" / "Add Document (open a Project first)" | Honest disabled placeholder when the real action isn't available — no Project open, or authenticated but not admin | Rendered in place of `menu.file.add-document` whenever its own two conditions aren't both met | active |
| `menu.file.print` | `<button>`, `data-reuse-control="doc-print"` | "Print" | Clicks the real `#doc-print` control verbatim (`static/js/app_menu.js`) — disabled whenever that control itself is disabled/not present (no PDF Source mounted) | Every authenticated page | active |
| `menu.file.export` | `<details>` submenu | "Export" | Contains `menu.file.export.rfi` when a Project is open, else an honest disabled placeholder (`menu.file.export.none`) — never a fabricated command | Every authenticated page | active |
| `menu.file.export.row` (CLAUDE-GO-DOCUMENT-EXPORT-01, new) | `<span>` (pattern, one per content kind) | "Whole project" / "Findings" / "Requirements" / "Investigations" | One row per exportable content kind, with its three formats on it — Product Owner: "My Copilot 365 can create PDF and Excel and Word to download. Make our app to have equal capabilities." Added to the EXISTING File > Export submenu rather than a second one, alongside `menu.file.export.rfi` rather than replacing it | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.file.export.docx`, `menu.file.export.xlsx`, `menu.file.export.pdf` (CLAUDE-GO-DOCUMENT-EXPORT-01, new) | `<a>` (pattern, three per row) | "Word" / "Excel" / "PDF" | Downloads a real .docx / .xlsx / .pdf of that content kind, through `workspace.export_document` — gated on `ACTION_EXPORT` via the same `_require_export_allowed` the RFI export already uses, never a second export door beside it. The file is a VIEW of governed state: nothing is summarised, ranked or reworded on the way out, and each row carries its own recorded status because a document outlives the screen that explained it | Same as above | active |
| `menu.file.export.rfi` | `<a>` | "Export RFI" | Navigates to `workspace.export_rfi` — the SAME existing RFI-export route/mechanism (`services/rfi_export.py`), not a second exporter | Only rendered when `project_id`/`workspace` are defined | active |
| `menu.file.export.none` | `<span>` (disabled placeholder) | "No exports available here" | Rendered instead of `.rfi` when no Project is open | Only rendered when `project_id`/`workspace` are NOT defined | active |
| `menu.file.close-document` | `<button>`, `data-action="close-active-tab"` | "Close Document" | Clicks the currently-active Document tab's own real close button (`document_tabs.js`'s existing `closeTab`) — disabled when no tab is active | Every authenticated page | active |
| `menu.file.publish-rfp` | `<button>`, `data-action="open-publish-panel"` | "Publish RFP…" | Only rendered when `can_publish_procurement_package` is true (`CLIENT_OWNER` + `pre_publication`, CLAUDE-RFP-BOUNDARY-01) — opens/scrolls to the SAME canonical Toolbox "Publish Procurement Package" panel and form, never a second publication path or an auto-submit; the real explicit per-Source selection and founding-document choice still happen in that one form | Only rendered when `can_publish_procurement_package` is true | active |
| `menu.edit.undo`, `menu.edit.redo`, `menu.edit.delete` | `<button>`, `data-reuse-control="doc-annotate-undo"/"doc-annotate-redo"/"doc-annotate-delete"` | "Undo Annotation" / "Redo Annotation" / "Delete Annotation" | Click the real, pre-existing PDF-annotation Undo/Redo/Delete controls verbatim — disabled whenever those controls are (no annotation history, nothing selected, no PDF mounted). Cut/Copy/Paste deliberately omitted — confirmed not to exist as an application-level command anywhere in this repository (see the completion report) | Every authenticated page | active |
| `menu.view.zoom-in`, `menu.view.zoom-out`, `menu.view.fit-width`, `menu.view.fit-page` | `<button>`, `data-reuse-control="doc-zoom-in"/"doc-zoom-out"/"doc-fit-width"/"doc-fit-page"` | "Zoom In" / "Zoom Out" / "Fit Width" / "Fit Page" | Click the real, pre-existing document-controls-bar equivalents verbatim — disabled whenever those are (no PDF mounted) | Every authenticated page | active |
| `menu.view.fullscreen` (new capability, CLAUDE-APP-MENU-01) | `<button>`, `data-action="toggle-fullscreen"`, `aria-pressed` | "Fullscreen" | The one genuinely NEW real command this stage adds — a plain browser Fullscreen API toggle (`document.documentElement.requestFullscreen`/`exitFullscreen`), `aria-pressed` kept honest via the real `fullscreenchange` event, never inferred from the click alone (Escape/F11 can exit fullscreen outside this control) | Every authenticated page | active |
| `menu.document.context` | `<button>`, `data-action="open-document-context"` | "Document Context…" | Only rendered when a Source is selected — opens/scrolls to the SAME existing `display.document.context` panel, never a second Document Context surface | Only rendered when `selected_source` is set | active |
| `menu.document.open`, `menu.document.download` | `<a>` | "Open externally" / "Download" | Selected Source file actions; both reuse `workspace.source_file`, with Download adding `?download=1` | Selected Source with a stored file | active |
| `menu.document.replace` | `<a>` | "Replace…" | Opens the same read-only replacement selector used by `display.document.replace`; submission delegates to the canonical Source-revision route | Selected active Source without a successor | active |
| `menu.document.remove` | `<button>` in POST form | "Remove…" | Calls the canonical recoverable `remove_document_route`; confirmation and soft-removal semantics are unchanged | Project owner or admin with a selected Source | active |
| `menu.document.admin-understanding` | `<button>`, `data-action="open-admin-document-understanding"` | "Admin: GO Document Understanding…" | Admin-only — opens/scrolls to the SAME existing `display.document.admin-qac` panel (CLAUDE-GO-QAC-01) | Only rendered when `selected_source` is set and `is_admin` | active |
| `menu.document.none` | `<span>` (disabled placeholder) | "No document selected" | Rendered instead of the above when nothing is selected | Only rendered when `selected_source` is NOT set | active |
| `menu.tools.annotate-text`, `menu.tools.annotate-highlight`, `menu.tools.annotate-ink`, `menu.tools.annotate-select`, `menu.tools.region-select` | `<button>`, `data-reuse-control="doc-annotate-*"/"doc-region-select"` | "Text Annotation" / "Highlight" / "Draw" / "Select / Delete Annotation" / "Capture Drawing Region" | Click the real, pre-existing annotation-tool buttons verbatim (Product Owner's own "a tool does an action, like a hammer" definition — these genuinely activate a tool, unlike a Toolbox list/container). Disabled whenever the underlying controls are (no PDF mounted) | Every authenticated page | active |
| `menu.window.hidden-tabs` | `<button>`, `data-action="open-hidden-tabs"` | "Show Hidden Tabs…" | Opens/scrolls to the real, pre-existing `#document-tabs-overflow` panel — disabled when the tab strip itself isn't present | Every authenticated page | active |
| `menu.window.close-all-tabs` | `<button>`, `data-action="close-all-tabs"` | "Close All Tabs" | Calls the real, pre-existing `closeAllTabs()` (`document_tabs.js`, newly exposed on `window.ArchioskDocumentTabs` for this purpose — the SAME function the tab-strip's own "Close All Tabs" overflow item already called) — disabled when no tabs are open | Every authenticated page | active |
| `menu.window.compare` | `<button>`, `data-reuse-control="toolbox-compare-btn"`, `aria-pressed` | "Compare Documents" | Clicks the real, pre-existing Eye Compare toggle verbatim (`#toolbox-compare-btn`) — `aria-pressed` mirrors its real state; introduces no second Compare state. **Not a statement that `toolbox-compare-btn`'s current Toolbox placement is permanent** — Product Owner standing decision (CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01, Disposition 8): Compare is a workspace state, not a Toolbox tool; two-document Compare should own the workspace and Toolbox should close/be unavailable in that state. This menu item only mirrors whatever real control exists at `#toolbox-compare-btn` today — it will keep working, unchanged, if/when that control's own placement changes under CLAUDE-COMPARE-AUTO-REPORT-REVIEW-01 | Every authenticated page | active |
| `menu.help.shortcuts` | `<button>`, `data-action="open-keyboard-shortcuts"` | "Keyboard Shortcuts…" | Opens/scrolls to `menu.help.shortcuts-panel` below | Every authenticated page | active |
| `menu.help.shortcuts-panel` | `<div>` (real content, initially `hidden`) | "Keyboard Shortcuts" heading + a `<dl>` | Lists only shortcuts that genuinely exist and were verified against the real JS: Escape (this stage's own `static/js/app_menu.js`), Left/Right/Home/End/Space/Enter for the Document tab strip (`document_tabs.js`'s pre-existing `onTabKeydown`) — no aspirational/undocumented shortcut listed | Every authenticated page | active |

**Document controls (CLAUDE-P40-VW7A-QA, Move Document Controls into the Top Application Menu — new surface, no prior identifiers to retire):**
the central region between `menu.context` and `menu.display-layout`/
`menu.appearance`/`menu.account`. The document viewer's own OLD
`<iframe>` embed never had any Archiosk-built toolbar or `data-ui-ref`
of its own — the "toolbar" reported as a defect was the browser's own
native PDF chrome rendering inside that iframe, entirely outside this
app's control (confirmed by direct inspection of the prior template
source before this stage's own change). Every reference below is
therefore genuinely NEW, not reparented from an old one — stated
honestly rather than fabricating a "moved from" history that doesn't
exist. Hidden (`[hidden]`) whenever no PDF Source is the active
Display target; `static/js/pdf_viewer.js` (vendored PDF.js — see
`static/js/vendor/pdfjs/README.md`) owns every control's real
behavior. PDF only — a drawing/DOCX/TXT Source has no page/zoom/
rotation concept this stage builds a renderer for (the pre-existing
`display.document`/`toolbox.document` pane-note about page navigation
still renders, unchanged, for those formats).

| Reference | Element | Label/summary | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `menu.document-controls` | `<div role="toolbar">` | (no visible label — `aria-label="Document controls"`) | The whole contextual region; `[hidden]` unless a PDF Source is active | Same as workspace access | active |
| `menu.document-controls.prev-page`, `menu.document-controls.next-page` | `<button>` | ‹ / › | Real page navigation — re-renders the PDF.js canvas at the new page; disabled at the first/last page respectively | Same as workspace access | active |
| `menu.document-controls.page-input` | `<input>` | current page number | Typing a number + blur/Enter jumps to that page (clamped to 1..page count) | Same as workspace access | active |
| `menu.document-controls.zoom-out`, `menu.document-controls.zoom-in` | `<button>` | − / + | Adjusts the render scale by 10%, clamped 25%–400% | Same as workspace access | active |
| `menu.document-controls.fit-width`, `menu.document-controls.fit-page` | `<button>` | "Fit width" / "Fit page" | Computes the scale that fits the current page's width, or both dimensions, to the canvas container | Same as workspace access | active |
| `menu.document-controls.rotate` | `<button>` | ↻ | Rotates the rendered page 90° per click (cumulative, wraps at 360°) | Same as workspace access | active |
| `menu.document-controls.mirror-h`, `menu.document-controls.mirror-v` | `<button>` | ↔ / ↕ | CLAUDE-MM4: toggles a horizontal/vertical view-only CSS mirror on the rendered page (never touches the stored PDF bytes) | Same as workspace access | active |
| `menu.document-controls.reset-orientation` | `<button>` | ↴ | CLAUDE-MM4: clears rotation and both mirror flags back to the identity view | Same as workspace access | active |
| `menu.document-controls.orientation-status` | `<span aria-live="polite">` | (no visible label at rest) | CLAUDE-MM4: reads e.g. "Rotated 90° clockwise and mirrored horizontally — source unchanged" whenever any orientation transform is active, empty at the identity view | Same as workspace access | active |
| `menu.document-controls.region-select` | `<button>` (joins the annotate-tool one-of-group) | ▢ | CLAUDE-MM4: drag-to-select-rectangle tool that creates a REAL, persisted `AddressableRegion` + `EvidenceItem` via `POST .../drawing-regions` — distinct from the ephemeral, never-saved annotation tools above | Same as workspace access | active |
| `menu.document-controls.region-status` | `<span aria-live="polite">` | (no visible label at rest) | CLAUDE-MM4: sheet lookup/registration prompts, in-progress "Saving region…", and the resulting citation + "Copy citation" action after a region is created | Same as workspace access | active |
| `menu.document-controls.search-input` | `<input type="search">` | "Search in document" | Real full-document text search via PDF.js's own `getTextContent()` (extracted and cached per page) across every page — not a placeholder | Same as workspace access | active |
| `menu.document-controls.search-prev`, `menu.document-controls.search-next` | `<button>` | ∧ / ∨ | Cycles to the previous/next match, jumping pages as needed; disabled with no matches | Same as workspace access | active |
| `menu.document-controls.download` | `<a download>` | ↓ | Direct link to the same `workspace.source_file` URL the canvas itself renders from | Same as workspace access | active |
| `menu.document-controls.print` | `<button>` | 🖶 | Opens the original PDF in a new tab (the browser's own native print/save chrome there is fully functional) — a real, working action, not a stub; re-implementing print pagination for a canvas-rendered page was judged unnecessary complexity given this already works reliably | Same as workspace access | active |
| `menu.document-controls.snapshot` (CLAUDE-SNAPSHOT-DUAL-SURFACE-01, new) | `<button>` | 📷 | Captures whichever surface (Main or Eye) currently owns this shared toolbar's own `<canvas>` at its current page/zoom/rotation, registers it as a real new Source (deterministic `<name>-01`/`-02`/... sequential naming, `POST /api/v1/documents/<project_id>/sources/<source_id>/snapshot`), then opens it as a new, active Main tab via the same real `?source=<id>` navigation every other document-open action already uses. Genuinely new capability this stage — no pre-existing screenshot/snapshot mechanism was found on repo-wide search before building it | Same as workspace access; write path is admin-gated (`_ADMIN_ONLY_ENDPOINTS`, same authority level as `eye.canvas.save`) | active |
| `menu.document-controls.snapshot-status` (CLAUDE-SNAPSHOT-DUAL-SURFACE-01, new) | `<span aria-live="polite">` | "Capturing…" / error text / empty | Transient status only — no persistent success message, since a successful Snapshot's own new Main tab is itself the confirmation | Same as workspace access | active |
| `menu.document-controls.overflow` | `<details>` | "…" | Only shown (CSS `.doc-controls-overflow-active`, JS-toggled) below a 900px viewport; `static/js/pdf_viewer.js` re-parents the SAME secondary-control DOM nodes into it, never a cloned duplicate | Same as workspace access | active |
| `display.document.pdf-canvas` | `<div>` (Display) | (no visible label) | Empty container `static/js/pdf_viewer.js` fills with a `<canvas>` on mount — replaces the old plain `<iframe src=raw-file-url>` for a PDF Source specifically (drawings keep their existing `<img>`; DOCX/TXT/anything without a dedicated viewer now use `display.document.no-preview` below, not an `<iframe>` — see CLAUDE-CA1D-RIVER-PO-02 CONSOLIDATION) | Same as workspace access | active |
| `display.document.context` (CLAUDE-POSTCAMEL-PROJECT-CONTEXT-01, new) | `<details>` | "Document Context" | Human-supplied orientation on THIS Source only (what it is, why it's present, its status, caveats, or intended use) — reuses `Source.note` (pre-existing field, never before wired to a route). Explicitly independent of Project Context (`menu.project-context`) in both directions | Same as workspace access | active |
| `display.document.context-text` | `<p>` | current note text | Rendered only when `selected_source.note` is set | Same as above | active |
| `display.document.context-empty` | `<p>` | "No Document Context set yet." | Empty state | Same as above | active |
| `display.document.context-form` | `<form>` | (edit form) | Posts to `workspace.set_document_context_route` — a plain overwrite (not a history, unlike Project Context) | Same as above | active |
| `display.document.context-textarea` | `<textarea>` | (current note, editable) | Pre-filled with the current note, if any | Same as above | active |
| `display.document.context-submit` | `<button>` | "Save Document Context" | Submits the form | Same as above | active |
| `display.document.admin-qac` (CLAUDE-GO-QAC-01, new) | `<details>` | "Admin: GO Document Understanding" | Admin-only, extends `display.document.context` above rather than replacing it — GO's own drafted Document Context claims, review, and a derived quality gauge | `is_admin` only | active |
| `display.document.admin-qac-gauge` | `<p>` | "GO understanding: {gauge}" | The 3-state `assess_document_context_quality` gauge (Good/Review/Weak) plus claim-count/inference-load signals | Same as above | active |
| `display.document.admin-qac-cause` | `<p>` | possible-cause text | Rendered only when `document_context_quality.possible_cause` is set (Review/Weak gauges only) | Same as above | active |
| `display.document.admin-qac-draft-form` | `<form>` | (draft trigger) | Posts to `workspace.draft_document_context_claims_route` | Same as above | active |
| `display.document.admin-qac-draft-submit` | `<button>` | "Draft Document Context with GO" | Submits the form | Same as above | active |
| `display.document.admin-qac-claims` | `<ul>` | (claim list) | Rendered when `document_context_claims_view` is non-empty | Same as above | active |
| `display.document.admin-qac-claim` | `<li>` (pattern, one per claim) | field/content-class/review-state summary | One `DocumentContextClaim` | Same as above | active |
| `display.document.admin-qac-review-form` | `<form>` (pattern, unreviewed claims only) | (accept/reject form) | Posts to `workspace.review_document_context_claim_route` | Same as above | active |
| `display.document.admin-qac-review-statement` | `<textarea>` (pattern) | current claim statement, editable | PM can edit before accepting | Same as above | active |
| `display.document.admin-qac-accept` | `<button>` (pattern) | "Accept" | Submits with `outcome=accepted` | Same as above | active |
| `display.document.admin-qac-reject` | `<button>` (pattern) | "Reject" | Submits with `outcome=rejected` | Same as above | active |
| `display.document.no-preview` (CLAUDE-CA1D-RIVER-PO-02 CONSOLIDATION, Section B "internal-first document opening", new) | `<div>` | "This file type isn't previewable inside ARCHIOSK yet…" | Replaces the old catch-all `<iframe src=workspace.source_file>` for any format with no dedicated in-app viewer (.docx/.txt/etc.) — that iframe silently triggered an OS download the moment the Source was selected, since the browser can't render those types inline. The Source itself is still fully selected/displayed inside ARCHIOSK (Document Context above still works); only the raw file content has no in-app preview | Same as workspace access | active |
| `display.document.open-externally` | `<a target="_blank">` | "Open externally" | The one explicit secondary action that opens the raw file in a new tab, `as_attachment=False` (browser decides how to handle it) — `workspace.source_file` unchanged from its pre-existing default behavior | Same as workspace access | active |
| `display.document.download` | `<a>` | "Download" | The other explicit secondary action — same `workspace.source_file` URL with `?download=1`, forcing `as_attachment=True` (a real Save dialog) | Same as workspace access | active |
| `display.document.actions` | `<div aria-label="Document actions">` | Download / Replace / Remove | Primary selected-document lifecycle actions, positioned together above Document Context | Selected Source | active |
| `display.document.replace` | `<a>` | "Replace" | Opens `display.document.replace-page`; the upload posts to the existing revision route and creates a new Source plus Supersession rather than overwriting bytes | Selected active Source without a successor | active |
| `display.document.remove` | `<button>` in POST form | "Remove" | Same `remove_document_route` used by the Document menu and Toolbox; existing confirmation and recoverable removal apply | Project owner or admin | active |
| `display.document.replace-page` | `<section>` | "Replace <document>" | Read-only file-selection/context step; explains lineage preservation and posts once to the canonical drawing/document revision endpoint | Same project access as the selected Source | active |

**Annotation tools (CLAUDE-P40-VW7A-QA2 — new, added to the same `menu.document-controls` region, inside `doc-controls-secondary` so they share its existing responsive overflow behavior; no prior identifiers to retire):** real, client-side-only PDF annotation tools — text/highlight/freehand ink, select+delete, undo/redo. One active tool at a time (`aria-pressed`, toggled by `static/js/pdf_viewer.js`'s `setActiveTool`). Coordinates are stored in PDF page space (`PageViewport.convertToPdfPoint`/`convertToViewportPoint`), not raw canvas pixels, so annotations stay correctly placed across zoom/rotation changes. Deliberately **no** Save/Export ref exists — the disclosed scope boundary: no PDF-writing library is vendored in this repo (only PDF.js's rendering half — `static/js/vendor/pdfjs/README.md`), so there is no reliable way to bake these into a real derived PDF file this stage. `menu.document-controls.annotation-status` is how "unsaved changes" is surfaced instead.

| Reference | Element | Label/summary | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `menu.document-controls.annotate-text`, `menu.document-controls.annotate-highlight`, `menu.document-controls.annotate-ink`, `menu.document-controls.annotate-select` | `<button>` (one-of-four, `aria-pressed`) | T / ▭ / ✎ / ↖ | Selects the active annotation tool; clicking the already-active one deselects it (no tool = clicks on the canvas do nothing) | Same as workspace access | active |
| `menu.document-controls.annotate-delete` | `<button>` | ✕ | Removes the currently-selected annotation (Select tool); disabled with no selection | Same as workspace access | active |
| `menu.document-controls.annotate-undo`, `menu.document-controls.annotate-redo` | `<button>` | ↶ / ↷ | Real undo/redo stacks over annotation add/delete operations for the current browser session only; disabled when the respective stack is empty | Same as workspace access | active |
| `menu.document-controls.annotation-status` | `<span aria-live="polite">` | (no visible label at rest) | Reads "Unsaved annotations (draft only — not saved to the Document)" whenever any page has at least one annotation, empty otherwise; a `beforeunload` prompt backs this up on tab close/navigation | Same as workspace access | active |

## Shell (structural chrome — `templates/base.html`)

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `shell.lists-divider` | `<button>` | (unlabeled divider) | Collapses/shows the Lists panel; `localStorage`-persisted, reviewer-wide | Every authenticated page | active |
| `shell.toolbox-divider` | `<button>` | (unlabeled divider) | Collapses/shows the WHOLE right column (Toolbox and Eye together, CLAUDE-P40-EYE1 — was Toolbox alone); `localStorage`-persisted, per-Project. **Product-owner browser correction:** now ALSO a real, mouse-draggable/keyboard-operable (`ArrowLeft` widens/`ArrowRight` narrows) WIDTH resize handle for the right column — a genuine drag (movement past a small threshold) is distinguished from a plain click, so the pre-existing collapse/show behavior is unchanged for an ordinary click. Width persisted separately (`beehive:panel:right-column-width:<project_id>`), clamped against a practical minimum for both the right column and the centre (Display/Chat) column | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.lists-thumbnails-divider` | `<div role="separator">` (CLAUDE-P40-VW7A-QA2) | (unlabeled divider) | Draggable (pointer + arrow-key) horizontal split between `lists.thumbnails-pane` and Lists above it; percentage-based, `sessionStorage`-persisted (deliberately weaker than the `localStorage` panel-show/hide prefs above — Section 3's own "may persist per session"; still survives an ordinary refresh, which is all CLAUDE-P40-LTH1's own persistence requirement needs); double-click restores the default 60/40 split. **CLAUDE-P40-LTH1 (correction):** no longer ever `[hidden]` — a permanent fixture, the same treatment `shell.toolbox-eye-divider` already has (below); real `:focus-visible` outline added | Every authenticated page | active |
| `shell.chat-divider-lock` (CLAUDE-EYE-TOOLBOX-LAYOUT-01, new) | `<button aria-pressed>` (`templates/_macros.html`'s `conversation_dock` macro — the resize handle itself lives there, not `base.html`, but is structural divider chrome, hence `shell.*` rather than `chat.*`) | 🔒 / 🔓 glyph only | The same compact lock-toggle pattern as `shell.toolbox-eye-lock` (below), applied to the Display/Chat resize handle (`#conversation-dock-resize-handle`). Persisted independently (`beehive:chat:height-locked:<project_id>`). **CLAUDE-EYE-TOOLBOX-LAYOUT-01-CORRECTION-01:** while `shell.chat-divider-link`/`shell.toolbox-eye-link` (below) are linked, locking either lock button locks BOTH together ("Linked + Locked" — both share one position, neither drags); while unlinked, this lock remains fully independent of `shell.toolbox-eye-lock`, as originally shipped | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.chat-divider-link` (CLAUDE-EYE-TOOLBOX-LAYOUT-01-CORRECTION-01, new) | `<button aria-pressed>` (`templates/_macros.html`'s `conversation_dock` macro) | 🔗 glyph only (color signals state, same as the lock glyphs) | Compact chain/link toggle beside `shell.chat-divider-lock` in the same strip. Shares a single boolean (`window.__splitterLink`, `beehive:panel:splitters-linked:<project_id>`) with `shell.toolbox-eye-link` — while linked, dragging/keyboard-resizing/maximizing EITHER of the two horizontal splitters also drives the other to the same fraction of their shared column height (`.workspace-right-column`'s height always equals `.workspace-main-column`'s), so the two division lines read as one continuous horizontal boundary. Enabling link immediately aligns them (using the Eye/Toolbox split as the source of truth) rather than waiting for the next drag | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.context.identity`, `shell.context.identity.toggle` (CLAUDE-MOBILE-PRIMARY-RESET-01, new) | `<details>` / `<summary>` | "<Project> › <Conversation>" | The context bar — one band answering "where am I", "what else is there", "start fresh". Opens OVER the page rather than pushing it down; a disclosure that reflows the frame is why the old history tray read as another band | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.context.identity.project`, `shell.context.identity.leaf` (CLAUDE-MOBILE-PRIMARY-RESET-01, new) | `<a>` | "Project conversation" / the Investigation's title | Switches conversation, using the SAME links `toolbox.investigations.leaf` and `chat.dock.history.leaf` used — read-only navigation, nothing here creates, renames or removes | Same as above | active |
| `shell.context.new-chat` (CLAUDE-MOBILE-PRIMARY-RESET-01, new) | `<button type="submit">` | "New" | Starts a new conversation, relocated here from the Composer's own dock header, which is removed on a phone | Same as above | active |
| `menu.view.panel.lists`, `menu.view.panel.display`, `menu.view.panel.eye`, `menu.view.panel.toolbox` (CLAUDE-MOBILE-PRIMARY-RESET-01, new) | `<button aria-pressed>` | "Lists" / "Display" / "Eye (drawing)" / "Toolbox" | The tray controls, RELOCATED from a permanent four-button band under the top bar into View, where a panel is asked for rather than imposed. Same `data-tray-focus-btn` mechanism `static/js/workspace_trays.js` already wires — never a second implementation; a tray this page does not have removes its own button | Every authenticated page; Eye/Toolbox only inside an open Workspace | active |
| `shell.tray-switcher` (CLAUDE-MOBILE-FRAME-02, new) | `<nav>` | "Active work tray" (landmark label only) | The bottom edge of the fixed top zone in the Product Owner's mobile frame (fixed compact header → one active work tray → fixed Composer). Container only; the four buttons below do the work. Rendered on every authenticated page at every width — desktop gets the same architecture with more room, not a separate one | Every authenticated page | active |
| `shell.tray-switcher.active` (CLAUDE-MOBILE-FRAME-02, new) | `<span aria-live="polite">` | The active surface's own name | Answers "what am I working on" in the header. Announced on change; hidden below 640px, where the pressed button beside it already carries the same fact and the switch targets are worth more than the label | Every authenticated page | active |
| `shell.tray-switcher.lists`, `shell.tray-switcher.display`, `shell.tray-switcher.eye`, `shell.tray-switcher.toolbox` (CLAUDE-MOBILE-FRAME-02, new) | `<button aria-pressed>` | "Lists" / "Display" / "Eye" / "Toolbox" | Makes one surface the active work tray, via `data-tray-focus` on `<html>` — except `shell.tray-switcher.eye`, which raises and lowers the FOREGROUND LAYER (`data-tray-layer`) instead: a tray REPLACES what is in the work area, a layer COVERS it, so the work beneath survives untouched (CLAUDE-MOBILE-Q-TRIAL-01 Section 4). Pressing it again shovels the layer back (`static/js/workspace_trays.js`) — an attribute holds one value, so "one active tray" is structural rather than four booleans kept in agreement. **Not new functions:** these are NPT-002/003/005/006, the same surfaces the Appearance menu already names; Documents, Spin, Findings, Project Context and the photo tray are contents of them. Reviewer-wide `localStorage` (`beehive:tray:focus`); presentation state only — focusing never mutates project state, evidence, or authorization. On desktop a second press restores the normal LAY-5A composition; on a phone some tray is always active, so it is a no-op there. Eye/Toolbox render only inside an open Workspace; `workspace_trays.js` removes any button whose tray is absent rather than leaving a dead control | Every authenticated page (Eye/Toolbox buttons: only when `project_id`/`workspace` are defined) | active |
| `shell.tray-composer-grabber` (CLAUDE-MOBILE-FRAME-02, new) | `<div role="slider">` | "Work area and Composer split" | The work/Composer boundary at thumb size, shown only below 640px — `shell.chat-divider-lock`'s own 9px handle is pointer-precision and is hidden there. Drag up for more Composer, down for more tray; a plain tap cycles three rest positions (expanded / working / collapsed) so the boundary needs no drag at all; ArrowUp/ArrowDown adjust, Enter/Space cycles. **Not a second sizing system** — every value goes through `window.__chatSplitter.setValue()` in `static/js/case_workspace.js`, the one existing write point for `--chat-height`, so it shares the desktop handle's clamps, persistence and linked-splitter mirroring | Only rendered when `project_id`/`workspace` are defined | active |

## Lists — cross-Project (`templates/base.html`, reviewer-wide)

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `lists.projects` | tree-toggle `<button>` | "Projects" | **CLAUDE-LEFT-RAIL-01:** now the ONE, always-present rail purpose — a live active-project switcher, unconditionally rendered and expanded (`aria-expanded="true"` unconditionally; the old `projects_open`/request.path-based conditional logic was retired as dead code), not only when no Project is open. Product Owner rule: "PROJECTS = the projects I can work on and switch between" | Every authenticated page | active |
| `lists.projects.leaf` | tree-leaf `<a>` (pattern) | Project display name | Navigates to that Project's own workspace (`workspace.show_workspace`) — only for a Project that is **not** the currently active one (the active one instead expands into `lists.project.self` and its branch, below). **CLAUDE-LEFT-RAIL-01:** now renders alongside `lists.project.self` too, not only in the portfolio-only state — this is the exact PM-facing gap the Product Owner named (switching without first leaving the currently open Project) | Filtered to `nav_recent_projects` (already access-scoped) | active |
| `lists.project-switch-dialog` | *(retired — CLAUDE-P40-VW7B)* | (dialog, no visible label — `aria-labelledby` its own heading) | **CLAUDE-P40-VW8:** interruption dialog shown when activating `lists.projects.leaf` for a Project other than the one currently open. **CLAUDE-P40-VW7B:** retired because its trigger, `lists.projects.leaf`, no longer rendered while a Project was open at all. **CLAUDE-LEFT-RAIL-01 correction:** that trigger DOES render again now (see `lists.projects.leaf` above) — the dialog itself stays retired regardless, per this stage's own explicit Product Owner instruction ("normal safe project switching should be immediate... do not add a confirmation dialog merely because the user selected another project"), not because the trigger is absent. Nothing renders this reference any more; retired rather than reused for a different control | — | retired |
| `lists.project-switch-dialog.stay` | *(retired — CLAUDE-P40-VW7B)* | "Stay in Current Project" | Retired alongside its parent dialog, above | — | retired |
| `lists.project-switch-dialog.switch` | *(retired — CLAUDE-P40-VW7B)* | "Switch in This Tab" | Retired alongside its parent dialog, above | — | retired |
| `lists.project-switch-dialog.open-new-tab` | *(retired — CLAUDE-P40-VW7B)* | "Open in New Tab" | Retired alongside its parent dialog, above | — | retired |

**Selection-state hierarchy correction (CLAUDE-P40-VW7A-QA, all identifiers above retained unchanged):**
`lists.projects` (an expanded root), `lists.project.self` (the current
Project), and whichever child leaf is actually selected (e.g.
`lists.project.chats`) used to share the literal same `active` CSS
class/fill, reading as three indistinguishable "selected" rows. Now
three genuinely distinct treatments, none of them renumbered or
reparented: `lists.projects`'s own structural-title typography no
longer paints a selection-style fill merely for being expanded
(`aria-expanded` alone conveys that); `lists.project.self` gets a new
`.current-project` CSS class (a restrained left-edge marker,
`aria-current="true"`) instead of `.active`; the `.active` class and
its `--surface-selected` fill are now reserved exclusively for the one
child leaf whose own href is what is actually displayed
(`aria-current="page"`). A new `.sibling-project-after-current` class
(not a UI reference — a pure layout hook, whitespace-only) marks
whichever sibling Project row renders immediately after the current
Project's own child group closes, so it doesn't visually read as a
continuation of that group.

## Lists — active Project branch (`templates/base.html`, only inside the currently open Project's own row)

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `lists.project.self` | tree-leaf `<a>`, `current-project` (CLAUDE-P40-VW7A-QA — was `active`, see note below) | active Project's own display name | Navigates to its own workspace (a no-op — already there); this row is what expands into every entry below. `aria-current="true"` identifies it as the current Project, distinct from `aria-current="page"` on whichever child leaf is actually selected | Same as `_load_workspace_or_404` (project owner/allow-list/admin) | active |
| `lists.project.overview` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `lists.project.self (same destination - the project home link already opened Overview; the separate leaf was a redundant second link to the same place)` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.documents` | retired visible wrapper | (none) | Project files now project directly beneath `lists.project.self`; underlying Source/document semantics are unchanged | Same as workspace access | retired |
| `lists.project.search-controls` | `<div>` filter region | Search, then Text / Image / Marked | Project-file search and secondary content filters sit above the project hierarchy; they are controls, never hierarchy nodes | Same as workspace access | active |
| `lists.project.documents.leaf` | tree-leaf `<a>`, `data-source-id` (pattern) | Document name | **Division 0 active target:** real navigation to `?source=<id>`. **Non-zero Display active target:** client-side `populateDivision(target, 'source', sourceId, name)` — unchanged since VW7A, a real file embedded via `<iframe src=file_url>`/`<img>`, never the `&panel=1` mechanism. Syncs Toolbox to `toolbox.document` | Same as workspace access | active |
| `lists.project.documents.search-mode-text`, `lists.project.documents.search-mode-image` (CLAUDE-DOCUMENT-RAIL-SEARCH-01, new) | `<button aria-pressed>` pair | "Text" / "Image" | Compact segmented secondary filters below Search and above the project tree — shares one result list/row grammar regardless of mode. Switching modes never navigates/replaces Main or Eye | Same as workspace access | active |
| `lists.project.documents.search` (CLAUDE-DOCUMENT-RAIL-SEARCH-01, new) | `<input type="search">` | Placeholder "Search..." | Positioned above the project hierarchy; client-side filename matching only over the current project's `active_sources` (`static/js/document_marks.js`) — multi-word terms are AND-combined, `"quoted phrases"` match as exact substrings, mixed quoted+bare queries combine both; a cheap exact-phrase-first ranking reorders the visible set (stable sort — no reorder on ties). **Filename only** — a real repo-wide audit (`services/bhive_parser.py`, `services/case_workspace.py`) found no stored extracted document text anywhere and no existing content-search route; `lists.project.documents.search-status` states this honestly rather than implying full-text was checked. Typing alone never navigates a document | Same as workspace access | active |
| `lists.project.documents.search-status` (CLAUDE-DOCUMENT-RAIL-SEARCH-01, new) | `<p aria-live="polite">` | "No filename matches. Document content search is not yet available." — only shown on a genuine zero-match search | Honesty surface distinguishing "no matches" from "content wasn't actually searched," per Part 6's own explicit requirement | Same as workspace access | active |
| `lists.project.documents.image-tray`, `lists.project.documents.image-search-run`, `lists.project.documents.image-search-replace`, `lists.project.documents.image-search-clear` (CLAUDE-DOCUMENT-RAIL-SEARCH-01, new), `lists.project.documents.image-search-capture` (CLAUDE-MOBILE-CAPTURE-01, new) | `<div role="group">` + buttons | "Paste, drop, or capture an image to search" (empty state) | Image mode's own temporary, larger paste/drop/preview surface (`static/js/document_marks.js`) — real capture and preview, same paste/drop idiom `eye.drop-target` already established. CLAUDE-MOBILE-CAPTURE-01 added `image-search-capture`, a plain `<input type="file" accept="image/*" capture="environment">` feeding the same `loadImageFile()` - paste and drop are unavailable on a phone, so the tray's own "capture" invitation was previously unfulfillable there; desktop ignores `capture` and opens a picker. "Search" (`image-search-run`) does not call any backend — no image/shape-matching capability exists anywhere in this codebase (`services/image_intelligence.py`'s own documented boundary: EXIF/dimensions only, "never interprets what an image DEPICTS") — it collapses the tray and shows the honest deferred status instead; see `governance/spare-parts-yard.md`'s own new Future Reserve record | Same as workspace access | active |
| `lists.project.documents.image-search-collapsed`, `lists.project.documents.image-search-expand`, `lists.project.documents.image-search-collapsed-replace`, `lists.project.documents.image-search-collapsed-clear` (CLAUDE-DOCUMENT-RAIL-SEARCH-01, new) | `<div>` + buttons | Compact thumbnail + Expand/Replace/Clear | The tray's own collapsed representation after "Search" is clicked — regains rail vertical space without discarding the query image; Expand restores the full tray with the same image still loaded | Same as workspace access | active |
| `lists.project.documents.image-search-status` (CLAUDE-DOCUMENT-RAIL-SEARCH-01; wording updated CLAUDE-GO-MULTIMODAL-PERCEPTION-GAMES-01) | `<p aria-live="polite">` | "Not found — ARCHIOSK doesn't yet compare this image against the document set. Open in Composer and GO can look at it directly." | The honest no-match state — shown once "Search" is clicked in Image mode, still never a fabricated result list, now with its own principal escape (see `image-search-open-composer`, next row) | Same as workspace access | active |
| `lists.project.documents.image-search-open-composer` (CLAUDE-GO-MULTIMODAL-PERCEPTION-GAMES-01, new) | `<button>` (inline in `image-search-status`) | "Open in Composer" | The ONE escape hatch out of the no-match state — submits the still-only-client-side query image (never uploaded/persisted by this surface) into a single bounded Composer turn (`routes/workspace.py`'s `open_image_in_composer`, a real vision-capable Anthropic call — the first and only one in this codebase). Not a second search implementation | Same as workspace access | active |
| `lists.project.documents.mark` (CLAUDE-DOCUMENT-RAIL-PROBE-EYE-TOOL-01, new) | `<input type="checkbox">` (pattern, one per Document row, `data-source-id`) | Unlabeled checkbox, `aria-label="Mark <name> for review"` | Row order (per the session's own final addendum): Name → Mark → Keep → Eye → Tool. Lightweight PM review-shortlist flag (`static/js/document_marks.js`, localStorage keyed by username+Project — same cross-account-leakage-safe pattern `document_tabs.js`'s own `PINNED_KEY` established) — never opens/projects/tabs/mutates evidence; independent of every other row control | Same as workspace access | active |
| `lists.project.documents.marked-filter` (CLAUDE-DOCUMENT-RAIL-PROBE-EYE-TOOL-01, new) | `<button aria-pressed>` | "Marked" | Secondary content filter beside Text/Image, below Search and above the project tree — client-side row show/hide only (`static/js/document_marks.js`), no navigation, no separate panel | Same as workspace access | active |
| `lists.project.documents.keep-open` (ADDENDUM — Document-Row Control Order / Mark vs Keep, new) | `<button aria-pressed>` (pattern, one per Document row, `data-source-id`) | ▷ glyph only, `aria-label="Keep <name> open as a retained tab"` | A direct per-row path to `document_tabs.js`'s own pre-existing `pinTab()`/`findPinned()` pinned-tab mechanism (previously reachable only via double-click on an open preview tab, or its "Keep Open" tab-menu item) — promotes the current transient probe into the persistent Main working-tab set; `aria-pressed` mirrors the real pinned state, synced from `render()` itself so it can never drift | Same as workspace access | active |
| `lists.project.documents.send-to-eye` (CLAUDE-EYE-COMPARE-01; CLAUDE-LEFTRAIL-ROUTING-01 added toggle/active-state; CLAUDE-DOCUMENT-RAIL-PROBE-EYE-TOOL-01 changed the active-state visual to a dashed row outline) | `<button aria-pressed>` (pattern, one per Document row, `data-source-id`/`data-source-name`/`data-source-kind`) | 👁 glyph only, `aria-label="Open <name> in Eye"` | Loads that Document into `eye.document-view` (`window.ArchioskEyePane.loadDocument`, `static/js/eye_pane.js`) — a pure client-side GET of the existing `workspace.source_file` route, never a navigation/mutation. Lets a Document be sent to Eye without replacing whatever is currently open in Display. `aria-pressed="true"` on whichever row's own document currently occupies Eye; the row's own `.tree-leaf` gets a `.eye-projected` dashed outline (never a filled color competing with Main's own selection highlight) — clicking the ALREADY-active icon again clears Eye instead of reloading it. Opening a document in Main (its own filename link) is a real page navigation, which structurally clears Eye's own client-side-only state by itself — no extra code needed for "moving a document to Main clears its Eye projection" | Same as workspace access | active |
| `lists.project.documents.send-to-toolbox` (CLAUDE-LEFTRAIL-ROUTING-01, new) | `<button aria-pressed>` (pattern, one per Document row, identical action on every row — not `data-source-id`-scoped) | ⚙ glyph only, `aria-label="Switch right side to Toolbox tools"` | A mode switch, not a document destination — clears Eye's own content and turns Compare off (`static/js/eye_pane.js`), which is all `eye.drop-target`'s own `eyeHasContent()` needs to fall back to the full-height Toolbox (see `shell.workspace-right-column` addendum-A behavior below). `aria-pressed` mirrors whether the right side is currently in Toolbox mode, kept in sync across every row by the same `refreshEyeLayout()` that drives the column collapse | Same as workspace access | active |
| `lists.project.documents.empty` | `<span>` | "No Documents yet." | Empty-state message (CLAUDE-P40-VW7B — untagged until CLAUDE-P40-VW8-QA's Complete Root and Subfolder UI Reference Tagging stage) | Same as workspace access | active |
| `lists.project.files` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "Files" | Removed from the rail outright (Product Owner: rail is "principally project switching + evidence navigation, not an action drawer"). The underlying `?view=files` content (`display.files`) stays fully Active and reachable via `display.overview.files-link` ("Open Files →") - de-duplication, not retirement of the capability itself. See "Retired references" below | — | retired |
| `lists.project.requirements` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.requirements` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.leaf` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations.leaf` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.leaf.archive` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations.leaf.archive` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.new` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations.new` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.continue-from-archive` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations.continue-from-archive` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.investigations.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.investigations.new.form` | `<form>` | (create form) | Posts to `workspace.create_case` (unchanged, pre-existing route/service — no parallel Investigation-creation implementation) | Same as workspace access — the route itself is `@login_required` | active |
| `lists.project.investigations.new.title`, `lists.project.investigations.new.objective` | `<input>` | "Investigation title" / "Objective (optional)" | Title required; objective optional — identical fields to the pre-existing Overview "+ Start Investigation" subdisclosure | Same as above | active |
| `lists.project.investigations.new.create` | `<button>` | "Create Investigation" | Submits the form | Same as above | active |
| `lists.project.investigations.new.cancel` | `<a>` | "Cancel" | Navigates to `?view=overview` — creates nothing | Same as above | active |
| `lists.project.investigations.new.validation-error` | `<div>` (flash message) | (validation error text) | Rendered only when `workspace.create_case` rejects an empty title — the SAME focused form re-appears (not the Overview page) so the reviewer's next attempt starts from where they were | Same as above | active |
| `lists.project.investigations.continue-from-archive.list` (CLAUDE-POSTCAMEL-INVESTIGATION-AR1, new) | `<ul>` | (archived Investigation rows) | Container for `archived_visible_cases` — archived Investigations this reviewer can see, within THIS Project only (`workspace` is already this one Project's own loaded state) | Same as workspace access, filtered to `visible_cases_for` | active |
| `lists.project.investigations.continue-from-archive.row` (CLAUDE-POSTCAMEL-INVESTIGATION-AR1, new) | `<li>`, `data-archive-case-id` (pattern) | Investigation title, archived date, Finding count | One row per archived Investigation — title, `archived_at`, and a cheap Finding count (`case_finding_counts`), plus a lineage note when the archive is itself a derivation | Same as above | active |
| `lists.project.investigations.continue-from-archive.snapshot` (CLAUDE-POSTCAMEL-INVESTIGATION-AR1, new) | `<button>` | "Request Snapshot" | Client-side (`static/js/investigation_snapshot.js`) POST to `workspace.snapshot_archived_case`, rendering the JSON result inline into the row's own output element. Read-only and ephemeral — writes no ConversationMessage/Finding/Task/Work Product/GovernanceLog entry; grounded only in that one archived Case's own title/objective/Findings/conversation (`services/investigation_snapshot.py`), never another Case's or Project's material | Same as workspace access to the archived Case (Case-privacy-aware) | active |
| `lists.project.investigations.continue-from-archive.continue` (CLAUDE-POSTCAMEL-INVESTIGATION-AR1, new) | `<button>` (inside its own `<form>`) | "Continue from this Archive" | Posts to the existing, unmodified `workspace.derive_case` route (`CaseWorkspaceStore.derive_case_from_archive`) — creates a new active Investigation with explicit lineage back to the archive; the archive itself is never mutated | Same owner-or-admin authority `derive_case_from_archive` already enforced before this stage | active |
| `lists.project.investigations.continue-from-archive.empty` (CLAUDE-POSTCAMEL-INVESTIGATION-AR1, new) | `<p>` | "No archived Investigations in this Project yet." | Empty-state message when `archived_visible_cases` is empty | Same as above | active |
| `lists.project.rfis` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.rfi` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.rfis.leaf` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.rfi.leaf` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.rfis.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.rfi.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.work-products` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.work-products` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.work-products.new` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.work-products.new` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.work-products.leaf` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.work-products.leaf` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.work-products.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.work-products.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.chats` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.conversation` in the Toolbox section below (same destination - the count-only leaf moved into the Toolbox's own Project Intelligence section), or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.open` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.open` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.completed` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.completed` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.leaf` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.leaf` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.complete` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.complete` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.reopen` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.reopen` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.open.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.open.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tasks.completed.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tasks.completed.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tags` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tags` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tags.group` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tags.group` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tags.leaf` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tags.leaf` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tags.remove` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tags.remove` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tags.empty` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.tags.empty` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tools` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "Project Tools" | The whole branch and every child below it removed from the rail outright - see "Retired references" below for each child's own replacement or Spare Parts Yard disposition (`governance/spare-parts-yard.md`) | — | retired |
| `lists.project.tools.remove-project` | *(retired — CLAUDE-GO-DNA-01)* | Relocated by the Panel Zoning correction (LEFT = evidence territory only) - see `toolbox.project-admin.remove-project` in the Toolbox section below, or `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md` for the full record. Nothing renders this reference in Lists any more. | — | retired |
| `lists.project.tools.add-document` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "+ Add Documents" | Promoted to a new Active home, not merely retired — see `pdm.add-documents.form` below | — | retired |
| `lists.project.tools.add-text-record` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "+ Add Text Record" | Reserve / Spare Parts Yard - see `governance/spare-parts-yard.md` §1 | — | retired |
| `lists.project.tools.revise-document` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "+ Register a Document Revision" | Reserve / Spare Parts Yard - see `governance/spare-parts-yard.md` §2 | — | retired |
| `lists.project.tools.revise-document.empty` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "No revisable documents…" | Retired alongside its parent form, above | — | retired |
| `lists.project.tools.add-external-source` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "+ Add External Source" | Reserve / Spare Parts Yard - see `governance/spare-parts-yard.md` §3 (was already inert - no real connector configured) | — | retired |
| `lists.project.tools.removed-items` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "Removed Items (`<count>`)" | Promoted to a new Active home, merged into Project Data Management's own "Archive documents" - see `pdm.archive-documents.archived-list` below | — | retired |
| `lists.project.tools.removed-items.restore` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)* | "Restore Document" | Promoted alongside its parent list - see `pdm.archive-documents.restore` below | — | retired |

## Lists — Page Thumbnails pane (`templates/base.html`, CLAUDE-P40-VW7A-QA2, corrected CLAUDE-P40-LTH1)

Nested inside the same `<nav id="launcher-panel">` as the Lists tree above (`lists.thumbnails-pane` is `lists.project.self`'s own sibling region, not a second panel). **CLAUDE-P40-LTH1 (correction):** used to be `[hidden]` unless the active Display target was a PDF (shown/hidden via a `window.ArchioskListsThumbnailsSplit.show()`/`.hide()` API) — a product-owner browser review found this meant NO visible Lists/Thumbnails split existed at all on Overview/Investigation/Chat/non-PDF-Document pages (Lists silently filled the whole column, a real reported screenshot defect). The pane is now a PERMANENT structural surface, the exact same correction CLAUDE-P40-EYE1 already made for `eye.panel` relative to Toolbox — content, not visibility, now carries the "nothing to show" case (`lists.thumbnails-pane.empty`, below). When no Document is actively selected on the current page (Overview/an Investigation/Chat/no Project), `static/js/pdf_viewer.js` additionally attempts to populate the pane from a client-side-remembered "last-viewed PDF Document" for this Project+reviewer (`localStorage`, revalidated on every load against the SAME authorized `#workspace-active-sources-data` JSON island every other client-side feature in this shell already trusts — never an arbitrary "first Document in the Project," and a stale/removed/unauthorized remembered id clears itself back to the empty state). Thumbnails are rendered lazily (`IntersectionObserver`) as real per-page `<canvas>` images, not placeholders.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `lists.thumbnails-pane` | `<div>` | "Thumbnails" (header text) | The whole pane; contains the maximize toggle, the empty-state message, and the thumbnail list. Never `[hidden]` (CLAUDE-P40-LTH1) | Every authenticated page | active |
| `lists.thumbnails-pane.maximize` | `<button aria-pressed>` | "Maximize" / "Restore" | Collapses Lists toward its header (keeping whatever proportion was active immediately before, on the toggle-back click) so Thumbnails takes most of the column; a manual toggle, not tied to drag state | Every authenticated page | active |
| `lists.thumbnails-pane.empty` | `<p>` (new, CLAUDE-P40-LTH1) | "Open a Document to view its pages." | Visible by default (server-rendered); hidden by `static/js/pdf_viewer.js`'s `buildThumbnails()` the moment real thumbnails exist, shown again by `clearThumbnails()`. Covers both "no Document selected or remembered" and "the active Document is not a PDF" (a drawing/DOCX/TXT genuinely has no pages) | Every authenticated page | active |
| `lists.thumbnails-pane.list` | `<div role="list">` | (no visible label — `aria-label="Page thumbnails"`) | Contains one real, clickable `<button role="listitem">` per PDF page (`.thumbnail-row`, unregistered as an individual ref — a repeated pattern, not a fixed set of controls, same convention as `lists.project.documents.leaf` etc.); clicking one calls `goToPage(n)` when a Document is actually mounted on this page, or navigates to the Document route (landing on the clicked page) when the thumbnails came from a remembered Document instead (CLAUDE-P40-LTH1); `aria-current="true"` follows the current page from ANY navigation source, with a non-color border-width/label-weight cue alongside the color one | Every authenticated page | active |

## Document Tabs (`templates/case_workspace.html`, CLAUDE-P40-DTAB1)

Documents only (never Investigations/RFIs/Chats/Tasks/Tags/Toolbox/Eye)
— a compact tab strip immediately above the active Document content,
Division-0-only. All server-rendered markup below is deliberately
empty/`[hidden]`; `static/js/document_tabs.js` builds every tab from
`#workspace-active-sources-data` (the same authorized, Project-scoped
JSON island `menu.document-controls`'s own populateDivision already
reads) cross-referenced against this browser's own `localStorage`
(pinned/hidden tabs, per-user per-Project) and `sessionStorage` (the
one replaceable preview tab). A tab is a real `<a href="?source=<id>">`
— activating one is a genuine page navigation, never client-side
routing, so stable URLs and Back/Forward are unaffected. Individual
`.document-tab` elements are a repeated pattern (one per open Document),
not a fixed set of controls — left without their own `data-ui-ref`,
the same convention already established for `.thumbnail-row` (CLAUDE-
P40-VW7A-QA2) and `lists.project.documents.leaf`.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `display.document-tabs` | `<div>` | — | The whole tab strip; `[hidden]` server-side, revealed by JS once at least one real tab (pinned or preview) exists | Only rendered when `project_id`/`workspace` are defined and not `panel_only` | active |
| `display.document-tabs.all-tabs` | `<details>` | "▾" (aria-label "All Tabs") | Contains "Close All Tabs" and the Hidden Tabs list (below) | Same as `display.document-tabs` | active |
| `display.document-tabs.all-tabs.summary` | `<summary>` | "▾" | The `<details>` disclosure trigger | Same as `display.document-tabs` | active |

**Per-tab pattern (JS-generated, one instance per open Document, no individual `data-ui-ref`):**

| Pattern | Element | Current behavior |
|---|---|---|
| `.document-tab` | `<a role="tab" href="?source=<id>">` | The tab itself — real navigation on click/Enter/Space; `aria-selected` reflects the current Document; roving `tabindex` (0 on the focused/active tab, -1 on the rest, Left/Right/Home/End move focus); `aria-label` communicates an alias plus the original Document name (never just the alias — Section 12's own "do not replace the actual Document accessible name"); `data-tab-color` drives the curated accent stripe; `.document-tab-preview` marks the one replaceable preview tab (italic label + dashed active-underline, never color alone) |
| `.document-tab-menu-btn` | `<button>` (per tab) | "⋯" — opens the per-tab context menu (Keep Open/Rename/Restore Original Name/Tab Color/Default Color/Hide/Close/Close Others/Show Original Document Name), only the state-applicable subset shown |
| `.document-tab-close` | `<button>` (per tab) | "×" — closes that one tab's workspace state only, never the Document |
| `.document-tab-rename-input` | `<input>` (transient) | Real inline rename field (never `window.prompt()`), commits on Enter/blur, cancels on Escape; empty/whitespace and duplicate-alias input is rejected with an inline message |
| `.document-hidden-tab-item` | `<button>` (inside `display.document-tabs.all-tabs`) | One row per hidden tab — shows alias + original name, selecting restores it to the visible strip and activates it (real navigation) |

## Investigation Attention Positions (`templates/case_workspace.html`, CLAUDE-P40-VW7B, new)

Up to four Investigations held in attention, the same compact tab-strip
idiom `display.document-tabs` above already established (deliberately
reused — Section 13's own "do not introduce a separate visual
language" — a genuinely distinct control family, never a shared
selector with Document tabs). Division-0-only, present regardless of
the current selection. Server-rendered markup is empty/`[hidden]`;
`static/js/investigation_attention.js` builds every position from
`#workspace-visible-cases-data` (the same authorized, privacy-filtered
`visible_cases_for` list the Lists Investigations branch already
reads) cross-referenced against this browser's own `localStorage`
attention-set (per-user per-Project). "Focused" = whichever Investigation
the current `?case=` already names (no separate persisted concept — the
same reasoning `menu.context.switch-project`'s own note gives for
"Foreground Project" needing no new state, one level down); the
attention SET (which up to four are held, independent of which is
focused) is the one genuinely new piece of client-side state. A
position is a real `<a href="?case=<id>">` — activating one is a
genuine page navigation, never client-side routing. Individual
`.attention-position` elements are a repeated pattern (one per
attended Investigation), left without their own `data-ui-ref`, the
same convention `.document-tab`/`.thumbnail-row`/
`lists.project.documents.leaf` already establish.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `display.attention-positions` | `<div>` | — | The whole strip; `[hidden]` whenever the attention set is empty (Section 8's own "compact, visually restrained" — never a permanent empty bar) | Only rendered when `project_id`/`workspace` are defined and not `panel_only` | active |
| `display.attention-positions.overflow-notice` (CLAUDE-INVESTIGATION-ATTENTION-02, new) | `<div aria-live="polite">` | e.g. "&lt;title&gt; is saved, but 4 Investigations are already pinned to Attention." | Non-blocking, dismissable replacement for the old auto-opening capacity dialog below — shown by post-load reconciliation whenever `?case=` names an Investigation not already in the attention set while it's already at 4, but never prevents this page's own real content (already loaded regardless) from being viewed or worked in. Investigation existence != Investigation currently held in Attention | Same as workspace access | active |
| `display.attention-positions.overflow-notice.pin` (CLAUDE-INVESTIGATION-ATTENTION-02, new) | `<button>` | "Pin this instead…" | Opens `display.attention-positions.capacity-dialog` below (now reached only by this voluntary choice, never automatically) | Same as above | active |
| `display.attention-positions.overflow-notice.dismiss` (CLAUDE-INVESTIGATION-ATTENTION-02, new) | `<button>` | "×" | Hides the notice in place — no navigation, no change to Attention | Same as above | active |
| `display.attention-positions.capacity-dialog` | `<div role="dialog">` | "Attention is full" | CLAUDE-INVESTIGATION-ATTENTION-02: no longer opened automatically by post-load reconciliation — reached only via `display.attention-positions.overflow-notice.pin`'s own voluntary click, once the reviewer decides they DO want to pin the Investigation that's already, legitimately, being shown. Still lets the reviewer Release or Conclude one of the four already-pinned Investigations to make room | Only rendered when `project_id`/`workspace` are defined and not `panel_only` | active |
| `display.attention-positions.capacity-dialog.cancel` | `<button>` | "Cancel" | CLAUDE-INVESTIGATION-ATTENTION-02: just closes the dialog in place now — no navigation. The reviewer was already legitimately viewing this Investigation's own real content before choosing to open this dialog, so cancelling means "don't pin it," never "leave what I was looking at" | Same as above | active |

**Per-position pattern (JS-generated, one instance per attended Investigation, no individual `data-ui-ref`):**

| Pattern | Element | Current behavior |
|---|---|---|
| `.attention-position` | `<a role="tab" href="?case=<id>">` | The position itself — real navigation on click/Enter/Space; `aria-selected` reflects whether this is the focused Investigation; roving `tabindex`, Left/Right/Home/End move focus; a real "Focused" text tag (non-color cue, Section 6) on the focused position, an "Archived" text tag on a frozen one (`CASE_STATUS_ARCHIVED`) |
| `.attention-position-release` | `<button>` (per position) | "×" — releases that Investigation from attention only (Section 8's own explicit "must not delete, close, resolve, archive, or otherwise falsify its real status"); never navigates, even when releasing the currently-focused position |
| `.attention-capacity-dialog-item` | `<li>` (inside the capacity dialog, one per currently-attended position) | Shows the Investigation's title with two real actions: "Release" (pure attention-set change, immediate, no page reload) and "Conclude" (the real, already-existing, owner-or-admin-gated `workspace.archive_case` route — the genuine governed completion action Section 9 requires, not a fabricated "soft close"; no "move to Waiting/Parked" option is offered, since no such governed Case state exists in this repository's actual model — grounded, not omitted by oversight) |

### CLAUDE-P40-VW8 (Governed Display Tab System) audit note

Distinct from the earlier, already-shipped "CLAUDE-P40-VW8"/"CLAUDE-
P40-VW8-QA" stage (Reference Mode completion, Appearance/theme
correction, etc. — see this file's own entries elsewhere and
`CONTINUATION_CHECKPOINT.md`); flagged per this repository's established
tag-collision discipline. This stage added no new, moved, or retired
`data-ui-ref` identifiers — every control above already existed and is
unchanged structurally. What it formalizes, grounded directly in the
repository rather than assumed:

- **The real governed tab-kind vocabulary** is `case_workspace.js`'s own
  `populateDivision(divisionIndex, kind, id, displayName)` dispatch —
  `'source'` (Document), `'case'` (Investigation), `'overview'`, and
  `'new-case'` are the only real kinds. `'files'` is now a documented,
  reserved, no-op kind (a comment only — no branch, no picker entry, no
  placeholder control anywhere) for the future dedicated Files Display
  tab.
- **`display.document-tabs`** (Documents) and **`display.attention-
  positions`** (Investigations) are this app's two real dynamic-record
  tab strips. Neither RFI, Task, nor Tag is a separate Display kind —
  an RFI leaf (`lists.project.rfis.leaf`) routes into its owning
  Investigation (`?case=`); a Task/Task leaf (`lists.project.tasks.leaf`
  / `lists.project.tags.leaf`) routes via `routes/workspace.py`'s own
  `_conversation_source_url` into either the bare workspace URL (Chats)
  or an Investigation's own conversation (`?case=`) with a `#conv-
  source-<id>` scroll anchor — never a `?source=` Document route.
  `lists.project.tools` is Lists-only and never touches Display at all.
- **`display.overview`** and the bare/"Chats" no-selection state are
  this app's two stable surfaces — Project-level singletons with no
  possible duplicate, represented through Lists' own server-rendered
  active-state plus the division header text, deliberately with no
  tab-strip pill of their own (a considered choice, not an omission —
  see this stage's own checkpoint entry for the full reasoning).
- Two small, targeted coherence fixes were made (both purely behavioral
  — no markup/ref changes): `.attention-position` now also activates on
  Space (this table's own row above already documented "click/Enter/
  Space" as its behavior — the code had not actually implemented Space
  until this stage closed that pre-existing doc/code gap); and
  `document_tabs.js`'s close-fallback now also considers an attended
  Investigation before falling back to the empty Display state, so
  "closing the active tab" is coherent across BOTH real dynamic-record
  tab strips, not just within whichever one was closed.

## Display (`templates/case_workspace.html`)

**CLAUDE-P40-VW7B, the `panel_only`/`panel_shell.html` mechanism:**
Division 0 is always the real, server-navigated page (`base.html`'s
full shell) — unchanged. A non-zero division (1-5) that shows a
Document embeds the real file directly (`<iframe src=file_url>`/
`<img>`, unchanged since VW7A). A non-zero division that shows an
Investigation or Overview instead embeds an `<iframe>` pointing back
at the SAME `workspace.show_workspace` route with `&panel=1` appended
(`routes/workspace.py`'s own `panel_only` flag) — `case_workspace.html`
extends `templates/panel_shell.html` (a minimal standalone document:
CSS links, CSRF meta + auto-inject, a `.app-main`-scoped Appearance-
mode script, `{% block content %}`/`{% block extra_scripts %}`) instead
of `base.html` when that flag is set, so the identical Division-0
content renders without Menu/Lists/Toolbox/Chat chrome. No new route,
no duplicated authorization — an unauthorized `?panel=1` request fails
at the exact same `_load_workspace_or_404` call every other view of
this data already goes through. Division 0's own header, Overview's
"← Projects" link, AND the entire divisions-1-5-plus-context-menu
block are all suppressed inside a panel (`{% if not panel_only %}`) —
the first two because the OUTER division already provides equivalent
chrome, the last because it is not merely redundant but actively
recursive if left unguarded: a panel's own `case_workspace.js` instance
would otherwise try to restore/populate its own divisions 1-5 from
`sessionStorage`, which could itself embed another panel iframe. Caught
by `tests/test_p40vw7b_root_system_and_projection.py`'s own structural
assertion before ever shipping, not discovered live.

| Reference | Element | Current behavior | Auth notes | Status |
|---|---|---|---|---|
| `display.divisions` | `#workspace-display-panel` | The whole Display surface — 1-6 divisions (VW4's independent Vertical/Horizontal axes), managed by `window.ArchioskDisplay` (`case_workspace.js`) | Same as workspace access | active |
| `display.division` | `.display-division` (pattern — division 0 is always server-rendered/real navigation; divisions 1-5 are client-side-only slots) | Shows whichever record is currently projected into it. **CLAUDE-P40-VW7B:** divisions 1-5 can now show a Document (real file, unchanged), an Investigation, or Overview (both via the `panel_only` iframe mechanism above) — previously Documents only | Division 0: same as workspace access. Divisions 1-5: a Document can only ever be one already in `active_sources` (enforced by `workspace.source_file`); an Investigation/Overview goes through the identical `_load_workspace_or_404`/`visible_cases` checks `?panel=1` shares with every other view | active |
| `display.division.picker` | `<select>` (pattern, divisions 1-5 only) | "Open a Document here…" — populates that division client-side via `window.ArchioskDisplay.populateDivision`. **Still Documents-only** — not extended to list Investigations this stage (a considered, deferred enhancement; the Lists-leaf-click path above is what VW7B's own prompt asked for, not this picker) | Options limited to `active_sources` | active |
| `display.division.close` | `<button>` (pattern, divisions 1-5 only) | Clears that division (any kind — Document, Investigation, or Overview), shrinks the Vertical/Horizontal layout by one (VW4's deterministic shrink rule) | — | active |
| `display.overview` | `#project-overview` | The Overview leaf's actual content: Operating Environment, Access/Settings, Needs Attention, Recent Focus, Investigation Quality, Participants, Go/No-Go, Accepted Knowledge, Instructions, RFIs, Key Dates, History — all consolidated under this one leaf (CLAUDE-P40-E3A, Section 5). **CLAUDE-POSTCAMEL-ROOT-I1:** Requirement Compliance/Requirements relocated to their own `display.requirements` leaf (canonical root branch 3) — this accordion is now a short projection pointer only (`display.overview.requirements-link`). **CLAUDE-POSTCAMEL-ROOT-I3:** the inner Project Operating Environment + Access pair is now tagged `data-root-branch="1.3" data-root-label="Access &amp; Operating Environment"` — canonical root branch 1.3 (ROOT-A1's "Access & Governance"); "Governance" was found to oversell what's actually there (plain access control + a one-time environment lock, no procurement-governance content exists to show), so the corrected label is attribute-only, no new heading text — both sections already describe themselves accurately on their own. Its own "← Projects" back-link is suppressed when rendered inside a panel (CLAUDE-P40-VW7B) | Same as workspace access | active |
| `display.overview.requirements-link` (CLAUDE-POSTCAMEL-ROOT-I1, new) | `<a>` | "Open Requirements →" | Real cross-page navigation to `?view=requirements` — replaces the old same-page `#governed-requirements` anchor, which stopped working once the content it pointed at moved off this page | Same as workspace access | active |
| `display.overview.files-link` (CLAUDE-POSTCAMEL-META-T01, new) | `<a>` inside the new "Project Territory" accordion | "Open Files →" | Territory-Before-Ontology: a projection pointer to `?view=files` placed before Needs Attention/governed-intelligence content, so a reviewer sees an honest count of their own registered material first, same relocation-not-reimplementation shape as `display.overview.requirements-link` | Same as workspace access | active |
| `display.requirements` (CLAUDE-POSTCAMEL-ROOT-I1, new) | `.requirements-surface` | Requirements' own stable Display surface (canonical root branch 3) — candidate-item promotion, Requirement registration, the governed Requirements list with adjudication/revision/perspective — a RELOCATION of markup that previously rendered unconditionally inside Overview (two separate accordions there, an existing duplication ROOT-A1's own audit flagged), not a reimplementation. Same shape as `display.files` (back-link + heading + panel-suppression) | Same as workspace access | active |
| `display.requirements.compliance` (CLAUDE-POSTCAMEL-ROOT-I2, new) | `.compliance-rollup` | Requirements branch 3.3 — a project-level Compliance rollup, computed as a pure projection over the existing `compliance_rollup`/`requirement_adjudication_state` values (no new canonical status field). Two sentences, kept deliberately separate: "N Requirement(s) need attention" (Not Satisfied/Partially Satisfied) and "N awaiting review" (Not Yet Assessed, explicitly labeled "not a finding of non-compliance"). Each links via a real `?status=` query param into the Governed Requirements list below (`requirements_view_display`, server-side filtered, no new JS) | Same as workspace access | active |
| `display.requirements.adjudication-attribution` (CLAUDE-POSTCAMEL-COMM-I5A, new) | `<span class="review-state-badge">` (pattern, one per rendered adjudication — latest-line and history-list both) | "Human-reviewed" / "Agent assessment" / "Unknown/legacy provenance" | Resolved read-time via `resolve_requirement_adjudication_attribution` — never stored on the record beyond the real `attribution` field itself; a pre-COMM-I5A record with no `attribution` resolves honestly to "Unknown/legacy provenance" unless it is one of the four specific, governance-documented COMM-I3 ids in `LEGACY_AGENT_ATTRIBUTED_ADJUDICATION_IDS` | Same as workspace access | active |
| `display.requirements.admin-phase-assessment` (CLAUDE-GO-QAC-01, new) | `<p>` (pattern, one per Requirement row) | outcome badge + reasoning | The latest `RequirementPhaseAssessment` for this Requirement — "GO must not judge a 30% submission by 90% expectations"; a provisional, GO-authored signal, never itself an adjudication | `is_admin` only | active |
| `display.requirements.admin-assess-phase-form` | `<form>` (pattern) | (assessment trigger) | Posts to `workspace.assess_requirement_phase_route` | Same as above | active |
| `display.requirements.admin-assess-phase-submit` | `<button>` (pattern) | "Assess phase conformance" | Submits the form | Same as above | active |
| `display.spin` (CLAUDE-SPIN-SURFACE-02, new) | `.spin-surface` | Spin's own stable Display surface (STABLE_DIRECTORY_KINDS' new `"spin"` entry) — the longitudinal Spin history / State Report view. Same shape as `display.requirements`/`display.files` (back-link + heading + panel-suppression). Reads `spin_runs_view`/`spin_state_report`/`selected_spin_run_view`, a relocation of what previously rendered only inline in `toolbox.spin` (see that row's own retirement note), not a reimplementation — `SpinRun`/`ComposerFinding` are completely untouched | Same as workspace access | active |
| `display.spin.active` | `<p>` | "A Spin is currently running…" — rendered while `spin_generation_in_progress` is true; the page still shows the last completed run underneath, never a blocking state | Same as workspace access | active |
| `display.spin.empty` | `<p>` | "No Spin has run yet for this project…" — rendered when `spin_runs_view` is empty | Same as workspace access | active |
| `display.spin.history` | `<div>` | The chronological run list — every `SpinRun`, newest first, each linking to `?view=spin&spin_run=<id>` | Same as workspace access | active |
| `display.spin.history.leaf` (pattern, one per run) | `<p>` | "<kind> — <date> · N finding(s)" or "failed" | Links to that run's own State Report; the currently-viewed run is visually distinguished (bold), never hidden or removed when a later run exists | Same as workspace access | active |
| `display.spin.report` | `<div>` | The selected run's Spin State Report — id, completion time, baseline link, evidence delta, classification summary, findings | Derived at render time from persisted `SpinRun`/`ComposerFinding` data only — no new persisted object | Same as workspace access | active |
| `display.spin.report.finding` (pattern, one per finding) | `<details>`/`<summary>` (raw, CLAUDE-BAUHAUS-CONSTRUCTIVIST-UI-01 — no longer `macros.subdisclosure`) | One finding's own summary ("F-00N — tag"); expands to Source/Concern/Unresolved question/Reassesses. Rendered in `spin_state_report.presented_findings` order (`routes/workspace.py`'s `_SPIN_CLASSIFICATION_TREATMENT` rank, derived from the finding's own `delta_classification`, not insertion order); `class` carries a `spin-weight-*` tier, `open` defaults on for the max/high tiers, and an inline accent bar (`border-left`) is set from the same per-classification treatment table — the classification value drives composition, never a second, model-generated signal | Same as workspace access | active |
| `display.spin.report.finding.classification` | `<span>` | The finding's own `delta_classification`, only rendered when set (never for a first_spin finding); text is always the literal, full classification value — composition treatment is presentation-only and never substitutes for it | Same as workspace access | active |
| `display.spin.report.world` (new, CLAUDE-HOLODECK-WORLDS-SPIN-01) | `<p>` | "World: Survival Mode" + "Objective: <product-defined text>" | Only rendered when `SpinRun.world` is set; objective text is `services.spin.SPIN_WORLD_OBJECTIVES`, looked up not duplicated | Same as workspace access | active |
| `display.spin.report.games-played` (new, CLAUDE-HOLODECK-WORLDS-SPIN-01) | `<div>` | "Games Played" section | Only rendered when the run's own `games_played` list is non-empty (World runs only) - an inspectable, model-self-reported trace, never hidden reasoning, never a second real call | Same as workspace access | active |
| `display.spin.report.games-played.leaf` (pattern, one per move) | `<p>` | "<game> — triggered by: <...> → <finding tag>" | Same as workspace access | active |
| `display.spin.report.helix-qa` (new, CODEX-HELIX-QA-ABSORPTION-01) | `<div>` | "Progressive Helix QA" interface-assessment section | Only rendered when the selected `SpinRun` has defensively parsed `helix_assessments`; keeps expected fit, observed assessment, consequence, and evidence sufficiency distinct | Same as workspace access | active |
| `display.spin.report.helix-qa.interface` (pattern, one per interface) | `<details>`/`<summary>` | One interface-specific Horizontal/Longitudinal Helix assessment | Expands to strands, project-evidenced maturity/expectation, dimensional relationship class, observed evidence, assessment, consequence, uncertainty, follow-on game, and governed question; never an aggregate health score | Same as workspace access | active |
| `display.spin.report.helix-qa.evidence` (pattern, one per observed source) | `<p>` | Source/revision/region/value/confidence supporting one interface assessment | Model-supplied evidence trace is defensively bounded and stored on the originating `SpinRun`; missing or insufficient evidence remains explicit | Same as workspace access | active |
| `display.files` | `.files-surface` (CLAUDE-P40-VW9, new) | The Files leaf's actual content — the two governed sibling roots below | Same as workspace access | active |
| `display.files.data-room` | `.files-root-data-room` | "Data Room" | Controlled, project-level root, real hierarchy (CLAUDE-RFP27-TERRITORY-01). Design-Builder Workspace's own folder routes still cannot reach `FOLDER_ROOT_DATA_ROOM` at all (structural, not merely a missing button) — its only mutation paths are the two admin-gated forms in `.admin-actions` below, never ordinary folder editing | Same as workspace access | active |
| `display.files.data-room.breadcrumb` | `<p>` | Data Room › ⟨open folder ancestors⟩ | Same derived-never-stored idiom as `display.files.design-builder.breadcrumb`, against the Data Room root's own separate `?data_room_folder=` param | Same as workspace access | active |
| `display.files.data-room.folder-list` | `<ul>` | Real Data Room folders/Sources at the open level | Rendered when the open Data Room folder has child folders and/or Sources — child folders link via `?data_room_folder=<id>`; Sources link the same way Lists' own Documents leaf does | Same as workspace access | active |
| `display.files.data-room.folder-row` | `<li>` | 📁 ⟨folder name⟩ | One per child folder inside `.folder-list` | Same as workspace access | active |
| `display.files.data-room.folder-empty` | `<p>` | "This folder is empty." | Rendered only when a real, open Data Room folder has zero child folders and zero Sources — proves "exists and is empty" honestly, distinct from `.empty` below (root-level, no folders registered at all) | Same as workspace access | active |
| `display.files.data-room.compatibility-list` | `<ul>` | Existing Document names (pattern, same `source-item`/`<a href=?source=<id>>` shape `lists.project.documents.leaf` already uses) | Root-level only (`open_data_room_folder` false) — every active Source not yet assigned a `folder_id` (`data_room_unfiled_sources`), honestly labeled as pre-Files/not-yet-organized, never reclassified or duplicated | Same as workspace access | active |
| `display.files.data-room.empty` | `<p>` | "No Documents yet…" | Root-level only, rendered when there are no unfiled Sources AND no Data Room folders registered yet | Same as workspace access | active |
| `display.files.data-room.admin-actions` | `<details>` (via `macros.subdisclosure`) | "Data Room admin actions" | Admin-only (`is_admin`), root-level only. One combined disclosure (not two) — real-browser click-reachability testing found two separate always-visible `<summary>` lines here pushed Design-Builder's own folder-menu triggers below the fixed Playwright viewport further down the page; merged to keep added collapsed height to one line | Admin only | active |
| `display.files.data-room.reconcile.form` | `<form>` | — | POSTs to `reconcile_data_room_route` — real `webkitdirectory` folder-select reconciliation, **analyze-first as of CLAUDE-DATA-ROOM-RECONCILE-01**: classifies every file (unchanged/new/modified/missing/renamed/ineligible/ambiguous) and stages the result, never registers anything in this same request — see `.reconcile.report`/`.reconcile.confirm` below for the separate approval step | Admin only | active |
| `display.files.data-room.reconcile.picker` | `<input type="file" webkitdirectory>` | — | The folder picker itself | Admin only | active |
| `display.files.data-room.reconcile.submit` | `<button>` | "Reconcile" | Submits `.reconcile.form` | Admin only | active |
| `display.files.data-room.reconcile.report` (new, CLAUDE-DATA-ROOM-RECONCILE-01) | `<div>` | Compact summary line ("N source file(s) compared — N already registered…") | Rendered inline in the same `.admin-actions` disclosure once `?reconcile_preview=<staging_id>` resolves to a real, still-valid staged comparison for THIS project (re-validated server-side, never trusted as an authorization boundary) — never a second, competing Reconcile page | Admin only | active |
| `display.files.data-room.reconcile.report.new`, `display.files.data-room.reconcile.report.modified`, `display.files.data-room.reconcile.report.missing`, `display.files.data-room.reconcile.report.renamed`, `display.files.data-room.reconcile.report.ineligible`, `display.files.data-room.reconcile.report.ambiguous` (new, CLAUDE-DATA-ROOM-RECONCILE-01) | `<details>` (via `macros.subdisclosure`, 6 distinct sections) | e.g. "New (1)" | One expandable section per non-empty exceptional classification — `unchanged` is deliberately a count only in the summary line, never its own list ("avoid flooding… emphasize exceptions, not unchanged inventory") | Admin only | active |
| `display.files.data-room.reconcile.confirm` (new, CLAUDE-DATA-ROOM-RECONCILE-01) | `<button>` (form submit) | "Add N new document(s)" | Only rendered when at least one file was classified `new`. POSTs to `reconcile_data_room_confirm_route` — the one, explicit, human-approved action a preview can trigger; reuses `reconcile_data_room_upload` unchanged for the actual registration, never touches modified/missing/renamed/ineligible/ambiguous items | Admin only | active |
| `display.files.data-room.reconcile.discard` (new, CLAUDE-DATA-ROOM-RECONCILE-01) | `<button>` (form submit) | "Discard" | POSTs to `reconcile_data_room_discard_route` — abandons the preview without registering anything (nothing was ever mutated by the preview itself) | Admin only | active |
| `display.files.data-room.register-paths.form` | `<form>` | — | POSTs to `register_folder_paths_route` — explicit empty-folder registration (Part 3/4), the one case a file upload can never report (browsers never emit a `File` for an empty directory); one relative path per line, explicit `root` choice (Data Room or Design-Builder), never inferred | Admin only | active |
| `display.files.data-room.register-paths.paths` | `<textarea>` | — | One relative folder path per line | Admin only | active |
| `display.files.data-room.register-paths.submit` | `<button>` | "Register" | Submits `.register-paths.form` | Admin only | active |
| `display.files.design-builder` | `.files-root-design-builder` | "Design-Builder Workspace" | Editable, team-organized root — the one root every folder route below can mutate | Same as workspace access | active |
| `display.files.design-builder.breadcrumb` | `<p>` | Design-Builder Workspace › ⟨open folder ancestors⟩ | Derived at read time from `CaseWorkspaceStore._folder_path` (never a stored path string); each ancestor is a real link to `?view=files&folder=<id>`. Distinct from the top application breadcrumb (`menu.context`), which stays "Files" regardless of which folder is open, the same way `display.overview`'s own internal accordions never change it either | Same as workspace access | active |
| `display.files.design-builder.new-folder` | `<details>` (via `macros.subdisclosure`) | "+ New Folder" | Contains the create-folder form below | Same as workspace access | active |
| `display.files.design-builder.new-folder.name` | `<input type="text">` | Folder name | POSTs to `create_folder_route` (`parent_folder_id` = the currently open folder, or empty for root) | Same as workspace access | active |
| `display.files.design-builder.new-folder.create` | `<button>` | "Create Folder" | Submits the form above | Same as workspace access | active |
| `display.files.design-builder.folder-list` | `<ul>` | — | Contains one `display.files.design-builder.folder-row` per child folder of the currently open folder (or the root) | Same as workspace access | active |
| `display.files.design-builder.folder-row` | `<li>` (pattern) | Folder name | Real navigation to `?view=files&folder=<id>` — drills into that folder. Contains the contextual action menu below | Same as workspace access | active |
| `display.files.design-builder.folder-row.options` | `<summary>` (pattern, `<details>`) | "⋮" (aria-label "Folder options for `<name>`") | Opens the contextual Rename/Move/Delete panel for that row | Same as workspace access | active |
| `display.files.design-builder.folder-row.rename.name`, `display.files.design-builder.folder-row.rename.submit` | `<input>`/`<button>` (pattern) | Rename form | POSTs to `rename_folder_route` — sibling-name uniqueness enforced server-side | Same as workspace access | active |
| `display.files.design-builder.folder-row.move.destination`, `display.files.design-builder.folder-row.move.submit` | `<select>`/`<button>` (pattern) | Move form | POSTs to `move_folder_route` — the `<select>`'s own options are server-computed per row (`design_builder_move_targets`) to exclude the folder itself and its own descendants, on top of `move_folder`'s independent, authoritative re-validation | Same as workspace access | active |
| `display.files.design-builder.folder-row.delete` | `<button>` (pattern) | "Delete" | POSTs to `delete_folder_route` — the confirm=yes/no gate below, never an immediate delete | Same as workspace access | active |
| `display.files.design-builder.empty` | `<p>` | "This folder is empty."/"No folders yet…" | Empty-state message, phrased differently for an open subfolder vs. the Design-Builder Workspace root itself | Same as workspace access | active |
| `display.files.design-builder.folder.delete.confirm-yes`, `display.files.design-builder.folder.delete.confirm-no` | `<button>` (`templates/confirm_delete_folder.html`) | "Yes — delete this folder" / "No — keep this folder" | The confirm=yes/no interruption page (same family as `confirm_remove_document.html`) — only reachable after `delete_folder_route`'s own empty-folder check would otherwise pass | Same as workspace access | active |
| `display.context-menu` | `#display-context-menu` (CLAUDE-P40-VW8-QA — formally registered; existed since CLAUDE-P40-E3A) | Right-click menu for the targeted division — Close/Divide | Hidden by default; opens via `contextmenu` on a `.display-division`, targets whichever one was clicked | Same as workspace access | active |
| `display.context-menu.close` | `<button>` | "Close this Display" | Clears the targeted division, reflows remaining divisions (VW4's deterministic shrink rule) | Same as workspace access | active |
| `display.context-menu.vertical-decrement`, `display.context-menu.vertical-increment`, `display.context-menu.horizontal-decrement`, `display.context-menu.horizontal-increment` | `<button>` (4 distinct, named controls) | −/+ steppers | Adjust the PENDING Vertical/Horizontal count for "Divide this Display" | Same as workspace access | active |
| `display.context-menu.apply` | `<button>` | "Apply" | Commits the pending Vertical × Horizontal count | Same as workspace access | active |

## Toolbox (`templates/case_workspace.html`)

| Reference | Element | Current behavior | Auth notes | Status |
|---|---|---|---|---|
| `toolbox.panel` | `#workspace-toolbox-panel` (`<aside>`, `templates/base.html`) | The panel container itself — always present within an open Workspace, empty elsewhere. CLAUDE-P40-EYE1: no longer owns its own width/background/scroll (moved to `#workspace-right-column`, below) — the upper pane inside that column, sized by the Toolbox/Eye divider | Only rendered when `project_id`/`workspace` are defined | active |
| `toolbox.maximize` | `<button aria-pressed>` (new, CLAUDE-P40-EYE1) | "Maximize Toolbox" / "Restore" | Expands Toolbox toward the right column's practical maximum (keeping whatever proportion was active immediately before, on the toggle-back click); floats over the pane's own top-right corner rather than a new header row, since `toolbox.heading` already occupies that position | Only rendered when `project_id`/`workspace` are defined | active |
| `toolbox.compare` (CLAUDE-EYE-COMPARE-01, new) | `<button aria-pressed>` (`templates/base.html`'s own shared Toolbox chrome, not `case_workspace.html`'s conditional `{% block toolbox %}` branches) | "Compare" | Compact toggle, floats beside `toolbox.maximize`. Persisted per-Project (`beehive:panel:compare-active:<project_id>`) — "remains active... until changed/cancelled," not a one-time click. While active AND Eye is empty, `eye.drop-target`'s own empty-state text becomes "Choose a document to compare." (`static/js/eye_pane.js`'s own `updateEmptyStateText`); available regardless of what's currently selected, so Document A can stay open in Display while a Document B is sent to Eye via `lists.project.documents.send-to-eye` | Only rendered when `project_id`/`workspace` are defined | active |
| `toolbox.eye-detached-status` (CLAUDE-DUAL-DOCUMENT-FOCUS-01 addendum, new) | `<button>` (`templates/base.html`'s own shared Toolbox chrome) | "Eye is open in another window · Bring Eye Back" | `hidden` unless Eye is currently popped out to a detached window; compact status/return affordance, floats beside `toolbox.compare` | Only rendered when `project_id`/`workspace` are defined | active |
| `toolbox.eye-thumbnails.heading` (CLAUDE-DUAL-DOCUMENT-FOCUS-01, new) | `<h2>` | "Eye · Pages" — static | Only visible while `toolbox.eye-thumbnails.list` itself is showing (Eye holds a paginated PDF) | active |
| `toolbox.eye-thumbnails.empty` (CLAUDE-DUAL-DOCUMENT-FOCUS-01, new) | `<p>` | "Eye has no paginated document open." | Reuses `lists.thumbnails-pane.empty`'s own `.thumbnails-empty-state` styling verbatim; toggled by `static/js/pdf_viewer.js`'s own per-surface `buildThumbnails`/`clearThumbnails` | active |
| `toolbox.eye-thumbnails.list` (CLAUDE-DUAL-DOCUMENT-FOCUS-01, new) | `<div role="list">` | Page thumbnails for whichever document is currently open in Eye — independent of `lists.thumbnails-pane.list` (Main's own). Toolbox swaps to this panel (hiding its own ordinary tool content, `toolbox.project-intelligence`/`toolbox.document`/`toolbox.investigation-findings`) whenever Eye holds a paginated PDF; returns to normal the moment Eye is cleared or holds a non-paginated format | Same access as Toolbox/Eye | active |
| `toolbox.heading` | `<h2>` | "Toolbox" — static | — | active |
| `toolbox.investigation-findings` | `<section>` | Rendered when an Investigation is the active selection (`active_case`) — Findings list, artifacts, RFI actions | Findings filtered to `findings_view` (already access/visibility-scoped) | active |
| `toolbox.document` | `<section>` | Rendered when a Document is the active selection (`selected_source`) — Document-level tools | Same as workspace access | active |
| `toolbox.empty` | *(retired — CLAUDE-GO-DNA-01)* | Replaced by `toolbox.project-intelligence` below - the bare neutral empty state no longer exists; the "nothing selected" Toolbox view now always shows the Project Intelligence section (Requirements/Investigations/RFI/Work Products/Conversation/Tasks/Tags, relocated from Lists), with Composer Findings appearing conditionally at its top. See `governance/current/go-dna-01-composer-result-contract-and-panel-zoning.md`. | — | retired |
| `toolbox.project-intelligence` | `<section>` (CLAUDE-GO-DNA-01, Panel Zoning) | Rendered when neither an Investigation nor a Document is selected - the Toolbox's own "nothing selected" home view. Contains, in order: Composer Findings (conditional - see `toolbox.composer-findings` below), then Requirements/Investigations/RFI Correspondence/Work Products/Conversation/Tasks/Tags/Project Administration, each relocated verbatim (same routes/forms/element ids) from Lists' own former per-project family branch (`base.html`) | Same as workspace access; each sub-section further scoped identically to its pre-relocation Lists equivalent (e.g. Investigations still filtered to `visible_cases`) | active |
| `toolbox.spin` | `<div>` (CLAUDE-DELTA-SPIN-01) | Rendered inside `toolbox.project-intelligence`, first (ahead of `toolbox.composer-findings`) — "Run First Spin"/"Run Delta Spin" triggers, a one-line latest-run summary, and a link into the full Spin tab. See `services/spin.py` and `governance/specified-unbuilt/spin-project-intelligence-preview.md` | `spin_runs_view` is project-scoped (`workspace.spin_runs`), already isolated by `CaseWorkspaceStore`'s own per-project registry path | active |
| `toolbox.spin.in-progress` | `<p>` | "A Spin is currently running…" — rendered instead of the trigger buttons while `store.spin_generation_in_progress_for` is true (duplicate-call/idempotency guard) | Same as workspace access | active |
| `toolbox.spin.run-first` | `<button>` (form POST) | "Run First Spin" — POSTs to `workspace.run_spin_route` with `spin_kind=first_spin` | Same as workspace access; gated by `ACTION_EXTERNAL_AI_REQUEST` security policy | active |
| `toolbox.spin.run-delta` | `<button>` (form POST) | "Run Delta Spin" — POSTs to `workspace.run_spin_route` with `spin_kind=delta_spin`; only rendered once at least one completed First Spin exists (`has_first_spin`) | Same as workspace access; gated by `ACTION_EXTERNAL_AI_REQUEST` security policy | active |
| `toolbox.spin.empty` | `<p>` | "No Spin runs yet." — rendered when `spin_runs_view` is empty | Same as workspace access | active |
| `toolbox.spin.world-survival` (new, CLAUDE-HOLODECK-WORLDS-SPIN-01; pattern, one per trigger form) | `<input type=checkbox name=world value=survival>` | "Survival Mode" | Optional - unchecked, ordinary Spin is completely unaffected; checked, the SAME `run_spin_route`/`run_spin` call is framed with Survival Mode's own attention priorities (`services/spin.py`'s `SPIN_WORLD_SURVIVAL`) | Same as workspace access; gated by the same `ACTION_EXTERNAL_AI_REQUEST` policy as the trigger button it belongs to | active |
| `toolbox.spin.latest-summary` (new, CLAUDE-SPIN-SURFACE-02) | `<p>` | "Latest: <kind> — <date> · N finding(s)" — rendered only when `spin_runs_view` is non-empty, the shrunk replacement for the full inline run list below | Same as workspace access | active |
| `toolbox.spin.history-link` (new, CLAUDE-SPIN-SURFACE-02) | `<a>` | "View Spin History →" — real navigation to `?view=spin` (see `display.spin` below) | Same as workspace access | active |
| `toolbox.spin.run` | *(retired — CLAUDE-SPIN-SURFACE-02)* | Relocated to `display.spin.history.leaf`/`display.spin.report` on the new Spin tab — see those rows below or `governance/current/` for the implementation record once committed. Nothing renders this reference in the Toolbox any more; the Toolbox keeps only the trigger buttons and `toolbox.spin.latest-summary`. | — | retired |
| `toolbox.spin.run.finding` | *(retired — CLAUDE-SPIN-SURFACE-02)* | Relocated to `display.spin.report.finding` below, same reason as `toolbox.spin.run` above. | — | retired |
| `toolbox.spin.run.finding.classification` | *(retired — CLAUDE-SPIN-SURFACE-02)* | Relocated to `display.spin.report.finding.classification` below, same reason as `toolbox.spin.run` above. | — | retired |
| `toolbox.composer-findings` | `<div>` (CLAUDE-GO-RIGHT-PANEL-01) | Rendered inside `toolbox.project-intelligence`, after `toolbox.spin`, only when this project has at least one Composer-emitted `ComposerFinding` NOT produced by a Spin run (`composer_findings_view`) — a compact "F-001 — tag" list, one row per finding | `composer_findings_view` is project-scoped (`workspace.composer_findings`), already isolated by `CaseWorkspaceStore`'s own per-project registry path | active |
| `toolbox.composer-findings.leaf` | `<details>`/`<summary>` (`macros.subdisclosure`) | One finding's own summary ("F-00N — tag"); expands in place to Source/Concern/Unresolved question/Urgency/Project stage — light on first touch, deep on demand, never auto-expanded | Same as workspace access | active |
| `toolbox.composer-findings.leaf.open-source` | `<a>` | "Open supporting source document" — only rendered when the project has an active Source; real navigation to `?source=<id>` (the project's active founding Source), never an auto-snap — only fires on an explicit click inside an already-expanded `toolbox.composer-findings.leaf` | Same as workspace access | active |
| `toolbox.requirements` | `<a>` (CLAUDE-GO-DNA-01, relocated from `lists.project.requirements`) | "Requirements (`<count>`)" | Real navigation to `?view=requirements` — same destination, unchanged | Same as workspace access | active |
| `toolbox.conversation` | `<a>` (CLAUDE-GO-DNA-01, relocated from `lists.project.chats`) | "Conversation (`<count>`)" | Real navigation to the bare workspace URL — same destination, unchanged | Same as workspace access | active |
| `toolbox.investigations` | `<summary>` (`macros.subdisclosure`, CLAUDE-GO-DNA-01, relocated from `lists.project.investigations`) | "Investigations (`<count>`)" | Collapsed by default; expands to `.new`/`.continue-from-archive`/`.leaf` rows | Same as workspace access, filtered to `visible_cases` | active |
| `toolbox.investigations.new` | `<a>` (relocated from `lists.project.investigations.new`) | "Start New Investigation" | Real navigation to `?view=new-case` — same destination, unchanged | Same as workspace access | active |
| `toolbox.investigations.continue-from-archive` | `<a>` (relocated from `lists.project.investigations.continue-from-archive`) | "Continue from Archive" | Real navigation to `?view=continue-from-archive` — same destination, unchanged | Same as workspace access | active |
| `toolbox.investigations.leaf` | `<a>` (pattern, relocated from `lists.project.investigations.leaf`) | Investigation title | Real navigation to `?case=<id>` — same destination, unchanged | Same as `visible_cases` | active |
| `toolbox.investigations.leaf.archive` | `<a>` (relocated from `lists.project.investigations.leaf.archive`) | "Archive" | Real navigation to `workspace.confirm_archive_case` — same destination, unchanged | Same authority as before relocation | active |
| `toolbox.investigations.empty` | `<p>` (relocated from `lists.project.investigations.empty`) | "No Investigations yet." | Empty-state message | Same as `visible_cases` | active |
| `toolbox.rfi` | `<summary>` (`macros.subdisclosure`, relocated from `lists.project.rfis`) | "RFI Correspondence (`<count>`)" | Collapsed by default; expands to `.leaf` rows, each targeting its owning Investigation exactly as before | Same as workspace access | active |
| `toolbox.rfi.leaf` | `<a>` (pattern, relocated from `lists.project.rfis.leaf`) | RFI question text (truncated) | Real navigation to `?case=<id>` — same destination, unchanged | Same as workspace access | active |
| `toolbox.rfi.empty` | `<p>` (relocated from `lists.project.rfis.empty`) | "No RFIs yet." | Empty-state message | Same as workspace access | active |
| `toolbox.work-products` | `<summary>` (`macros.subdisclosure`, relocated from `lists.project.work-products`) | "Work Products (`<count>`)" | Collapsed by default; expands to the inline creation form plus `.leaf` rows | Same as workspace access | active |
| `toolbox.work-products.new` | `<button>` (inline `<form>` submit, relocated from `lists.project.work-products.new`) | "+ New Work Product" | Posts to `workspace.create_work_product`, unchanged | Same as workspace access | active |
| `toolbox.work-products.leaf` | `<a>` (relocated from `lists.project.work-products.leaf`) | Work product title + `(state)` | Real navigation to `?work_product=<id>` — same destination, unchanged | Same as workspace access | active |
| `toolbox.work-products.empty` | `<p>` (relocated from `lists.project.work-products.empty`) | "No Work Products yet." | Empty-state message | Same as workspace access | active |
| `toolbox.tasks` | `<summary>` (`macros.subdisclosure`, relocated from `lists.project.tasks`) | "Tasks (`<total>`)" | Collapsed by default; contains `.open`/`.completed` sub-groups. `id="lists-tasks-*"` element ids UNCHANGED - `static/js/case_workspace.js`'s live-update code (`patchTasksListOnCreate`) finds them via `document.getElementById`, position-agnostic, so relocation needed no JS change | Same as workspace access | active |
| `toolbox.tasks.open` | sub-heading `<p>` (relocated from `lists.project.tasks.open`) | "Open (`<count>`)" | Plain grouping label | — | active |
| `toolbox.tasks.completed` | sub-heading `<p>` (relocated from `lists.project.tasks.completed`) | "Completed (`<count>`)" | Plain grouping label | — | active |
| `toolbox.tasks.leaf` | `<a>`/`<span>` (pattern, relocated from `lists.project.tasks.leaf`) | Task title | Real navigation to the same source URL — unchanged | Same as workspace access | active |
| `toolbox.tasks.complete` | `<label>` wrapping a checkbox (relocated from `lists.project.tasks.complete`) | unchecked square | Same `complete_task_route` POST, unchanged | Same as workspace access | active |
| `toolbox.tasks.reopen` | `<label>` wrapping a checkbox (relocated from `lists.project.tasks.reopen`) | checked square | Same `reopen_task_route` POST, unchanged | Same as workspace access | active |
| `toolbox.tasks.open.empty` | `<p>` (relocated from `lists.project.tasks.open.empty`) | "No open Tasks." | Empty-state message | Same as workspace access | active |
| `toolbox.tasks.completed.empty` | `<p>` (relocated from `lists.project.tasks.completed.empty`) | "No completed Tasks." | Empty-state message | Same as workspace access | active |
| `toolbox.tags` | `<summary>` (`macros.subdisclosure`, relocated from `lists.project.tags`) | "Tags (`<total occurrences>`)" | Collapsed by default; contains one `.group` per tag in use. `id="lists-tags-*"`/`data-tag-group`/`data-tag-occurrence-id` UNCHANGED - `patchTagsListOnRemove` finds them the same way, position-agnostic | Same as workspace access | active |
| `toolbox.tags.group` | sub-heading `<p>` (pattern, relocated from `lists.project.tags.group`) | tag name + color swatch + occurrence count | Plain grouping label; always expanded | — | active |
| `toolbox.tags.leaf` | `<a>`/`<span>` (pattern, relocated from `lists.project.tags.leaf`) | quoted passage (truncated) | Real navigation to the same source URL — unchanged | Same as workspace access | active |
| `toolbox.tags.remove` | `<button>` in a `<form>` (relocated from `lists.project.tags.remove`) | "Remove" | `fetch()` POST to `remove_tag_occurrence_route`, live-patches counts/DOM without reload — unchanged | Same as workspace access | active |
| `toolbox.tags.empty` | `<p>` (relocated from `lists.project.tags.empty`) | "No Tags yet." | Empty-state message | Same as workspace access | active |
| `toolbox.spin-launcher` (new, CLAUDE-SPIN-00A; text renamed CLAUDE-SPIN-SURFACE-02) | `<summary>` (`macros.subdisclosure`) | "Evidence Isolation (legacy prototype, unrelated to Spin)" | Collapsed by default; contains `.open` only — a prototype-stage entry point, unrelated to the real `toolbox.spin`/`display.spin` Spin capability above (renamed specifically to prevent that confusion) | Same as workspace access | active |
| `toolbox.spin-launcher.open` (new, CLAUDE-SPIN-00A; text renamed CLAUDE-SPIN-SURFACE-02) | `<a>` | "Open Prototype" | Navigates to `?spin=1` on this same authorized workspace URL — no new authorization surface | Same as workspace access | active |
| `toolbox.project-admin` | `<summary>` (`macros.subdisclosure`, new, CLAUDE-GO-DNA-01) | "Project Administration" | Collapsed by default; contains `.remove-project` only - a project-lifecycle/governed action, not file-territory, so it moved here rather than staying among Lists' own file-adding controls | **Owner or admin** — `is_project_owner or is_admin` (the `{% if %}` gate itself, not merely the route) | active |
| `toolbox.project-admin.remove-project` | `<button>` in a `<form>` (relocated from `lists.project.tools.remove-project`) | "Remove Project" | Same `remove_project_route` POST (Approval-Gate `confirm=yes\|no` vocabulary), unchanged | **Owner or admin** — `is_project_owner or is_admin` | active |
| `toolbox.project-admin.publish` (new, CLAUDE-RFP-BOUNDARY-01) | `<summary>` (`macros.subdisclosure`) | "Publish Procurement Package" | Only rendered when `can_publish_procurement_package` is true (`CLIENT_OWNER` project, still `pre_publication`) — the Owner-side "Publish RFP" action; a governed, one-time `pre_publication -> published` transition (`CaseWorkspaceStore.publish_procurement_package`), never "publish everything" | **Admin only** — matches `publish_procurement_package_route`'s own `@admin_required` | active |
| `toolbox.project-admin.publish-form` | `<form>` | (publish form) | Posts to `workspace.publish_procurement_package_route`; response is the exported zip itself, streamed, no new persistent storage | Same as above | active |
| `toolbox.project-admin.publish-include` | `<input type="checkbox">` (pattern, one per active Source) | (Source name) | Explicit per-Source inclusion — `source_ids` form field. Never inferred from Mark/localStorage state; a real, governed, server-side decision | Same as above | active |
| `toolbox.project-admin.publish-founding` | `<input type="radio">` (pattern, one per active Source) | "Founding document" | Names which included Source is the principal/founding document — required because the Proponent side's own registration mechanism (`services.ingestion.ingest_folder_upload`) requires a `founding_index` it never infers itself; decided here, by the Owner, at publish time | Same as above | active |
| `toolbox.project-admin.publish-submit` | `<button>` | "Publish RFP" | Submits the form | Same as above | active |

## Spin (prototype) (`templates/_spin_prototype.html` — CLAUDE-SPIN-00A container comparison, grammar finalized CLAUDE-SPIN-01)

Container/interaction-selection prototype — still no real evidence filtering, no persistence, never touches `operating_environment` or any other real project fact. CLAUDE-SPIN-00A rendered three comparison alternatives (A/B/C) behind a dev-only switcher; CLAUDE-SPIN-01 removed that switcher once the Product Owner selected Alternative A, plus one incorporated pairing from Alternative B (see below) — the refs below are what that selection converged onto. `spin.toggle`/etc. sub-refs repeat once per discipline (pattern), not because they're distinct controls.

| Reference | Element | Current behavior | Auth notes | Status |
|---|---|---|---|---|
| `spin.dev-switcher` (+ its own search/a/b/c sub-refs) | *(retired — CLAUDE-SPIN-01)* | "DEV COMPARISON" + three buttons — removed once the Product Owner made a selection, nothing left to switch between | — | retired |
| `spin.variant-a` (+ its own pane/status/all-on/all-off/reset/summary/toggle/pulse-slot sub-refs) | *(retired — CLAUDE-SPIN-01)* | Alternative A: "Spin — Compact Control Stack" — selected as the base, converged into the unprefixed `spin.*` refs below (no longer namespaced per-variant, since only one variant remains) | — | retired |
| `spin.variant-b` (+ its own pane/status/summary/all-on/all-off/reset/engaged-list/disengaged-list/toggle/pulse-slot sub-refs) | *(retired — CLAUDE-SPIN-01)* | Alternative B: "Spin — Instrument Panel" — not selected as the base; its status-strip pairing (badge + active-set summary together) WAS incorporated (see `spin.status-strip` below), its physical Engaged/Not-Engaged row regrouping was explicitly not adopted (rows must stay spatially stable for repeated use) | — | retired |
| `spin.variant-c` (+ its own pane/status/all-on/all-off/reset/sequence/toggle/pulse-slot sub-refs) | *(retired — CLAUDE-SPIN-01)* | Alternative C: "Spin — Progressive Engagement" — not selected; its always-visible engagement-sequence display had no analytical payoff yet. The underlying engagement-ORDER tracking still exists in `static/js/spin_prototype.js` (harmless, unsurfaced, per the Product Owner's own "preserve... but do not surface" instruction) but nothing renders it | — | retired |
| `spin.pane` (new, CLAUDE-SPIN-01 — converged from the retired variant-a pane ref; text renamed CLAUDE-SPIN-SURFACE-02) | `<section>` (`macros.tool_pane`) | "Evidence Isolation (Legacy Prototype)" — the one canonical container for this unbuilt, client-side-only prototype (unrelated to the real Spin capability) | Same as workspace access | active |
| `spin.status-strip` (new, CLAUDE-SPIN-01) | `<div>` | (wrapper, no visible label) — pairs `spin.status` and `spin.summary` together in one header block, immediately visible without scrolling; the one characteristic incorporated from Alternative B | Same as workspace access | active |
| `spin.status` (new, CLAUDE-SPIN-01 — converged from the retired variant-a status ref) | `.review-state-badge` (pattern) | "Baseline" / "Spin Active" — client-side only, reflects whether any discipline is currently toggled ON | Same as workspace access | active |
| `spin.summary` (new, CLAUDE-SPIN-01 — converged from the retired variant-a summary ref) | `<p>` | active-set text (e.g. "Architecture + Structural") — client-side, updates immediately on toggle | Same as workspace access | active |
| `spin.all-on`, `spin.all-off`, `spin.reset` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01, Part I)* | "All ON" / "All OFF" / "Return to Baseline" — the button trio itself is gone; superseded by `spin.trigger` (the one SPIN action) + `spin.all-toggle` (the All row's own selection circle) | — | retired |
| `spin.toggle` | *(retired — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01, Part I)* | "ON" / "OFF" — the rectangular ON/OFF button itself is gone; superseded by `spin.selector` (a compact selection circle, meaning "included in the next Spin," not a persistent power state) | — | retired |
| `spin.trigger` (new, CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01; text renamed CLAUDE-SPIN-SURFACE-02) | `<button>` | "APPLY" (was "SPIN" — renamed to remove the naming collision with the real Spin capability) | The single action that commits the current row selections as the active/engaged set — updates `spin.status`/`spin.summary`. Selecting a row alone never triggers this | Same as workspace access | active |
| `spin.all-row` (new, same stage) | `<div>` | "All" | Sits immediately above the first discipline row, same row grammar (`spin.all-toggle`/`spin.all-findings`/`spin.all-disclosure`) as every discipline row below it | Same as workspace access | active |
| `spin.all-toggle` (new, same stage) | `<button aria-pressed>` | (compact circle, no text) | Selects/deselects every discipline row's own `spin.selector` at once — a convenience that applies existing per-row state, never a persistent state of its own (same idiom as `menu.appearance.all`); stays in sync, checked only when every row already agrees | Same as workspace access | active |
| `spin.all-findings` (new, same stage) | `<span>` | "[ ]" | Always blank — the All row has no aggregate finding of its own to report | Same as workspace access | active |
| `spin.all-disclosure` / `spin.all-detail` (new, same stage) | `<button aria-expanded>` / `<div>` | "+" / explanatory text | Progressive disclosure explaining what All does | Same as workspace access | active |
| `spin.selector` (new, same stage — pattern, one per discipline) | `<button aria-pressed>` | (compact circle, no text) | "Included in the next Spin" — local selection state only; clicking it never updates `spin.status`/`spin.summary` by itself | Same as workspace access | active |
| `spin.findings` (new, same stage — pattern, one per discipline) | `<span>` | "[ ]" | Deliberately, permanently blank in this prototype layer — would only ever show "[N]"/"[0]" once a real investigation completes, which nothing here does yet (real evidence filtering is SPIN-01's own separate, paused increment); never a fabricated count | Same as workspace access | active |
| `spin.row-disclosure` / `spin.row-detail` (new, same stage — pattern, one per discipline) | `<button aria-expanded>` / `<div>` | "+" / "No investigation has run for this discipline yet." | Per-row progressive disclosure, collapsed by default | Same as workspace access | active |
| `spin.pulse-slot` (new, CLAUDE-SPIN-01 — converged from the retired variant-a pulse-slot ref) | `.gauge-slot` | "Project Pulse — Future analytical layer" — neutral placeholder only, no fabricated score | Same as workspace access | active |

## Right column: Eye above Toolbox (`templates/base.html`, CLAUDE-P40-EYE1; order flipped CLAUDE-EYE-TOOLBOX-LAYOUT-01)

Mirrors the Lists/Thumbnails split (CLAUDE-P40-VW7A-QA2) on the opposite
side — `#workspace-right-column` is now the full-height column (a
sibling of Lists AND `.workspace-main-column` inside `.app-shell-body`,
spanning the same vertical extent as Display+Chat, never stopping at
the Display/Chat divider), containing Eye (upper — CLAUDE-EYE-TOOLBOX-
LAYOUT-01 moved it here; Eye is "current guidance, comparison state,
findings, next-step instruction," Toolbox is "persistent working
tool/mode") and Toolbox (lower, see above) split by a draggable,
lockable divider. The existing
`shell.toolbox-divider` show/hide control (see the Shell section above)
now collapses/restores this WHOLE column, not Toolbox alone, **and**
(product-owner browser correction) is also a real mouse-draggable/
keyboard-operable WIDTH resize handle for the column — a genuine drag
is distinguished from a plain click via a movement threshold, so the
existing collapse/show behavior is unchanged for an ordinary click.
Eye itself is a structural scaffold only this stage (Section 4's own
explicit boundary) — a heading, a neutral empty state, and a real (not
decorative) paste/drop target with a responsive zoom/pan viewing canvas
(browser correction, Section 3) that previews an image in-session with
no persistence, no editing, no annotation, and no AI interpretation.

`shell.toolbox-divider`'s own row (Shell section above) already documents this extended behavior in full — not repeated as a second row here to avoid a duplicate ref in the registry.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `shell.toolbox-eye-divider` | `<div role="separator">` (new) | (unlabeled divider) | Draggable (pointer + arrow-key) horizontal split between Eye (above) and Toolbox (below — CLAUDE-EYE-TOOLBOX-LAYOUT-01 flipped the order, `--toolbox-height` still names Toolbox's own size regardless); percentage-based, `localStorage`-persisted per-Project (matching Toolbox's own existing show/hide preference scoping); double-click restores the default 60/40 split; never `[hidden]` — Eye is a permanent pane, only the split proportion is adjustable. Lockable via `shell.toolbox-eye-lock` (below) — while locked, drag/keyboard/double-click resizing all no-op | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.toolbox-eye-lock` | `<button aria-pressed>` (new) | 🔒 / 🔓 glyph only | Compact lock toggle living inside `shell.toolbox-eye-divider`'s own strip; hidden at rest, revealed on divider hover/focus or its own focus, persistently visible while locked (`aria-pressed="true"`) — the one "tiny persistent lock-state cue" this pattern allows, never a full-width bright line. Locked state persisted per-Project (`beehive:panel:toolbox-eye-locked:{project_id}`). **CLAUDE-EYE-TOOLBOX-LAYOUT-01-CORRECTION-01:** independent of `shell.chat-divider-lock` only while unlinked; while `shell.toolbox-eye-link` is linked, the two locks are driven together | Only rendered when `project_id`/`workspace` are defined | active |
| `shell.toolbox-eye-link` (CLAUDE-EYE-TOOLBOX-LAYOUT-01-CORRECTION-01, new) | `<button aria-pressed>` (new) | 🔗 glyph only | Compact chain/link toggle beside `shell.toolbox-eye-lock` in the same strip — see `shell.chat-divider-link`'s own row (Shell section above) for the full shared-state/mirroring behavior; this is the other half of the same pair | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.panel` | `<div>` (new) | "Eye" (header text) | The whole pane; contains the heading, the maximize toggle, and the drop target | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.heading` | `<h2>` (new) | "Eye" — static | — | active |
| `eye.layer-dismiss` (CLAUDE-MOBILE-Q-TRIAL-01, new) | `<button>` | ⇣ | "Shovel it back" — lowers the Eye foreground layer and reveals the work beneath it exactly as it was left. The same action as pressing `shell.tray-switcher.eye` again; this one is reachable from where the reviewer's attention already is once a drawing is in front of them. Rendered only while the layer is up (`html[data-tray-layer="eye"]`), so it is never a control sitting beside Maximize with nothing to do. Deliberately NOT a second maximize: `eye.maximize` divides the right COLUMN between Eye and Toolbox, a different question, and is itself hidden while the layer is up | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.maximize` | `<button aria-pressed>` (new) | "Maximize Eye" / "Restore Eye" | **Two-dimensional** (product-owner browser correction): collapses Toolbox toward its practical minimum (height) AND expands the right column to its largest practical width (via `shell.toolbox-divider`'s own resize logic) in one action; restores BOTH dimensions exactly on the toggle-back click. The maximized width is deliberately never persisted, so a mid-maximize page reload can't leave the reviewer stuck there | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.drop-target` | `<div role="group">` (new) | "Paste or drop an image here to preview it." (empty state — **CLAUDE-EYE-COMPARE-01:** becomes "Choose a document to compare." instead, while `toolbox.compare` is active and Eye holds nothing) | A real drop target: `static/js/eye_pane.js` handles `dragover`/`drop`/`paste`, reads an image file via `FileReader`, and hands it to `eye.canvas` (below) for display — held only in this tab's own memory until the reviewer explicitly saves it (`eye.canvas.save`, below). Non-image drops/pastes show an inline error, not a silent failure. Dropping/pasting a new image while one is shown replaces it (and clears any Project Document that was showing — `eye.document.clear`'s own row below, Eye holds exactly one of {nothing, image, document} at a time). **CLAUDE-MM5** turned this from a structural-scaffold-only preview into the real governed visual-evidence surface Section 7 describes — see `eye.canvas.save`/`.rotate`/`.mirror-h`/`.mirror-v` below. **CLAUDE-EYE-COMPARE-01** added the second, sibling content state — see `eye.document.label`/`.clear` below | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas` | `<div>` (new, product-owner browser correction) | — | The responsive image-viewing canvas — `[hidden]` until an image is loaded, then fills `eye.drop-target`'s own available area (not a small fixed thumbnail). Contains the view-control toolbar and `eye.canvas.viewport` | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.zoom-out`, `eye.canvas.zoom-in` | `<button>` (new) | − / + | Multiplies/divides the current scale by 1.25, clamped 5%–800%; switches to manual (non-auto-refitting) mode | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.fit` | `<button>` (new) | "Fit" | Scales the image to fill the viewport on whichever axis is tighter, preserving aspect ratio (not capped at 100% — a small image is scaled UP to use the available area too); the initial view for every newly-loaded image | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.actual-size` | `<button>` (new) | "1:1" | Sets scale to exactly 100% (the image's real pixel size); switches to manual mode | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.reset` | `<button>` (new) | "Reset" | Returns to Fit (same target as `eye.canvas.fit`) and re-centers, AND (CLAUDE-MM5) clears any rotate/mirror view state back to identity | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.rotate` | `<button>` (new, CLAUDE-MM5) | ↻ | Rotates the UNSAVED preview 90° clockwise per click, cumulative — a view-only CSS transform, nothing persisted (Section 4/11) | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.mirror-h`, `eye.canvas.mirror-v` | `<button>` (new, CLAUDE-MM5) | ↔ / ↕ | Toggles a horizontal/vertical view-only mirror on the UNSAVED preview | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.remove` | `<button>` (new) | "Discard preview" (CLAUDE-MM5 relabel — was "Remove") | Clears the loaded, UNSAVED preview, returning `eye.drop-target` to its neutral empty state. Has no effect once the image is saved (`eye.canvas.save`, below) — the saved Source is real project data, not a discardable preview | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.save` | `<button>` (new, CLAUDE-MM5) | "Save to project" | Uploads the real, original file bytes (never the transformed/rotated view) to `POST /api/v1/documents/<project_id>/eye-capture`; on success, replaces this pane's content with the SAME real drawing/image viewer MM4 built (`static/js/drawing_image_viewer.js`), now backed by a persisted Source with full rotate/mirror/region-select/citation | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.canvas.viewport` | `<div>` (new) | — | The actual scroll/pan surface — the image is sized to its real scaled pixel dimensions (never CSS percentage-based sizing, so no stretching/distortion at any zoom); native browser scroll is the pan mechanism (keyboard/touch/trackpad all work for free); mouse-wheel/trackpad zoom is active only while this element itself has focus; a `ResizeObserver` recalculates Fit automatically whenever its own container is resized or Eye is maximized/restored, but only while still in Fit mode — a deliberate manual zoom is never silently overridden | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.document.label` (CLAUDE-EYE-COMPARE-01, new) | `<span>` | "PROJECT DOCUMENT" — static | Identifies Eye's second real content state (a genuine, already-registered Source) as distinct from the unsaved-pasted-image state above — Part 5's own "clearly show whether it currently contains... an image; a project document" | Only rendered when `project_id`/`workspace` are defined | active |
| `eye.document.clear` (CLAUDE-EYE-COMPARE-01, new) | `<button>` (new) | "Clear" | Clears the loaded Project Document from Eye, returning `eye.drop-target` to its neutral empty state (or "Choose a document to compare." while `toolbox.compare` is active). Working-view only — never touches the underlying Source (no archive/remove/duplicate/register call of any kind), the same non-mutation guarantee `eye.canvas.remove` already gives the unsaved-image state | Only rendered when `project_id`/`workspace` are defined | active |

## Chat (`templates/_macros.html`'s `conversation_dock` macro + `templates/case_workspace.html`)

| Reference | Element | Current behavior | Auth notes | Status |
|---|---|---|---|---|
| `chat.dock` | `#chat-region` (`templates/base.html`) | The whole Chat surface (dock header, resize handle, thread, composer) — full application width, bottom row of the shell | Only rendered when `project_id`/`workspace` are defined | active |
| `chat.thread` | `.conversation-thread` (pattern — case-scoped and project-scoped share this same reference) | The scrollable message list; case-scoped when an Investigation is open, project-scoped otherwise (mutually exclusive) | Same as workspace access | active |
| `chat.next-step` (CLAUDE-POSTCAMEL-CA1, new) | `.conv-next-steps` (pattern — case-scoped and project-scoped conversation loops both use this same reference, rendered only on the most recent message) | A small, real, deterministic, server-computed set of `<a>` next-step offers (e.g. "Open Files", "Open Requirements") attached to `ConversationMessage.next_steps` — every `step.view` is an existing Display view, never model-generated prose interpreted as a command; free-form typing in the composer remains available regardless | Same as workspace access | active |
| `chat.operational-actions` (CLAUDE-CA1D-RIVER-01, new) | `.conv-operational-actions` (pattern — `macros.operational_action_offers`, case-scoped and project-scoped conversation loops both use this same reference, rendered only on the most recent system message) | The "fourth beat" — real, already-implemented next actions ("Make a Task from this"/a contextual deadline-or-risk variant, "Highlight this answer") attached to `ConversationMessage.operational_actions`, offered when that reply's own `grounded_in` OR `river_actions` is genuinely non-empty (a real, evidence-backed answer, never a "not covered" one). Each is its own real `<form>` POSTing to the existing `create_task_route`/`add_tag_occurrence_route` — CLAUDE-CA1D-RIVER-PO-02: the anchor is the top-ranked River Action's own text when `river_actions` is present (the smallest reliable source unit available), falling back to the whole answer message otherwise; no new backend mechanism either way. Capability-true by construction: only "task"/"tag" kinds are ever emitted, because Create Finding/Add to Risk Register/Delegate are not genuinely implemented as one-click conversational actions today | Same as workspace access | active |
| `chat.river-action-stack` (CLAUDE-CA1D-RIVER-PO-01, new) | `<ol class="conv-river-action-stack">` (pattern — `macros.river_action_stack`, case-scoped and project-scoped conversation loops both use this same reference, rendered only when that reply's own `river_actions` is non-empty) | A small, ranked ("normally 1-5, never padded") set of the reply's most consequential next moves — `ProjectQAResult.river_actions`, populated by the SAME single QA call as the answer itself (no second AI call, no new route), ranked by genuine consequence (blocking work, gating authority, deadlines, uncertainty, submission eligibility) never document order. Collapsed by default; see `chat.river-action-stack.item` for the per-item disclosure | Same as workspace access | active |
| `chat.river-action-stack.item` | `<details>` via `macros.subdisclosure` (pattern — one per ranked action) | Collapsed heading is "`<rank>`. `<action>`"; expanding (native disclosure, the app's one existing collapse/expand grammar — no new "+"/"−" affordance) reveals Why it matters/Consequence/Uncertainty/Evidence, each independently. Ordinary factual answers never carry this — the model leaves `river_actions` empty for anything that isn't genuinely a "what should I do next" question | Same as workspace access | active |
| `chat.selection-hint` (CLAUDE-CA1D-RIVER-03, new) | `<p id="conv-selection-hint">` (pattern — same two conversation loops as `chat.operational-actions`, rendered alongside it, never on its own) | "Tip: you can select part of this answer to Tag it, Highlight it, or make a Task from it." | A real Product Owner could not discover the (fully implemented, not dormant — see `chat.selection-toolbar` below) selection-to-action toolbar through ordinary use. Server-rendered `hidden`; `static/js/case_workspace.js` reveals it only when this browser has never actually used the toolbar before (a real `localStorage` flag), and hides it for good — setting that flag — the first time a genuine text selection actually opens the toolbar, whether or not this exact hint was the one rendered. Never a permanent affordance; appears only where `chat.operational-actions` already appears | Same as workspace access | active |
| `chat.context-indicator` (CLAUDE-POSTCAMEL-CA1B; repositioned CLAUDE-CA1D-COMPOSER-CONTEXT-LABEL-01) | `.composer-context-label` (pattern — embedded in `.composer-context-rule`, both case-scoped and project-level composers) | "`<Type>` — `<label>`" (was "Currently working with: ...") plus a `×` clear control (`formaction`, posts to `workspace.clear_selected_context`) | Resolved read-time every render against the persisted `selected_object:{project_id}` session slot via `_resolve_anchor_object` — never shown for a stale/deleted selection. Repositioning moved this from a standalone div above the whole composer into the composer's own upper rule, reading as a tab/folder caption cut into the line — same ref, same underlying data, new DOM position/visual treatment only | Same as workspace access | active |
| `chat.composer` | `<form>` | Posts a new message — to `post_message` (case-scoped) or `quick_start` (project-scoped) | Same as workspace access | active |
| `chat.selection-toolbar` | `#conv-selection-toolbar` (CLAUDE-P40-VW7; moved/reparented CLAUDE-P40-VW8-QA — see note below) | The contextual toolbar on a meaningful text selection — Add Tag/Make Task/Highlight/Important/Question/Copy. Positioned beside the live selection, vertical (one action per row), hidden whenever nothing meaningful is selected | Same as workspace access (server-side re-checked on every mutation) | active |
| `chat.selection-toolbar.tag`, `chat.selection-toolbar.task`, `chat.selection-toolbar.highlight`, `chat.selection-toolbar.important`, `chat.selection-toolbar.question`, `chat.selection-toolbar.copy` | `<button>` (6 distinct, named actions) | toolbar action buttons | See `chat.selection-toolbar`'s own row — one reference per action | Same as workspace access | active |
| `chat.selection-toolbar.remove-tag` | `<button>` (CLAUDE-P40-VW8-QA, reversibility correction) | "Remove Tag (N)" — hidden unless 1+ non-built-in Tag occurrences overlap the selection; opens `chat.remove-tag-dialog` | Same as workspace access | active |
| `chat.selection-toolbar.remove-highlight`, `chat.selection-toolbar.unmark-important`, `chat.selection-toolbar.unmark-question` | `<button>` (JS-toggled state — CLAUDE-P40-VW8-QA) | The SAME physical `.highlight`/`.important`/`.question` buttons, with `data-conv-action`/`data-ui-ref`/label swapped to this "remove" identity only while that built-in Tag is currently applied to the selection (`applyAppliedTagState` in `static/js/case_workspace.js`) — never both identities on the same element at once, and never inherited from the add/apply reference (a distinct id per state, per the correction's own explicit requirement) | Same as workspace access | active (rendered conditionally, by JS, not statically in template source — not picked up by the static template scan, hence not in this file's own automated consistency test) |
| `chat.selection-toolbar.undo` | `#conv-selection-undo` (CLAUDE-P40-VW8-QA) | Short-lived (8s) Undo for the most recent Tag/Highlight/Important/Question removal — re-POSTs the same add-Tag route with the removed occurrence's own tag id + anchor fields | Same as workspace access | active |
| `chat.remove-tag-dialog` | `#conv-remove-tag-dialog` (CLAUDE-P40-VW8-QA) | "Remove Tag" dialog — lists every currently-applied custom Tag on the selection (name + swatch, never color alone), each with its own Remove | Same as workspace access | active |
| `chat.selection-toolbar.applied-tags-list` | `#conv-remove-tag-list` (CLAUDE-P40-VW8-QA) | The list itself, inside `chat.remove-tag-dialog` | Same as workspace access | active |
| `chat.tag-dialog` | `#conv-tag-dialog` (CLAUDE-P40-VW7) | "Add Tag" dialog — existing/custom tag + color picker | Same as workspace access | active |
| `chat.task-dialog` | `#conv-task-dialog` (CLAUDE-P40-VW7) | "Make Task" dialog — editable title | Same as workspace access | active |
| `chat.composer.attach` (CLAUDE-GO-COMPOSER-CAPTURE-01, new) | `<label>` for a hidden `<input type="file">` | + | The Composer's photo attachment — `accept="image/*"`, `capture="environment"` so a phone opens the REAR camera and a desktop offers a file picker, one control for both with no device check. The bytes ride the hidden `image_data_url` field on the ORDINARY composer submit, so the message text and the photo arrive together through the same route with the same context envelope — which is what lets "make a new Q" be one action instead of three, and why the older `lists.project.documents.image-search-open-composer` handoff is no longer the only way a photo reaches GO | Only rendered when `project_id`/`workspace` are defined | active |
| `chat.composer.attach.preview` (CLAUDE-GO-COMPOSER-CAPTURE-01, new) | `<span>` with thumbnail | (the photo's own filename) | Shows WHICH photo is about to be sent, before it is transmitted. Hidden until one is attached; never a reserved empty strip | Same as above | active |
| `chat.composer.attach.make-q` (CLAUDE-GO-COMPOSER-CAPTURE-03, new) | `<button>` | "Make a new Q" | The next step, made visible — Product Owner: "I took the picture and chose use it then the next step is not clear?" "Make a new Q" was a phrase invented in conversation and written nowhere in the product, so the one action this entry point exists for was discoverable only by having been told it. Fills the message box with those words and submits, rather than posting silently, so the phrase lands in the conversation as the reviewer's OWN message — which is how they learn they could have typed it, and that they may type something else instead. Same words, same submit, same handler as typing it by hand: no second route and no second way to create an Investigation. Appears and disappears with the attached photo | Only rendered when `project_id`/`workspace` are defined | active |
| `chat.composer.attach.clear` (CLAUDE-GO-COMPOSER-CAPTURE-01, new) | `<button>` | × | Removes the attached photo before sending. The field is also cleared automatically on submit, so a sent photo can never ride along silently with the next message | Same as above | active |
| `chat.dock.new-conversation` (CLAUDE-GO-COMPOSER-LIFECYCLE-01, new) | `<button type="submit">` | "New" | Starts a new conversation from the Composer itself, through `workspace.start_new_conversation` → the same `CaseWorkspaceStore.create_case` every other creation path uses. Reach, not new machinery: this existed via the Toolbox Investigations list, which on a phone is behind a tray switch and a scroll | Every authenticated workspace page | active |
| `chat.composer.input` | `<input>` | the ONE conversation composer's text field | Posts to `post_message`/`quick_start` on submit | Same as workspace access | active |
| `chat.composer.send` | `<button>` | "Send" | Submits `chat.composer.input`'s form | Same as workspace access | active |
| `chat.composer.voice` (CLAUDE-POSTCAMEL-VOICE1-PRE, new) | `<button id="dock-composer-voice">` | 🎙️ (Push-to-Talk) | Never submits the form itself - fills `chat.composer.input` via the browser's own `SpeechRecognition` (Web Speech API), then the reviewer reviews/edits and clicks `chat.composer.send` exactly as if they had typed it. Hidden by `static/js/case_workspace.js` at load time on any browser without `SpeechRecognition` support - never shown as a false promise. No audio is ever captured or stored by this application's own code | Same as workspace access | active |
| `chat.composer.voice.status` (CLAUDE-VOICE1-LIVE-FIX-01/02) | `<span id="dock-composer-voice-status">` | "Listening…" / "Downloading speech model…" (CLAUDE-VOICE1-LIVE-FIX-02, first use per language only) / "Transcribing…" / a specific failure ("Microphone permission denied", "No microphone available", "No speech detected", "Speech recognition unavailable in this browser", "Recognition stopped") | Server-rendered empty (never a placeholder a screen reader would announce on load); `static/js/case_workspace.js`'s `chat.composer.voice` click handler writes every reachable outcome here as plain text via `aria-live="polite"` - fixed a real Product Owner report that pressing the mic showed nothing at all, with no way to tell why. Prefers the browser's real on-device (`processLocally`) speech recognition when available, confirmed live via `SpeechRecognition.available()`/`.install()` - never sends audio anywhere when it's used | Same as workspace access | active |
| `chat.composer.execution-status` (CLAUDE-CA1D-INSTRUMENT-RAIL-01, new) | `<span id="dock-composer-execution-status">` | "Working on your request…" | The composer-adjacent execution strip proof — the transient, per-instruction half of the four-part Instrument Rail spatial model. Server-rendered empty; `static/js/case_workspace.js` sets this text right before the composer's own classic (un-intercepted) form-POST fires, so it stays visible for the real duration of the server round trip and disappears the moment the response replaces the page — no fetch/AJAX, no new backend infrastructure | Same as workspace access | active |
| `chat.tag-highlight` | `<mark class="tag-highlight-inline">` (pattern, CLAUDE-P40-VW8-QA, Section 11) | inline tagged-text treatment | Rendered by `app.py`'s own `hotlinks` filter around the exact tagged substring of a persisted message — the corrected "Add Tag has no visible consequence" defect. `data-tag-occurrence-id` disambiguates which occurrence; `data-tag-id`/`data-tag-name` added (CLAUDE-P40-VW8-QA, reversibility correction) for client-side applied-state detection | Same as workspace access (never rendered for a message the reviewer can't already see) | active |
| `chat.dock.label` | *(retired — CLAUDE-P40-VW7A-QA)* | A compact "Chat (N)" `<label>` briefly lived beside the composer input (moved down from the old top-of-panel heading) — the immediate product-owner follow-up rejected it as a duplicate of the Lists panel's own "Chats" row (which now carries the count instead, via `lists.project.chats` — see Lists section below). Nothing renders this reference any more; retired rather than reused for a different control | — | retired |

**`chat.selection-toolbar*` — moved/reparented, not replaced (CLAUDE-P40-VW8-QA):**
the HTML (`#conv-selection-toolbar`, its `hidden` attribute) and the
JS (`static/js/case_workspace.js` selection-tracking, anchor
computation, viewport-clamped positioning, per-action availability,
Escape/outside-click/selection-clear close handling, real Tag/Task
dialog + built-in-tag POST wiring for Highlight/Important/Question,
clipboard Copy) were already correct — audited end to end, none of
the 6 actions were decorative or inert. The one real defect was
`static/css/main.css`'s `.conv-selection-toolbar` rule having no
`[hidden]` override, so the class's `display: flex` beat the `hidden`
attribute at equal cascade specificity and the toolbar rendered
permanently regardless of what the JS correctly set — the same bug
CLASS as the R3 tokens.css comment-boundary regression. Fixed by
adding `.conv-selection-toolbar[hidden] { display: none; }` and
changing the layout to `flex-direction: column` (one action per row,
per product-owner request) with `text-align: left` on
`.conv-selection-btn`. All six `chat.selection-toolbar*` identifiers
above are retained unchanged — their meaning did not change, only
their visibility bug and row/column arrangement did.

**Native-popup-overlap correction (same identifiers, no new ones):**
a browser/OS-owned text-selection popup (most identifiably Microsoft
Edge's own "mini menu on text selection," `edge://settings/appearance`
→ "Show mini menu when I select text," or the enterprise
`QuickSearchShowMiniMenu` policy) is not created by, mergeable into, or
controllable from this page — no `contextmenu` listener exists in this
toolbar's own setup (confirmed by reading `static/js/case_workspace.js`
in full; the file's one `contextmenu` listener is the unrelated
Display-division picker). `positionToolbar` now prefers BELOW the
selection first (was: above first) specifically to sit on the opposite
side from where that native popup conventionally appears, and a new
`repositionOrHideOnViewportChange` (bound to `window`'s `scroll`,
capture phase, and `resize`) keeps the toolbar correctly placed — or
hides it — if the containing panel scrolls or the viewport resizes
while a selection is held, which nothing previously handled. Copy was
already, and remains, fully self-sufficient (copies the complete
captured selection text), so a reviewer never needs the native popup
for ordinary copying.

## Public Landing (`templates/landing_shell.html`, `landing.html`, `explore.html`, `start_trial.html` — CLAUDE-CA1D-PUBLIC-LANDING-01, new surface)

Renders inside its own standalone shell (`landing_shell.html`), never
`base.html`/`gateway_base.html`/`auth_shell.html` — an anonymous public
visitor has no session context those shells assume. `/` serves this for
every unauthenticated visitor (superseding CLAUDE-P40-VW5's own
redirect-to-Sign-in behavior by explicit Product Owner decision);
`/explore` and `/start-trial` are both public and reachable directly.

| Ref | Element | Label/text | Notes | Scope | Status |
|---|---|---|---|---|---|
| `landing.explore` | `<a>` | "Explore" | Navigates to `portal.explore` | Unauthenticated visitors at `/` | active |
| `landing.start-trial` | `<a>` | "Request Trial Access" | Navigates to `portal.start_trial` — no billing/self-service registration exists yet, see that route's own honest copy | Same as above | active |
| `landing.sign-in` | `<a>` | "Sign In" | Navigates to `portal.login` — the existing, unchanged authentication flow | Same as above | active |
| `landing.voice.button` (new, CLAUDE-CA1D-PUBLIC-LANDING-03; icon-only per LANDING-05 Section 8) | `<button type="button">` | "🎙️" (icon only — no visible "Speak to Archiosk" text; `aria-label`/`title` carry the same meaning) | Hidden server-side by default (`hidden` attribute); unhidden by `static/js/landing.js` only when `window.SpeechRecognition`/`webkitSpeechRecognition` is feature-detected — never a false promise on an unsupported browser. Push-to-talk (click to start, click again to stop) via the browser's own built-in Web Speech API — the exact same mechanism as the authenticated composer's own `chat.composer.voice` (CLAUDE-POSTCAMEL-VOICE1-PRE); no server round trip, no audio blob ever handled by this code | Unauthenticated visitors at `/` | active |
| `landing.voice.status` (new, CLAUDE-CA1D-PUBLIC-LANDING-03) | `<span aria-live="polite">` | (empty at rest) | "Listening…"/"Transcribing…" while active, a specific failure (permission denied, no microphone, no speech detected, recognition unavailable, recognition stopped — same `ERROR_MESSAGES` vocabulary as `chat.composer.voice.status`), or (LANDING-05, Section 9) a brief "Opening Sign In…"/"Opening Explore…"/"Opening Request Trial Access…" confirmation immediately before a direct-navigation voice command executes | Same as above | active |
| `landing.voice.result` (new, CLAUDE-CA1D-PUBLIC-LANDING-03; scope narrowed by LANDING-05 Section 9/10) | `<div aria-live="polite">` | (hidden until a final transcript exists) | Renders ONLY for a genuinely informational transcript (LANDING-05's own `INFORMATIONAL` classifiers — Learning Holodeck, "without an account", RFP/procurement) — the transcript plus a small, deterministic, client-side-only keyword-routed response (never a generative/LLM call, never a network request), or an honest "I'm not sure yet" fallback. A clear, safe, local navigation command (Sign In/Explore/Request Trial Access) now executes directly instead (`landing.voice.status` above) — no redundant card duplicating a button already on the page. Never higher than Level 2/3 (Suggest / reversible local navigation) of the future Voice authority ladder — see `governance/specified-unbuilt/voice-conversational-presence.md` | Same as above | active |
| `landing.sound-toggle` (new, CLAUDE-LANDING-SONIC-01 — EXPERIMENTAL, not accepted into brand governance) | `<button type="button" aria-pressed>` | "🔈" / "🔇" (icon only; `aria-label`/`title` carry the state in words) | Turns the ~1.4s synthesised startup cue on or off, remembered per browser in `localStorage`. Hidden server-side by default (`hidden`); unhidden by `static/js/sonic_cue.js` only where `AudioContext`/`webkitAudioContext` is feature-detected — same "hidden until feature-detected" pattern as `landing.voice.button` above, so a browser that can never make the sound never offers to silence it. Carries NO application meaning: the cue is decorative by requirement, nothing about navigation or state depends on hearing it, and it is skipped entirely under `prefers-reduced-motion`. Requests no microphone permission and touches no `speechSynthesis` — unrelated to `landing.voice.button` | Unauthenticated visitors at `/` | active |
| `explore.back` | `<a>` | "← Archiosk" | Navigates back to `portal.index` (the landing page) | `/explore` | active |
| `explore.start-trial` | `<a>` | "Request Trial Access" | Same destination as `landing.start-trial` | `/explore` | active |
| `explore.sign-in` | `<a>` | "Sign In" | Same destination as `landing.sign-in` | `/explore` | active |
| `start-trial.back` | `<a>` | "← Archiosk" | Navigates back to `portal.index` | `/start-trial` | active |
| `start-trial.request-form` (new, CLAUDE-CA1D-TRIAL-ACCESS-HOTFIX-01) | `<form method="post">` | (no visible label) | POSTs to the same `/start-trial` route (GET renders it, POST processes it) — `routes/portal.py`'s `start_trial()`, rate-limited 5/hour same as `/forgot-password`. Only rendered pre-submission (`submitted` is falsy) | `/start-trial` | active |
| `start-trial.request-error` (new, CLAUDE-CA1D-TRIAL-ACCESS-HOTFIX-01) | `<p role="alert">` | (validation error text) | Rendered only when the POST is missing a plausible email address — the one validation this route performs | `/start-trial` | active |
| `start-trial.request-submit` (new, CLAUDE-CA1D-TRIAL-ACCESS-HOTFIX-01) | `<button type="submit">` | "Request Access" | Submits `start-trial.request-form` — name/message optional, email required. On success, hands the request to `services/trial_request.py` (logs it always; emails `TRIAL_REQUEST_NOTIFY_EMAIL` via the existing SMTP transport when configured) and re-renders this same page with `submitted=True` | `/start-trial` | active |
| `start-trial.sign-in` | `<a>` | "Already have an account? Sign In" (was "Sign In" — CLAUDE-CA1D-TRIAL-ACCESS-HOTFIX-01, Section 6) | Same destination as `landing.sign-in` | `/start-trial` | active |
| `start-trial.explore` | `<a>` | "Explore Archiosk" | Same destination as `landing.explore` | `/start-trial` | active |

## Gateway (`templates/gateway_shell.html`, `gateway.html`, `project_chooser.html` — CLAUDE-P40-VW8-QA, new surface)

Gateway/the chooser render inside `gateway_shell.html`, never `base.html`
— no Lists/Display/Toolbox/Chat exists here, and none of these
references may ever start with `lists.`/`display.`/`toolbox.`/`chat.`/
`menu.` (see `tests/test_p40vw7a_ui_reference_map.py`'s own
`SignInGatewayIsolationTests`, which asserts exactly this).

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `gateway.account` | `<details>` popup | "…" (username) | Contains UI Reference Mode toggle, Removed Projects, Sign out. **CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01, Disposition 2/3**: admin-only Security relocated OUT of this menu — Archiosk > Admin > Security (the shared application menu bar, `templates/_app_menu.html`, now included on this page too) covers it, same relocation `menu.account` on `base.html` already went through this session | Every authenticated page (this shell only renders post-login) | active |
| `gateway.account.sign-out` (new, CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01) | `<a>` | "Sign out" | Navigates to `portal.logout`. Shares the SAME `guardDeparture()` unsaved-input `confirm()` in `static/js/app_menu.js` as `menu.archiosk.exit`/`menu.account.sign-out` — one canonical safe-departure mechanism (Disposition 4), a harmless no-op here since Gateway has no Composer/tab-rename/annotation DOM for `hasUnsavedInput()` to ever find | Every authenticated page (this shell only renders post-login) | active |
| `gateway.create-client-owner` | *(retired — CLAUDE-GO-NEUTRAL-ENTRY-01)* | "New Project" (was inside the "Client / Owner Projects" context group) | A real Product Owner report named the two-door front-door split itself as the defect: the user enters ARCHIOSK, not a stakeholder category. Replaced by `gateway.new-project` below, one unfiltered action for every admin regardless of which side a project turns out to be — `operating_environment` is unchanged as a real, governed fact, still collected at `/upload` (now with no `?environment=` preset), just no longer gating which Gateway card a user sees | — | retired |
| `gateway.create-design-builder` | *(retired — CLAUDE-GO-NEUTRAL-ENTRY-01)* | "New Project" (was inside the "Design-Builder / Proponent Projects" context group) | Same correction and replacement as `gateway.create-client-owner` above | — | retired |
| `gateway.open-existing-client-owner` (+ its own search/open-form/leaf/open-button/empty sub-refs) | *(retired — CLAUDE-GO-NEUTRAL-ENTRY-01)* | "Open Existing Project" (was inside the "Client / Owner Projects" context group) | Replaced by `gateway.open-existing-projects` below — one unfiltered inline reveal over every authorized project (same underlying `_environment_projects` data, called once instead of twice), not partitioned by operating environment | — | retired |
| `gateway.open-existing-design-builder` (+ its own search/open-form/leaf/open-button/empty sub-refs) | *(retired — CLAUDE-GO-NEUTRAL-ENTRY-01)* | "Open Existing Project" (was inside the "Design-Builder / Proponent Projects" context group) | Same correction and replacement as `gateway.open-existing-client-owner` above | — | retired |
| `gateway.open-existing` | *(retired — CLAUDE-CA1D-PROJECT-GATEWAY-LABELS-01)* | "Open an existing project" | A live report found the previous three-flat-card layout (two "Create" cards plus this one, environment-agnostic, ungated) reading as if "Existing Project" were a third project context alongside Client/Owner and Design-Builder/Proponent, rather than an independent New/Existing dimension that applies within either context. Replaced at the time by the two context-scoped refs above (themselves now also retired — see `gateway.open-existing-projects` below, the neutral successor this whole lineage converges back to); the underlying route (`portal.choose_project`) is unchanged and still reachable unfiltered by direct URL or search | — | retired |
| `gateway.new-project`, `gateway.open-existing-projects` (+ its own `.search`/`.open-form`/`.leaf`/`.open-button`/`.empty` sub-refs) | *(retired — CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01, Option C)* | "New Project" / "Open Existing Project" | `templates/gateway.html` itself is retired — the separate authenticated Gateway shell it rendered is consolidated onto `portal.index` (`/`). Ported (same routes/data, physical move only) to `index.zero-state.new-project` / `index.resolved.new-project` (the New Project action, now environment-aware) and `index.resolved.open-existing` (+ its own `index.resolved.project-list`/`.item`/`.empty` — a plain project-summary list, not the radio-select-then-submit pattern; see that ref's own row) | — | retired |
| `gateway.chooser` | `<h2>` | "Open an existing project" | Section heading, non-interactive | Every authenticated page | active |
| `gateway.chooser.search` | `<form>` | project search | `?q=` filter, server-side, same `_accessible_documents` matching `projects_list` uses; preserves `?current=` (below) via a hidden field so searching never drops the Current Project context | Every authenticated page (results scoped to `_accessible_documents`) | active |
| `gateway.chooser.leaf` | `<label>` (pattern, was `<a>` — CLAUDE-CA1D-RECEPTION-FIX-01 addendum) | a Project card, wrapping a radio | Select-then-confirm now, not a direct link — see `gateway.chooser.open-button` below. Navigates to the same `workspace.show_workspace` URL for that Project once confirmed, the exact same authorized route every other Project-opening path uses | Filtered to `_accessible_documents` (already access-scoped) | active |
| `gateway.chooser.open-form` (new, CLAUDE-CA1D-RECEPTION-FIX-01 addendum) | `<form>` | (no visible label) | Wraps the radio group; submit rewrites to the selected Project's real URL client-side (no new backend route) | Same as above | active |
| `gateway.chooser.open-button` (new, CLAUDE-CA1D-RECEPTION-FIX-01 addendum) | `<button>` | "Open Project" | Disabled until a radio is checked; Enter from a checked radio submits the form natively | Same as above | active |
| `gateway.chooser.current` | `<section>` (new, CLAUDE-P40-VW7B) | "Current Project" | This route/template is now also the Project Vestibule (Section 4) — only rendered when arriving with a valid, authorized `?current=<project_id>` (set by `menu.context.switch-project`, below); a soft display hint, never a new persisted "current project" concept or an authorization boundary of its own | Every authenticated page; `current` silently ignored if unauthorized/stale/omitted | active |
| `gateway.chooser.current.leaf` | `<a>` | Current Project's own card, "Currently entered" badge | Re-enters the same already-open Project via `workspace.show_workspace` — a plain link, no confirmation (workspace state is safely persisted per-Project already; see `menu.context.switch-project`'s own note on why no interruption dialog guards this) | Same as `gateway.chooser.leaf` | active |
| `gateway.chooser.available` | `<p>` (new, CLAUDE-P40-VW7B) | "Available Projects" | Section label shown only alongside `gateway.chooser.current` — the current Project is excluded from this list below it, never duplicated | Every authenticated page | active |
| `gateway.chooser.removed-projects` | `<a>` (new, CLAUDE-P40-VW7B) | "Removed Projects" | Navigates to `portal.removed_projects` — Section 4's own "Archived or removed Projects... only if this is already a real governed concept," linked out to the real page rather than duplicated inline | Every authenticated page | active |
| `gateway.chooser.removed-match-hint` | `<a>` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | "Removed Projects" (inline, within "It may be under...") | Only rendered when a `q=` search returns zero active matches AND the same already-access-filtered `_accessible_documents(include_removed=True)` call finds a removed match — never a second, separately-authorized lookup, so this can't surface a Project the current user isn't already authorized to see | Every authenticated page; query-and-outcome-conditional | active |
| `gateway.chooser.back-top` (renamed from the retired gateway.chooser.back ref — CLAUDE-CA1D-RECEPTION-FIX-01 addendum) | `<a>` | "← Back" | Upper-left, in `gateway_base.html`'s new `gateway_back_action` block. href is `portal.index` (was `portal.gateway`, which now only redirects here — CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01, Option C); JS upgrades to real `history.back()` when `document.referrer` and history both indicate a real prior page. The old bottom "← Back to Gateway" duplicate was removed — a live report named the two differently-worded "back" affordances at opposite ends of the page as confusing | Every authenticated page | active |
| `gateway.chooser.new-project` (new, CLAUDE-CA1D-PUBLIC-LANDING-01 consolidated addendum, Section A4) | `<a>` | "New Project" | Only rendered in the genuine zero-Project empty state (no Current Project, no Available Projects at all) — navigates to `portal.upload`, the same establishment route `menu.account.admin.new-project` already uses | **Admin only** — `is_admin` | active |
| `gateway.orientation.form` (+ its own voice/voice-status/submit/reply sub-refs, CLAUDE-VOICE-CONSISTENCY-01) | *(retired — CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01, Option C)* | "Ask Archiosk" / "🎙️" / "Ask" | `templates/gateway.html` itself is retired. Ported verbatim (same backend route `portal.gateway_orientation`, same voice wiring, same rule-based responder) onto `index.html` under `index.orientation.*` — see that ref's own row | — | retired |
| `index.projects-directory` (new, CLAUDE-P40-E3A carried-forward fix) | `<a>` | "All projects" | Navigates to `portal.projects_list` — the full, unfiltered-by-environment administrative Projects directory (search/sort/counts/Delete forms). Lists' own "Projects" heading became a plain expand/collapse tree toggle rather than a navigating link (CLAUDE-P40-E3A, Section 2/4), which left `/` as this page's own real access point — carried forward from the retired minimal index.html rather than dropped. Available in every one of index.html's three states, unfiltered (same "choose_project's own unfiltered default stays unfiltered" carve-out Option C's own narrow supersession already preserves) | Every authenticated page reaching `/` | active |
| `index.heading`, `index.zero-state.copy`, `index.zero-state.new-project`, `index.choice.copy`, `index.choice` (+ its own per-environment `index.choice.client_owner`/`index.choice.design_builder_proponent` leaves), `index.resolved.copy`, `index.resolved.switch`, `index.resolved.new-project`, `index.resolved.open-existing` (+ its own `index.resolved.project-list`/`index.resolved.project-list.item`/`index.resolved.project-list.empty`/`index.resolved.see-all` sub-refs) (new, CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01, Option C) | mixed (`<h1>`/`<p>`/`<a>`/`<details>`/`<ul>`) | varies | `portal.index` (`/`)'s own three-state entry surface — see that route's docstring for the full state machine (zero/multiple/resolved accessible operating environments). Each `index.choice.<environment>` leaf links to `?environment=<value>`, resolving ONLY against the current user's own `accessible_environments` (never a silent bypass). `index.resolved.open-existing` reuses `recent_projects` (already environment-scoped, capped at 6) with a plain list — no radio-select-then-submit, each row a direct link — plus `index.resolved.see-all` linking to the full `portal.choose_project` Vestibule when the cap is hit | Every authenticated page reaching `/` | active |
| `index.orientation.form` (+ its own `index.orientation.voice`/`index.orientation.voice.status`/`index.orientation.submit`/`index.orientation.reply` sub-refs, new, CLAUDE-VOICE-CONSISTENCY-01 / CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01) | `<form>` / `<button type="button">` / `<span>` / `<button>` / `<p>` | "Ask Archiosk" (`aria-label`) / "🎙️" / "Ask" / (hidden until a reply exists) | Ported verbatim from the retired `gateway.orientation.*` (see above) — POSTs to `portal.gateway_orientation`, the same rule-based responder that never opens a `CaseWorkspaceStore`/calls `interpret_message` (never claims project truth — constitutional-invariants.md #8). New: an optional hidden `environment` field, scoping project-name matching to the caller's own `resolved_environment` when one exists (never crossing Owner/Proponent isolation) | Every authenticated page reaching `/` | active |

## Auth (`templates/auth_shell.html`, `login.html` — CLAUDE-P40-VW8-QA, new surface)

Pre-authentication. `auth_shell.html` has no toggle of its own (the
Account menu the toggle lives in only exists once authenticated) — it
only ever HONORS an already-enabled `beehive:ui-reference-mode`
preference set during a previous authenticated session (a device/
browser preference, unaffected by sign-out), never sets one.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `auth.signin.username` | `<input>` | "Username" | Sign-in form field | Pre-authentication | active |
| `auth.signin.password` | `<input type="password">` | "Password" | Sign-in form field | Pre-authentication | active |
| `auth.signin.password-toggle` | `<button>` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | eye / eye-off icon | Toggles `auth.signin.password`'s `type` between `password`/`text`; client-side display only, no effect on the posted credential or `services/auth.py`'s authentication logic | Pre-authentication | active |
| `auth.signin.submit` | `<button>` | "Sign in" | Submits the sign-in form | Pre-authentication | active |
| `auth.signin.voice` (new, CLAUDE-VOICE-CONSISTENCY-01) | `<button type="button">` | "🎙️" (icon only) | Hidden server-side by default; unhidden by `static/js/login.js` only when `SpeechRecognition` is feature-detected. Mirrors `landing.voice.button`'s own DIRECT_NAV pattern exactly (same authority ceiling, client-side keyword match only, no server round trip, no LLM) — recognized speech only ever drives local navigation/focus ("forgot password" navigates, "sign in" focuses the username field), never touches the password field, never reads back or exposes any field value, never auto-submits the form. Level 2/3 of the future Voice authority ladder at most — see `governance/specified-unbuilt/voice-conversational-presence.md` | Pre-authentication | active |
| `auth.signin.voice.status` (new, CLAUDE-VOICE-CONSISTENCY-01) | `<span aria-live="polite">` | (empty at rest) | Same `ERROR_MESSAGES` vocabulary/`Listening…`/`Transcribing…` states as `chat.composer.voice.status`, plus a short "Try: 'forgot password'" hint when the transcript matches no recognized command | Pre-authentication | active |

## Upload / Project creation (`templates/upload.html`, `templates/errors/error.html` — CLAUDE-P40-VW8-QA, Project-Creation Upload-Capacity Correction)

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `upload.limits` (CLAUDE-POSTCAMEL-META-T01, re-scoped; CLAUDE-HYBRID-ENTRY-01, wording corrected; CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01 addendum, simplified) | `<p>` | "Choose how this Project's documents connect to Archiosk." | Short framing sentence for the Link-vs-Upload choice below (`upload.storage.*`); the fuller custody explanation formerly held inline here now lives behind each option's own "Details" disclosure | `portal.upload` — **admin only** | active |
| `upload.limits.formats` (CLAUDE-POSTCAMEL-META-T01, new) | `<p>` | (accepted formats/size copy) | States accepted formats, `MAX_CONTENT_LENGTH`-derived max size, and the scanned-drawing-package processing limitation — the same content `upload.limits` held before this stage, now its own ref | `portal.upload` — **admin only** | active |
| `upload.error` | `<p>` | (server-side error text) | Renders `UploadError`/`GovernanceError` messages from a rejected POST | Same as above | active |
| `upload.client-size-error` | `<p>` | (client-side size-check text) | Hidden until the chosen file exceeds the max; JS-populated, never server-rendered | Same as above | active |
| `upload.help` (+ its own `upload.help.form`/`upload.help.voice`/`upload.help.voice.status`/`upload.help.submit`/`upload.help.reply` sub-refs, new, CLAUDE-POST-SIGNIN-GATEWAY-SIMPLIFICATION-01, Addendum H) | `<details>` (`macros.subdisclosure`), containing `<form>`/`<button type="button">`/`<span>`/`<button>`/`<p>` | "Ask Archiosk about this form" | A lightweight, collapsible project-less Composer/help surface for this form specifically — reuses the SAME `.gateway-orientation-form` pattern/JS shape as `index.orientation.*` (never a project-creation-specific chat implementation), POSTing to `portal.gateway_orientation` with a hidden `context=establish-project` field that routes to a SECOND, genuinely different classifier (`_classify_establish_project_help` — explains field meaning/consequences for the five Product-Owner-named questions; never silently chooses operating environment or storage connection on the user's behalf, never submits the real form). Collapsed by default so it never obscures the form fields beneath it | `portal.upload` — **admin only** | active |
| `upload.operating-environment.client_owner` | `<input type="radio">` | "Client / Owner" | Selects the Client/Owner Project Operating Environment | Same as above | retired |
| `upload.operating-environment.design_builder_proponent` | `<input type="radio">` | "Design-Builder / Proponent" | Selects the Design-Builder/Proponent Project Operating Environment | Same as above | retired |
| `upload.entry-choice.client_owner` | `<input type="radio">` | "Client / Owner" | Declares the project's working position as Client/Owner (CLAUDE-PERSPECTIVE-GATE-04). Context only - establishes no contractual authority | Same as above | active |
| `upload.entry-choice.lead_design_consultant` | `<input type="radio">` | "Lead Design Consultant" | Declares the project's working position as lead design consultant | Same as above | active |
| `upload.entry-choice.subconsultant` | `<input type="radio">` | "Subconsultant / Specialist Consultant" | Declares the project's working position as a specialist consultant retained below a lead consultant or design-builder | Same as above | active |
| `upload.entry-choice.prime_contractor` | `<input type="radio">` | "Prime / Design-Builder / GC" | Declares the project's working position as prime contractor, design-builder or general contractor | Same as above | active |
| `upload.entry-choice.trade_bidder` | `<input type="radio">` | "Subcontractor / Trade Bidder" | Declares the project's working position as a downstream trade bidder | Same as above | active |
| `upload.retained-by.lead_design_consultant` | `<select>` | "Who engaged you" | Upstream relationship offered for that position only, so a visitor is never shown one that cannot apply. Required for the two consultant positions, where it is what resolves the project's capability side (CLAUDE-ENTRY-REDUNDANCY-01) | Same as above | active |
| `upload.retained-by.subconsultant` | `<select>` | "Who engaged you" | Upstream relationship offered for that position only, so a visitor is never shown one that cannot apply. Required for the two consultant positions, where it is what resolves the project's capability side (CLAUDE-ENTRY-REDUNDANCY-01) | Same as above | active |
| `upload.retained-by.prime_contractor` | `<select>` | "Who engaged you" | Upstream relationship offered for that position only, so a visitor is never shown one that cannot apply. Required for the two consultant positions, where it is what resolves the project's capability side (CLAUDE-ENTRY-REDUNDANCY-01) | Same as above | active |
| `upload.retained-by.trade_bidder` | `<select>` | "Who engaged you" | Upstream relationship offered for that position only, so a visitor is never shown one that cannot apply. Required for the two consultant positions, where it is what resolves the project's capability side (CLAUDE-ENTRY-REDUNDANCY-01) | Same as above | active |
| `upload.project-name` | `<input>` | "Project name" | Optional display name; defaults to the filename | Same as above | active |
| `upload.file` | `<input type="file">` | (file picker) | Carries `data-max-upload-bytes`/`data-max-upload-mb`, read by the client-side size-check script | Same as above | active |
| `upload.actor` | `<input>` | "Your name" | Optional free-text audit-trail field | Same as above | active |
| `upload.role` | `<input>` | "Your role" | Optional free-text audit-trail field | Same as above | active |
| `upload.submit` | `<button>` | "Create project and parse document" | Submits the upload form via its default action (`portal.upload`). Single-file upload — not gated by `upload_entitled`; the addendum names only the folder path and Project Data Management's Add Documents as the two entitlement-gated surfaces (residual, see final report) | Same as above | active |
| `upload.storage.link` (new, CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01 addendum) | `<div>` | "Link to Storage (not yet configured)" | Shown-but-permanently-disabled — no real linked-storage mechanism exists yet; deliberately never disguised as available (Part 4) | `portal.upload` — **admin only** | active |
| `upload.storage.link.details` (new, same stage) | `<details>` (`macros.subdisclosure`) | "Details" | Explains the future Link direction (org SharePoint / other enterprise repo / Archiosk-hosted storage where offered) as an architectural concept only — no SharePoint integration implemented | Same as above | active |
| `upload.storage.upload` (new, same stage) | `<div>` | "Upload to Storage" | Wraps the real, functional folder-upload mechanism (`upload.folder.*` below, unchanged); greyed (`storage-option-disabled`) when `upload_entitled` is False | Same as above | active |
| `upload.storage.upload.disabled-reason` (new, same stage) | `<p>` | "Not available in public trial. Available on managed/organization plans." | Only rendered when `upload_entitled` is False — today always entitled (`services.auth.user_can_upload_to_storage` is an honest no-op returning `True`), so never rendered in real current usage | Same as above | active |
| `upload.storage.upload.details` (new, same stage) | `<details>` (`macros.subdisclosure`) | "Details" | The fuller "uploads every eligible document… local files are not watched/referenced/left in place" custody explanation, moved out of the normal surface per Part 1 | Same as above | active |
| `upload.folder.picker-button` (new, CLAUDE-CA1D-RECEPTION-FIX-01; relabeled CLAUDE-HYBRID-ENTRY-01; regrouped under the Upload-to-Storage option, CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01 addendum) | `<button>` | "Choose Folder to Upload…" | Opens the hidden `webkitdirectory` file input; `disabled` when `upload_entitled` is False (server-side gate is `portal.upload_folder`'s own `abort(403)`, this is convenience-only) | `portal.upload` — **admin only** | active |
| `upload.folder.picker-input` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | `<input type="file" webkitdirectory>` | (hidden) | Browser-native folder picker — one-shot selection, no live directory handle; every file's own `webkitRelativePath` becomes its renamed `.name` before submission (see `folder_files` below) | Same as above | active |
| `upload.folder.summary` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | `<div>` | "N file(s) found, M eligible…" | Client-side count of the selected folder's eligible/skipped files, before submission — the server's own `upload_folder` route is the real authority on what's actually skipped | Same as above | active |
| `upload.folder.founding-picker` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | `<div>` | "Which of these is the principal RFP/document…" | Founding-document confirmation/selection — pre-selects a single unambiguous top-level candidate but never submits without explicit confirmation; zero/multiple candidates require an explicit choice | Same as above | active |
| `upload.folder.error` (new, CLAUDE-CA1D-RECEPTION-FIX-01) | `<p>` | (client-side folder-selection error) | E.g. "No eligible documents… were found in that folder" | Same as above | active |
| `upload.folder.submit` (new, CLAUDE-CA1D-RECEPTION-FIX-01; relabeled + demoted CLAUDE-HYBRID-ENTRY-01; re-simplified CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01 addendum) | `<button>` | "Upload Folder Contents" | Submits via `formaction` to `portal.upload_folder` — disabled until a founding document is confirmed (and always disabled when `upload_entitled` is False, since the picker button above is disabled too). "(Legacy)" dropped — Upload is now one of two named, equally-honest options (Link/Upload), not a demoted legacy path | Same as above | active |
| `errors.upload-too-large` | `<a>` | "Choose a different file" | The 413 error page's own return action, back to `portal.upload` — no Project/Document/workspace is ever created for a request that hits this handler (Werkzeug rejects it before the view function runs) | Every request path (413 can occur pre-authentication-check on a route, though `/upload` itself is admin-only) | active |
| `upload.confirm.no-native-text-notice` | `<p>` | (image-only-PDF notice) | States that no text could be automatically read (Section 6's honest-degradation report) — never rendered for a document with real extractable text | `portal.upload_confirm` — **admin only** | active |
| `upload.confirm.no-candidates-notice` | `<p>` | (no-candidates notice) | States that no title-block-style fields were detected, when the file had real text but nothing matched a known field pattern | Same as above | active |
| `upload.confirm.error` | `<p>` | (server-side error text) | Renders `UploadError`/`GovernanceError` from a rejected confirm POST | Same as above | active |
| `upload.confirm.name-conflict` | `<fieldset>` | "Project name — conflicting values found" | Only rendered when the user-entered name and the drawing-derived candidate name differ — requires an explicit choice (Section 4: "present both... require an explicit choice") | Same as above | active |
| `upload.confirm.name-conflict.entered`, `upload.confirm.name-conflict.candidate`, `upload.confirm.name-conflict.custom` | `<input type="radio">` | (the three name choices) | Selects which Project name to use — never a silent default | Same as above | active |
| `upload.confirm.name-conflict.custom-input` | `<input>` | (custom name) | Free-text Project name, used only when the "different name" radio is selected | Same as above | active |
| `upload.confirm.fields` | `<fieldset>` | "Drawing-derived details" | Wraps every candidate field input | Same as above | active |
| `upload.confirm.field.project_name`, `upload.confirm.field.project_number`, `upload.confirm.field.project_address`, `upload.confirm.field.owner_client`, `upload.confirm.field.drawing_title`, `upload.confirm.field.sheet_number`, `upload.confirm.field.discipline`, `upload.confirm.field.consultant`, `upload.confirm.field.issue_date`, `upload.confirm.field.revision`, `upload.confirm.field.scale` | `<div>` (one per `services/drawing_intake.py`'s `CANDIDATE_FIELDS`) | (field label) | One row per candidate field (`project_name` only rendered here when not in conflict — see `upload.confirm.name-conflict` above) | `portal.upload_confirm` — **admin only** | active |
| `upload.confirm.field.project_name.input`, `upload.confirm.field.project_number.input`, `upload.confirm.field.project_address.input`, `upload.confirm.field.owner_client.input`, `upload.confirm.field.drawing_title.input`, `upload.confirm.field.sheet_number.input`, `upload.confirm.field.discipline.input`, `upload.confirm.field.consultant.input`, `upload.confirm.field.issue_date.input`, `upload.confirm.field.revision.input`, `upload.confirm.field.scale.input` | `<input>` | (field value) | Editable — a machine candidate is always a proposal, never authoritative on its own (Section 5) | Same as above | active |
| `upload.confirm.field.project_name.evidence`, `upload.confirm.field.project_number.evidence`, `upload.confirm.field.project_address.evidence`, `upload.confirm.field.owner_client.evidence`, `upload.confirm.field.drawing_title.evidence`, `upload.confirm.field.sheet_number.evidence`, `upload.confirm.field.discipline.evidence`, `upload.confirm.field.consultant.evidence`, `upload.confirm.field.issue_date.evidence`, `upload.confirm.field.revision.evidence`, `upload.confirm.field.scale.evidence` | `<small>` | (evidence text) | Shows confidence, source page, extraction method, and the exact matched line — only rendered when a candidate was actually found for that field | Same as above | active |
| `upload.confirm.submit` | `<button>` | "Confirm and create Project" | Submits the confirm form — creates the Project from confirmed/corrected values via the same `ingest_upload` every other upload path uses | Same as above | active |
| `upload.confirm.discard` | `<button>` | "Discard and start over" | Discards the staged upload (raw bytes + candidates) and returns to `/upload` | Same as above | active |

---

## Security Department (`templates/security_department.html` — CLAUDE-P40-VW8-QA, Complete Root and Subfolder UI Reference Tagging)

An "equivalent root/subfolder on an administrative page" per this stage's own explicit scope — reached via `menu.account.admin.security` (admin only; relocated from `lists.security` by CLAUDE-LEFT-RAIL-01). Each top-level accordion is a real, distinct governed record family (mirrors the Lists tree's own "one toggle per family" shape, via `templates/_macros.html`'s `accordion`/`subdisclosure` macros, both extended this stage with an optional `ui_ref` parameter — backward-compatible; every pre-existing caller that omits it, including every Overview-page accordion in `case_workspace.html`, is unaffected and deliberately left untagged, consolidated under `display.overview` per CLAUDE-P40-E3A). Refs are macro CALL ARGUMENTS (`ui_ref='security.floor'`) — the literal `data-ui-ref="..."` attribute text only exists inside the macro body itself, not in this template's own source.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `security.floor` | `<details class="accordion-section">` | "Mandatory ARCHIOSK Security Floor" | Always expanded by default; read-only — no configurable action can weaken it | **Admin only** — `security.department_home` route | active |
| `security.claims` | `<details class="accordion-section">` | "Security Claims Registry" | What this deployment can/cannot truthfully promise | Same as above | active |
| `security.baselines` | `<details class="accordion-section">` | "Organization Security Baseline" | Draft/activate/withdraw baselines; add per-action control decisions | Same as above | active |
| `security.policies` | `<details class="accordion-section">` | "Source Policies" | Lists ingested policies; each may record a policy statement | Same as above | active |
| `security.policies.add` | `<details class="add-source-details">` (nested inside `security.policies`) | "+ Ingest a source policy" | POST (multipart) to `security.record_source_policy` | Same as above | active |
| `security.controls` | `<details class="accordion-section">` | "Proposed Controls" | Lists proposed control decisions | Same as above | active |
| `security.controls.add` | `<details class="add-source-details">` (nested inside `security.controls`) | "+ Propose a control" | POST to `security.propose_control` | Same as above | active |
| `security.qa` | `<details class="accordion-section">` | "Governed Q&A" | Lists recorded Q&A entries; provisional ones may be approved | Same as above | active |
| `security.qa.add` | `<details class="add-source-details">` (nested inside `security.qa`) | "+ Record a Q&A entry" | POST to `security.record_qa_entry` | Same as above | active |
| `security.exceptions` | `<details class="accordion-section">` | "Exceptions" | Lists active/expired exceptions; active ones may be revoked | Same as above | active |
| `security.exceptions.add` | `<details class="add-source-details">` (nested inside `security.exceptions`) | "+ Grant an exception" | POST to `security.grant_exception` | Same as above | active |
| `security.projects` | `<details class="accordion-section">` | "Projects" | Per-Project security-profile classification | Same as above | active |
| `security.learning` | `<details class="accordion-section">` | "Learning Contribution Requests" | Records governed intent only — no shared-learning pipeline exists in this deployment | Same as above | active |
| `security.learning.add` | `<details class="add-source-details">` (nested inside `security.learning`) | "+ Request a learning contribution review" | POST to `security.create_learning_request` | Same as above | active |
| `security.assurance-activity` | `<details class="accordion-section">` | "Assurance — Activity" | Read-only governance-event activity log (actor/project/action/decision only — no project content) | Same as above | active |
| `security.self-check` | `<details class="accordion-section">` | "Assurance — Self-Check" | Read-only automated consistency findings | Same as above | active |

## Operations (`templates/operations.html` — CLAUDE-CA1D-INSTRUMENT-RAIL-01)

The admin-page half of the four-part Instrument Rail spatial model (Plan-Mode report, Section D.1) — reached via `menu.account.admin.operations` (admin only; relocated from `lists.operations` by CLAUDE-LEFT-RAIL-01), exactly the same shape `security_department.html` already established (`_macros.html`'s `accordion` macro, refs as macro CALL arguments, not literal `data-ui-ref` attributes in this template's own source). Deliberately minimal — two small accordions proving a previously-unwired backend diagnostic primitive has a legitimate peripheral home, not the full Instrument Rail programme. Repository/git diagnostics, subagent orchestration detail, and terminal integration are explicitly out of scope for this tranche.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `operations.telemetry` | `<details class="accordion-section">` | "System Telemetry" | Always expanded by default; renders `services.diagnostics.TechnicalTelemetry` fields (app version, parser version, generated-at, diagnostic id, route) — real values, built fresh per request, never previously rendered anywhere | **Admin only** — `operations.department_home` route | active |
| `operations.ai-calls` | `<details class="accordion-section">` | "AI Calls Kill Switch" | Read-only, live `AI_CALLS_DISABLED` (CLAUDE-P27-B) state — the same fact `menu.status` surfaces in the top bar, shown here in detail | Same as above | active |

## Project Data Management (`templates/reset_project_data.html` — CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01)

Reached via `menu.account.admin.project-data-management` (admin only). Same
route (`portal.reset_project_data`) and template file this page has always
used — its own primary identity changed from a single destructive,
deployment-wide "Reset Project Data" operation to the governed surface for
changing the ACTIVE project's own evidence set (Add/Archive Documents), with
that destructive reset demoted into a collapsed `pdm.recovery-advanced`
disclosure rather than defining the ordinary page. `?project_id=` (optional)
identifies the active Project; absent or invalid, the Add/Archive sections
render an honest "no active project" state instead of guessing.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `pdm.active-project` | `<section>` | "Active Project" | The unmistakable identification the Product Owner required — real `workspace.display_title`/`document.filename` for whichever `?project_id=` was given and is genuinely accessible | Rendered only when a valid, accessible `project_id` was given | active |
| `pdm.no-active-project` | `<p>` | "No project is currently active…" | Honest empty state — never guesses which Project is meant | Rendered only when no valid `project_id` was given | active |
| `pdm.add-documents.disabled-reason` (new, CLAUDE-PROJECT-SURFACE-CONSOLIDATION-01 addendum) | `<p>` | "Not available in public trial. Available on managed/organization plans." | Only rendered when `upload_entitled` is False — same `user_can_upload_to_storage()` choke point `upload.storage.upload.disabled-reason` uses; today always entitled | Same as `pdm.active-project` | active |
| `pdm.add-documents.form` | `<form>` | — | POST (multipart) to the SAME, unchanged `workspace.add_document_source` route the retired `lists.project.tools.add-document` used — reuse, not a parallel ingestion path. That route now also enforces `user_can_upload_to_storage()` server-side (`abort(403)`) — real gate, this section's `disabled` styling is convenience-only | Same as `pdm.active-project` | active |
| `pdm.add-documents.file` | `<input type="file">` | — | Accepts `.pdf,.docx,.txt,.csv,.md,.xlsx` — same allowlist hint as the retired rail form; `disabled` when `upload_entitled` is False | Same as above | active |
| `pdm.add-documents.submit` | `<button>` | "Add Documents" | Submits `pdm.add-documents.form`; `disabled` when `upload_entitled` is False | Same as above | active |
| `pdm.archive-documents.active-list` | `<ul>` | active Document names, each with an "Archive" button | Each button POSTs to the SAME, unchanged `workspace.remove_document_route` (its own existing `confirm=yes\|no` gate, `confirm_remove_document.html`, fires unchanged) | Same as `pdm.active-project` | active |
| `pdm.archive-documents.archive` | `<button>` in a `<form>` (pattern) | "Archive" | POST to `remove_document_route` | Same as above | active |
| `pdm.archive-documents.empty` | `<p>` | "No active Documents to archive." | Empty state | Same as above | active |
| `pdm.archive-documents.archived-list` | `<ul>` | already-archived Document names, each with a "Restore" button | Promoted from the retired `lists.project.tools.removed-items` - POSTs to the SAME, unchanged `workspace.restore_document_route` | Same as `pdm.active-project` | active |
| `pdm.archive-documents.restore` | `<button>` in a `<form>` (pattern) | "Restore" | POST to `restore_document_route` | Same as above | active |
| `pdm.recovery-advanced` (`html_id="pdm-recovery-advanced"`) | `<details>` (via `macros.subdisclosure`) | "Recovery / Advanced Administration" | Collapsed by default - contains the complete, unmodified deployment-wide reset (inventory + typed-confirmation-phrase form) and the "View Reset Snapshots" link. Every safeguard (atomic lock, pre-reset snapshot, snapshot restoration) is unchanged; only where it renders moved | **Admin only** — whole page is `admin_required` | active |

## Projects Directory (`templates/projects.html` — CLAUDE-P40-VW8-QA, Complete Root and Subfolder UI Reference Tagging)

The full administrative Project-management page (`portal.projects_list`) — distinct from `gateway.chooser` (the focused picker) and from `lists.projects`/`lists.projects.leaf` (the Lists-tree root); reachable directly by URL and via `removed-projects.back-link`. A different top-level prefix (`projects-directory`, not `projects`) deliberately avoids reading as a sub-family of `lists.projects`, which it isn't.

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `projects-directory.removed-link` | `<a>` | "Removed Projects" | Navigates to `portal.removed_projects` | Every authenticated page | active |
| `projects-directory.search` | `<form>` | project search | `?q=` filter | Every authenticated page | active |
| `projects-directory.list` | `<ul>` | the Project card list | Wraps every `projects-directory.leaf` row | Filtered to `_accessible_documents` (already access-scoped) | active |
| `projects-directory.leaf` | `<a>` (pattern) | a Project card | Navigates to `workspace.show_workspace` for that Project | Same as above | active |
| `projects-directory.leaf.delete` | `<button>` in a `<form>` (pattern) | "Delete" | POST to `portal.delete_project` | **Admin only** — `is_admin` | active |
| `projects-directory.empty` | `<div>` | "No projects yet." / "No projects match…" | Empty/no-results state; admin-only "Create New Project" link when genuinely empty | Every authenticated page | active |

## Removed Projects (`templates/removed_projects.html` — CLAUDE-P40-VW8-QA, Complete Root and Subfolder UI Reference Tagging)

Reached via `menu.account.removed-projects` (relocated from `lists.removed-projects` by CLAUDE-LEFT-RAIL-01). A different top-level prefix from that link ref itself (that ref is the Account-menu LINK to this page, not the page's own content).

| Reference | Element | Label | Current behavior | Auth notes | Status |
|---|---|---|---|---|---|
| `removed-projects.back-link` | `<a>` | "← Back to Projects" | Navigates to `portal.projects_list` | Every authenticated page | active |
| `removed-projects.list` | `<ul>` | the removed-Project card list | Wraps every `removed-projects.leaf` row | Every authenticated page | active |
| `removed-projects.leaf` | `<li>` (pattern — not a link; a removed Project has no workspace to navigate to) | a removed Project card | Non-interactive except its own Restore button | Every authenticated page | active |
| `removed-projects.leaf.restore` | `<button>` in a `<form>` (pattern) | "Restore" | POST to `workspace.restore_project_route` | Every authenticated page | active |
| `removed-projects.empty` | `<div>` | "No removed Projects." | Empty state | Every authenticated page | active |

---

## Deliberately NOT instrumented this stage

Per-instance content inside a family (a single Finding card, a single
`ReviewThread` comment inside `toolbox.investigation-findings`, a
single Appearance-matrix radio, a single Display-Layout stepper
button) — these are either per-record data (not stable UI controls in
the sense this registry tracks) or fine-grained widgets inside an
already-referenced family/dialog. Add a reference for any of these
only when a future stage needs to individually track, preserve, or
retire one — this registry stays proportional to what VW7B and beyond
actually need to cite, not maximal coverage for its own sake.

## Retired references

| Reference | Retired by | Reason | Replacement |
|---|---|---|---|
| `chat.dock.history`, `chat.dock.history.toggle`, `chat.dock.history.project`, `chat.dock.history.leaf`, `chat.dock.history.empty` | CLAUDE-MOBILE-PRIMARY-RESET-01 | Directly beneath the message box the conversation list spent a band of the smallest screen answering "which conversations exist" — a question that belongs to context, not to composing | `shell.context.identity` and its `.project`/`.leaf` items, one band higher and shared with project identity |
| `chat.dock.archive-conversation` | CLAUDE-GO-COMPOSER-LIFECYCLE-01 (same stage that added it) | Product Owner, explicit: "Remove 'Archive this conversation' or anything in that regard" — concluding an Investigation is not something to offer from beside the message box | `toolbox.investigations.leaf.archive` (unchanged, and still the way to archive an Investigation) |
| `lists.project.tools.data-management` | CLAUDE-P40-VW7B | Nested inside the active Project's own "Project Tools" branch, but `portal.reset_project_data` operates on the whole `REGISTRY_STORE_PATH` — every Project in the deployment, not this one. Misrepresented scope, evidenced by reading that route directly before moving anything (Section 3's own "Administrative/account functions do not automatically belong inside the active Project root") | `lists.system-data-management` (CLAUDE-P40-VW7B), itself later retired by CLAUDE-LEFT-RAIL-01 — see below for the current replacement |
| `gateway.open-existing` | CLAUDE-CA1D-PROJECT-GATEWAY-LABELS-01 | Environment-agnostic "Open an existing project" read as a third project context alongside Client/Owner and Design-Builder/Proponent, when New/Existing is actually an independent dimension from project context | `gateway.open-existing-client-owner` / `gateway.open-existing-design-builder` (same underlying `portal.choose_project` route, now filtered by `?environment=`) |
| `lists.new-project` | CLAUDE-LEFT-RAIL-01 | Product Owner decision: "PROJECTS = the projects I can work on and switch between" — project creation is an administrative function, not a rail-switching concern | `menu.account.admin.new-project` (same route, same admin-only gate, relocated to the top-right Account/Admin menu) |
| `lists.removed-projects` | CLAUDE-LEFT-RAIL-01 | Same rationale as `lists.new-project` above — recovery/restoration is not project-switching | `menu.account.removed-projects` (same route, same `@login_required`-only/P32-filtered gate — deliberately NOT admin-gated, unlike its new neighbors) |
| `lists.security` | CLAUDE-LEFT-RAIL-01 | Same rationale as `lists.new-project` above — this comment's own predecessor (`templates/base.html`'s "Security stays in Lists per the explicit instruction not to move it") is itself superseded; the Product Owner has since clarified the opposite | `menu.account.admin.security` (same route, same admin-only gate) |
| `lists.operations` | CLAUDE-LEFT-RAIL-01 | Same rationale as `lists.new-project` above | `menu.account.admin.operations` (same route, same admin-only gate) |
| `lists.system-data-management` | CLAUDE-LEFT-RAIL-01 | Same rationale as `lists.new-project` above — no longer a Lists concept at all | `menu.account.admin.project-data-management` (same route, same admin-only gate, same `html_id="project-data-management"` DOM anchor preserved) |
| `menu.appearance.all` (the row/wrapper), `menu.appearance.all.light`, `menu.appearance.all.dark`, `menu.appearance.all.tinted`, `menu.appearance.all.deep-forest` | CLAUDE-APPEARANCE-SIMPLIFY-01 | Product Owner live-browser correction: "do not allow panel-by-panel theme mixing" — All is no longer a control layered over five independent per-surface choices, it IS the only choice, so the `.all` namespace segment (which only ever meant "distinct from a per-surface row") no longer means anything | `menu.appearance.light`, `menu.appearance.dark`, `menu.appearance.tinted`, `menu.appearance.deep-forest` (unprefixed — the same 4 modes, now the only radios that exist), plus new `menu.appearance.deep-ocean` |
| `menu.appearance.menu`, `menu.appearance.lists`, `menu.appearance.display`, `menu.appearance.toolbox`, `menu.appearance.chat` (the 5 per-surface row wrappers) and their 20 child radios (`menu.appearance.<surface>.<light\|dark\|tinted\|deep-forest>`) | CLAUDE-APPEARANCE-SIMPLIFY-01 | Same Product Owner correction as above — per-surface independence itself is retired, not merely restyled; there is no longer a concept of "Lists' own appearance" distinct from "Display's own appearance" | `menu.appearance.light`/`.dark`/`.tinted`/`.deep-forest`/`.deep-ocean` (one global choice applies to every former surface at once) |

Future stages: when a control this map names is genuinely removed (not
just renamed/reparented — see the ID scheme note above), move its row
here with the stage that retired it and why, and never reuse the same
`data-ui-ref` value for a different control afterward.

## Known gap, not yet fixed

This map is hand-maintained, like `MANIFEST.md` — nothing here is
auto-synced from the templates. `tests/test_p40vw7a_ui_reference_map.py`
enforces that every `data-ui-ref` value actually present in
`templates/base.html`/`case_workspace.html`/`_macros.html` has a
matching row here (and vice versa, for `active`-status rows), but that
test cannot catch a row whose *description* has drifted from actual
behavior — treat any specific behavioral claim above with the same
"verify against the actual file before relying on it" caution
`MANIFEST.md`'s own header asks for.
