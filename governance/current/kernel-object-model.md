# Current Implemented Kernel Object Model

**Status:** Implemented. **Ground truth:** `services/case_workspace.py` in `C:\Archiosk\Research\archiosk` (backend repo). Test suite: `tests/test_foundation_batch_a.py` through `test_foundation_batch_k.py` plus `tests/test_requirement_promotion.py`, `tests/test_case_privacy.py`, `tests/test_case_collaboration.py`, `tests/test_case_archive.py`, `tests/test_case_derivation.py`, `tests/test_case_adoption.py`, `tests/test_workflow_integration.py`, `tests/test_discussion_workflow.py`, `tests/test_rfi_compliance_workflow.py`, `tests/test_route_authorization_hardening.py`, `tests/test_rfi_draft_export.py`, stdlib `unittest`, 393 passing tests as of the per-Finding RFI export tranche (commit follows this document). Every anchor below was checked directly against current code, not recalled from an earlier round — line numbers shift as the file grows; re-verify before citing in future work if material time has passed.

Each entry: implementation status, code anchor, what question it answers, known gaps.

---

### `Source` — implemented
`case_workspace.py:688`. Answers: what document/drawing/model is this, and where did it come from. Fields include `document_id`, `revision`, `issue_date`, `issuer`, `document_status`, `document_authority`, `file_hash`, `origin_type`/`origin_reference` (open-world, already the correct mechanism for importing external/cross-project material — see `specified-unbuilt/cross-boundary-architecture.md`).

### `Requirement` — implemented
`case_workspace.py:732`. Answers: what did the Owner/source document state, independent of any Finding. Never a compliance claim — `set_requirement_status` (`~3054`) enforces a hard denylist against compliance-shaped status values. `registration_method` distinguishes machine-extracted from manually-registered content, feeding directly into `promote_requirement_item()`'s finalized design contract (see `specified-unbuilt/investigation-lifecycle-extensions.md`).

### `Finding` — implemented
`case_workspace.py:822`. Answers: what discrepancy/observation has a machine or human asserted. `claim_status` defaults to `provisional` — the object is weak-by-default, unlike `Requirement`, by design (this asymmetry is why Requirement branching needed a `"proposed"` status extension and Finding did not — see `specified-unbuilt/investigation-lifecycle-extensions.md`).

### `Relationship` — implemented, one known consistency gap
`case_workspace.py:1105`. Answers: how are two objects connected (open-world `relationship_type`: supports/contradicts/qualifies/references/depicts/corresponds_to/implements/depends_on/blocks/affects/resulted_in/**derived_from**). `provisional`/`confirmed_by` correctly separate machine-asserted from human-confirmed status at the field level — but `confirm_relationship` (`~3776`) mutates these fields **in place** rather than following the append-only/successor pattern used everywhere else in this model. This is a real, honestly-preserved inconsistency: at any single point in time the object's status is still correctly readable, but the pre-confirmation state is not separately reconstructable the way `Finding`→`ReviewerValidation` preserves its full history. Flagged, not yet fixed. **`RELATIONSHIP_TYPE_DERIVED_FROM` (Case-lineage tranche) is a deliberate exception to "provisional by default"** — `derive_case_from_archive` creates it with `provisional=False` directly, since it records a structural fact the method itself establishes at write time, not a machine claim awaiting later human confirmation; see the "Case Archive → Derived Active Case" entry below.

### `Case` / `CaseRecord` — implemented; visibility (Private/Shared/Collaborative) and status (Open/Archived) both implemented as two orthogonal axes, remaining lifecycle extensions unbuilt
`case_workspace.py:1550`. Answers: what investigation is this. **Two separate axes, confirmed and kept separate rather than conflated — this is the architectural correction made during the Archive tranche:** `visibility` (`CASE_VISIBILITY_PRIVATE`/`CASE_VISIBILITY_SHARED`/`CASE_VISIBILITY_COLLABORATIVE`) answers *who can see/participate*; `status` (`CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED`, using the field's own pre-existing name — previously present since before this tranche sequence but unenforced/decorative) answers *whether the Case is still alive and mutable*. A Case can validly be, e.g., PRIVATE+ARCHIVED or COLLABORATIVE+ARCHIVED — ARCHIVED was deliberately **not** added as a fourth `visibility` value, since that would have conflated two questions a single enum can't cleanly answer. `created_by`/`shared_by`/`shared_at`/`collaboration_established_by`/`collaboration_established_at`/`collaboration_contribution_type`/`collaboration_contribution_id`/`retracted_by`/`retracted_at`/`archived_by`/`archived_at`/`archive_authority`/`archive_prior_visibility`/`derived_from_case_id` are all implemented. The remaining designed extension set (`CaseLock`, publication anchors) is still specified but unbuilt — see `specified-unbuilt/investigation-lifecycle-extensions.md`.

### Case visibility (PRIVATE/SHARED) — implemented (Private + Explicit Share Only tranche)
`case_workspace.py`: `CASE_VISIBILITY_PRIVATE`/`CASE_VISIBILITY_SHARED` constants immediately before `CaseRecord`; `CaseWorkspaceStore.visible_cases_for` (the one real enforcement point — filters by `visibility != private or created_by == actor`); `CaseWorkspaceStore.share_case` (owner-only PRIVATE→SHARED transition, rejects already-shared/nonexistent/non-owner/no-recorded-owner cases, logs a `case_shared` `GovernanceLog` event). New Cases default `CASE_VISIBILITY_PRIVATE` unconditionally; `created_by` is optional only for backward compatibility with pre-existing `create_case` callers across Batches A–K that predate visibility. Route wiring: `routes/workspace.py`'s `show_workspace` resolves case listing/switching/default-selection exclusively through `visible_cases_for`, never raw `workspace.cases`; `artifact_image` and the `preview_finding_id` query-param path in `show_workspace` are additionally guarded against indirect-identifier bypass (an artifact or finding id typed/guessed directly, belonging to a private Case the requester doesn't own, still 404s/is excluded). New route: `share_case` (`POST /projects/<project_id>/workspace/cases/<case_id>/share`). Tests: `tests/test_case_privacy.py` (24 tests: 17 store-layer, 7 route-layer). **The write-side route-visibility gap named here in earlier revisions of this entry (`add_drawing_source`, `add_message`/`post_message`, `apply_findings`, RFI-draft routes, `validate_finding`, `set_disposition`, `promote_requirement_item_route` not independently re-verifying the requester's visibility) is now closed** — see the "Case-scoped route authorization hardening" entry below.

### Case-scoped route authorization hardening — implemented (route-layer tranche, no domain-model change)
`routes/workspace.py`: every Case-scoped write/download route now derives the *authoritative* Case server-side and rejects (404) if the requester's own `visible_cases_for` doesn't include it, via the shared `_require_visible_case(store, workspace, case_id)` helper — reused, not duplicated, across every route below; no parallel authorization framework was introduced. For routes keyed by an object id rather than a bare `case_id` path segment, the object's *own* recorded `case_id` is looked up server-side first (`_finding_case_id`, `_thread_case_id`, `_rfi_draft_case_id`) — a client-supplied hidden `case_id` form field is never trusted for the authorization decision, only ever used (where still present at all) for redirect/log cosmetics. Routes hardened this tranche: `add_drawing_source`, `post_message`, `validate_finding`, `set_disposition`, `promote_requirement_item_route`, `apply_findings`, `cancel_rfi_intent`, `preview_rfi_draft`, `create_rfi_draft` (checks **both** the URL's `case_id` and the submitted `finding_id`'s own real Case — a visible Case in the URL must not become cover for drafting an RFI against a Finding that actually belongs to a different, private Case), `update_rfi_question`, `issue_rfi_draft`. `create_thread`/`add_thread_message`/`request_thread_attention`/`resolve_thread`/`reopen_thread` (Human Discussion UI tranche) and `artifact_image`/the `preview_finding_id` path in `show_workspace` (Case-visibility tranche) already had equivalent guards before this tranche.

**Deliberately not touched, by design, not oversight:** `share_case`/`retract_case`/`archive_case`/`derive_case`/`adopt_finding`/`adopt_review_message` already enforce their own owner-or-admin authority inside the store layer — a strictly *stronger* check than mere visibility, so adding a visibility pre-check there would be redundant, not protective. `adjudicate_requirement` was inspected and left unchanged: `Requirement` has no `case_id` of its own (project-scoped, not Case-scoped — see the `Requirement`/`Case` entries above), so there is no Case to derive or check; its `case_id` form field is cosmetic (redirect target only). `revise_source` was inspected and left unchanged: a `Source` is not Case-private in this model (the Sources list already renders unconditionally, unfiltered by `visible_cases_for`), and revising one can affect multiple Cases at once (`register_source_revision` returns one notice per affected Case) — there is no single Case to check visibility against. Project isolation was confirmed structural, not requiring a new check: each project's `CaseWorkspaceStore`/`RequirementsRegistry` state lives in its own file, so an id from Project A is simply absent from Project B's `workspace` and fails the store layer's own existing not-found check.

Domain-layer authority checks are unchanged and unweakened by this tranche — route hardening only ensures the *correct* Case reaches those checks (e.g. `_require_case_not_archived`), never substitutes for them. Tests: `tests/test_route_authorization_hardening.py` (14 tests: case-id URL/form spoofing, object-id-from-a-different-Case laundering via a legitimate-looking URL `case_id`, cross-project id reuse, continued SHARED-Case collaboration, and archived-Case write rejection — all via real HTTP requests with genuinely separate authenticated sessions).

**Amendment (per-Finding RFI export tranche):** while wiring the new `export_rfi_draft` route, found `update_rfi_question`'s mutation was inlined directly in the route (`draft["question_text"] = ...; store.save(workspace)`), bypassing the store layer entirely — the one `RFIDraft` write path that never called `_require_case_not_archived`, unlike `issue_rfi_draft` which already did. Fixed by adding `CaseWorkspaceStore.update_rfi_draft_question` (same guard, same single-save pattern as every other governed write) and updating the route to call it instead of mutating the dict directly.

### Collaboration threshold + pre-collaboration retraction — implemented (Constitutional Invariant 12 tranche)
`case_workspace.py`: `CASE_VISIBILITY_COLLABORATIVE` constant, added alongside Private/Shared. `CaseWorkspaceStore._cross_collaboration_threshold_if_qualifying` is the single enforcement point — mutates a Case's visibility to Collaborative in memory only when (a) the Case is currently Shared, (b) the acting actor is non-empty and not the recorded owner; the caller's own subsequent `save()` commits this atomically alongside the qualifying write itself, so a write can never succeed while the transition silently fails or vice versa. Invoked from exactly six call sites, each with its own human-vs-machine origin filter applied *before* calling it: `record_reviewer_validation` and `record_disposition` (both inherently human by construction, no separate origin field needed), `add_review_message` (only `origin == MESSAGE_ORIGIN_HUMAN` qualifies), `request_attention` (no machine analogue exists in this codebase), `confirm_relationship` (uses `confirmed_by`, not `created_by`/creation — a `Relationship`'s `created_by` has no structural guarantee of being human, but confirmation, i.e. `provisional: True → False`, is unambiguously a human act, resolved against every Case referencing the relationship's `from_id`/`to_id` via a new `_cases_referencing_object` helper, since `Relationship` carries no `case_id` of its own), and `record_analysis` (only `trigger.trigger_type == ANALYSIS_TRIGGER_USER_INITIATED` qualifies — `AGENT_INITIATED`/`CLOCK_INITIATED`/etc. never do, regardless of what `triggered_by_actor` holds). **Two deliberate, documented exclusions, not silent omissions:** `Activity` (no origin field exists to distinguish machine- from human-authored, and it has zero real callers anywhere in this codebase to establish a convention either way — see `record_activity`'s own docstring); `RequirementAdjudication` (the ratified spec's own previously-flagged boundary case, now resolved explicitly — adjudicating a Requirement is a contribution to the Requirement's own governed record, a different object from the Case whose evidence it cites, even when that evidence is referenced — see `record_requirement_adjudication`'s own docstring).

`CaseWorkspaceStore.retract_case_to_private` implements the one Shared→Private path: owner-only, rejects outright (before touching any state) once the Case is Collaborative — Constitutional Invariant 12 is enforced at this single validation gate, not merely documented. Does not clear `shared_by`/`shared_at` on retraction — the historic fact of having been shared remains on the record. Route wiring: `share_case`/`retract_case` at `POST /projects/<project_id>/workspace/cases/<case_id>/share` and `.../retract`; the `case_workspace.html` case list shows a Share button for owned Private Cases and a Retract button for owned Shared (not-yet-Collaborative) Cases. `record_reviewer_validation`/`record_disposition`/`confirm_relationship`/`record_analysis` all gained an optional `governance_log` parameter as part of this tranche (backward-compatible, previously absent on the first two) so a `case_became_collaborative` event can be logged uniformly regardless of which of the six paths triggers it. Tests: `tests/test_case_collaboration.py` (26 tests).

### Case Archive — implemented (terminal/frozen lifecycle status tranche)
`case_workspace.py`: `CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED` constants, added immediately after `KNOWN_CASE_VISIBILITY_STATES`, reusing `CaseRecord`'s own pre-existing `status` field rather than introducing a new one or extending `visibility` — see the `Case`/`CaseRecord` entry above for the two-axis rationale. `CaseWorkspaceStore.archive_case` performs the one-way OPEN→ARCHIVED transition: rejects a case that's already archived or doesn't exist; authority is owner-only (`actor == case["created_by"]`) **or** an actor whose `actor_role` equals the system's existing `models.ROLE_ADMIN` value (`"admin"`, passed through as a plain string — this domain module still does not import `models`/`services.auth`, preserving its existing framework-decoupling) — no new role architecture was introduced, the admin-override path exists specifically so an unavailable/former owner never permanently locks a Case. Permitted uniformly from PRIVATE, SHARED, or COLLABORATIVE — `visibility` is recorded into `archive_prior_visibility` and left completely untouched by archiving itself (archive never publishes, never privatizes). Records `archived_by`/`archived_at`/`archive_authority` on the `CaseRecord` and logs a `case_archived` `GovernanceLog` event in the same single `save()` call — atomic, no partial states on failure.

**Centralized frozen-state enforcement:** one helper, `CaseWorkspaceStore._require_case_not_archived(workspace, case_id)`, called explicitly at the top of every identified Case-mutating write path rather than scattered/cosmetic per-route checks — currently wired into `attach_source_to_case`, `add_message`, `record_analysis` (case-scoped branch only), `record_reviewer_validation`, `record_disposition`, `add_review_message`, `request_attention`, `confirm_relationship` (checked against every Case referencing either end of the Relationship, via the existing `_cases_referencing_object` helper), `record_activity`, `apply_findings`, `create_rfi_draft`, `issue_rfi_draft`, `share_case`, `retract_case_to_private`, and — added during the Human Discussion UI tranche, once those methods gained real route/UI callers for the first time — `set_review_thread_status`, `resolve_review_thread`, `reopen_review_thread`, and `respond_to_attention` (resolved via the responding `Attention`'s own thread lookup) — i.e. both content-mutation paths and the Case's own visibility-transition paths, including every ReviewThread lifecycle transition, are frozen once archived. `case_id=None` is a legitimate no-op (a Project-level write with no Case at all); a missing Case is left to the caller's own existing existence check.

Archiving is content-preserving, not content-resolving: no method requires resolving open `ReviewThread`s, and no code path rewrites an unresolved item as resolved as a side effect of archiving — unresolved comments, their authorship, and their unresolved state all survive archival completely unchanged, and archiving never depends on the original contributing actor being available or participating again. Route wiring: `routes/workspace.py`'s `archive_case` (`POST /projects/<project_id>/workspace/cases/<case_id>/archive`), passing the real session role through so the store-layer owner-or-admin check can recognize an admin override without the route inventing separate authorization logic. `case_workspace.html`'s case list shows an Archive button (owned or admin-role) only while `case.status != 'archived'`, and the pre-existing Share/Retract buttons now also hide once archived. Tests: `tests/test_case_archive.py` (29 tests).

### Case Archive → Derived Active Case — implemented (Case-lineage tranche)
`case_workspace.py`: `CaseWorkspaceStore.derive_case_from_archive` is the one authorized path forward after archival — "Archive is terminal for the object, not for the work." Source must currently be `CASE_STATUS_ARCHIVED` (rejects an active/open source outright — general Copy/Adopt of an active Case is out of this tranche's scope); authority is the same owner-or-admin pattern as `archive_case` (no new role architecture). Creates a brand-new `CaseRecord` (new id, `created_by`/`created_at` set to the deriving actor/time, `status=CASE_STATUS_OPEN`, `visibility=CASE_VISIBILITY_PRIVATE` unconditionally) and a `Relationship` (`from_type`/`to_type=OBJECT_KIND_CASE`, `from_id`=new Case, `to_id`=archived Case, `relationship_type=RELATIONSHIP_TYPE_DERIVED_FROM`, `provisional=False`) in exactly one `self.save(workspace)` call — atomic, no partial states on failure. Never mutates the archived Case's own record in any way (read-only access to it throughout).

**Working context copied, history referenced, never cloned** — the tranche's own governing rule, implemented literally: `title`/`objective` (overridable via optional `title=`/`objective=` parameters, defaulting to the archived Case's own values) and `source_ids` (a plain list copy — this duplicates a reference list, never the referenced `Source` documents themselves) are the only fields carried onto the new Case. `conversation`/`analysis_ids`/`finding_ids`/`artifact_ids`/`activity_ids` all start empty on the new Case — every historical contribution (review threads/messages, Findings, `ReviewerValidation`, `Disposition`, `Attention`, analysis records, collaboration/archive events) remains attached exclusively to the archived Case, reachable only by following the lineage pointer back to it, never duplicated.

**Lineage, structural and queryable in both directions, deliberately distinct from Supersession/Sharing/Copying:** forward (`derived Case → archived source`) via the new `CaseRecord.derived_from_case_id` denormalized field (cheap direct read) plus the authoritative `Relationship` record (same denormalized-pointer-plus-governed-record pattern already used for `Source.supersedes_source_id`/`Supersession` and `shared_by`/`shared_at`/`GovernanceLog`); reverse (`archived source → every Case derived from it`, one-to-many, no uniqueness constraint) via the new `CaseWorkspaceStore.derived_cases_of(workspace, archived_case_id)`, a filter over `derived_from_case_id`. `RELATIONSHIP_TYPE_DERIVED_FROM` is its own distinct open-world relationship type (`KNOWN_RELATIONSHIP_TYPES`), never written to `workspace.supersessions` and never confused with `share_case`/general Copy — those remain entirely separate mechanisms.

**Visibility, resolved per Constitutional Invariant 11:** the derived Case always begins `CASE_VISIBILITY_PRIVATE`, regardless of the archived source's own visibility (Private, Shared, or Collaborative alike) — creating a new working object is never itself an act of sharing. The archived source's own `visibility` field is never touched by derivation. Route wiring: `derive_case` (`POST /projects/<project_id>/workspace/cases/<case_id>/derive`, `case_id` here is the archived source; redirects into the newly created derived Case on success). `case_workspace.html`'s case list shows a "Derive Active Case" button in place of the (now-hidden) Share/Retract/Archive controls once a Case is archived, for its owner or an admin-role actor. Tests: `tests/test_case_derivation.py` (29 tests).

### Selective Adopt / Carry-Forward — implemented, deliberately narrow object scope (Finding + ReviewMessage only)
`case_workspace.py`: `CaseWorkspaceStore.adopt_finding_into_case` and `adopt_review_message_into_case` are the only two supported carry-forward operations — "history remains where it happened; what still matters may be deliberately carried forward." Both require the source item's own Case to be `CASE_STATUS_ARCHIVED`, the target Case's `derived_from_case_id` to equal that source Case's id (carry-forward is only defined from an archived Case into its own recorded derivation, never an arbitrary destination), and owner-or-admin authority checked **against the target Case** (`_validate_carry_forward_authority`, the shared validation helper both methods call) — "unauthorized participants must not carry historical material into another person's private derived Case" is a statement about who controls the destination. Nothing is carried forward automatically by Derive itself; adoption is always a separate, explicit, per-item human act. No adoption path exists for `Disposition`, `ReviewerValidation`, `RequirementAdjudication`, `AnalysisRun`, or `Attention` — the object scope is closed by omission (only two `adopt_*` methods exist on the store at all, asserted directly in `tests/test_case_adoption.py::test_unsupported_object_types_have_no_adoption_path`), not by a runtime type-check that could silently expand later. See `specified-unbuilt/investigation-lifecycle-extensions.md` for why each excluded type was excluded.

**New governed primitive, `CarriedForwardAdoption`** (`workspace.carried_forward_adoptions`, one list per project) — the single, uniform, directly-filterable record answering "which active items in a Case were carried forward, from which historical item, by whom, when, from which predecessor Case," queried via `CaseWorkspaceStore.carried_forward_adoptions_for_case`. Deliberately not a reuse of the `Relationship` substrate (unlike Derive's own `RELATIONSHIP_TYPE_DERIVED_FROM`): `Relationship` has no source-Case/target-Case concept, and reconstructing that from a bare from/to-object edge would need an extra `_cases_referencing_object`-style join on every query — this record captures `source_case_id`/`target_case_id`/`object_type`/`source_object_id`/`successor_object_id`/`adopted_by`/`adopted_at` directly. `object_type` is open-world (`KNOWN_CARRIED_FORWARD_OBJECT_TYPES` — `"finding"`/`"review_message"` today), matching `Supersession`'s own precedent for extensible-but-currently-narrow typed lineage.

**Finding carry-forward — the original Finding is never touched, moved, or re-cased.** A genuinely new `Finding` is created in the target Case (new id, `claim_status=FINDING_STATUS_PROVISIONAL` — Finding's own ordinary default, never anything stronger) because continued review (`ReviewerValidation`/`Disposition`/Apply) requires a real, mutable Finding, not a read-only reference — an old conclusion never silently becomes current truth merely by being carried forward; renewed `ReviewerValidation`/`Disposition` is required exactly as for any other Finding (Apply's existing Confirmed-Disposition gate is untouched and still enforced). Finding has no generic related-object pointer of its own, so per-item provenance back to the original rides on the accompanying `AnalysisRun`'s `AnalysisTrigger.trigger_reference_type`/`trigger_reference_id` (`engine_name="carry_forward_adoption"`) — existing fields reused rather than a new one added to `Finding`, mirroring `promote_requirement_item`'s own precedent of creating an honest accompanying `AnalysisRun` rather than fabricating provenance. `Finding`/`AnalysisRun`/`CarriedForwardAdoption` are written in exactly one `self.save(workspace)` call.

**ReviewMessage carry-forward — the original message and its thread are never touched.** A new `ReviewThread` (anchored to the target Case) + `ReviewMessage` are created so the reconsideration can actually be discussed/resolved in the target Case's own live context. `actor` on the new message is the **adopting** actor, never the original commenter — "the original commenter must not falsely become the author of the new active item," and the reason an unavailable/former contributor is never a blocker: nothing requires them to act again. The original voice is preserved without misattribution: `text` quotes the original verbatim inside an explicit "[Carried forward from archived Case ...]" frame (plus an optional adopting-actor `note`), and `ReviewMessage`'s own existing `related_object_type`/`related_object_id` fields (no new field needed) point directly at the original message — `message_type=MESSAGE_TYPE_CARRIED_FORWARD` (new, open-world, `KNOWN_MESSAGE_TYPES`). `ReviewThread`/`ReviewMessage`/`CarriedForwardAdoption` are written in exactly one `self.save(workspace)` call.

Privacy: adoption never changes either Case's `visibility` — the target Case (already `PRIVATE` per Derive) stays exactly as it was, and the archived source's own visibility is read-only throughout. Route wiring: `adopt_finding` (`POST /projects/<project_id>/workspace/cases/<case_id>/adopt-finding`, form field `finding_id`) and `adopt_review_message` (`POST /projects/<project_id>/workspace/cases/<case_id>/adopt-message`, form fields `message_id`/`note`) — `case_id` in both is the TARGET (derived) Case. **No picker UI was added this tranche** — these routes exist for programmatic/future-UI use, matching the tranche's own scope discipline around avoiding a broader UI build; selecting *which* historical item to adopt currently requires knowing its id directly. Tests: `tests/test_case_adoption.py` (31 tests).

### `ReviewerValidation` — implemented
`case_workspace.py:835`. Answers: is this Finding accurate (Correct/Incorrect/Partial/Needs Evidence/Not Applicable) — deliberately separate from `Disposition`.

### `Disposition` — implemented
`case_workspace.py:847`. Answers: what happens to this Finding next (Confirmed/Rejected/Deferred/Known Pending Acceptance/Known Accepted). Gates `apply_findings` (`~2908`) — Apply requires a Confirmed Disposition on record.

### `RequirementAdjudication` — implemented (Foundation Batch K)
`case_workspace.py:860`, methods at `~3091`–`3176`. Answers: does current evidence satisfy this Requirement (Satisfied/Partially Satisfied/Not Satisfied/Not Applicable/Accepted Alternative). Mandatory, non-defaulted `reasoning` field. Validated evidence references (`evidence_finding_ids`/`evidence_relationship_ids`) resolve only against the current project's own `workspace.findings`/`workspace.relationships` — structurally incapable of accepting a cross-project or legacy-`RequirementItem` reference. `requirement_adjudication_state` is derived, never stored, matching `review_state_for_finding`'s pattern. **Route wiring implemented** — `routes/workspace.py`'s `adjudicate_requirement` (`POST /projects/<project_id>/workspace/requirements/<requirement_id>/adjudicate`) calls this directly, passing its own `governance_log` through rather than double-logging.

### `promote_requirement_item()` — implemented (ratified governance baseline tranche)
`case_workspace.py`, method on `CaseWorkspaceStore`, immediately after `requirement_adjudication_state`. Bridges a `RequirementItem` (`services/bhive_parser.py`'s legacy extraction pipeline, still-decoupled by design — this module does not import `bhive_parser`) into a governed `Requirement`. `source_id` has no default and cannot be omitted (`TypeError` if the caller tries) — no inference path exists. Creates the Requirement's accompanying `Finding` + `AnalysisRun`/`AnalysisTrigger` (preserving extraction confidence and an explicit, caller-supplied trigger) and the `Requirement` itself (`registration_method=REQUIREMENT_REGISTRATION_MACHINE_EXTRACTED`) in memory, then writes all three in exactly one `self.save(workspace)` call — the same single-save-per-governed-operation pattern `register_table_evidence` already uses, so a failure between records is structurally impossible, not just tested for. Never sets `Requirement.status` to anything but its ordinary default and never creates a `Disposition`/`RequirementAdjudication` — the promoted Requirement is exactly as un-adjudicated as a manually-registered one. Route wiring: `routes/workspace.py`'s `promote_requirement_item_route` (`POST /projects/<project_id>/workspace/cases/<case_id>/requirement-items/<requirement_item_id>/promote`), form-required `source_id`, `AnalysisTrigger(trigger_type=ANALYSIS_TRIGGER_USER_INITIATED, triggered_by_actor=<reviewer>)` constructed from the real requesting session. Tests: `tests/test_requirement_promotion.py` (19 tests: 14 store-layer, 5 route-layer).

### `Snapshot` — implemented
`case_workspace.py:1690`, `create_snapshot` at `~4316`. Answers: what did the whole project look like at a point in time. `project_state_version` (an integer, incremented on every governed write via `ProjectWorkspace.version`) is the correct, collision-proof identity for point-in-time state — proven not vulnerable to the same-calendar-day-different-state problem a bare timestamp would have. Can only capture the *current* version at creation time; cannot retroactively reconstruct a past version if none was taken at the time.

### `Supersession` — implemented
`case_workspace.py:1060`. Answers: what replaced what, by whom, under what authority, non-destructively. Deliberately reserves (but has never been exercised for) multiple simultaneous proposed successors per predecessor — the basis for the `"proposed"`/`"not_adopted"` status-extension pattern reused across Requirement branching, Scenario, and `needs_revalidation` in `specified-unbuilt/`.

### `TemporalObligation` — implemented, one extension reserved
`case_workspace.py:1148`. Answers: what's due, by when. Current shape models a single due-date, not a recurring series — recurring-obligation support (30-year P3 maintenance schedules) is a real, named, deferred extension — see `deferred-reserved/reservations.md`.

### `AnalysisRun` / `AnalysisTrigger` — implemented
`case_workspace.py:935` / `908`. Open-world trigger vocabulary already includes `ANALYSIS_TRIGGER_SOURCE_CHANGE` — confirmed still present, and confirmed to be the correct, already-existing mechanism for authoritative-metamorphosis delta-detection (see `specified-unbuilt/metamorphosis-and-dormancy.md`) rather than requiring a new object.

### `GovernanceLog` — implemented
`services/governance.py`. Append-only JSONL audit trail, one file per project (`{project_id}.governance.jsonl`), confirmed to have no cross-project read path — this is the mechanism confirmed safe against cross-party leakage in the Owner/Proponent boundary review.

### Legacy, distinct object — `RequirementItem` / `RequirementsRegistry`
`services/bhive_parser.py`, `services/requirements_registry.py`. The original, day-one (`4ce97fd`) requirement-extraction pipeline, still live in production via `services/ingestion.py` → `routes/api.py`/`routes/portal.py`. Structurally separate storage from `ProjectWorkspace` — no code path exists that could resolve a `RequirementItem` id through any `Requirement`-scoped lookup. Confirmed a different stage of the same conceptual river (extracted candidate vs. governed Requirement), not a duplicate-truth risk, not a legacy-vs-current split requiring cleanup before Batch K can be trusted.

### Project Operating Environment — implemented (CLAUDE-P29, locked-classification tranche)
`services/environment_capabilities.py` (new module — a closed enum plus a strict validator plus one concrete capability mapping, deliberately not a plugin/registry framework): `CLIENT_OWNER`/`DESIGN_BUILDER_PROPONENT`, `OPERATING_ENVIRONMENTS` (the closed tuple — no `"other"` escape hatch exists, unlike `KNOWN_PARTICIPANT_ROLES`), `is_valid_operating_environment`, `allowed_participant_roles`. Answers a question no existing mechanism answered: which side of a procurement/delivery relationship (Client/Owner vs. Design-Builder/Proponent) is this *project's workspace itself* configured to serve — fixed once, at project creation, for the life of the project. This is deliberately **not** the same concept as `represented_party_by`/`PerspectiveAssessment` (the existing per-reviewer comparative mechanism, CLAUDE-P12R/P17, unmodified by this tranche) and **not** the governed, still-`NOT AUTHORIZED` "Perspective" object in `specified-unbuilt/perspective-and-contract-dna.md`: `represented_party_by` is mutable, per-reviewer, and answers "whose eyes am I reading this Finding through today, within one shared project"; Operating Environment is immutable, project-wide, and answers "which side is this entire workspace instance structurally configured for" — a coarser, one-time, higher-stakes classification that gates which `Participant.role_type` values are even selectable (`allowed_participant_roles`), not which lens a reviewer applies to shared content. A reviewer changing their `represented_party_by`, or a Case's Private/Shared/Collaborative visibility changing, has no code path that can reach `operating_environment` — the two mechanisms share no mutation logic.

`ProjectWorkspace.operating_environment`/`operating_environment_set_by`/`operating_environment_set_at` (new fields, default `None` — a pre-P29 project loads with `operating_environment=None`, never a fabricated value). `CaseWorkspaceStore.set_operating_environment` is the single, centralized write gate: validates against `is_valid_operating_environment` (rejects, does not silently coerce, an unrecognized value — the deliberate opposite of this codebase's dominant `normalize_open_world_value` open-world-preserving pattern, which is the wrong shape for a field that must be a closed, structurally-gated enum), then raises the new `OperatingEnvironmentAlreadySetError` (a `CaseWorkspaceError` subclass) if the field is already non-`None` — no other method anywhere writes this field, so there is exactly one place immutability could fail, and it is tested directly (service-layer direct-call, forged-route, and same-value-resubmission cases all covered). Logs an `operating_environment_established` `GovernanceLog` event (`previous_state`/`operating_environment`) in the same call, atomic with the field write.

**Creation path:** `services/ingestion.py`'s `ingest_upload` now takes a required `operating_environment` argument (no default — the same "no inference path exists" discipline as `promote_requirement_item`'s `source_id`), validated before any file/parsing work begins, and the `ProjectWorkspace` is now created eagerly (previously lazy, only when `project_name` was given) so `set_operating_environment` always has a workspace to lock immediately after creation — every new project is classified atomically at birth, never in a separate step where an unclassified project could transiently exist. Route wiring: `routes/portal.py`'s `/upload` (two-entrance gateway in `templates/gateway.html`, pre-selecting the environment via `?environment=`) and `routes/api.py`'s `ingest_document` both require and pass this field through; a missing/invalid value is rejected with 400 before ingestion starts, leaving no project behind.

**Historical/legacy projects:** a pre-P29 workspace simply has `operating_environment=None` — never backfilled or inferred. `routes/workspace.py`'s `classify_operating_environment` (`POST /projects/<project_id>/workspace/classify-environment`, `@admin_required` — the same authority level as project creation itself, since this codebase has no separate "project owner" concept for `ProjectWorkspace` to check against) is the one-time path to establish it for such a project, calling the identical `set_operating_environment` gate — not a separate, weaker mechanism, and refuses exactly the same way on a second attempt. `case_workspace.html` offers the classification form only while `operating_environment` is still `None`, and never offers any control once it is set.

**Environment-dependent behavior, kept narrow and honest rather than speculative:** the one implemented differentiation is `allowed_participant_roles(operating_environment)` gating which `Participant.role_type` values `register_participant_route` (`routes/workspace.py`) accepts — Client/Owner projects admit `owner`/`consultant`/`other`; Design-Builder/Proponent projects admit `design_builder`/`contractor`/`proponent`/`other`; a legacy `None` environment returns `None` from this function, meaning "no gating, full `KNOWN_PARTICIPANT_ROLES` allowed" — deliberately preserving every pre-P29 project's existing behavior unchanged. No new AI-prompt content was written for this tranche — extending `services/bhive_parser.py`'s adversarially-tuned prompts to be environment-aware was deliberately out of scope, consistent with this codebase's standing caution around that module.

**Export stamping:** `services/rfi_export.py`'s `build_rfi_docx`/`build_rfi_draft_docx` both gained an optional, additive `operating_environment_label` parameter, stamped into the exported `.docx` metadata block when supplied. `routes/api.py`'s own unauthenticated-turned-authenticated RFI export was left unstamped as of this tranche — it only has `RequirementsRegistry` access, not `CaseWorkspaceStore` — **closed in CLAUDE-P30, see that entry below.**

Tests: `tests/test_operating_environment.py` (29 tests — creation requirement, persistence/reopening, immutability at service/route layers, role/perspective independence, capability gating, cross-environment project isolation with a shared source document, historical/legacy one-time classification, export stamping) plus `tests/test_forward_ingestion_metadata.py` (updated for the new required `ingest_upload` parameter).

### Environment Capability Architecture — implemented (CLAUDE-P30, capability-grammar + RFI directionality + Go/No-Go tranche)
`services/environment_capabilities.py` gained a capability grammar: `CAPABILITY_NEUTRAL`/`CAPABILITY_COUNTERPART`/`CAPABILITY_PARALLEL`/`CAPABILITY_CLIENT_ONLY`/`CAPABILITY_PROPONENT_ONLY`/`CAPABILITY_COMPARATIVE_BOUNDED`/`CAPABILITY_FUTURE_NOT_AUTHORIZED`, `CapabilityDefinition`, and `CAPABILITY_REGISTRY` (a plain dict, not a plugin framework — one small entry per real capability). `capability_availability(capability_id, operating_environment)` is the single derivation rule (`client_variant`/`proponent_variant` not `None`, with `operating_environment=None` ungated except for `CAPABILITY_FUTURE_NOT_AUTHORIZED` — the same precedent `allowed_participant_roles` already established for legacy projects); `capability_denial_reason` is the one stable, non-implementation-leaking denial-message producer, reused by every route via the new `routes/workspace.py`'s `_require_capability` helper (mirrors `_require_visible_case`'s own shape) rather than each route inventing its own message. No `CAPABILITY_CLIENT_ONLY`/`CAPABILITY_PROPONENT_ONLY` registry entries were actually registered this tranche — every genuinely single-sided capability found on inspection (RFI origination/response) had a real counterpart on the other side, so it was classified `CAPABILITY_COUNTERPART` instead; see the CLAUDE-P30 final report's Part XI critique for why manufacturing a bare "exclusive" label for either side was rejected as dishonest to what was actually found.

**RFI/clarification directionality — `rfi_originate` (Design-Builder/Proponent) / `rfi_respond` (Client/Owner), a `CAPABILITY_COUNTERPART` pair.** `create_rfi_draft`/`update_rfi_question`/`issue_rfi_draft`/`preview_rfi_draft` (`routes/workspace.py`) are now gated by `_require_capability(..., "rfi_originate", ...)` — mechanically proponent-only (a Client/Owner project's `client_variant` for this capability is `None`). The new counterpart, `respond_to_rfi_draft` (`POST /projects/<project_id>/workspace/rfi-drafts/<draft_id>/respond`, gated `"rfi_respond"`, mechanically client-only), calls the new `CaseWorkspaceStore.respond_to_rfi_draft`: requires `RFIDraft.status == RFI_STATUS_ISSUED` (a response follows issuance, the same "one issuance ever" refusal shape `issue_rfi_draft` already uses), writes the new `RFIDraft.response_text`/`responded_at`/`responded_by` fields and transitions `status` to the new terminal `RFI_STATUS_ANSWERED` — never a status a draft can leave once reached. `services/rfi_export.py`'s `build_rfi_draft_docx` renders a "Workflow Direction" line and, once answered, a "Response" section — both exports (`export_rfi_draft` and `routes/api.py`'s own `export_rfi`, newly stamped this tranche, closing the CLAUDE-P29-noted scope limitation above) identify which side originated the question and which side answered it. `case_workspace.html`'s RFI-draft card and delegation-choice panel both branch on the new `can_originate_rfi`/`can_respond_rfi` template flags (template-layer visibility only — the routes are the real enforcement).

**Go/No-Go — `go_no_go`, a `CAPABILITY_PARALLEL` pair; new primitive `GoNoGoAssessment`.** `workspace.go_no_go_assessments` (one shared record shape: `decision_stage`/`decision`/`anomalies`/`rationale`/`decided_by`/`decided_at`, plus `operating_environment` denormalized at decision time). `CaseWorkspaceStore.record_go_no_go_decision` validates `decision` against the closed `GO_NO_GO_DECISIONS` (`go`/`no_go`/`conditional_go`) and `decision_stage` against whichever vocabulary `environment_capabilities.decision_stages_for_environment(workspace.operating_environment)` returns for THIS project's own locked environment — `CLIENT_OWNER_DECISION_STAGES` (procurement-oriented: `release_rfq`/`release_rfp`/`shortlist`/`award`/...) and `DESIGN_BUILDER_PROPONENT_DECISION_STAGES` (pursuit-oriented: `bid_rfp`/`accept_commercial_terms`/`submit_final_proposal`/...) are genuinely disjoint vocabularies, not the same checklist relabeled. Unlike RFI/participant-role gating, a legacy/unclassified project (`operating_environment is None`) cannot record a Go/No-Go decision at all — `decision_stages_for_environment(None)` raises `ValueError`, and `record_go_no_go_decision` itself raises `CaseWorkspaceError` first — there is no sensible vocabulary-free fallback, an explicit, tested exception to the "`None` means ungated" precedent used everywhere else in this module. Route: `POST /projects/<project_id>/workspace/go-no-go` (`routes/workspace.py`'s `record_go_no_go`). **CLAUDE-P38 (OBS-04):** actually recording a decision is now admin-only, server-side enforced in the route body (a non-admin gets a clear flash, not a bare 403) — until this stage any authenticated participant, including `read_only`, could record the real decision. `GoNoGoAssessment` gained an optional `decided_by_role` field (defaulted `None`, existing records unaffected) so a decision shows the decider's role, not just a bare username.

**Deliberately deferred, not done this tranche:** no `services/bhive_parser.py` prompt received `operating_environment` context — environment-aware consequence/risk/opportunity framing, question generation, and Go/No-Go anomaly suggestion are all real candidates, documented as a deliberate deferral (not an oversight) in `environment_capabilities.py`'s own module docstring, consistent with this module's standing multi-session caution around `bhive_parser.py`'s adversarially-tuned prompts.

Tests: `tests/test_capability_architecture.py` (39 tests — capability resolution for both environments and legacy/unset, denial-message content, Go/No-Go stage-vocabulary resolution and cross-environment rejection at both route and service layers, RFI origination/response direction enforcement including forged-route and direct-service-call attempts, navigation reflecting capability resolution, reviewer-perspective/session-role independence, export direction/environment stamping including the newly-closed `routes/api.py` gap, no-tenancy/project-isolation checks).

### Organizational Security and Information Governance — implemented, bounded (CLAUDE-P31)
`services/security_policy.py`: `GOVERNED_ACTIONS` (8 actions: `external_ai_request`/`local_ai_request`/`technical_telemetry`/`content_bearing_support_package`/`export`/`cross_project_reference`/`organization_private_learning`/`shared_archiosk_contribution`), `MANDATORY_FLOOR_DEFAULTS` (the unweakenable default per action — no governed action exists anywhere in this model for disabling authentication/CSRF/rate-limiting/audit, which is what actually keeps those unweakenable, not a runtime check), `evaluate_action` (floor → baseline → profile → exception, most-restrictive-wins via `_more_restrictive`, an exception's loosening capped structurally at `DECISION_ALLOW`). `INFORMATION_CLASSIFICATIONS` (`standard`/`confidential`/`restricted`/`highly_restricted`) each resolve to an explicit `CLASSIFICATION_PROFILE_DECISIONS` control bundle — never a bare label. `SECURITY_CLAIMS_REGISTRY` records what this deployment can/cannot truthfully promise today, including the eight claims (data locality, customer-managed keys, air-gapped, no-provider-retention, local-AI-only, tamper-proof logs, complete org isolation, zero-knowledge support) explicitly marked `prohibited_from_claiming`.

`services/security_governance.py`'s `SecurityGovernanceStore` manages **one global, deployment-wide record** — this repository has no multi-organization/tenancy model (`specified-unbuilt/tenancy-and-project-authorization.md` remains unimplemented), so "organization baseline" here means exactly one deployment-wide configuration, never a per-tenant one; this limitation is stated directly in the module's own docstring, not left implicit. `SourcePolicy`/`PolicyStatement`/`ProposedControl`/`QAEntry` preserve "Original Written Policy ≠ Machine Interpretation ≠ Proposed Application Controls ≠ Ratified Executable Security Baseline" — every `ProposedControl`/baseline control-decision carries required provenance (`CONTROL_SOURCE_POLICY_STATEMENT`/`CONTROL_SOURCE_QA_ENTRY`/`CONTROL_SOURCE_MANDATORY_FLOOR`/`CONTROL_SOURCE_ARCHIOSK_DEFAULT`), never disguising an ARCHIOSK recommendation as a customer policy requirement. `BaselineVersion`'s ratification lifecycle (`draft`→`under_review`→`approved`→`active`→`superseded`/`withdrawn`) requires `acknowledge_capability_impact` before `activate_baseline` will accept it; activating a new baseline supersedes (never deletes) whichever was previously active. `SecurityException` is time-bounded/scoped (project-specific takes precedence over org-wide for that project) and, like every exception, can never exceed `evaluate_action`'s own `DECISION_ALLOW` ceiling.

`ProjectWorkspace.security_profile`/`_set_by`/`_set_at` (`services/case_workspace.py`): unlike `operating_environment`, deliberately **re-settable** (policy legitimately changes over a project's life) via `CaseWorkspaceStore.set_project_security_profile` — every change still fully accountable, the previous value always captured in the logged `security_profile_set` event before being overwritten.

**Real enforcement, at two representative points, not a speculative full rollout:** `services/ingestion.py` evaluates `external_ai_request` before every new project's classification and wires a `DENY`/`ISOLATE`/etc. decision straight into `BHiveParser`'s own pre-existing `ai_calls_disabled` kill switch (CLAUDE-P27-B's `AI_CALLS_DISABLED` mechanism, already tested) — zero lines changed inside `bhive_parser.py` itself, both `classify()` and `_check_consistency()` already degrade to their existing rule-based/skipped fallback whenever that flag is set. `routes/workspace.py`'s `_require_export_allowed` gates both `export_rfi_draft` and `export_rfi` against `export`, consulting the project's own `security_profile` (via `profile_decision_for`) alongside the active organization baseline. A `security_decision` `GovernanceLog` event is recorded for every ingestion-time AI evaluation regardless of outcome (allow or deny), not only denials.

`services/diagnostics.py`: `TechnicalTelemetry` is structurally incapable of holding project content (a closed, content-free field set, not a redaction rule) and is never gated at all — the floor default for `technical_telemetry` is `DECISION_ALLOW`. `SupportPackage` can only be constructed by `build_support_package` given an already-favorable `SecurityDecision`; `SupportPackageDeniedError` otherwise. Neither dataclass carries any field that could be read as authorizing reuse beyond its own support case.

`services/learning_governance.py`: `LearningContributionRequest` models `ZONE_PROJECT_PRIVATE`/`ZONE_ORGANIZATION_PRIVATE`/`ZONE_SHARED_ARCHIOSK_IMPROVEMENT` and a five-stage required review sequence (`authority_check`/`eligibility_check`/`confidentiality_review`/`personal_information_review`/`minimization_or_synthesis`) that `decide_contribution` refuses to skip past on the way to `contribution_approved`; a `shared_archiosk_improvement`-targeted approval additionally requires a decider distinct from the original requester (no self-approval). **This module records governed intent and approval state only — it moves zero data.** No shared-learning/training/fixture-collection mechanism exists anywhere in this repository for an approved request to actually feed into; confirmed both structurally (no import edge between this module and `case_workspace.py`'s quality/validation machinery in either direction) and behaviorally (recording a "Correct" `ReviewerValidation` creates no `LearningContributionRequest`).

`services/security_assurance.py`: `aggregate_security_activity` is a pure read-side aggregation over every project's own already-existing `GovernanceLog` (no new audit substrate, no new storage) filtered to `SECURITY_RELEVANT_EVENT_TYPES` — `SecurityActivityEntry` carries actor/project/action-category/decision only, structurally never Requirement/Finding text, so "content-level inspection remains separately controlled" holds because no code path back to real project content exists here at all, not because of a permission check that could be bypassed. `run_security_self_check` re-verifies five representative invariants independently of the writers meant to maintain them (valid `operating_environment`/`security_profile` values, control-decision provenance actually resolving, at most one `ACTIVE` baseline, no stale-but-still-`active`-flagged exception). Honesty boundary stated directly in the module's own docstring: `GovernanceLog`'s append-only guarantee is by *convention* (every write path only ever appends), not a cryptographic/filesystem integrity guarantee — `SECURITY_CLAIMS_REGISTRY["tamper-proof logs"]` is `prohibited_from_claiming`, not merely omitted.

`routes/security.py` (new blueprint, `/security`, `@admin_required` throughout — no dedicated Security Officer role exists yet, same reasoning `classify_operating_environment` already used) + `templates/security_department.html`: source-policy ingestion (with optional file upload, fingerprinted via the same `hashlib.sha256`-over-raw-bytes pattern `services/ingestion.py` already uses for project sources), policy-statement recording, control proposals, governed Q&A (6-state authority model: `established_by_policy`/`authorized_answer`/`provisional_pending_approval`/`unresolved`/`conflicting`/`not_technically_supported`), baseline draft/control-decision/acknowledge/activate/withdraw, exception grant/revoke, per-project security-profile assignment, learning-contribution request/advance/decide, and the assurance activity feed + self-check findings — all in one admin-only workspace, added to `templates/base.html`'s side-rail nav.

**Deliberately not done this tranche, and why:** no AI-assisted policy-clause extraction (statements/proposals are human-entered only — extending this stage's caution about `bhive_parser.py`'s fragile prompts to "write no new prompt at all" rather than a narrower gate); no real shared-learning data-movement mechanism (none exists to move data into); no multi-organization tenant isolation (blocked on `tenancy-and-project-authorization.md`, still unimplemented); no content-level assurance viewer. See `specified-unbuilt/organizational-security-department.md` for the full target architecture this bounded foundation deliberately does not yet reach.

Tests: 80 across `tests/test_security_policy_engine.py` (17 — floor defaults, baseline/profile precedence and the "cannot loosen" rule, the exception ceiling, classification control-bundle mapping, claims registry), `tests/test_security_governance.py` (20 — source-policy/Q&A distinction, Q&A authority/scope, control provenance, full baseline ratification lifecycle including supersession, exception expiry/revocation/project-scoping), `tests/test_learning_governance.py` (12 — required-stage gating, self-approval prohibition, project-private/organization-private separation, structural + behavioral proof that quality ratings never trigger a contribution), `tests/test_security_enforcement.py` (18 — deterministic network-independent proof of the `ingestion.py`/`ai_calls_disabled` wiring via a `BHiveParser.parse` spy, export gate denial and capability-impact notice content, support-package authorization, operating-environment/security-profile independence, reviewer-perspective/session-role non-bypass, CLAUDE-P30 capability gate + this stage's security gate both independently applying, no-tenancy-model structural check), `tests/test_security_assurance.py` (13 — lower-authority-cannot-override, troubleshooting/learning separation, activity-vs-content-level assurance, audit append-only accumulation, self-check anomaly detection, no-unsupported-isolation-claim).

### Project-Level Access Control — implemented, bounded (CLAUDE-P32)
`services/project_access.py`: `can_access_project(workspace, username, is_admin)` is the single centralized decision every route-layer loader calls into — an admin always passes; otherwise `workspace.owner == username` or `username in workspace.access_allow_list`. `workspace.owner is None` (not yet established) fails **closed** — admin-only, never "open to everyone" — the deliberate opposite of every other None-means-ungated field in this codebase (`environment_capabilities.allowed_participant_roles`, `security_policy.capability_availability`): those default open because there was real pre-existing behavior to preserve when the field was introduced; this field has no such precedent, since no project-level access check existed anywhere before this tranche.

**The gap this closes, confirmed by direct investigation, not assumed:** `governance/specified-unbuilt/tenancy-and-project-authorization.md`'s own CLAUDE-P27 finding — any authenticated user, `admin` or `read_only`, could open any project by knowing or guessing its `project_id` — remained true through CLAUDE-P29/P30/P31, each of which built locked-environment/capability/security-governance machinery on top of an object model any logged-in account could still fully read. AST-verified: 47 of `routes/workspace.py`'s 50 routes already funneled through `_load_workspace_or_404`; the one exception (`export_rfi`) was refactored this stage to do the same, rather than left as a second access path. `routes/api.py`'s 6 project-scoped routes and `routes/portal.py`'s `delete_project`/`dashboard`/`projects_list`/`global_search`/`index` (the listing routes needed filtering, not just single-project blocking, or a project's name/metadata would still leak through them) were the confirmed bypasses, all closed. A genuinely new bypass was found mid-implementation and closed: `app.py`'s `_nav_recent_projects` — the side-rail's project list, rendered via the global `inject_globals` context processor on **every** authenticated page including error pages — listed every project's display name completely unfiltered; a denied project's own name was visible in the nav rail on the very 404 page proving access to it was denied.

**Owner/allow-list, not tenancy.** `ProjectWorkspace.owner`/`owner_set_by`/`owner_set_at`/`access_allow_list` (new fields). Unlike `operating_environment`, `owner` is deliberately **not** a permanent lock — `CaseWorkspaceStore.set_project_owner` may be called again (admin-only, enforced at the route layer; a wrong backfill inference must be recoverable, not permanent), and every change is still fully logged (`project_owner_set`, previous value always captured first). `grant_project_access`/`revoke_project_access` reuse the exact owner-or-admin authority pattern `archive_case`/`derive_case` already established (`actor_role` passed through as a plain caller-supplied string — this module still does not import `models`/`services.auth`); an allow-listed user gains no grant/revoke authority of their own. This is genuinely narrower than the tenancy design's `Organization`/`OrganizationMembership`/`ProjectOwnership` model (still unimplemented) — a per-project ACL, not an organizational boundary; stated explicitly in the module's own docstring and in `governance/STATUS.md`'s row, not left to be inferred.

**New-project creation.** `services/ingestion.py`'s `ingest_upload` gained a required `owner` parameter, deliberately separate from the pre-existing free-text `actor` field (real repository evidence confirmed `actor` values like `"Mehrzad Dadras, Design Manager"` — a human typing a display name, never validated against any account — see `services/governance.py`'s own docstring). `owner` must be the caller's real, already-authenticated `session['username']`; both real call sites (`routes/portal.py`'s `upload()`, `routes/api.py`'s `ingest_document()`) are `@admin_required` and pass it directly, never `request.form.get('actor')`. Locked onto the workspace immediately after creation, the same treatment `operating_environment` already gets.

**Legacy-project compatibility, deterministic only.** `infer_owner_from_ingestion_actor`/`ensure_owner_backfilled`: a pre-P32 project's own first `document_ingested` governance event's `actor` is checked for an **exact match** against a real `models.User.username` — never a fuzzy guess. Confirmed against this repository's own real local `instance/registry/` data: 3 of 6 existing projects were ingested by the real `workspacetester` account (backfilled); the other 3 have free-text actors (`"agent1"`, `"agent2"`, a human display name) with no matching account (stay unowned, admin-only, until an admin explicitly assigns one via `set_project_owner_route`). Backfill runs as an incidental write inside a GET request — the same precedent `show_workspace`'s own `open_project` temporal reconciliation already established, not a new pattern. Idempotent: once `owner` is set, by any path, never re-derived.

**Denial semantics.** A generic 404 everywhere (`routes/workspace.py`, `routes/api.py` via `abort(404)` — `app.py`'s existing error handler already renders this as JSON for any `/api/` path — `routes/portal.py`), identical whether a project genuinely doesn't exist or the caller simply isn't authorized for it, matching `_require_visible_case`'s own established "don't confirm existence to a non-member" convention, applied one layer up from Case privacy to project-level access itself. Project-level access composes with, and neither replaces nor widens, every other existing check: an authorized `read_only` user remains unable to perform `admin_required` actions inside a project they can now open; Case-level privacy (`visible_cases_for`) is completely unaffected.

**Two ratified governance test suites' fixtures updated, not their assertions.** `tests/test_case_privacy.py`'s `CasePrivacyRouteTests` and `tests/test_route_authorization_hardening.py` both model two genuinely different authenticated sessions both reaching the same project (Case-level privacy, not project-level access, is what each actually verifies). Project-level access is a new precondition those fixtures now establish directly at the store layer (`set_project_owner`/`grant_project_access`) before their real subject runs — the same "add the new required precondition to existing test call sites" shape CLAUDE-P29 already used when `operating_environment` became a required `ingest_upload` parameter. No assertion in either file changed.

**A real, pre-existing, unrelated defect surfaced during this stage's own verification pass, not introduced by it:** at least one project in this repository's actual local `instance/registry/` directory has an on-disk workspace JSON with a `'reviews'` key `ProjectWorkspace(**data)` no longer accepts — a schema-drift artifact from some earlier tranche, previously invisible because nothing iterated every project's workspace on every page load. `app.py`'s `_nav_recent_projects` and `routes/portal.py`'s `_accessible_documents` both now fail closed (exclude, do not crash) on a `TypeError` from a workspace file they can't parse, restoring this file's own pre-existing defensive-degradation guarantee. The corrupted record itself was left untouched — investigating/repairing real `instance/` data was out of this stage's bounded scope.

**Deliberately not built this stage:** separate read/write project-level permissions (would exceed the bounded objective — the existing role axis already answers "what may this user do," this only answers "may they be here at all"); any `services/case_workspace.py` decomposition (P32 was checked against making a future split harder — every new field/method landed as a small, clearly-P32-tagged addition, not interleaved with unrelated code); an ownership-transfer UI beyond the existing admin reassignment route.

Tests: 41 in `tests/test_project_access_control.py` (owner/unauthorized access, admin bypass — documented and directly verified, not assumed, per this stage's own required Devil's-Advocate review — authorized-`read_only`-still-role-restricted, API mutation/export denial and listing filtering, every identified portal bypass path, two-user/two-project cross-isolation including grant/revoke and allow-listed-user non-escalation, legacy backfill determinism including the free-text-actor negative case and idempotency, new-project owner sourced from session not the spoofable `actor` field, and the centralized resolver's own edge cases).

### `Folder` — implemented, bounded (CLAUDE-P40-VW9, Governed Files Display and Project File Architecture)
`services/case_workspace.py`: `FOLDER_ROOT_DATA_ROOM`/`FOLDER_ROOT_DESIGN_BUILDER`/`KNOWN_FOLDER_ROOTS` — a closed, two-value vocabulary (unlike `Source.kind`'s open-world shape) naming two DIFFERENT GOVERNANCE SPACES: Data Room (controlled, externally-issued material) and Design-Builder Workspace (editable, team-organized material). Both roots are GOVERNED VIRTUAL roots — neither is itself a persisted `Folder` record; a project's `workspace.folders` holds only real, user-created Design-Builder Workspace folders. `create_folder` has no `root` parameter at all and unconditionally writes `FOLDER_ROOT_DESIGN_BUILDER` — there is structurally no route or method in this codebase that can create a Data Room folder record this stage, making "ordinary Design-Builder actions cannot reach Data Room" true by construction, not merely by convention.

`Folder` dataclass (`id`/`project_id`/`root`/`name`/`created_at`/`created_by`/`parent_folder_id`/`removed_at`/`removed_by`). Canonical identity is `id` (uuid4 via the same `_new_id()` every other record uses) — mirrors the exact principle `Source`'s own docstring already states ("folder locations and filenames are external representations only"). A folder's full path is always DERIVED at read time by walking `parent_folder_id` (`CaseWorkspaceStore._folder_path`, root-most first) — never stored as a string, the same "store flat, derive structure at read time" shape `visible_cases_for` and everything else in this module already use. `ProjectWorkspace.folders` (new `list[dict]` field, empty-list default) is purely additive — a legacy workspace predating this stage simply lacks the key and loads with the default, identical to `tags`/`tag_occurrences`/`tasks`' own precedent; directly verified by stripping the key from a real persisted workspace JSON and confirming it still loads and can create folders going forward.

**Mutation methods, no owner/admin gate.** `create_folder`/`rename_folder`/`move_folder`/`delete_folder` are reachable by any project-authorized actor — deliberately mirrors `create_task`/`create_custom_tag`'s own precedent (no authority check beyond project access) rather than `remove_source`/`remove_project`'s owner-or-admin pattern, since Design-Builder Workspace is explicitly "editable working organization created by the Project team" — collaborative team structure, not owner-controlled evidence. `create_folder`/`rename_folder` enforce sibling-name uniqueness scoped to (project, root, parent) via `_reject_if_sibling_folder_name_taken` (style mirrors `services/ingestion.py`'s own `_reject_if_name_taken` — exact-match string comparison, short plain error, no id leaked, though narrower in scope: sibling-only, not whole-registry). `move_folder` prevents cycles via `_folder_descendant_ids` (rejects moving a folder into itself or any of its own descendants) and refuses a foreign-project/removed/non-Design-Builder destination. `delete_folder` refuses a non-empty folder outright and is a recoverable soft-delete (`removed_at`/`removed_by`) — the same tombstone convention `Source`/`Project` already establish (Constitutional Invariant 5: correction is non-destructive) — though deliberately no restore UI was built this stage (the data stays recoverable; a future stage could add a "Removed Folders" surface analogous to Removed Items/Removed Projects).

**Project-boundary enforcement, verified in two independent ways, not just one.** A folder id from a different Project is unreachable through the ordinary route path (each Project's `CaseWorkspaceStore` state lives in its own file, so `workspace.folders` can only ever contain that Project's own records) — but `rename_folder`/`move_folder`/`delete_folder` ALSO independently check the found record's own `project_id` field against the calling workspace before acting, mirroring `Source.project_id`'s own defensive shape. Both guards were directly falsified during this stage's own review (each temporarily disabled, confirmed the relevant test genuinely failed, then restored) — the structural guard alone does not protect against a corrupted/hand-edited record whose own `project_id` disagrees with the file it's found in, which is exactly the scenario the second, explicit check exists for.

**No `folder_id` field exists on `Source`.** A Document cannot be assigned into a Folder in this slice — Document identity and every existing Investigation/RFI/Task/Tag/conversation/citation relationship (all keyed off `Source.id`, never location) are completely untouched by this addition, both now and by construction for whatever a future stage adds. Route wiring: `routes/workspace.py`'s `create_folder_route`/`rename_folder_route`/`move_folder_route`/`delete_folder_route` (`/projects/<id>/workspace/folders...`), authorized exactly like every other route via `_load_workspace_or_404` — no new authorization path. `show_workspace` gained a Files-specific resolution block (`open_folder`/`folder_ancestors`/`design_builder_children`/`design_builder_move_targets`/`data_room_sources`), computed only when the new `STABLE_DIRECTORY_KINDS["files"]` stable-directory-kind (the second real entry in the CLAUDE-P40-VW8-QA1 extension point) is the active view.

**File lifecycle vocabulary — named, documented, deliberately not implemented.** `linked → retrieved → ingested → used → preserved`, evaluated against this stage's own governing prompt: `ingested` is the only state genuinely real today (a Document is a first-class `Source` record — true since long before this stage, unchanged by it); `linked`/`retrieved` describe pre-`Source` states with no record type yet; `used` is inferable only by querying existing Finding/Requirement/Relationship references (no stored field — the natural future shape is a DERIVED property, matching `review_state_for_finding`'s own "derive at read time" precedent, not a new stored flag); `preserved` is partially precedented by `Source.file_hash` (exists, optional, unenforced) and `Supersession` (already real) but not wired to any actual immutability gate. No code path marks an existing Document with any of these four unimplemented states merely to populate the Files interface.

Tests: 40 in `tests/test_p40vw9_files_display_and_folder_architecture.py` (architecture/registry, persistence/identity including legacy-compatibility and cross-project falsification, governance, display behavior including multi-Display and the click-interceptor regression this stage's own real-browser verification found and fixed).

### `StructuralUnit` / `AddressableRegion` / `EvidenceItem` / `DerivedObservation` — implemented, bounded (CLAUDE-MM1, Multimodal Foundation and Evidence Contract)
`services/case_workspace.py`: the shared evidence contract `camel-multimodal-programme.md`'s own MM1 framing calls for — extends today's kernel, which addresses whole `Source` records only, to sub-document regions (a page, a sheet range, a drawing detail, an image region) as first-class, addressable, citable things. Deliberately **generalizes rather than duplicates** what already existed: `Table`/`TableRow` (Batch J) remain the real, tested, tabular-specific realization of "structural subdivision" and "addressable cell" — nothing here replaces them, and a future adapter could describe a `Table` as a `StructuralUnit` without migrating `Table`/`TableRow`'s own storage. Evidence relationships reuse `Relationship` as-is (new `OBJECT_KIND_STRUCTURAL_UNIT`/`OBJECT_KIND_ADDRESSABLE_REGION`/`OBJECT_KIND_EVIDENCE_ITEM`/`OBJECT_KIND_DERIVED_OBSERVATION` object kinds; new `same_subject_as`/`compares_with`/`calculated_from`/`mitigates`/`validates`/`invalidates`/`associated_with` relationship types) — no second edge mechanism.

`StructuralUnit` (`id`/`project_id`/`source_id`/`unit_type`/`order_index`/`created_at`/`created_by`/`label`/`parent_structural_unit_id`/`modality_metadata`) — `unit_type` deliberately open-world (no OCR/PDF-page-renderer/drawing-parser exists yet — MM1 establishes representability, not extraction). `AddressableRegion` (`id`/`project_id`/`structural_unit_id`/`region_type`/`address`/`created_at`/`created_by`/`parent_region_id`) — `address` is a normalized dict whose shape depends on `region_type` (`{"start_offset","end_offset"}` for a text span, `{"x","y","width","height"}` for a bbox, etc.), deliberately not fixed coordinate fields, so this is never overfit to one modality's geometry.

`EvidenceItem` (`id`/`project_id`/`source_id`/`evidence_class`/`content`/`content_type`/`created_at`/`created_by`/`region_id`/`confidence`/`validation_status`/`security_classification`/`extractor_version`/`version`/`content_hash`) — `evidence_class` is the one **closed, validated** vocabulary in this whole addition (`KNOWN_EVIDENCE_CLASSES`: direct-source/extracted/normalized/user-entered/imported-structured-value/calculated/AI-generated-proposal/externally-researched), matching `REQUIREMENT_ADJUDICATION_OUTCOMES`/`REVIEWER_VALIDATION_STATES`'s own closed-vocabulary discipline rather than `Source.kind`'s open-world one — this is the fact/measurement/expert-judgment/user-assumption/AI-suggestion distinction the whole Camel programme's "no silent AI-to-authoritative promotion" rule depends on staying disciplined. `region_id` is the finest-grained anchor available, not required — `source_id` alone honestly satisfies "exact source anchor" when no finer region exists yet (the same "honest absence over a fabricated anchor" principle `Requirement.source_location` already establishes).

`DerivedObservation` (`id`/`project_id`/`statement`/`author_type`/`author`/`method`/`created_at`/`supporting_evidence_ids`/`case_id`/`confidence`/`validation_status`/`contradiction_state`/`superseded_by_observation_id`/`related_analysis_id`) — `author_type` closed (`KNOWN_OBSERVATION_AUTHOR_TYPES`: human/deterministic-process/AI). **Deliberately not `Finding`, and not merged with it:** `Finding` requires a `case_id` (no default); an evidence-contract observation must also work at the Source/project level before any Case exists, the same reason `Requirement` stays independent of `Case`. `case_id` here is optional, mirroring `AnalysisRun`/`ConversationMessage`'s own `case_id=None` convention. **Left an open question, not silently resolved:** whether Case-scoped `Finding`s and cross-modal `DerivedObservation`s should eventually converge is a genuine future design-review item, flagged here rather than decided by omission.

**Citation contract:** `CaseWorkspaceStore.resolve_region_citation` — a stable internal anchor (`region_id`, unaffected by rename/relabel) resolved into a human-readable rendering at **read time**, never stored, the same "store flat, derive structure at read time" convention `Folder`'s own path resolution already uses. Returns `{"status": "resolved", "label", ...}` or the honest `{"status": "unavailable", "region_id"}` broken-anchor state — falsification-tested: removing the underlying `Source` (`removed_at` set) correctly flips a previously-resolved citation to `unavailable`. Full source-version-aware staleness detection is a deferred future extension (MM8/MM9), not attempted here — `Source` itself has no version counter yet, only `supersedes_source_id` lineage.

**Additive `Source` fields** (`mime_type`/`size_bytes`/`security_classification`/`extractor_version`) — every existing `Source` simply lacks these keys and loads with the honest `None` default, identical to every other addition to this dataclass. `security_classification` is per-`Source` (narrower than CLAUDE-P31's project-wide `security_profile`, not a replacement for it).

**Project-boundary enforcement, mirroring `Folder`'s own defense-in-depth:** every mutation (`create_structural_unit`/`create_addressable_region`/`register_evidence_item`/`record_derived_observation`) re-validates each referenced id's own `project_id` against the calling workspace before acting — a foreign `source_id`/`structural_unit_id`/`region_id`/`evidence_id` is rejected, not silently accepted. Falsification-tested directly (a deliberately unguarded bypass proves the real, guarded method's rejection is load-bearing, not incidental).

**Retrieval — a tightly bounded diagnostic read surface, no new UI built this stage** (`routes/api.py`, `/api/v1/documents/<project_id>/structural-units`, `.../evidence`, `.../citations/<region_id>` — all read-only, authenticated/authorized exactly like every existing `/api/v1` route via the shared `_load_authorized_project_or_404`).

**Security/Intelligence-Airlock compatibility (planning-level, no connector built):** `EvidenceItem.security_classification` and `evidence_class=externally_researched_evidence` + `validation_status=None`-until-reviewed already give the model what `specified-unbuilt/external-intelligence-airlock.md`'s inbound-untrusted-provenance/human-adoption-state requirements need; every write is `GovernanceLog`-audited. No outbound connector, sanitization pipeline, or new governed action exists — none was built this stage.

**Design-Manager/structured-work compatibility (planning-level):** spreadsheet cells/formulas, risk-register rows, Monte Carlo inputs/outputs, and P50/P80/P90 results are all representable as `AddressableRegion`/`EvidenceItem`/`DerivedObservation` combinations without a schema change; `DerivedObservation.related_analysis_id` carries model/engine-version context the same way `Relationship.related_analysis_id` already does. No Monte Carlo engine, spreadsheet editor, or drawing/OCR extraction was built this stage — see `camel-multimodal-programme.md`'s own MM2-MM9 stage boundaries.

Tests: 25 in `tests/test_mm1_evidence_contract.py` (identity, project isolation and cross-project-linkage rejection with falsification, direct-evidence-vs-derived-observation distinction, closed-vocabulary validation for both `evidence_class` and `author_type`, relationship direction and the new relationship-type registrations, citation resolution/rename-stability/broken-anchor states with falsification, address-label rendering across three distinct region-type shapes, persistence round-trip, backward compatibility with a pre-MM1 workspace JSON, provenance/validation-status/security-classification preservation, and concurrent-modification detection against the new methods) plus 3 new routes added to `tests/test_api_authentication.py`'s existing route-auth matrix.

### PDF page structure and citation — implemented, bounded (CLAUDE-MM2, PDF and Document Intelligence)
`services/case_workspace.py`'s `CaseWorkspaceStore.register_pdf_page_structure(workspace, source_id, pages, ...)` — the first real MM1 producer: one `StructuralUnit` (`unit_type="page"`) per PDF page unconditionally (a page's identity is real regardless of whether it carries text — Section 11's own "do not claim 'no usable document' merely because text is absent"), and for any page with real text, a blank-line-delimited (deterministic, no NLP) paragraph split into one `AddressableRegion` (`region_type="paragraph"`, `address={"page_index","paragraph_index"}`) + one `EvidenceItem` (`evidence_class=EVIDENCE_CLASS_DIRECT_SOURCE`) per non-empty paragraph. Mirrors `register_table_evidence`'s own already-parsed-input shape exactly — takes `pages: list[str]`, never raw bytes or a pypdf call, preserving `case_workspace.py`'s standing "does not import `bhive_parser`" rule (`promote_requirement_item()`'s own precedent).

`PDF_CLASSIFICATION_TEXT_NATIVE`/`IMAGE_ONLY`/`MIXED`/`EXTRACTION_FAILED`/`ENCRYPTED_OR_UNSUPPORTED` (`KNOWN_PDF_CLASSIFICATIONS`) — the first three are outcomes of a **successful** read (the document opened; its pages do or don't carry a text layer); the last two are the read **failing**, a materially different fact never conflated with "no text" (Section 11's own distinction). `register_pdf_page_structure` itself only ever returns the first three — a failed read never reaches it (see `services/pdf_intelligence.py` below).

`_render_region_address_label` gained a `"paragraph"` branch (`"paragraph N"`, 1-indexed for the human-facing rendering only — the stored `paragraph_index` stays 0-indexed) — composed with the existing `unit_label` in `resolve_region_citation` to produce exactly the `"RFP.pdf · page 14 · paragraph 3"` shape this stage's own governing prompt specifies. `resolve_region_citation` itself gained source-version awareness (Section 13): a region anchored to a `Source` with `superseded_by_source_id` set now resolves `"status": "stale"` (label still renders — **"preserved old citation"**, never silently invalidated) instead of `"resolved"`, reusing the existing `Source.superseded_by_source_id` pointer (`register_source_revision`, drawings-only today) — no new version mechanism, no redline comparison. `update_source_identity` gained `mime_type`/`size_bytes`/`security_classification`/`extractor_version` parameters (the MM1-added `Source` fields, previously unpopulated by any caller) rather than a second partial-update method.

**`services/pdf_intelligence.py` (new module)** — the thin orchestration layer that DOES import `bhive_parser` (`BHiveParser.extract_pdf_pages`, the one real pypdf call site, already existed pre-MM2 for `services/drawing_intake.py`'s own use) and case_workspace.py, so neither has to import the other directly. `register_pdf_evidence_for_source(store, workspace, source_id, ...)`: loads the Source's own already-persisted original bytes (`Source.file_path` — reliably present for any Source ingested through `ingest_upload`/`add_document_source`, confirmed by direct code inspection of `services/ingestion.py`'s "persist the ORIGINAL uploaded bytes" step), reads it, classifies `PdfReadError`/`PdfStreamError`/etc. as `EXTRACTION_FAILED`, `FileNotDecryptedError`/`WrongPasswordError` as `ENCRYPTED_OR_UNSUPPORTED`, and any other exception (matching `drawing_intake.py`'s own `except Exception` precedent) also as `EXTRACTION_FAILED` — never a raw 500, never a corrupted Source record (every failure branch returns before any Source mutation). `PdfIntelligenceError` is raised only for a caller error (bad source id, wrong project, not a `.pdf`, no stored file) — distinct from "attempted and failed," which is always a real, honest classification return value, never an exception.

**Capability boundary, reused not re-derived:** `services/drawing_intake.py`'s own repository-grounded audit (CLAUDE-P40-VW8-QA-R2A) already established, and this stage re-confirms rather than re-investigates, that PDF-to-image rendering and local OCR are **not available** in this environment (no pypdfium2/PyMuPDF/pdf2image, no pytesseract, no tesseract OS binary) — an image-only PDF is classified honestly (`IMAGE_ONLY`, real page-level `StructuralUnit`s, zero regions/evidence), never rendered, never OCR'd, never claimed unsupported.

**Retrieval — one new write route, no new UI:** `POST /api/v1/documents/<project_id>/sources/<source_id>/pdf-structure` (`routes/api.py`), admin-gated (`_ADMIN_ONLY_ENDPOINTS`, the same authority level `ingest_document` already uses for anything that creates governed records from a file) — the one MM2 route that mutates state; every existing MM1 read route (`structural-units`/`evidence`/`citations`) already supports `?source_id=` filtering, so no new GET route was needed. The existing PDF.js-backed document viewer (`static/js/pdf_viewer.js`) and its page-navigation controls were reused as-is (Part 10's own "prefer the current document viewer... rather than building a new one") — real-browser-verified against a real, multi-page PDF with no code changes to either file.

**Real-file verification, not mocks alone:** a hand-built, valid, minimal PDF (~600 bytes, no reportlab/fpdf dependency added) with genuine embedded text is used for one deliberate full-stack unit test and was also used for real end-to-end live-application verification — real upload through the existing `/upload` flow, real page navigation in the existing browser viewer, then the new API route triggered via a real authenticated session, producing real `StructuralUnit`/`AddressableRegion`/`EvidenceItem` records and a citation rendering exactly `"<filename>.pdf · Page 2 · paragraph 1"`. Every other test mocks `BHiveParser.extract_pdf_pages`, matching `tests/test_p40vw8qa_r2a_drawing_intake.py`'s own established hermetic pattern for PDF-adjacent code.

**Finding/DerivedObservation usage evidence (Section 14):** real MM2 evidence (PDF-sourced `EvidenceItem`s feeding a `DerivedObservation`) continues to show the two concepts serving genuinely different grains — `EvidenceItem` is anchored, verbatim, unauthored-interpretation content; `DerivedObservation` is an interpretation built from one or more `EvidenceItem`s, still Case-optional. Consistent with MM1's own finding: **not merged, not migrated** — the convergence question remains open, deferred to whichever later stage's real usage makes it unavoidable.

**Deliberately not built this stage:** OCR, PDF-to-image rendering, handwritten recognition, advanced table extraction (Batch J's `Table`/`TableRow` remains the tabular path, untouched), PDF annotation editing, redaction, digital-signature validation, form filling, embedded-file extraction, malware sandboxing beyond the existing broad-exception containment, a new document-region selection UI (the existing viewer/thumbnails/search were reused, not extended), full semantic redline/version comparison (only the existing `superseded_by_source_id` staleness flag was reused).

Tests: 23 in `tests/test_mm2_pdf_document_intelligence.py` (page structural-unit identity/ordering, addressable-region identity and page linkage, extracted-text-to-page linkage, all three successful-read classifications plus both failure classifications, image-only pages still getting real `StructuralUnit`s, citation rendering for page+paragraph, source-version stale-citation binding with the old citation preserved, broken/unresolved anchor states, cross-project denial with falsification, persistence round-trip, backward compatibility with a pre-MM2 workspace JSON, concurrent-modification detection, direct-evidence-vs-derived-observation distinction via a real PDF-sourced chain, parser-failure-without-corruption for both encrypted and malformed cases, an unexpected-exception-type fallback proof, non-PDF/missing-file/cross-project source rejection, and one deliberate real-PDF-file end-to-end test) plus 2 new tests and 1 new route added to `tests/test_api_authentication.py`'s existing route-auth matrix.

### Worksheet/row structure and bounded editing — implemented, bounded (CLAUDE-MM3, Spreadsheet and Structured-Data Intelligence)
**New dependency:** `openpyxl==3.1.5` (`requirements.txt`) - checked against `tools/dependency_fit.py` (clean PASS on every established constraint) before being added; directly analogous in role to the already-accepted `pypdf`/`python-docx`, and required for any part of this stage to exist at all. Legacy `.xls` deliberately NOT supported (would need a second, separate dependency - modern `xlrd` dropped `.xlsx` support and only reads `.xls` - a `.xls` upload is refused, honestly, never misread as `.xlsx`).

**`.xlsx` is an add-to-existing-project Source format only, deliberately NOT a project-creation format.** `routes/workspace.py`'s `ALLOWED_DOCUMENT_EXTENSIONS` (Add Documents, `add_document_source`) gained `.xlsx`; `services/ingestion.py`'s `ALLOWED_UPLOAD_EXTENSIONS` (project creation, `ingest_upload`) was deliberately left untouched - a spreadsheet is not an RFP/RFQ-shaped text document, and routing it through `BHiveParser`'s requirement-extraction/classification pipeline (repeatedly flagged across many prior stages as fragile and adversarially-tuned) would be exactly the "forced fit" this stage's own governing prompt cautions against. A project must already exist (from an ordinary text document) before a workbook can be added - the same precedent drawing/image intake already established for a different non-text format. **Real-browser-verified defect found and fixed during this stage's own verification:** `templates/base.html`'s Add-Documents file input still had a hardcoded `accept=".pdf,.docx,.txt,.csv,.md"` HTML attribute - Chrome silently filters an already-selected `.xlsx` out of the form's `FileList` before submission even though the server-side check was already correct; fixed to include `.xlsx`, confirmed via a subsequent real upload.

`services/case_workspace.py`'s `register_spreadsheet_structure(workspace, source_id, sheets, ...)` mirrors `register_pdf_page_structure`'s own already-parsed-input shape exactly (`sheets: list[dict]` in, never raw bytes/openpyxl - this module still does not import `openpyxl`). Reuses MM1/MM2's own primitives with zero new domain objects: a worksheet IS a `StructuralUnit` (`unit_type="worksheet"`, one per sheet unconditionally - a hidden or empty sheet is still real and addressable, the same "do not claim no usable document merely because content is sparse" principle MM2's image-only PDF pages already established); a row IS an `AddressableRegion` (`region_type="row"`) whose own `address` dict carries the row's real structured cell data (`{col_letter: {"value","formula","cached_value","data_type"}}`) directly, not just a citation label - `AddressableRegion.address` has always been a free-form dict. `create_addressable_cell_region` adds the one single-cell region type (`region_type="cell"`) for a caller that needs one specific cell as its own citable anchor, distinct from register_spreadsheet_structure's bulk per-row regions. New `SPREADSHEET_CLASSIFICATION_SUPPORTED`/`MALFORMED`/`ENCRYPTED_OR_UNSUPPORTED`/`EXCESSIVE_SIZE` (`KNOWN_SPREADSHEET_CLASSIFICATIONS`) - the same "successful read, real finding" vs. "read itself failed" distinction PDF classification already establishes. `_render_region_address_label`'s pre-existing `"cell"/"row"` + `"label" in address` branch (from MM1) already renders spreadsheet citations correctly with zero new code - `"<source>.xlsx · <Sheet> · row N"`/`"· cell H27"`, matching this stage's own governing prompt's citation examples exactly. `resolve_region_citation`'s MM2-added `superseded_by_source_id`-driven `"stale"` state applies unchanged here too.

**`services/spreadsheet_intelligence.py` (new module)** - the thin orchestration layer that imports `openpyxl` (case_workspace.py deliberately does not). `inspect_workbook(raw_bytes, filename)`: `.xlsx` via a two-pass openpyxl read (`data_only=False` for formulas, `data_only=True` for cached results - openpyxl never recalculates; a formula's `cached_value` is honestly `None` when the workbook was never opened/recalculated by real Excel, never fabricated); `.csv` via stdlib `csv` (leading zeros/identifiers preserved automatically - every field is a plain string, nothing coerces it). Detects and warns on: hidden sheets, sheet protection, native Excel tables (`worksheet.tables`), external links (`workbook._external_links`), and formula presence (with the explicit "cached values... not recalculated by ARCHIOSK" disclosure Section 9 requires). Bounded (Section 16): `MAX_ROWS_PER_SHEET=200`, `MAX_SHEETS=50`, `MAX_UNCOMPRESSED_BYTES=50MB` (checked against the zip's own declared uncompressed size before any decompression - the actual decompression-bomb guard), each truncation reported as an honest warning, never silent. **Macro detection is content-based, not extension-based** - a real check for `xl/vbaProject.bin` inside the zip regardless of claimed filename, defense in depth against a renamed `.xlsm`; VBA is never executed, matching this module's own stated capability boundary. Encrypted/legacy-container `.xlsx` detected by its OLE2/CFBF magic-number prefix, before ever attempting to unzip it.

**`apply_bounded_cell_edit`** - the one edit operation this stage implements, each restriction load-bearing: refuses outright if the target cell currently holds a formula (`data_type == "f"`, "no silent formula replacement"); an optional `expected_file_hash` closes a real race the JSON-workspace-level `ConcurrentModificationError` alone would not catch (two in-memory copies editing different cells of the same physical file); the pre-edit bytes are copied to a sibling `<name>.pre-edit-<timestamp>.xlsx` backup BEFORE the original is overwritten - "preserve original workbook" stays literally true; the new value is written with the SAME data type the old value had, so a leading-zero identifier ("007") is never silently coerced to a number. Deliberately no new Source/Supersession lineage record for a single-cell edit (register_source_revision remains drawings-only, not generalized this stage) - `GovernanceLog`'s existing append-only mechanism carries the full before/after/actor/timestamp provenance instead. **"Export a revised workbook" needed zero new code**: the pre-existing generic `routes/workspace.py`'s `source_file` download route already serves whatever bytes currently sit at `Source.file_path`, so an edit's in-place overwrite makes that same route the export/re-download mechanism for free - real-browser-verified (downloaded the edited workbook, reopened it, confirmed the edit persisted and every untouched formula/cell was byte-for-byte unaffected).

**Retrieval and editing - two new write routes, no new UI**, mirroring MM2's own pattern: `POST /api/v1/documents/<project_id>/sources/<source_id>/spreadsheet-structure` and `.../spreadsheet-cell` (`routes/api.py`, both admin-gated via `_ADMIN_ONLY_ENDPOINTS`). The existing Display panel shows a blank content area when an `.xlsx` Source is opened (no binary-content viewer exists for it, the same pre-existing behavior any non-renderable format already had) - an honest, explicitly in-scope gap (Section 19 permits a diagnostic-API surface in place of a full spreadsheet grid), not a defect.

**Design-Manager/risk-register compatibility (planning-level, no schema forced prematurely):** a risk-register row (Risk ID, description, probability, impact, owner, etc.) is already representable as one `AddressableRegion`'s own structured `address["cells"]` dict plus its accompanying `EvidenceItem` - no dedicated `RiskRecord` dataclass was created, matching this stage's own explicit "do not force a permanent risk schema prematurely" instruction. The canonical Progressive Design-Build case's own spreadsheet foundation (open the workbook, identify the risk-register table, preserve formulas, edit selected fields, link a row to evidence, export a revised workbook) is proven end-to-end by this stage's own live verification; Monte Carlo itself (P50/P80/P90, correlation, dominant-driver identification) was not attempted - no repository evidence yet justifies it as a small, dependency-neutral extension.

**Finding/DerivedObservation usage evidence:** real spreadsheet-sourced evidence (a risk-register row, its formula-derived score) continues to show the same MM1/MM2 pattern - `EvidenceItem` as anchored verbatim row content, `DerivedObservation` as a separate interpretation built from it. Not merged, not migrated; the convergence question remains explicitly open.

Tests: 33 in `tests/test_mm3_spreadsheet_intelligence.py` (worksheet structural-unit identity/order, hidden-sheet handling, cell/row identity, value-type and formula/cached-value distinction, single-cell regions, citation rendering, project isolation and cross-project denial, persistence, backward compatibility, concurrency, direct-evidence-vs-derived-observation; real-openpyxl-built-workbook classification for all four states including a content-based macro-detection proof against a renamed `.xlsm`, hidden-sheet/table/external-link detection, formula preservation and the honest-`None`-cached-value case; edit provenance/before-after/backup-file proof with a falsified concurrency-guard test, refusal to edit a formula cell, leading-zero preservation on edit; CSV formula-injection export safety, CSV leading-zero read preservation, and a regression proof that the pre-existing CSV-as-plain-text `BHiveParser` ingestion path is completely unaffected) plus 4 new tests and 2 new routes added to `tests/test_api_authentication.py`'s route-auth matrix, and 2 functional API tests proving real registration + edit through the live route stack.

### Drawing sheets, reversible orientation, and evidence sachets — implemented, bounded (CLAUDE-MM4, Drawing Intelligence and Orientation-Normalized Comparison)

**No new dependency.** `pypdf` (already accepted, MM2) reads a drawing-oriented PDF's per-page text/mediabox/`/Rotate`; `Pillow` (already accepted, pre-MM1 drawing-image intake) reads a standalone `.png`/`.jpg`/`.jpeg`'s dimensions and provides its own built-in decompression-bomb guard (`Image.DecompressionBombError`, `MAX_IMAGE_PIXELS`) for free.

**A drawing sheet IS a `StructuralUnit`** (`unit_type="sheet"`, one per PDF page or exactly one for a standalone image, unconditionally - Section 6's own "do not invent sheet numbers or titles when they cannot be extracted reliably" applies to the UNIT's existence too: a sheet with zero extractable title-block text is still real and addressable). `services/case_workspace.py`'s new `register_drawing_sheet_structure(workspace, source_id, sheets, ...)` mirrors `register_pdf_page_structure`/`register_spreadsheet_structure`'s own already-parsed-input shape exactly (`sheets: list[dict]` in, never raw bytes/pypdf/PIL - this module still does not import either). Deliberately does NOT auto-create any `AddressableRegion` the way MM2's paragraphs/MM3's rows do - a drawing sheet has no natural sub-structure to enumerate automatically; a region only ever exists because a reviewer drew one. New `create_addressable_drawing_region(workspace, structural_unit_id, x, y, width, height, ...)` creates a `region_type="rectangular"` region (bounds-validated, sequential 1-based `region_index` per sheet, never renumbered) - `_render_region_address_label` gained one new clause (`"region_index" in address` → `"region N"`), giving the `"A101 · Ground Floor Plan · region 3"` citation shape this stage's own governing prompt asks for with zero change to `resolve_region_citation` itself. `register_source_revision`'s pre-MM1 `superseded_by_source_id` mechanism (already drawing-oriented, unchanged) makes an MM4 region's citation flip to `"stale"` automatically after a drawing revision - proven by a dedicated test, not new code.

**Title-block field extraction reuses `services/drawing_intake.py`'s own LABEL: VALUE pattern vocabulary** (`_FIELD_PATTERNS`/`_SHEET_PREFIX_DISCIPLINE`/`FIELD_LABELS`) rather than a second, drifting regex set - applied PER PAGE (a new `_title_block_fields_for_page` in `services/drawing_intelligence.py`), unlike `drawing_intake.py`'s own aggregate-first-match-across-the-whole-document shape (right for ITS use case, a single cover sheet's project identity; wrong for a multi-sheet drawing set with an independent title block on every sheet). Every field is tagged with one of the new `METADATA_RELIABILITY_DIRECTLY_EXTRACTED`/`_USER_ENTERED`/`_INFERRED`/`_UNAVAILABLE`/`_UNVERIFIED` tiers (`KNOWN_METADATA_RELIABILITY_TIERS`) - a standalone raster image, with no text layer to mine (OCR remains out of scope, Section 15), reports every field as honestly `unavailable` rather than silently omitting them. New `DRAWING_CLASSIFICATION_SUPPORTED`/`MALFORMED`/`ENCRYPTED_OR_UNSUPPORTED`/`EXCESSIVE_SIZE` (`KNOWN_DRAWING_CLASSIFICATIONS`) - the same "successful read" vs. "read itself refused/failed" distinction PDF/spreadsheet classification already establish, shared across BOTH container formats (a drawing sheet is a drawing sheet regardless of whether it arrived as a PDF page or a PNG).

**Reversible orientation is pure, deterministic geometry - a genuinely new module, `services/drawing_intelligence.py`'s own `transform_point_to_display`/`transform_point_to_original` (and their rectangle-shaped counterparts).** Composition is MIRROR-then-ROTATE (a normalized 0-1, origin-top-left point is mirrored first, then rotated clockwise by 0/90/180/270°); the inverse is exact (round-trip proven for every rotation × mirror combination in `tests/test_mm4_drawing_intelligence.py`). Every `AddressableRegion` is always stored in the sheet's ORIGINAL (unrotated, unmirrored) frame - rotation/mirror are VIEW-only state, never written to the region or the Source. `static/js/drawing_image_viewer.js` (new, per-element-mounted, not a page singleton) reimplements this exact math in JS for its own standalone-image zoom/pan/rotate/mirror/region-select viewer - a deliberate, small, independently-verified duplication (no shared runtime between Flask and the browser). `static/js/pdf_viewer.js` gained mirror-H/mirror-V/reset ON TOP of its own pre-existing PDF.js-baked rotation, via a plain CSS transform on the same `pageWrap` element that already holds the canvas+annotation overlay together - composing as ROTATE-then-MIRROR instead (PDF.js bakes rotation into the raster first, CSS mirrors it second), a DIFFERENT but equally valid composition order than the image viewer's, since each pipeline's own forward/inverse only need to be internally consistent with themselves, never with each other (documented explicitly in `pdf_viewer.js`'s own comment, not left as an unexplained divergence). Both surfaces render the required visible status text ("Rotated 90° clockwise and mirrored horizontally — source unchanged").

**A drawing region is selected on-screen and stored in original coordinates in one step**, via each viewer's own coordinate-correction: `pdf_viewer.js` extends its pre-existing VW7A-QA2 annotation-overlay pointer/`convertToPdfPoint` machinery with a new `region` tool and a mirror-flip pre-correction (`flippedCanvasPoint`) shared by every tool, not just regions; `drawing_image_viewer.js` reads the transformed `<img>`'s own `getBoundingClientRect()` (the browser's own post-CSS-transform box) and inverts via `toOriginalPoint`. Both call the new, admin-gated `POST /api/v1/documents/<project_id>/sources/<source_id>/drawing-regions` (`routes/api.py`, `create_drawing_region` → `services/drawing_intelligence.create_drawing_region_and_evidence`), which creates the region AND its `EvidenceItem` (`evidence_class=EVIDENCE_CLASS_DIRECT_SOURCE` - the region IS a crop of the Source, not a typed fact) in one call, returning the resolved citation.

**Comparison reuses the EXISTING multi-Display infrastructure with one small, deliberate extension**, not a new mechanism: a `source`-kind Display division (`static/js/case_workspace.js`'s `populateDivision`) is a direct DOM insertion into the SAME top-level page (never an `<iframe>` - confirmed by reading that function before designing anything), so mounting `drawing_image_viewer.js` on a drawing `<img>` in ANY division (division 0 or a comparison division) gives it its own fully independent zoom/pan/rotate/mirror/region state via ordinary JS closure scope - `populateDivision`'s own `kind === 'drawing'` branch now calls `window.ArchioskDrawingImageViewer.mount(img)` after inserting it. A drawing-oriented PDF's own comparison-Display behavior is UNCHANGED (still the pre-existing plain `<iframe src=file_url>`, native browser PDF chrome, no Archiosk controls) - an explicit, documented, bounded deferral (Section 8 permits the "independent side-by-side workflow must still be real" requirement to be satisfied by two IMAGE drawings, which this stage's own live-browser verification does).

**The Governed Evidence Sachet (Section 14) is implemented for the first time this stage** - `CaseWorkspaceStore.build_evidence_sachet(workspace, region_id, task_description=None)`, a pure, read-time ASSEMBLY of already-governed records (no new persisted object, the same "derive at read time" convention `resolve_region_citation` already uses): the region's own sheet (with its title-block metadata), its SIBLING regions on that SAME sheet only (named via `region_id`/`region_type`/label, not fully expanded - "minimum necessary does not mean minimum intelligible"), its citation, and its Source's own sensitivity classification - every OTHER sheet and every OTHER Source in the project is deliberately excluded, with an honest `excluded.summary` count. Exposed read-only (not admin-gated, matching `get_region_citation`'s own authority level) via `GET /api/v1/documents/<project_id>/regions/<region_id>/evidence-sachet`. No external connector was implemented or wired to it - Section 14's own explicit boundary.

**Scale/measurement (Section 11): recorded, never authoritative.** A title-block's own `scale:` text is extracted and tagged like any other field (directly-extracted or unavailable); no calibration mechanism, no pixel-to-real-world conversion, and no measurement tool exist. `drawing_image_viewer.js`'s own sheet-metadata panel always shows an explicit warning ("Measurements are not reliable... do not take dimensions from this view") alongside whatever title-block fields were found - Section 11's own required visible warning, not left to a completion-report disclosure alone.

**Security (Section 16): content-based, defense in depth, reusing already-established patterns rather than inventing new ones per format.** PDF: pypdf's own text-only extraction path never executes embedded JavaScript or active content (the same fact MM2 already established); malformed/encrypted reads are classified, never raised as an unhandled exception; page count is bounded (`MAX_DRAWING_SHEETS=300`) before full parsing. Raster images: Pillow's own built-in `DecompressionBombError`/`MAX_IMAGE_PIXELS` guard (no new code needed) plus a raw-byte ceiling (`MAX_RAW_BYTES=75MB`) checked BEFORE any parsing library is invoked. Every new write route is admin-gated (`_ADMIN_ONLY_ENDPOINTS`); every new store method re-validates `project_id` on every referenced id, the same defense-in-depth every prior MM stage already establishes - falsification-tested (a foreign-project structural unit id is proven rejected, not merely that the happy path works).

**Document-contextual toolbar (Section 13): the smallest coherent behavior, explicitly NOT unified into the top application menu bar this stage.** A drawing-image Source gets its own self-contained, IN-PANE toolbar (`.drawing-viewer-toolbar`, built directly by `drawing_image_viewer.js` next to the image it controls) rather than depending on `templates/base.html`'s own `#workspace-document-controls` (which only ever shows for a `.pdf` Source, and - critically - does not exist at all inside a comparison Display's own panel context in every case this stage exercises) - this is what makes the SAME toolbar work identically in the primary document pane and inside a comparison Display division with no new plumbing. A drawing-oriented PDF's own primary-pane toolbar IS the existing top-menu bar, extended in place with the new mirror/reset/region controls. Unifying both surfaces into one shared toolbar component remains explicit future work, not attempted this stage (Section 13's own "do not redesign the entire toolbar" permission).

**Finding/DerivedObservation usage evidence:** a drawing region's own `EvidenceItem` (anchored, verbatim - "this crop of this sheet") and a `DerivedObservation` built from it (an interpretation - Section 18's own worked example, "Grid line labels appear reversed after horizontal mirroring") continue to show the same MM1-MM3 pattern: two distinct grains, never merged, never migrated. The convergence question remains explicitly open.

Tests: 42 in `tests/test_mm4_drawing_intelligence.py` (coordinate-transform math - identity, single-axis mirror, all four rotations against known corner mappings, round-trip for every rotation × mirror combination for both points and rectangles, a composition-order falsification proving mirror-then-rotate is actually pinned down and not merely "some self-consistent order"; sheet structural-unit identity/ordering/metadata-reliability-tier preservation; rectangular region creation, sequential region-index, bounds rejection, citation rendering, cross-project denial, stale-anchor-after-revision; evidence-sachet assembly, sibling-inclusion, cross-source exclusion; real-pypdf/real-Pillow orchestration - title-block extraction per sheet, PNG classification with honestly-unavailable metadata, malformed PDF/image classification, oversized-file refusal before parsing, unsupported-extension/unknown-source/cross-project rejection, end-to-end region-then-evidence-then-DerivedObservation linkage; functional `/api/v1` route tests for structure registration, region creation, and evidence-sachet retrieval) plus 6 new tests and 3 new routes added to `tests/test_api_authentication.py`'s route-auth matrix, and one pre-existing MM-era test (`test_p40vw7a_qa2_thumbnails_annotations_layout.py`'s own `test_original_document_is_never_written_to`) updated to reflect the real, narrower invariant it was always meant to protect (the Document's own file route is never a fetch target) now that `pdf_viewer.js` legitimately calls `fetch()` for the new, separate region/structure API routes.

### Eye becomes a real governed visual-evidence surface, `unit_type="image"`, markers, and derivative crops — implemented, bounded (CLAUDE-MM5, Image, Screenshot, and Camera Evidence)

**No new dependency.** `Pillow` (pre-MM1) covers PNG/JPEG/WebP decode, EXIF extraction (`Image.getexif()`/`get_ifd()`, part of the same `Pillow` install, no separate `piexif`-style package), and derivative-crop generation.

**`register_drawing_sheet_structure` (MM4) gained one new, backward-compatible parameter: `unit_type: str = "sheet"`.** MM5 is the first caller to pass `unit_type="image"` - Section 6's own distinct vocabulary ("image; frame; page rendering; screenshot; camera capture") for a Source that is genuinely NOT a drawing SET, reusing the identical `AddressableRegion`/`EvidenceItem`/citation/evidence-sachet machinery MM4 already built rather than a parallel, duplicated registration path. Every existing MM4 call site (which never passes `unit_type`) is completely unaffected - falsification-tested (`tests/test_mm5_image_intelligence.py`'s own `BackwardCompatibilityWithMM4Tests`).

**Two new region-adjacent primitives, both reusing MM1's fully generic `create_addressable_region` underneath (no new domain object):** `create_addressable_marker_region` (`region_type="marker"`, a POINT - `{"x","y","region_index"}`, no width/height) for Section 13's one bounded annotation type; `_render_region_address_label` gained a matching `"marker N"` clause (distinct from `"region N"` so a citation never implies spatial extent a marker doesn't have) - both region types share ONE sequential `region_index` counter per StructuralUnit, by design (a marker and a rectangular region on the same sheet get "marker 1" and "region 2", never both "1"). New `IMAGE_CLASSIFICATION_SUPPORTED`/`_UNSUPPORTED_FORMAT`/`_MALFORMED`/`_EXCESSIVE_SIZE` and `METADATA_EXPOSURE_PRESERVED_INTERNAL`/`_SHOWN_TO_AUTHORIZED`/`_STRIPPED_FROM_EXPORT`/`_UNAVAILABLE`/`_UNVERIFIED` - Section 10's own five-state classification, deliberately a SEPARATE vocabulary from MM4's `METADATA_RELIABILITY_*` (a field can be perfectly reliably extracted while still being `EXPOSURE_PRESERVED_INTERNAL` - GPS is exactly this case, see below). New `SOURCE_ORIGIN_TYPE_EYE_CAPTURE`/`_DERIVATIVE_CROP` added to `KNOWN_SOURCE_ORIGIN_TYPES`.

**`services/image_intelligence.py` (new module), deliberately SEPARATE from `services/drawing_intelligence.py` (MM4)** rather than an extension of it - a genuinely different metadata vocabulary (EXIF camera/capture facts) from `drawing_intake.py`'s own title-block vocabulary MM4 already reuses; a small, explicit, accepted amount of duplication with MM4's own Pillow-open/decompression-bomb-guard boilerplate, in exchange for never touching MM4's already-shipped, already-tested code. `register_eye_capture(raw_bytes, filename, description, ...)` - Section 17's "validate actual image content, not extension alone" applied literally: an unrecognized/missing filename extension is not an automatic refusal, Pillow's own content sniff (`_sniff_supported_extension`) decides, before any file is written or Source created. A generic/blank filename (`"image"`, `"blob"`, `"clipboard"`, `"screenshot"`, or empty) gets a generated `"Eye capture <timestamp>"` name (Section 8); a real, meaningful filename is preserved. `extract_bounded_crop(source_id, region_id, ...)` crops the ORIGINAL bytes at the region's already-stored original-frame coordinates, producing a real derivative PNG registered as its own Source (`origin_type=SOURCE_ORIGIN_TYPE_DERIVATIVE_CROP`, `origin_reference=region_id` - Section 6/12's own required source/region/export-time/actor/checksum record, carried on `Source`'s own existing fields). `create_marker_and_evidence` mirrors MM4's own `create_drawing_region_and_evidence` shape exactly, `EVIDENCE_CLASS_USER_ENTERED` (the note IS the evidence, unlike a rectangular region's `DIRECT_SOURCE` crop-of-the-image evidence).

**Privacy boundary (Section 9/10), the one genuinely new policy decision this stage makes:** GPS coordinates are DETECTED (`gps_present: true/false`) but their actual latitude/longitude VALUES are never read into memory in the first place - not redacted after extraction, never extracted at all. Falsification-tested: a real GPS-bearing JPEG fixture is registered, and the test asserts the literal coordinate values used to construct the fixture do not appear anywhere in the returned metadata dict's own string form. The values remain physically present in the preserved original file bytes on disk (never stripped from the original); a derivative crop is EXIF-free BY CONSTRUCTION (Pillow's `Image.save()` never copies EXIF unless explicitly re-attached) - this doubles as Section 10's own "sanitized derivative export" requirement, with `extract_bounded_crop`'s own `removed_metadata_fields` return value naming exactly which fields the original had that the derivative therefore lacks (the manifest Section 10 requires). EXIF `orientation` is captured and reported as metadata but NOT auto-applied to rotate the displayed image - a documented, deliberate scope limitation (browsers disagree on native EXIF-orientation handling; auto-correcting it was judged a separate, non-trivial feature outside this stage's own minimal slice).

**Eye (`templates/base.html`) is now the real governed visual-evidence surface Section 7 describes**, not merely a structural-scaffold preview. `static/js/eye_pane.js` gained: view-only rotate/mirror/reset on the UNSAVED preview (a plain CSS transform on `#eye-canvas-image`, nothing persisted - Section 4/11); an explicit, always-visible temporary-vs-saved status (`#eye-save-status`); a real "Save to project" action (`POST /api/v1/documents/<project_id>/eye-capture`, multipart, the real `File` object kept in memory specifically for this - never the transformed/rotated view, always the original bytes). On save success, Eye hands off to `static/js/drawing_image_viewer.js` (MM4) rather than reimplementing rotate/mirror/region-select/citation a second time - the SAME real, persisted-Source viewer MM4 already built, now also loaded from `templates/base.html` directly (previously only `case_workspace.html`'s own `extra_scripts`) so it's available wherever Eye is, not only the primary document pane; `mount()`'s own idempotency guard (`dataset.drawingViewerMounted`) makes the resulting double-`<script>`-tag load harmless. "Discard preview" (renamed from "Remove") only ever discards the UNSAVED state - once saved, the Source is real project data, not a discardable preview.

**Two new bounded tools added to `drawing_image_viewer.js` itself** (shared by MM4 drawings and MM5 images alike, since both mount the same viewer): a marker tool (click-to-place, an inline text-note input reusing the same coordinate-correction math as region selection) and an "Export crop" action appearing alongside a newly-created rectangular region's own citation, calling the new `derivative-crop` route.

**Three new routes** (`routes/api.py`): `POST /api/v1/documents/<project_id>/eye-capture` (creates a brand-new Source - unlike MM2-MM4's own structure-registration routes, which all act on an ALREADY-uploaded Source), `POST .../sources/<source_id>/markers`, `POST .../sources/<source_id>/derivative-crop` - all three admin-gated, the same authority level every prior MM stage's own write routes already use.

**Comparison workflow (Section 16): no new code needed.** An Eye-saved photo is a `Source(kind="drawing")` exactly like an MM4 drawing image, so it works in a comparison Display division through the identical mechanism MM4 already built and tested (`populateDivision` mounting `drawing_image_viewer.js` per-element) - live-verified beside an MM4 drawing image with independent orientation controls on each side.

**Finding/DerivedObservation usage evidence:** a marker's own `EvidenceItem` (`EVIDENCE_CLASS_USER_ENTERED` - the reviewer's own observation text) and a `DerivedObservation` built from it continue to show the same MM1-MM4 pattern - Section 19's own worked example ("A dark linear crack is visible within the selected crop" / "The observed crack requires structural review before acceptance") is a hypothesis, not forced; the two concepts remain distinct, not merged.

Tests: 26 in `tests/test_mm5_image_intelligence.py` (real-Pillow/EXIF orchestration - supported/unsupported-format/malformed/oversized classification, content-based extension sniffing, generated-vs-preserved capture names, EXIF field extraction with reliability+exposure tiers, honest-unavailable absent fields, GPS-presence-without-value falsification, original-bytes-unchanged proof; marker creation end-to-end with DerivedObservation linkage, required-note validation, cross-project denial, citation distinctness from rectangular regions, stale-anchor-after-revision; derivative-crop creation, EXIF-free-by-construction + removed-fields manifest, original-unchanged proof, marker-region rejection, cross-project denial; `unit_type` backward compatibility with MM4; functional `/api/v1` route tests for the full eye-capture → marker → region → derivative-crop chain) plus 6 new tests and 3 new routes in `tests/test_api_authentication.py`'s route-auth matrix. Three pre-existing EYE1-era tests (`test_p40eye1_correction_resize_canvas.py`, `test_p40eye1_toolbox_eye_column.py`) updated to their real, MM5-superseded invariants rather than reverted: the "no persist/annotate/ingest" scope-boundary guard narrowed to what MM5 itself still defers (chat/terminal attachment, external AI); the "never sent anywhere" guard narrowed to "not sent until the reviewer explicitly saves"; the Reset button's own exact-string match updated for its new combined fit+orientation-reset behavior. `UI_REFERENCE_MAP.md` gained rows for the four new Eye controls plus updated three existing ones.
