# BEEHIVE Governance Baseline — STATUS

**This is the mandatory first read for any future session, human or AI, working on ARCHIOSK/BEEHIVE.**

**Placement notice:** this baseline was authored in a staging area and, on explicit authorization, committed here at `governance/` in this repository (`C:\Archiosk\Research\archiosk`, the backend repo) as a documentation-only commit. This is its interim live home, not necessarily its final one — the ratified target architecture envisions a single consolidated repository (`backend/` + `explorer/` + `governance/`), and repository consolidation itself remains separately unauthorized and frozen (see the authorization table below). Nothing in `C:\Archiosk\App\archiosk-explorer` was touched, moved, or merged to produce this placement — its historic governance corpus remains exactly where it is; see `history-mapping.md` for the full inventory and mapping.

---

## What BEEHIVE is

BEEHIVE is a governed project-development intelligence environment that accompanies a construction/infrastructure project through its lifecycle — from earliest conception through procurement, contractual commitment, design, construction, and, where the delivery model requires it, operations and handback — while preserving provenance, authority, temporal state, project isolation, and human decision rights at every stage. It is not inherently an Owner application or a Proponent application.

A particular project's governed environment emerges from: **BEEHIVE Constitutional Kernel + Perspective + Contract/Delivery DNA + Project-Specific Governed Sources.**

**BEEHIVE is a user-facing project application, not a governance-document interface.** An ordinary authorized user creates a project, works in it, saves, and reopens it later — conceptually comparable to how modern AI applications let a user create and reopen a persistent Project — with all of the governance/provenance/authority/lifecycle machinery described in this baseline operating underneath that experience, never as something a user must read, understand, or manually manipulate to do ordinary project work. See `specified-unbuilt/perspective-and-contract-dna.md` for the Create-Project/Pursuit UX flow this baseline already supports.

Everything actually implemented today is a strict, uncontradicted subset of this thesis. Nothing built needs to be reworked as the remaining pieces are designed and built. See `constitutional-invariants.md` for the rules that never change, and `current/kernel-object-model.md` for what exists in code today.

---

## What's actually implemented (real, tested, running code)

`services/case_workspace.py` (backend repo), 10 Foundation Batches plus the `promote_requirement_item()` bridge, Case-visibility (Private/Shared), the collaboration-threshold/retraction tranche, the Archive tranche, the Case-lineage/Derive tranche, the Selective Adopt/Carry-Forward tranche, the Camel MM1 Multimodal Foundation and Evidence Contract (`StructuralUnit`/`AddressableRegion`/`EvidenceItem`/`DerivedObservation`), the Camel MM2 PDF and Document Intelligence stage (`register_pdf_page_structure`, `services/pdf_intelligence.py`), the Camel MM3 Spreadsheet and Structured-Data Intelligence stage (`register_spreadsheet_structure`, `services/spreadsheet_intelligence.py`), the Camel MM4 Drawing Intelligence and Orientation-Normalized Comparison stage (`register_drawing_sheet_structure`, `create_addressable_drawing_region`, `build_evidence_sachet`, `services/drawing_intelligence.py`), the Camel MM5 Image, Screenshot, and Camera Evidence stage (`create_addressable_marker_region`, `services/image_intelligence.py`, the real governed Eye pane), the Camel MM6 Cross-Document and Cross-Modal Relationship River stage (`record_evidence_relationship`, `resolve_relationship_status`, `explain_evidence_trust`, the bounded river viewer in `static/js/drawing_image_viewer.js`), and the Camel MM7 Governed Investigation, Analytical Reasoning, and Trustworthy Answers stage (`Claim`, `record_investigation_claim`, `explain_investigation_answer`, `services/cross_modal_investigation.py`), 324 passing `unittest` tests as of the tranche this sentence was last updated for (see `current/kernel-object-model.md` for the current real count). Full detail and code anchors: `current/kernel-object-model.md`. In one sentence: `Source`, `Requirement`, `Finding`, `Relationship`, `Case`, `ReviewerValidation`, `Disposition`, `RequirementAdjudication`, `Snapshot`, `Supersession`, `TemporalObligation`, `AnalysisRun`/`AnalysisTrigger`, `GovernanceLog`, the `promote_requirement_item()` bridge, Case visibility (`CASE_VISIBILITY_PRIVATE`/`CASE_VISIBILITY_SHARED`, `visible_cases_for`, `share_case`), the Collaborative state/threshold (`CASE_VISIBILITY_COLLABORATIVE`, `retract_case_to_private`), terminal Case Archive (`CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED`, `archive_case`, the centralized `_require_case_not_archived` frozen-state guard), Derive (`derive_case_from_archive`, `derived_cases_of`, `RELATIONSHIP_TYPE_DERIVED_FROM`), and Selective Adopt/Carry-Forward for Finding + ReviewMessage (`adopt_finding_into_case`, `adopt_review_message_into_case`, `carried_forward_adoptions_for_case`, the new `CarriedForwardAdoption` primitive) all exist, are tested, and are the ground truth for everything else in this baseline. `RequirementAdjudication`, the promotion bridge, and the full Private→Shared→Collaborative→Archived→Derived→Adopted Case lifecycle are also now reachable through minimal route wiring in `routes/workspace.py` (`promote_requirement_item_route`, `adjudicate_requirement`, `share_case`, `retract_case`, `archive_case`, `derive_case`, `adopt_finding`, `adopt_review_message`).

## What's specified but unbuilt (fully designed, zero code)

Scenario/Scenario-Delta, `ViabilityAssessment`, `Template`, `ReferenceStandard`, Contract/Delivery DNA, Perspective (application layer), the graduated Contract Commitment event sequence, authoritative project metamorphosis (composes from existing primitives, no new object), Owner/Proponent publication (Published Procurement Instrument), the Neutral/Adjudication Workspace, the Experience Corpus (all forms), Project Security Policy (as governed `Requirement` content), the remainder of the investigation lifecycle extensions **beyond Case visibility/collaboration/Archive/Derive/Adopt(Finding+ReviewMessage) itself** — `CaseLock`, general Copy/Adopt (carry-forward of any object type beyond Finding/ReviewMessage), automatic predecessor-issue applicability analysis, publication anchoring — Deficiency/completion-resolution, dormancy/restart. Full detail in `specified-unbuilt/`. (Case visibility — Private/Shared/Collaborative, the explicit Share transition, the collaboration threshold, pre-collaboration Shared→Private retraction, terminal Archive from any visibility state, Derive (Archive → new active Case with minimal working context), and Selective Adopt/Carry-Forward for Finding + ReviewMessage — is now implemented; see the authorization table below and `current/kernel-object-model.md`.)

**The Camel Multimodal Programme (MM1–MM9)** — recorded `CLAUDE-P40-VW9A` (2026-08-05), closing a continuity gap: this programme was maintained outside the repository and could not be located during the CLAUDE-P40-VW9 final report. Multimodal foundation/evidence contract, PDF, spreadsheet, drawing, image/camera intelligence, cross-document/cross-modal relationships, governed multimodal analytics, routine in-app creation/editing, and consolidated end-to-end validation — plus the cross-cutting Design-Manager-as-expert-integrator requirement (canonical case: a Progressive Design-Build Excel risk-register/Monte Carlo workflow). **MM1 (Multimodal Foundation and Evidence Contract) is now IMPLEMENTED** — `CLAUDE-MM1` (2026-08-05), authorized following the accepted cockpit gate (`CLAUDE-CGP-02`); see the authorization table below and `current/kernel-object-model.md`. **MM2 (PDF and Document Intelligence) is now IMPLEMENTED, bounded** — `CLAUDE-MM2` (2026-08-05), authorized following the accepted MM1 seal; text-native/image-only/mixed PDF classification, page-level structure, and paragraph-level evidence/citation, deliberately not OCR, PDF rendering, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM3 (Spreadsheet and Structured-Data Intelligence) is now IMPLEMENTED, bounded** — `CLAUDE-MM3` (2026-08-05), authorized following the accepted MM2 seal; worksheet/row structure, formula-preserving evidence, and one bounded single-cell edit with a real, live-verified export/reopen round trip, deliberately not Monte Carlo, a spreadsheet grid UI, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM4 (Drawing Intelligence and Orientation-Normalized Comparison) is now IMPLEMENTED, bounded** — `CLAUDE-MM4` (2026-08-06), authorized following the accepted MM3 seal; drawing-sheet structure, on-demand rectangular regions, reversible mirror/rotate view transforms with exact original-coordinate round-tripping, real independent-Display comparison (two image drawings), and the Governed Evidence Sachet, deliberately not native CAD/BIM parsing, automatic symbol/dimension recognition, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM5 (Image, Screenshot, and Camera Evidence) is now IMPLEMENTED, bounded** — `CLAUDE-MM5` (2026-08-06), authorized following the accepted MM4 seal; a real governed Eye pane (paste/drop/preview/rotate/mirror/save), `unit_type="image"` structural units reusing MM4's own region/citation machinery, point-marker annotations, and EXIF-free derivative crop export with a GPS-value-never-extracted privacy boundary, deliberately not a photo editor, automatic object/defect/facial recognition, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM6 (Cross-Document and Cross-Modal Relationship River) is now IMPLEMENTED, bounded** — `CLAUDE-MM6` (2026-08-06), authorized following the accepted MM5 seal; a validated, endpoint-checked relationship layer reusing the existing `Relationship`/`Supersession` primitives, read-time status derivation (proposed/confirmed/disputed/rejected/stale/broken/superseded), the Trustworthy Answer Contract (`explain_evidence_trust`), a relationship-path Governed Evidence Sachet, and a bounded river-viewer panel, deliberately not a free-form graph canvas, automatic relationship inference, broad semantic search, or any other later-stage engine; see the authorization table below and `current/kernel-object-model.md`. **MM7 (Governed Investigation, Analytical Reasoning, and Trustworthy Answers) is now IMPLEMENTED, bounded** — `CLAUDE-MM7` (2026-08-06), authorized following the accepted MM6 seal; a new `Claim` adapter object with closed claim-classification/confidence-state/adoption-state vocabularies, structural no-citation-laundering enforcement, a deterministic cross-modal investigation engine, the Trustworthy Answer Contract/"Why should I trust this?" as one real assembly function, human adoption into real DerivedObservation/Finding records, and correction integrity via Supersession's third real consumer, deliberately not a broad semantic knowledge graph, automatic relationship/claim acceptance, or Layer-3 trusted federation; see the authorization table below and `current/kernel-object-model.md`. **MM8 (Governed Creation, Editing, Review, and Accountable Work Products) is now IMPLEMENTED, bounded** — `CLAUDE-MM8` (2026-08-07), authorized following the accepted MM7 seal; a new `WorkProduct`/`WorkProductSection` adapter with a draft→review→approve→issue lifecycle, structural issued-version immutability, non-destructive revision as Supersession's fourth real consumer, human-vs-AI content provenance that survives editing, and real DOCX/XLSX export with formula-injection sanitization, deliberately not a general office-document editor, a Monte Carlo engine, or Layer-3 federation; see the authorization table below and `current/kernel-object-model.md`. **MM9 (Whole-System Integration and Consolidated Validation) is now IMPLEMENTED, bounded** — `CLAUDE-MM9` (2026-08-07), authorized following the accepted MM8 seal; closed a real integration gap this stage's own repository-grounded investigation found — MM2's page/paragraph and MM3's worksheet/row evidence-registration routes existed, were fully tested, and were reachable by direct API call, but had zero UI trigger anywhere in the running application, so a first-time user could never turn an uploaded PDF or .xlsx Source into citable evidence — via a new lazy per-Source registration widget (`static/js/document_structure_registration.js`) mirroring MM4's own "Register this drawing" precedent exactly, reusing the existing `/pdf-structure`/`/spreadsheet-structure` routes unchanged (no new backend route, no new domain object); live-browser-verified end to end for both a real PDF and a real .xlsx Source against a freshly restarted dev server, deliberately NOT the full canonical Design-Manager/Monte-Carlo acceptance case (still blocked on the deliberately-unbuilt Monte Carlo engine — see the Camel programme's own Design-Manager mapping table), a broad automated real-browser test harness, or any other later-programme engine; see the authorization table below and `current/kernel-object-model.md`. Full detail: `specified-unbuilt/camel-multimodal-programme.md`.

**External Intelligence Airlock and Constructive Boundary Response** — recorded `CLAUDE-CGP-02` (2026-08-05) as a planning-level record ahead of the Camel programme's own MM7 (governed analytics) and any future outbound-AI capability: a controlled outbound/inbound vestibule for questions requiring public or external knowledge (minimum-necessary mission packets, allow-listing over after-the-fact redaction, disposable aliases/expiry, untrusted-inbound handling, prompt-injection detection on returned content, no silent AI-to-authoritative promotion of external evidence) and a Constructive Boundary Response pattern for legitimate users reaching a protected boundary (redirect without disclosing internal security detail, log internally, escalate only for repeated/hostile probing). Composes with, and does not duplicate, the existing `ACTION_EXTERNAL_AI_REQUEST` gate (`specified-unbuilt/organizational-security-department.md`) and `services/project_access.py`'s existing non-disclosure precedent. **NOT AUTHORIZED** for implementation — a planning-level intent record, not a build order. Full detail: `specified-unbuilt/external-intelligence-airlock.md`.

**Governed Voice / Conversational Presence** — recorded `CLAUDE-FUTURE-VOICE-A1`/`CLAUDE-FUTURE-VOICE-REC1` (2026-08-07), a repository-grounded architecture investigation performed after the accepted Camel close-out and the accepted CLAUDE-POSTCAMEL-P01 pilot-readiness seal, not an implementation. Voice-Typer/Ushering Agent/Shoulder Counsellor as three authority/behaviour profiles over one governed interaction engine (never separate AI engines); a six-level authority ladder (Hear/Understand/Suggest/Reversible-local-action/Governed-project-mutation/External-boundary-action) requiring the same confirmation the mouse/keyboard equivalent already requires at Level 4+; an ephemeral-by-default speech model (raw audio never persisted, spoken thought session-only, promotion into a durable record reusing existing forms/provenance/Approval-Gate/GovernanceLog unchanged); a Context Envelope resolved narrowest-first (active selection/`Anchor` → active document → current project → broader corpus, never cross-project); and an intervention model reusing existing MM6/MM7 severity vocabulary rather than a new AI-importance score. Found that several primitives this design needs already exist and should be reused, not duplicated: `Anchor`, the session-scoped `focused_finding` referent pattern, `interpret_message`'s intent classification, the Approval Gate, `GovernanceLog`, and the `ACTION_EXTERNAL_AI_REQUEST` gate. **GO LATER** — no dependency vetted, the companion Engineering Observatory (`CLAUDE-FUTURE-DT1-A1`, also GO LATER, also unimplemented) itself unbuilt, and a new UI/browser-microphone surface is not warranted immediately after the pilot-readiness baseline. **NOT AUTHORIZED** for implementation — a planning-level architecture record, not a build order. Full detail: `specified-unbuilt/voice-conversational-presence.md`.

**Presentation Intelligence / Design-Intent Workflow** — recorded `CLAUDE-FUTURE-PRES-A1` (2026-08-07), a repository-grounded architecture investigation performed after the accepted Camel close-out, the accepted CLAUDE-POSTCAMEL-P01 pilot-readiness seal, and the recorded Voice architecture, not an implementation. Reframes the future concept from "PowerPoint support" to a format-independent, bidirectional (Client→Proponent briefing, Proponent→Client design-intent assembly) communication artifact. Found that the proposed "Presentation Obligation" and "Presentation Assertion" objects need **no new kernel primitive** — they map directly onto the existing `Relationship` (`responds_to`/`implements`) and `Claim` (`KNOWN_CLAIM_CLASSES`/evidence-linked support-checking) mechanisms respectively; a `"slide"` `StructuralUnit.unit_type` would extend the already-open unit-type vocabulary (`page`/`worksheet`/`sheet`/`image`) the same way each prior MM stage's own modality did; `Supersession` already covers deck revision; a presentation template is a `WorkProduct` created with placeholder sections. The **one genuine gap found**: `Task` deliberately has no assignee field today, so contributor/section-ownership assignment is real, new, currently-unauthorized capability, not free reuse. Rendering strategy (native/server-side/conversion-tool) is flagged as entirely unresolved and the first time this codebase would take on real subprocess/external-conversion risk — requires `tools/dependency_fit.py` and a dedicated security review before any technology choice. **GO LATER** — not required for the accepted pilot baseline, and the rendering-strategy question remains open. **NOT AUTHORIZED** for implementation — a planning-level architecture record, not a build order. Full detail: `specified-unbuilt/presentation-intelligence.md`.

**Adaptive Attention & Context Circulation** — recorded `CLAUDE-POSTCAMEL-COMM-I1` (2026-08-08), a concept-preservation record required by that stage's own Part K, not a repository-grounded architecture investigation (unlike the Voice/Presentation records above) and not an implementation. Names, for future reference only: change-velocity-aware attention hierarchy; compound-eye passive/active attention; attention surviving handoff across sessions/reviewers; bounded Investigation "beads" connected by intent/evidence/dependency; selective context circulation; contextual defence against unrelated retrieval; human authority over significance remaining final regardless of mechanism. **NOT AUTHORIZED** for implementation or further design — a name preserved for a possible future architecture gate, nothing more. Full detail: `specified-unbuilt/adaptive-attention-and-context-circulation.md`.

**Trust Exchange & Security Commissioning** — recorded `CLAUDE-POSTCAMEL-COMM-I1` (2026-08-08), a concept-preservation record required by that stage's own Part K, not a repository-grounded architecture investigation and not an implementation. Names, for future reference only: a read-only Admin Security & Trust policy surface; organization-admin policy submission; policy negotiation without requiring disclosure of defensive techniques; a mutually-approved handshake before any testing begins; bounded, agency-authored trust testing (never open-ended/autonomous); restraint/non-retrieval treated as positive evidence, not merely absence of failure; a deficiency/retest/authorization cycle; and expected recommissioning after material policy/environment change. A real security-domain concept requiring its own dedicated review — explicitly out of scope for ARCHIOSK's own application-development commissioning work. **NOT AUTHORIZED** for implementation or further design. Full detail: `specified-unbuilt/trust-exchange-and-security-commissioning.md`.

**Bug Eye — Data Room Source Continuity** — recorded `CLAUDE-POSTCAMEL-COMM-I3B` (2026-08), a concept-preservation record required by that stage's own Section 6, following a bounded (not exhaustive) repository-grounded check rather than a full architecture investigation. Names, for future reference only: a watcher over document territory and governed Source relationships, not users; confident-relocation auto-rerouting, probable-match user authorization, ambiguous-match preserve-and-ask, unavailable-Source identity/relationship preservation with recovery marking, superseding-document explicit revision (never collapsed into "relocated"), and never silently deleting governed relationships because a Source can't currently be found. The bounded check confirmed this names no present defect: canonical Source identity is already id-based, not path-based (`Source`'s own docstring and every `_find`-based lookup, confirmed by direct search); no external Data Room/file-source connector exists today for anything to protect; `origin_type`'s already-named but unwired `external_connector` value is the real future extension point. Adjacent to, and narrower than, the adopted OPR's own `FPR-7` (External-source vestibule). **NOT AUTHORIZED** for implementation or further design. Full detail: `specified-unbuilt/bug-eye-data-room-source-continuity.md`.

## What's deliberately deferred (acknowledged, not designed)

Machine Model / Installed Asset Identity; infrastructure/tenant hosting isolation; recurring `TemporalObligation` implementation; Experience Corpus partition mechanics; multi-decade storage strategy; template-library staleness governance; detailed workspace-module/application-surface structure; a future governed legal/dispute context (formal termination, default, insolvency, negotiated termination, court/arbitral intervention). Full list: `deferred-reserved/reservations.md`.

---

## Implementation authorization status — verbatim, do not silently change

| Item | Status |
|---|---|
| Architecture (the nine-round review this baseline documents) | **RATIFIED** |
| Application implementation, broadly | **STILL FROZEN** |
| Foundation Batch K (`RequirementAdjudication`) | Already implemented in code; **further feature expansion NOT AUTHORIZED** |
| `promote_requirement_item()` design | READY (superseded by implementation below) |
| `promote_requirement_item()` implementation | **IMPLEMENTED** — `services/case_workspace.py` (`CaseWorkspaceStore.promote_requirement_item`), 14 tests in `tests/test_requirement_promotion.py::PromoteRequirementItemTests`. Further feature expansion beyond this narrow tranche remains **NOT AUTHORIZED**. |
| `RequirementAdjudication` route wiring (`routes/workspace.py`) | **IMPLEMENTED** — `promote_requirement_item_route` and `adjudicate_requirement`, 5 tests in `tests/test_requirement_promotion.py::RequirementRouteWiringTests`. No broader UI/route architecture authorized beyond these two routes. |
| Case visibility — PRIVATE / SHARED / COLLABORATIVE states | **IMPLEMENTED** — `CaseRecord.visibility`/`created_by`/`shared_by`/`shared_at`/`collaboration_established_by`/`collaboration_established_at`/`collaboration_contribution_type`/`collaboration_contribution_id`/`retracted_by`/`retracted_at`, `CaseWorkspaceStore.visible_cases_for` (the real enforcement point — every case listing/switching/default-selection query in `routes/workspace.py` goes through it), plus indirect-identifier guards on `artifact_image` and `preview_finding_id`. 50 tests total across `tests/test_case_privacy.py` (24) and `tests/test_case_collaboration.py` (26). |
| Explicit Case PRIVATE → SHARED transition | **IMPLEMENTED** — `CaseWorkspaceStore.share_case` (owner-only authority), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/share`. |
| Collaboration threshold (SHARED → COLLABORATIVE) | **IMPLEMENTED** — `CaseWorkspaceStore._cross_collaboration_threshold_if_qualifying`, invoked from the six qualifying write paths (`record_reviewer_validation`, `record_disposition`, `add_review_message` [human origin only], `request_attention`, `confirm_relationship`, `record_analysis` [`ANALYSIS_TRIGGER_USER_INITIATED` only]) atomically alongside each write's own `save()`. `Activity` and `RequirementAdjudication` are deliberately excluded, reasoned and documented in code, not silently omitted — see `current/kernel-object-model.md`. |
| Pre-collaboration Case SHARED → PRIVATE retraction | **IMPLEMENTED** — `CaseWorkspaceStore.retract_case_to_private` (owner-only, rejects outright once Collaborative), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/retract`. |
| Post-collaboration privacy-reversion prohibition (Constitutional Invariant 12) | **IMPLEMENTED** — enforced at the single validation gate in `retract_case_to_private`; no other method mutates `visibility`, so there is exactly one place irreversibility could fail, and it is tested directly. |
| Case Archive — terminal/frozen lifecycle status | **IMPLEMENTED** — `CaseRecord.status` (`CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED`), confirmed as a **separate axis from `visibility`, not a fourth visibility value** — see `current/kernel-object-model.md`'s "Case Archive" entry for the two-axis rationale. |
| Authorized Archive transition (owner-or-admin) | **IMPLEMENTED** — `CaseWorkspaceStore.archive_case`, permitted uniformly from PRIVATE/SHARED/COLLABORATIVE, atomic single-`save()`, route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/archive`. Machine agents cannot perform this — no automated/triggered call site exists anywhere in the codebase. |
| Archived-Case mutation prohibition (centralized frozen-state enforcement) | **IMPLEMENTED** — `CaseWorkspaceStore._require_case_not_archived`, called at the top of every identified Case-mutating write path (content and visibility-transition alike), not scattered per-route checks. Unresolved comments/Findings are explicitly **not** required to be resolved before archiving — archive preserves the governed record exactly as-is, including unresolved items, rather than forcing resolution or discarding them. 29 tests in `tests/test_case_archive.py`. |
| Archive → Derived Active Case | **IMPLEMENTED** — `CaseWorkspaceStore.derive_case_from_archive`: source must be `CASE_STATUS_ARCHIVED`, owner-or-admin authority (same narrow pattern as `archive_case`), new Case gets a new id, `status=OPEN`, `visibility=PRIVATE` unconditionally (Constitutional Invariant 11 — deriving from a Shared/Collaborative archive never silently publishes the new Case), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/derive`. Not Supersession, not Copy/Adopt. |
| Immutable derivation lineage | **IMPLEMENTED** — `CaseRecord.derived_from_case_id` (denormalized forward pointer) plus an authoritative `Relationship` (`relationship_type=RELATIONSHIP_TYPE_DERIVED_FROM`, `provisional=False`), structurally distinct from `Supersession`. Forward lookup (derived→archive) and reverse lookup (archive→every Case derived from it, via `CaseWorkspaceStore.derived_cases_of`) both implemented and tested. The archived source is never mutated by derivation. |
| Clean working-surface semantics | **IMPLEMENTED** — only `title`/`objective` (overridable) and `source_ids` (a reference-list copy, never the underlying `Source` documents) carry onto the derived Case; all historical contributions (comments, Findings, `ReviewerValidation`, `Disposition`, `Attention`, analysis records, collaboration/archive events) remain exclusively attached to the archived Case, never cloned. 29 tests in `tests/test_case_derivation.py`. |
| Selective Adopt/Carry-Forward — Finding only | **IMPLEMENTED** — `CaseWorkspaceStore.adopt_finding_into_case`: source Finding's Case must be archived, target Case must be that Case's own recorded derivation, owner-or-admin authority on the TARGET Case. Creates a genuinely new `Finding` (`claim_status=FINDING_STATUS_PROVISIONAL` — never authoritative merely by adoption; renewed `ReviewerValidation`/`Disposition` still required) plus an honestly-labeled accompanying `AnalysisRun`, atomic single-`save()`. General carry-forward of any other object type remains **NOT AUTHORIZED**. |
| Selective Adopt/Carry-Forward — ReviewMessage/comment only | **IMPLEMENTED** — `CaseWorkspaceStore.adopt_review_message_into_case`: same source/target/authority rule as above. Creates a new `ReviewThread`+`ReviewMessage` in the target Case authored by the **adopting** actor, never the original commenter (original message/thread untouched, unresolved comments carry forward exactly as archived); original voice preserved via a verbatim quote plus `related_object_type`/`related_object_id`, not by misattributing authorship. Atomic single-`save()`. |
| `CarriedForwardAdoption` lineage | **IMPLEMENTED** — `workspace.carried_forward_adoptions`, `CaseWorkspaceStore.carried_forward_adoptions_for_case`: one uniform, directly-filterable governed record per adopted item (source Case, target Case, object type, source object id, successor object id, adopting actor, timestamp) — deliberately not folded into the `Relationship` substrate; see `current/kernel-object-model.md` for why. 31 tests in `tests/test_case_adoption.py`. |
| `CaseLock`, general Copy/Adopt (carry-forward beyond Finding/ReviewMessage), automatic predecessor-issue applicability analysis, publication anchoring | **NOT AUTHORIZED** — specified only; explicitly *not* implied by the rows above. `Disposition`, `ReviewerValidation`, `RequirementAdjudication`, `AnalysisRun` (as a directly-adoptable object), and `Attention` are explicitly excluded from carry-forward, each reasoned in `specified-unbuilt/investigation-lifecycle-extensions.md`. See that document for full detail. |
| Scenario, ViabilityAssessment, Perspective, Contract DNA, Published Procurement Instrument, Neutral Workspace, Experience Corpus | **NOT AUTHORIZED** — specified only |
| Project Operating Environment (CLAUDE-P29) — locked Client/Owner vs. Design-Builder/Proponent classification | **IMPLEMENTED, bounded** — `ProjectWorkspace.operating_environment`, `CaseWorkspaceStore.set_operating_environment` (single write gate, `OperatingEnvironmentAlreadySetError` on any second call), `services/environment_capabilities.py`. **This is a distinct, narrower authorization from the still-`NOT AUTHORIZED` Perspective/Contract-DNA row above — do not read this row as reopening that one.** Perspective (per `specified-unbuilt/perspective-and-contract-dna.md`) is a mutable, per-reviewer UI lens over a shared project; Operating Environment is an immutable, project-wide, creation-time classification that gates which `Participant.role_type` values are selectable. Required at creation for every new project (`services/ingestion.py`'s `ingest_upload`, no default); a pre-P29 project loads `operating_environment=None` and may be classified exactly once via the admin-only `classify_operating_environment` route — never inferred, never backfilled automatically. Further feature expansion (additional environment-gated behavior beyond participant-role selection, additional environment values beyond the two above) remains **NOT AUTHORIZED** by this row and needs its own fresh authorization. 29 tests in `tests/test_operating_environment.py`. See `current/kernel-object-model.md`'s "Project Operating Environment" entry for full detail. |
| Environment Capability Architecture (CLAUDE-P30) — capability grammar, RFI/clarification directionality, Go/No-Go decision family | **IMPLEMENTED, bounded** — `services/environment_capabilities.py`'s `CAPABILITY_REGISTRY`/`capability_availability`/`capability_denial_reason` (the single resolution point routes/templates/exports call into, explicitly not a plugin framework); `rfi_originate` (Design-Builder/Proponent only)/`rfi_respond` (Client/Owner only, new `RFIDraft.response_text`/`RFI_STATUS_ANSWERED`) as a `CAPABILITY_COUNTERPART` pair; `go_no_go` as a `CAPABILITY_PARALLEL` pair (new `GoNoGoAssessment` primitive, one shared record shape validated against `CLIENT_OWNER_DECISION_STAGES`/`DESIGN_BUILDER_PROPONENT_DECISION_STAGES` — genuinely different vocabularies, no fallback for a legacy/unclassified project). Builds on, and does not reopen, the CLAUDE-P29 row above — this is the "natural next step" the P29 row's own "further feature expansion... needs its own fresh authorization" clause anticipated, authorized narrowly to these two representative workflows. **Deliberately not done this stage, remaining NOT AUTHORIZED:** any AI-prompt/`services/bhive_parser.py` environment-awareness (documented as a deliberate deferral in `environment_capabilities.py`'s own module docstring, not an oversight); any `CAPABILITY_CLIENT_ONLY`/`CAPABILITY_PROPONENT_ONLY` registry entries with no counterpart (none were found to be genuinely exclusive on inspection — see the CLAUDE-P30 final report's Part XI critique); any third Project Operating Environment value; multi-organization tenancy; the organizational security-team/policy-ingestion architecture reserved for a separate future stage. 39 tests in `tests/test_capability_architecture.py`. See `current/kernel-object-model.md`'s "Environment Capability Architecture" entry for full detail. |
| Organizational Security and Information Governance (CLAUDE-P31) — mandatory floor, single-deployment security baseline, project security profiles, written-policy ingestion, governed Q&A, centralized action-decision engine, assurance, self-check | **IMPLEMENTED, bounded** — this is an infrastructure/application-layer authorization (like CLAUDE-P27-B's tenancy/CSRF/rate-limiting work), not a domain-model concept, and explicitly distinct from `specified-unbuilt/security-policy.md`'s "Project Security Policy" (a customer's own security requirements as governed `Requirement` content — that document is untouched by this row). `services/security_policy.py`'s `evaluate_action` (floor → baseline → profile → exception, most-restrictive-wins, exception ceiling capped at `DECISION_ALLOW`) is the single resolver; `services/security_governance.py`'s `SecurityGovernanceStore` manages ONE global, deployment-wide record (no multi-organization tenancy exists — see honesty boundary in that module's own docstring). Real enforcement wired at two representative points: `services/ingestion.py` gates `ACTION_EXTERNAL_AI_REQUEST` via `BHiveParser`'s pre-existing `ai_calls_disabled` kill switch (CLAUDE-P27-B) with zero changes inside `bhive_parser.py` itself; `routes/workspace.py` gates `ACTION_EXPORT` on both RFI export routes. `services/learning_governance.py`'s `LearningContributionRequest` is approval-tracking only — no shared-learning/training pipeline exists in this repository to move data into, and this row makes no claim otherwise. **Deliberately NOT built, remaining NOT AUTHORIZED:** AI-assisted policy-clause extraction (statements/proposals are human-entered only — no new prompt was written against `bhive_parser.py` or anywhere else); real learning-zone data movement; multi-organization tenant isolation; a content-level assurance viewer; cryptographic audit integrity; a dedicated Security Officer role distinct from `admin`. See `specified-unbuilt/organizational-security-department.md` for the full target architecture and `current/kernel-object-model.md`'s matching entry. 80 tests across `tests/test_security_policy_engine.py`, `test_security_governance.py`, `test_learning_governance.py`, `test_security_enforcement.py`, `test_security_assurance.py`. |
| Project-Level Access Control (CLAUDE-P32) — which authenticated accounts may open a given project at all | **IMPLEMENTED, bounded** — closes the P27-review-flagged gap that any authenticated user could open any project by knowing/guessing its `project_id`. `services/project_access.py`'s `can_access_project` (the single centralized decision: owner, explicit `access_allow_list`, or admin — `owner is None` fails CLOSED, admin-only, the deliberate opposite of every other None-means-ungated field in this codebase) is enforced at `routes/workspace.py`'s `_load_workspace_or_404` (all 50 routes, including `export_rfi`, refactored this stage to use it), `routes/api.py`'s `_load_authorized_project_or_404` (6 routes) plus a filtered `GET /documents`, and `routes/portal.py`'s `_accessible_documents`/`_require_project_access_or_404` (`/`, `/projects`, `/search`, `/dashboard/<id>`, `/projects/<id>/delete`) — including `app.py`'s global nav-sidebar context processor, discovered during this stage's own bypass inventory to be leaking project names on every authenticated page (including error pages) regardless of access. Denial is a generic 404 everywhere, identical whether a project doesn't exist or the caller isn't authorized for it. `ProjectWorkspace.owner`/`access_allow_list` (`services/case_workspace.py`) — a new project's `owner` is locked at creation from the real authenticated session identity (`services/ingestion.py`'s `owner` parameter, deliberately never the free-text `actor` governance field); a pre-P32 project is backfilled only by deterministic exact-match against a real `models.User.username` recorded in its own `document_ingested` governance event, else stays unowned (admin-only) rather than guessed. **This is project-level access control within one deployment, not multi-organization tenancy** — `governance/specified-unbuilt/tenancy-and-project-authorization.md` remains unimplemented, and this row makes no isolation claim beyond a single deployment's own accounts; see `services/security_policy.py`'s `SECURITY_CLAIMS_REGISTRY` for the exact claim boundary. **Deliberately NOT built this stage:** separate read/write project-level permissions (the existing admin/read_only role axis is unchanged and still governs in-project actions); any case-workspace decomposition (explicitly deferred, see `current/kernel-object-model.md`'s own note); ownership-transfer UI beyond the existing admin reassignment route. 41 tests in `tests/test_project_access_control.py`, plus fixture updates to two ratified governance test suites (`tests/test_case_privacy.py`, `tests/test_route_authorization_hardening.py`) to grant both their test sessions project access as a new precondition, preserving what each already verified (Case-level privacy) unchanged. See `current/kernel-object-model.md`'s "Project-Level Access Control" entry for full detail. |
| Project-Scoped Conversation Tags and Tasks (CLAUDE-P40-VW7) — a OneNote-style selection toolbar on Project Conversation text, built-in Important/Question/Highlight tags, a small custom-tag color picker, real persisted Tasks, and two new Lists branches (Tasks/Tags) | **IMPLEMENTED, bounded** — an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to exactly this capability. `services/case_workspace.py`'s `ConversationSourceAnchor`/`Tag`/`TagOccurrence`/`Task` dataclasses, `BUILT_IN_TAGS` (Important/Question/Highlight — fixed code-level identities, never stored per-project), and the `create_custom_tag`/`add_tag_occurrence`/`remove_tag_occurrence`/`create_task`/`complete_task`/`reopen_task`/`resolve_conversation_anchor` methods (all additive `ProjectWorkspace` fields — `tags`/`tag_occurrences`/`tasks` — legacy records simply lack the key and load with the empty-list default). Anchoring is a text-quote-selector (scope + case/message/guidance identity + start/end offsets + exact quotation + limited prefix/suffix), not DOM coordinates; `_validate_source_anchor` performs a real existence check against the current workspace before persisting, and `resolve_conversation_anchor` decides "Source unavailable" at read time rather than assuming a record's target still exists. Route authorization is exactly `_load_workspace_or_404` — the same owner/allow-list/admin rule already governing the source conversation, no separate Tag/Task-specific gate. `routes/workspace.py`'s `add_tag_occurrence_route`/`remove_tag_occurrence_route`/`create_task_route` return JSON for live, no-reload Lists updates (this repository's first use of `fetch()`, confirmed architecturally compatible via `tools/dependency_fit.py` before use); `complete_task_route`/`reopen_task_route` stay classic form-POST + redirect. 54 tests in `tests/test_p40vw7_conversation_tags_and_tasks.py`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** cross-project intelligence or any tag/task view spanning more than one project; machine-generated tag/task suggestions; organization-wide task management, user assignment, due dates, notifications, calendar integration, or external task services; permanent inline in-message highlight rendering (Tag/Highlight occurrences are discoverable via the Lists panel and a scroll-and-flash navigation, not a per-character `<mark>` wrap reconciled against the existing `hotlinks` filter — a deliberate, documented scope simplification, not a silent corner-cut). |
| Governed Files Display and Project File Architecture (CLAUDE-P40-VW9) — a Files Display surface with two governed sibling roots (Data Room, Design-Builder Workspace) and a `Folder` domain model for Design-Builder Workspace only | **IMPLEMENTED, bounded** — an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to exactly this capability, following the same pattern CLAUDE-P40-VW7's Tag/Task addition already established. `services/case_workspace.py`'s `FOLDER_ROOT_DATA_ROOM`/`FOLDER_ROOT_DESIGN_BUILDER` (a closed, two-value vocabulary — different governance spaces, not open-world), `Folder` dataclass (`id`/`project_id`/`root`/`name`/`parent_folder_id`/`removed_at`/`removed_by` — canonical identity is `id`, path always derived at read time via `CaseWorkspaceStore._folder_path`, never stored), and `create_folder`/`rename_folder`/`move_folder`/`delete_folder` (all additive `ProjectWorkspace.folders` field — legacy records simply lack the key and load with the empty-list default, identical shape to VW7's own `tags`/`tasks`). Both roots are GOVERNED VIRTUAL roots — neither is a persisted `Folder` row; `create_folder` has no `root` parameter at all and always writes `FOLDER_ROOT_DESIGN_BUILDER`, so there is structurally no route or method that can create a Data Room folder this stage. `routes/workspace.py`'s new `STABLE_DIRECTORY_KINDS["files"]` entry (the second real entry in the CLAUDE-P40-VW8-QA1 extension point) and `/projects/<id>/workspace/folders...` routes — route authorization is exactly `_load_workspace_or_404`, the same rule already governing every other Project-scoped view, no separate Files/folder-specific gate; no owner/admin gate on folder mutations (mirrors `create_task`/`create_custom_tag`'s own precedent — Design-Builder Workspace is collaborative team structure, not owner-locked evidence). 40 tests in `tests/test_p40vw9_files_display_and_folder_architecture.py`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** the final government/client-issued Data Room hierarchy or any invented folder-name standard; bulk/ZIP import; external cloud-drive integration or remote retrieval; assigning existing Documents into a folder (no `folder_id` field exists on `Source`); the full `linked → retrieved → ingested → used → preserved` lifecycle (named/documented as a future vocabulary only — `ingested` is the only state genuinely real today, unchanged by this stage); addenda reconciliation; supersession/version comparison; checksum-based evidence preservation beyond `Source.file_hash`'s own pre-existing, unenforced field; a Files restore-UI (the soft-delete data is recoverable, but no "Removed Folders" surface was built); cross-Project access; global search; P41. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM1 — Multimodal Foundation and Evidence Contract (CLAUDE-MM1) — `StructuralUnit`/`AddressableRegion`/`EvidenceItem`/`DerivedObservation`, reusing `Relationship` for evidence relationships and a read-time citation resolver | **IMPLEMENTED, bounded** — authorized following the accepted cockpit gate (`CLAUDE-CGP-02`, GO recommendation), an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM1 only — MM2–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations (see the Camel Multimodal Programme row above). Deliberately generalizes rather than duplicates `Table`/`TableRow` (Batch J) — those remain the real tabular-specific realization, untouched by this row. `evidence_class` (`KNOWN_EVIDENCE_CLASSES`) and `author_type` (`KNOWN_OBSERVATION_AUTHOR_TYPES`) are the two closed, validated vocabularies; every other new field is additive (`ProjectWorkspace.structural_units`/`addressable_regions`/`evidence_items`/`derived_observations`, four new `Optional` `Source` fields, new `OBJECT_KIND_*`/`RELATIONSHIP_TYPE_*` constants) — legacy records simply lack the new keys and load with the empty-list/`None` defaults, the same backward-compatible shape every prior addition uses. `DerivedObservation` is deliberately NOT `Finding` (which requires a `case_id`) and NOT merged with it — flagged as an open future design-review question, not silently resolved. Retrieval: three read-only routes on the existing `/api/v1` surface (`.../structural-units`, `.../evidence`, `.../citations/<region_id>`) — no new UI built. 28 tests (25 in `tests/test_mm1_evidence_contract.py`, 3 added to `tests/test_api_authentication.py`'s route-auth matrix), including falsified cross-project-guard and broken-citation-anchor cases. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** any real extraction engine (OCR, PDF rendering, spreadsheet parsing, drawing/image intelligence — MM2–MM5); cross-document/cross-modal analysis (MM6/MM7); the Monte Carlo/Design-Manager engine itself (MM3/MM7, though the model is shown compatible); any External Intelligence Airlock connector (planning-level compatibility only); full source-version-aware citation staleness detection; wiring structural-unit/region/evidence extraction into the live ingestion pipeline. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM2 — PDF and Document Intelligence (CLAUDE-MM2) — text-native/image-only/mixed PDF classification, page-level `StructuralUnit`s, paragraph-level `AddressableRegion`/`EvidenceItem`s, and a strengthened citation resolver | **IMPLEMENTED, bounded** — authorized following the accepted MM1 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM2 only — MM3–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. `services/case_workspace.py`'s `register_pdf_page_structure` mirrors `register_table_evidence`'s own already-parsed-input shape (takes `pages: list[str]`, never raw bytes — this module still does not import `bhive_parser`); the actual pypdf read and encrypted/malformed classification live in the new `services/pdf_intelligence.py`, a thin orchestration layer, not a second parser. `resolve_region_citation` gained source-version staleness awareness (a region anchored to a superseded `Source` now resolves `"status": "stale"`, label still preserved) reusing the existing `Source.superseded_by_source_id` pointer — no new version mechanism; this closes part of the MM1 row's own "full source-version-aware citation staleness detection" deferral above, though only via the existing drawings-scoped `register_source_revision`, not a general PDF/document revision path. Re-confirmed, not re-investigated, that OCR and PDF-to-image rendering remain unavailable in this environment (`services/drawing_intake.py`'s own CLAUDE-P40-VW8-QA-R2A audit) — an image-only PDF is classified honestly, never rendered or OCR'd. One new admin-gated write route (`POST /api/v1/documents/<project_id>/sources/<source_id>/pdf-structure`); the existing PDF.js-backed viewer (`static/js/pdf_viewer.js`) was reused as-is for page navigation, real-browser-verified against a real multi-page PDF with zero code changes to it. 48 tests (23 in `tests/test_mm2_pdf_document_intelligence.py`, 2 new tests + 1 new route added to `tests/test_api_authentication.py`'s route-auth matrix), including a deliberate real-PDF-file end-to-end test (a hand-built, valid, minimal PDF — no reportlab/fpdf dependency added) alongside the established mocked-`extract_pdf_pages` pattern for the rest. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** OCR, PDF-to-image rendering, handwritten recognition, advanced table extraction, PDF annotation editing, redaction, digital-signature validation, form filling, embedded-file extraction, a new region-selection UI, full semantic redline/version comparison, and every other MM3–MM9 modality engine. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM3 — Spreadsheet and Structured-Data Intelligence (CLAUDE-MM3) — worksheet/row `StructuralUnit`/`AddressableRegion`/`EvidenceItem`s, workbook classification, and one bounded single-cell edit | **IMPLEMENTED, bounded** — authorized following the accepted MM2 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM3 only — MM4–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **New dependency `openpyxl==3.1.5`** — checked against `tools/dependency_fit.py` (clean PASS) before adding, directly analogous to the already-accepted `pypdf`/`python-docx`; legacy `.xls` deliberately not supported (would need a second dependency). `.xlsx` is an add-to-existing-project Source format only (`routes/workspace.py`'s `ALLOWED_DOCUMENT_EXTENSIONS`), deliberately not a project-creation format (`services/ingestion.py`'s `ALLOWED_UPLOAD_EXTENSIONS` untouched) — avoids forcing a spreadsheet through `BHiveParser`'s fragile requirement-classification pipeline. `services/case_workspace.py`'s `register_spreadsheet_structure` reuses MM1/MM2's own primitives with zero new domain objects (a worksheet is a `StructuralUnit`, a row is an `AddressableRegion` carrying real structured cell data in its own `address` dict); citation rendering needed zero new code (MM1's existing `"cell"/"row"` label branch already covers it). New `services/spreadsheet_intelligence.py`: two-pass openpyxl read distinguishing formula/cached-value/entered-value; content-based macro detection (`xl/vbaProject.bin` inside the zip, regardless of claimed extension); OLE2-magic-number encrypted/legacy detection; decompression-bomb and row/sheet-count bounds; `apply_bounded_cell_edit` refuses formula cells, checks an optional `expected_file_hash` against a real file-level race, backs up the pre-edit original before overwriting, and preserves the edited value's original data type (no leading-zero coercion). "Export a revised workbook" needed zero new code — the pre-existing generic Source-download route already serves the post-edit bytes. Two new admin-gated write routes (`POST .../spreadsheet-structure`, `POST .../spreadsheet-cell`). **Real-browser-verified defect found and fixed during this stage's own verification:** `templates/base.html`'s Add-Documents file input had a stale hardcoded `accept` attribute silently filtering `.xlsx` out of the browser's own FileList before submission even though the server-side check was already correct - fixed. 81 tests (33 in `tests/test_mm3_spreadsheet_intelligence.py` including a content-based macro-detection proof and a falsified concurrency-guard test, 4 new tests + 2 new routes in `tests/test_api_authentication.py`'s route-auth matrix), full end-to-end live verification (real upload, real registration, real edit, real re-download confirming the edit persisted and every formula/untouched cell was byte-for-byte unaffected). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** Monte Carlo simulation, a spreadsheet grid/editor UI, full Excel recalculation, VBA/macro execution, Power Query, pivot-table/chart editing, arbitrary formula authoring, legacy `.xls` support, full workbook-wide semantic comparison, and every other MM4–MM9 modality engine. A permanent risk-record schema was deliberately NOT created — a risk-register row is already representable via the general row-region mechanism, per this stage's own "do not force a permanent risk schema prematurely" instruction. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM4 — Drawing Intelligence and Orientation-Normalized Comparison (CLAUDE-MM4) — drawing-sheet `StructuralUnit`s, on-demand rectangular `AddressableRegion`s, reversible mirror/rotate view transforms, real independent-Display comparison, and the Governed Evidence Sachet | **IMPLEMENTED, bounded** — authorized following the accepted MM3 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM4 only — MM5–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency** — `pypdf` (MM2) and `Pillow` (pre-MM1) already cover PDF/raster reads; Pillow's own built-in `DecompressionBombError` guard is reused for free. `services/case_workspace.py`'s `register_drawing_sheet_structure` mirrors MM2/MM3's own already-parsed-input shape (a drawing sheet is a `StructuralUnit`, `unit_type="sheet"`); unlike MM2/MM3, no region is auto-created — `create_addressable_drawing_region` (`region_type="rectangular"`, bounds-validated, sequential `region_index`) is reviewer-driven, on demand. New `services/drawing_intelligence.py` owns the one genuinely new piece of math this stage needs: `transform_point_to_display`/`transform_point_to_original` (mirror-then-rotate, exact round-trip, deterministically tested for every rotation × mirror combination) — the required visible "Rotated/Mirrored — source unchanged" status, with every region always stored in the sheet's ORIGINAL frame regardless of the reviewer's current view transform. `static/js/pdf_viewer.js` gained mirror-H/mirror-V/reset plus a new persisted "region" tool (reusing its own pre-existing VW7A-QA2 annotation-overlay coordinate machinery); a new `static/js/drawing_image_viewer.js` gives a standalone raster drawing (previously a bare, uncontrolled `<img>`) real zoom/pan/rotate/mirror/region-select for the first time, mounted per-element (not a page singleton) so it works identically in the primary document pane AND inside a comparison Display division — `static/js/case_workspace.js`'s `populateDivision` now mounts it there too, the one deliberate extension to the existing multi-Display mechanism this stage makes (a `source`-kind division is a direct DOM insertion, not an `<iframe>` — confirmed, not assumed, before this change). Governed Evidence Sachet (`CaseWorkspaceStore.build_evidence_sachet`) implemented for the first time — a read-time-assembled, allow-listed evidence packet for one region plus its sheet/siblings/citation, explicitly excluding every other sheet and Source in the project; exposed read-only via `GET .../regions/<region_id>/evidence-sachet`; no external connector wired to it. Title-block metadata (sheet number/title/discipline/revision/scale) is extracted per PAGE by reusing `services/drawing_intake.py`'s own field-pattern vocabulary, each field tagged with an honest reliability tier (directly-extracted/inferred/unavailable) — a standalone image, with no text layer to mine, honestly reports every field unavailable. Scale/measurement is recorded, never authoritative — a visible "measurements are not reliable" warning ships in the drawing-image viewer itself. Three new routes (`POST .../drawing-structure`, `POST .../drawing-regions` both admin-gated; `GET .../regions/<id>/evidence-sachet` not admin-gated, matching the existing citation route's own authority level). 48 tests (42 in `tests/test_mm4_drawing_intelligence.py` including a composition-order falsification, real-pypdf/real-Pillow end-to-end orchestration, and functional API-route tests, 6 new tests + 3 new routes in `tests/test_api_authentication.py`'s route-auth matrix), plus one pre-existing test (`test_p40vw7a_qa2_thumbnails_annotations_layout.py`) updated to its real, narrower invariant now that `pdf_viewer.js` legitimately calls `fetch()`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** native DWG/DXF/RVT/IFC parsing, full BIM navigation, automatic symbol/room/dimension recognition, authoritative takeoff, full drawing-overlay registration/clash detection, sophisticated redline comparison, handwritten-markup recognition, a full annotation editor, synchronized pan/zoom/opacity comparison controls, a unified top-menu drawing toolbar, and every other MM5–MM9 modality engine. A drawing-oriented PDF's own comparison-Display behavior is unchanged (still the pre-existing plain `<iframe>`, no Archiosk controls) — an explicit, documented, bounded deferral; the independent-orientation comparison proof itself uses two image drawings, which is real and fully independent. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM5 — Image, Screenshot, and Camera Evidence (CLAUDE-MM5) — a real governed Eye pane (paste/drop/preview/rotate/mirror/save), `unit_type="image"` structural units, point-marker annotations, and EXIF-free derivative crop export | **IMPLEMENTED, bounded** — authorized following the accepted MM4 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM5 only — MM6–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency** — `Pillow` (pre-MM1) already covers PNG/JPEG/WebP decode and EXIF extraction. `register_drawing_sheet_structure` (MM4) gained one backward-compatible `unit_type` parameter (default `"sheet"`, unchanged for every existing MM4 caller); MM5 is the first caller to pass `unit_type="image"`, reusing MM4's own `AddressableRegion`/`EvidenceItem`/citation/evidence-sachet machinery rather than a parallel path. New `services/image_intelligence.py` (deliberately separate from `services/drawing_intelligence.py` — a different metadata vocabulary, EXIF camera facts vs. drawing title-blocks): `register_eye_capture` (content-sniffed format detection, not extension-gated alone — Section 17), `extract_bounded_crop` (a real derivative PNG, EXIF-free by construction, `origin_type=derivative_crop`/`origin_reference=<region_id>`), `create_marker_and_evidence` (`region_type="marker"`, a point annotation, `EVIDENCE_CLASS_USER_ENTERED`). **Privacy boundary, the one genuinely new policy decision this stage makes:** GPS coordinates are DETECTED (`gps_present: true/false`) but their VALUES are never read into memory at all — falsification-tested, not merely asserted. `templates/base.html`'s Eye pane is now the real governed visual-evidence surface Section 7 describes: view-only rotate/mirror/reset on the unsaved preview, an explicit temporary-vs-saved status, and a real "Save to project" action that hands off to MM4's own `drawing_image_viewer.js` (now loaded from `base.html` directly, not only `case_workspace.html`) rather than reimplementing rotate/mirror/region-select/citation a second time. Two new bounded tools added to that shared viewer: a marker tool and an "Export crop" action. Three new routes (`POST .../eye-capture`, `POST .../sources/<id>/markers`, `POST .../sources/<id>/derivative-crop`, all admin-gated). Comparison with an MM4 drawing needed zero new code — an Eye-saved photo is a `Source(kind="drawing")` like any MM4 drawing image, so it already works in a comparison Display division. 74 tests (26 in `tests/test_mm5_image_intelligence.py` including a GPS-value-never-extracted falsification and `unit_type` backward-compatibility proof, 6 new tests + 3 new routes in `tests/test_api_authentication.py`'s route-auth matrix), plus three pre-existing EYE1-era tests updated to their real, MM5-superseded invariants (the "no persist" scope guard narrowed to what MM5 itself still defers; the "never sent anywhere" guard narrowed to "not sent until the reviewer explicitly saves"). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** a full photo editor, filters/artistic effects, automatic object/defect/facial recognition, OCR over arbitrary images, panorama stitching, a direct mobile application, continuous camera streaming, a full annotation suite beyond the one point-marker type, full overlay registration, external AI/image services, and every other MM6–MM9 modality engine. EXIF `orientation` is captured/reported but not auto-applied to rotate the displayed image — a documented, deliberate limitation. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM6 — Cross-Document and Cross-Modal Relationship River (CLAUDE-MM6) — a validated relationship layer reusing `Relationship`/`Supersession`, read-time relationship-status derivation, the Trustworthy Answer Contract, a relationship-path Governed Evidence Sachet, and a bounded river-viewer panel | **IMPLEMENTED, bounded** — authorized following the accepted MM5 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM6 only — MM7–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency.** New `CaseWorkspaceStore.record_evidence_relationship` is an ADDITIVE, validating wrapper around the existing, deliberately unchanged `record_relationship` (endpoint-existence and cross-project checks, falsification-tested against both a nonexistent endpoint and a real endpoint belonging to another project — and a paired test proves the same cross-project call against the older unguarded primitive would have silently succeeded, showing the new guard is genuinely load-bearing, not redundant). Three new relationship types (`observes`/`deviates_from`/`requires_follow_up`); every other candidate type name this stage's own prompt suggested was deliberately mapped onto an already-existing MM1 type instead of minting a near-duplicate. `resolve_relationship_status` derives proposed/confirmed/disputed/rejected/stale/broken/superseded status at READ TIME (never a stored, driftable field) — human dispute/reject always outranks an earlier confirm (falsification-tested). `Supersession` (Prompt 8) becomes its second real consumer, via `record_supersession`/`supersede_relationship` — a correction creates a NEW relationship and links it back; the original is never mutated or deleted, full history reconstructable via `supersessions_for`. `explain_evidence_trust` implements the Trustworthy Answer Contract, reusing MM1's own closed `EVIDENCE_CLASS_*` vocabulary as its trust-basis classification; supporting and contradicting relationships are kept as separate lists, falsification-tested that a contradiction is never hidden behind a co-existing support edge. `build_relationship_sachet` extends the existing Governed Evidence Sachet (MM4) to a two-endpoint relationship path, same allow-listed/excluded shape. Nine new routes (`routes/api.py`): create/confirm/dispute/reject/supersede admin-gated; list/status/sachet/trust not admin-gated, matching the citation/evidence-sachet routes' own existing authority level. UI: a small "Relationships" panel added to the existing `static/js/drawing_image_viewer.js` (shared by MM4/MM5), anchored to a just-created region's/marker's own `EvidenceItem` — deliberately a bounded panel, not a free-form graph canvas, reusing `tokens.css`'s own existing status-color semantics rather than minting new ones. 29 tests in `tests/test_mm6_relationship_river.py` plus 10 new tests + 5 new admin-gated routes in `tests/test_api_authentication.py`'s route-auth matrix (which also had `ADMIN_ONLY_ROUTE_PATHS` refactored from path-only to `(method, path)` keys — the first route where an admin-gated POST and a non-admin-gated GET share one path). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** broad semantic search, automatic knowledge-graph construction, automatic relationship acceptance (every relationship starts `provisional` unless explicitly confirmed), a full free-form graph visualization, Navisworks/external-model-coordination integration, drone-mission/Bee-Scout-Colony integration (both named as future relationship models in this stage's own governing prompt, recorded as a pointer only — not reproduced here, see `current/kernel-object-model.md`'s matching entry for why), and every other MM7–MM9 modality/analytics engine. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM7 — Governed Investigation, Analytical Reasoning, and Trustworthy Answers (CLAUDE-MM7) — the `Claim` adapter, structural no-citation-laundering enforcement, a deterministic cross-modal investigation engine, the Trustworthy Answer Contract/"Why should I trust this?", human adoption into Finding/DerivedObservation, and correction integrity | **IMPLEMENTED, bounded** — authorized following the accepted MM6 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM7 only — MM8–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency** — the one optional real-AI extension point (`propose_ai_assisted_claim`) reuses the already-accepted `anthropic` package via the same lazy-import/graceful-degrade pattern `project_qa.py`/`requirement_investigation.py` already established. New `Claim` (`services/case_workspace.py`) is the one genuinely new domain object - never stores evidence content of its own, only validated `evidence_links` resolved through the SAME `_MM6_ENDPOINT_LISTS` MM6 already validates relationship endpoints against, so a hallucinated citation is structurally impossible to persist, not merely discouraged (falsification-tested, including cross-project denial). Reuses `InvestigationStep` (Prompt 8/CLAUDE-P04) unchanged as the investigation container (one new `step_kind`, `cross_modal_investigation`) rather than a second parallel record system. Four vocabularies: `KNOWN_CLAIM_CLASSES` (Section 4's seven required distinctions, closed), `KNOWN_CONFIDENCE_STATES` (seven categorical states with a defined, testable meaning each - `CONFIDENCE_STATE_MEANINGS` - deliberately no vague float alongside them), `KNOWN_ANALYTICAL_METHODS` (open-world), `KNOWN_CLAIM_ADOPTION_STATES` (nine states, `superseded` derived at read time exactly like `RELATIONSHIP_STATUS_SUPERSEDED`). `record_investigation_claim` structurally refuses an AI-authored claim classified as `directly_verified`/`deterministic_calculation` (Section 13's "do not claim deterministic computation when AI-generated", falsification-tested). `resolve_claim_status`/`explain_investigation_answer` (one assembly serving both the Trustworthy Answer Contract and "Why should I trust this?")/`build_investigation_evidence_sachet` all derive at read time, never store a driftable status field. `accept_claim_as_observation`/`accept_claim_as_finding` reuse the EXISTING `record_derived_observation`/`record_analysis` paths unchanged - no second Finding-creation mechanism. `supersede_claim` makes `Supersession`'s third real consumer (after Source and Relationship), flagging downstream Finding/DerivedObservation review at the moment of correction. New `services/cross_modal_investigation.py`: a deterministic engine (`investigate_cross_modal_question`) walking real Relationships from an anchor object into classified claims (contradiction/stale/ordinary support/abstention), reproducible by construction, plus an optional, NOT route-wired `propose_ai_assisted_claim` and a real, tested prompt-injection heuristic (`contains_likely_prompt_injection`) applied before any evidence text reaches a model prompt. Eleven new routes (`routes/api.py`): create-investigation/accept-observation/accept-finding/dispute/reject/request-specialist/request-authority/supersede-claim admin-gated; answer/sachet/claim-status not admin-gated. UI: a small "Investigate" sub-form and inline claim list added to MM6's own river panel (`static/js/drawing_image_viewer.js`) - no `window.prompt()` anywhere, matching this codebase's own established convention. 31 tests in `tests/test_mm7_governed_investigation.py` plus 16 new tests + 8 new admin-gated routes in `tests/test_api_authentication.py`'s route-auth matrix. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** unrestricted external search, generalized autonomous agents, automatic legal/contractual/code-compliance/design-approval/risk-acceptance conclusions, a general semantic knowledge graph, Navisworks/drone-mission integration, a Monte Carlo engine, broad report authoring, full office-document editing, Layer-3 trusted-agency federation, a broad cockpit redesign, and every other MM8–MM9 engine. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM8 — Governed Creation, Editing, Review, and Accountable Work Products (CLAUDE-MM8) — the `WorkProduct`/`WorkProductSection` adapter, draft→review→approve→issue lifecycle, non-destructive revision, human-vs-AI content provenance, and real DOCX/XLSX export | **IMPLEMENTED, bounded** — authorized following the accepted MM7 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM8 only — MM9 remains its own separate, still-**NOT AUTHORIZED** authorization. **No new dependency** — both exporters (`services/work_product_export.py`) reuse already-accepted packages (`python-docx`, `openpyxl`) exactly as-is. New `WorkProduct`/`WorkProductSection` (`services/case_workspace.py`) - Section 5's "work product is not evidence" enforced by construction (neither is ever an `EvidenceItem`, both are made citable via `_MM6_ENDPOINT_LISTS`, which also gains `Claim` as a citable kind for the first time here). Six stored lifecycle states plus a read-time-derived `superseded`; `KNOWN_CONTENT_CLASSES` (Section 8's seven per-section provenance classes). Section 8's "AI content must not become human-approved simply because a user opens the document" enforced two ways: editing an `ai_proposed` section auto-transitions it to `edited_ai_proposal` (never silently to `human_authored`, falsification-tested); accepting one records `accepted_by`/`accepted_at` WITHOUT touching `content_class` at all. `_require_work_product_editable` is the single centralized immutability guard (an issued work product refuses every mutation, falsification-tested including a byte-for-byte-checksum-unchanged proof after a refused edit); `issue_work_product` requires the full reviewed→approved→issued chain and computes a permanent SHA-256 `issued_checksum`. `revise_work_product` makes `Supersession`'s FOURTH real consumer (after Source/Relationship/Claim) - a genuinely new WorkProduct with deep-copied sections, the original never mutated, live-verified byte-for-byte unchanged after editing the revision. `stale_evidence_for_work_product` warns without rewriting issued content, reusing MM7's own endpoint-status helper. Four new relationship types (`based_on`/`summarizes`/`responds_to`/`resolves`); every other Section-31 candidate mapped onto an existing type. `services/work_product_export.py`: real DOCX (narrative) and XLSX (tabular) renderers, Section 27's formula-injection safeguard applied to every exported cell (falsification-tested), export checksum computed from the actual bytes produced. UI: classic form-POST + redirect throughout (RFI's own established precedent, not MM6/7's JSON convention) - a new "Work Products" Lists sidebar branch and `?work_product=<id>` detail view, satisfying Section 15's unsaved-state contract by architecture (no client-side draft state exists to desync). A real defect was found and fixed during live-browser verification: work-product redirects originally also carried `case=<id>`, which made `case_workspace.html`'s own `{% if active_case %}` branch win over `{% elif selected_work_product %}` for any Case-scoped work product, silently rendering the Case view instead - fixed by dropping `case=` from all twelve work-product redirects, matching `?source=<id>`'s own "one selection at a time" convention. 38 tests (34 in `tests/test_mm8_work_products.py` plus 4 functional route tests exercising the real Flask app end-to-end, including the full create→review→approve→issue Approval-Gate chain). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** full Microsoft Word/Excel parity, desktop publishing, arbitrary PDF editing, CAD authoring, a full Bluebeam replacement, live collaborative co-authoring, electronic/legally-binding digital signatures, a generalized workflow designer, a full Monte Carlo engine, a schedule engine, Navisworks integration, drone mission operations, external AI/internet connectors, trusted-agency federation, the Learning Vessel/education product, and a broad cockpit redesign. RFI (`RFIDraft`) was deliberately left unmigrated onto the new WorkProduct model this stage - it remains its own, separately proven capability. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM9 — Whole-System Integration and Consolidated Validation (CLAUDE-MM9) — a real, click-driven UI trigger closing the MM2/MM3 evidence-registration gap, live-browser-verified end to end | **IMPLEMENTED, bounded** — authorized following the accepted MM8 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to exactly this gap-closure. **No new dependency, no new backend route, no new domain object.** This stage's own repository-grounded investigation (required by every prior MM-stage's own precedent) found that MM2's `/pdf-structure` and MM3's `/spreadsheet-structure` routes (`routes/api.py`) were real, fully tested, and reachable by direct API call, but had zero template/JS reference anywhere in the running application — a first-time user who added a PDF or `.xlsx` as a Project Source through the real "Add Documents" form had no way to ever turn it into citable page/paragraph or worksheet/row evidence, even though that evidence layer was real and governed underneath. `static/js/document_structure_registration.js` (new) closes this: a lazy, per-Source widget mirroring MM4's own `drawing_image_viewer.js` "Register this drawing" precedent exactly — checks `GET .../structural-units`/`.../evidence` for existing registration, otherwise offers a button that POSTs to the existing, unmodified `/pdf-structure`/`/spreadsheet-structure` routes. `templates/case_workspace.html` gained one mount point after the existing PDF-canvas branch and a new `.xlsx`-iframe branch (spreadsheets previously fell through to the generic iframe branch with no distinguishing mount point at all), plus the new script's `<script>` tag; `STATIC_VERSION` bumped to 61 in the same work session per this repository's own CLAUDE.md discipline. **Deliberately no dedicated Python test file** — matching MM4/MM5's own established precedent that a JS-only viewer/widget mounted onto already-tested backend routes is proven by live-browser verification, not a second layer of route tests for routes MM2/MM3 already cover; the full suite (2,888 passed, 0 failed, 65 subtests) was re-run against the changed template/JS to confirm zero regression. **Live-browser-verified against a freshly restarted dev server, not by test alone:** a real throwaway account and project, a real hand-built minimal PDF and a real openpyxl-written `.xlsx` workbook added as Sources; the PDF's "Register this document" button produced "Registered for citation: 1 page, 1 citable paragraph," confirmed to persist across a fresh page load; the workbook's "Register this workbook" button produced "Registered for citation: 1 worksheet, 3 citable rows." Throwaway account and project fully removed afterward (permanently deleted, not merely soft-removed). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** the full canonical Design-Manager/Monte-Carlo end-to-end acceptance case from the Camel programme's own cross-cutting requirement (structurally blocked on the still-unbuilt Monte Carlo engine — MM7's own STATUS row), a broad automated real-browser test harness spanning MM1–MM8, an in-app citable-paragraph/row browser (individual citations remain reachable only through the diagnostic evidence API, exactly as MM2/MM3 already documented), and any other later-programme engine. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Independent Expert Pilot Readiness and Zero-Founder Hardening (CLAUDE-POSTCAMEL-P01) — the first post-Camel programme; a Zero-Founder audit and bounded hardening pass, not a capability build | **ASSESSED, GO** — authorized following the accepted Camel MM1–MM9 close-out (`d7df9a3`), explicitly not a new broad capability programme. A repository-grounded audit followed by three live-browser Zero-Founder walkthroughs (procurement, Design Manager, non-RFP start) found and fixed four real, bounded defects: a "for this prototype" founder-language leak in `add_drawing_source`'s error message; the MM9 registration-panel link positioned entirely below the fold on any multi-line rendered document (repositioned above the rendered content, directly under the document title); and a Work Product "Add/Edit a section" form that rendered every field from every section type at once with no indication that only the selected type's own fields persist, silently discarding a reviewer's input on save (fixed via a progressive-enhancement JS field-group toggle for the add form, and a pure Jinja per-type conditional for the edit form — both fully backward-compatible, the add form's existing "works with JavaScript disabled" guarantee unchanged). One new regression test (`tests/test_mm8_work_products.py`). **Real, documented, deliberately-not-built gaps**, none blocking: the MM7 formal Claim/"Investigate" trust engine remains wired only to drawing/image evidence (PDF/spreadsheet evidence gets a real but simpler grounded-Q&A path instead); MM3's bounded spreadsheet cell-edit has no UI trigger (the governed Design-Manager path is the MM8 risk-register WorkProduct, which does work); Requirements have no sidebar branch of their own. See `current/pilot-readiness-postcamel-p01.md` for the full pilot-readiness matrix, blocker classification, Elephant-Test clear-eyes audit, and Go/No-Go recommendation (**GO — ready for independent expert pilot**, no blockers found; several fast-follow items tracked). **Deliberately NOT built this stage:** Monte Carlo, External Intelligence Airlock, Navisworks/model coordination, drone/micro-drone field-reality stream, Learning Vessel, and any saved-workspace-template system (a safe extension point was identified — Display Layout/Appearance are already separated preference axes — but not implemented). |
| Canonical Project Root (ROOT-A1/ROOT-I1/ROOT-A2/ROOT-I2/ROOT-I3) — a read-only domain-model root audit, its first navigation implementation, two independent stress-tests (Infrastructure Ontario procurement, universal-venue generalization), a Requirements Compliance rollup, and a canonical-language reconciliation pass | **IMPLEMENTED, bounded** — authorized following the accepted CLAUDE-POSTCAMEL-P01 pilot-readiness seal. ROOT-A1 derived a nine-branch canonical project root from the 55 governed dataclasses already in `services/case_workspace.py` (Project/Sources/Requirements/Investigations/Findings & Accepted Knowledge/Work Products/Discussion/Action/History & Governance Log), finding the domain layer already disciplined about canonical ownership — the real gap was navigation drift, not the data model. ROOT-I1 implemented the first slice: Requirements promoted to a real numbered Listings branch (`data-root-branch="3"`), relocated off Overview's own accordion stack, with a machine-legible `data-root-branch`/`data-root-label` numbering scaffold added across the sidebar. ROOT-A2 (two passes) stress-tested the root against real, evidence-grounded Infrastructure Ontario RFQ/RFP/DBFM procurement material and against the harder question of whether the same interaction grammar could honestly host non-construction work without hard-coding — validated the root unchanged, found the Toolbox's six contextual panes never shaped root-branch count, and confirmed only one small refinement queue (RFI 6.3 label, Discussion/7 label, Access & Governance/1.3 wording — Requirements' own missing 3.3 Compliance rollup was the fourth). ROOT-I2 built that Compliance rollup as a pure projection over existing `requirement_adjudication_state` (never a new canonical field), keeping "awaiting review" and "needing attention" as deliberately separate counts. ROOT-I3 closed the remaining three: "RFIs" → "RFI Correspondence", "Chats" → "Conversation" (newly tagged branch 7), and an attribute-only `data-root-branch="1.3"` tag correcting "Governance" to "Operating Environment" without adding capability the branch doesn't have. Full suite 2969 passed at every seal point (`bc94da1`, `98e52dc`, `a89d612`). **Deliberately NOT built across this sequence, remaining NOT AUTHORIZED:** Project Object Registry/human numbering (`REQ-001`-style), a canonical Risk object, the Punch List projection, a merged event/history timeline view, and the broader Venue/Director universal-workspace programme — each evaluated and explicitly deferred, not silently dropped. **Governance-corpus gap, noted not fixed:** ROOT-A1/ROOT-A2's own read-only findings were delivered as Artifact documents during their sessions and were not, at the time, separately committed here — this row is the first point that sequence is referenced from `governance/STATUS.md` itself. |
| Self-Project Commissioning Readiness (CLAUDE-POSTCAMEL-COMM-A1) — an audit/readiness gate, not a capability build, asking whether ARCHIOSK can host the creation of ARCHIOSK itself as a governed project ahead of a separately-authored Owner Project Requirements document (in reconciliation, not ingested here) | **ASSESSED, READY WITH PREPARATION** — authorized following the closed ROOT-A1–ROOT-I3 sequence (`a89d612`), explicitly not a continuation of that sequence. A real, disposable project ("ARCHIOSK Application Development Project") was created, exercised, and permanently deleted through the ordinary product to test the idea adversarially rather than assume it. Found real, all-workable friction: `operating_environment`'s binary Client/Owner vs. Design-Builder/Proponent split has no neutral third option (Client/Owner recommended, costing `rfi_originate` — workable via ordinary Investigation/Discussion instead); the auto-registered founding Source is internally `rfq_rfp_document` regardless of content, though `formatting.py`'s own `_SOURCE_KIND_LABELS` already glosses this to neutral "Project Document" at display time (confirmed live — zero user-visible impact); the requirement-extraction pipeline produces noisy, never-auto-promoted "candidate requirements" from ordinary narrative prose. All seven completion states (Commissioning/Substantial Completion/Operational Readiness/Punch List/Accepted Residual/Future Programme/Final Completion) were found to map onto existing objects (`Investigation`, `RequirementAdjudication`, `ProjectContextEntry`, `GovernanceLog`, `governance/specified-unbuilt/`) with no new canonical object required; a Punch List is recommended as a future projection over existing Requirement/Finding/Disposition/Task records, the same pattern Needs Attention and Compliance already proved twice, not a new `PunchListItem` class. Project Object Registry/Numbering and a canonical Risk object were each separately classified **not required before commissioning** (the Risk finding strengthened by confirming a `risk_register` `WorkProduct` type already exists and is clickable today); a First-Run/New-User Preview was classified a useful but non-required aid. Eight of ten Zero-Founder commissioning scenarios were found already testable without the pending Owner RFP. See `current/comm-a1-self-project-commissioning-readiness.md` for the full readiness report, Owner-RFP ingestion test plan, reference-schedule accuracy test design, minimum preparatory work sequence, and current-baseline boundary list. **Deliberately NOT built or begun this stage:** ingestion of the still-unreconciled Gemini OPR, Project Object Registry/Numbering, a canonical Risk object, the Punch List projection, First-Run Preview, and the Venue/Director programme. |
| Curated Self-Project Commissioning Setup (CLAUDE-POSTCAMEL-COMM-I1) — executing COMM-A1's bounded RFP-independent preparation and establishing the real commissioning specimen; not a capability build, not permission to make ARCHIOSK the live manager of its own development | **RFP-INDEPENDENT PREPARATION COMPLETE; HOLD ON OWNER OPR INGESTION** — authorized following COMM-A1's READY WITH PREPARATION verdict (`2c78aa8`). The approved Gemini OPR Revision 0.2A was verified not supplied (repository-wide search found only the unrelated pre-existing `NREOCRC-OPR-001` fixture), so work stopped before Owner-OPR ingestion/reconciliation/a real Compliance baseline/a first commissioning pass, per this stage's own explicit instruction — none were fabricated or simulated. What was done: a real, persistent project ("ARCHIOSK Application Development Project," Client/Owner, id `0b743d80-13b0-4253-b411-9fa17ff11927`) was created through the ordinary product by a new persistent operator account (`archiosk_commissioning`, never given the real `admin` account's password), with ownership then set to the real `admin` account via the ordinary "Set owner" control; a neutral founding charter (not the Gemini OPR) was written and used as the founding document; a small, deliberately non-exhaustive curated corpus (a COMM-A1 summary and a CAMEL/POST-CAMEL/ROOT summary, both added via "+ Add Text Record") was assembled. **Repository-grounded finding new this stage:** requirement-extraction is founding-document-only — `add_document_source`/`add_text_record_source` (the only paths for adding a Source to an existing project) never invoke `BHiveParser`, confirmed live by adding two Text Records with the candidate count staying unchanged; the future Gemini OPR will therefore need its Requirements registered manually via the existing "+ Register a Requirement" path, not automatic extraction. Two new future-programme concepts named in this stage's own Part K (Adaptive Attention & Context Circulation, Trust Exchange & Security Commissioning) were recorded as concept-preservation-only, NOT AUTHORIZED entries — see the two new paragraph entries above and `specified-unbuilt/adaptive-attention-and-context-circulation.md`/`specified-unbuilt/trust-exchange-and-security-commissioning.md`. No application code was modified; the existing 2969-passed baseline is unchanged and was not re-run. See `current/comm-i1-commissioning-specimen-setup.md` for the full record. **Deliberately NOT built or begun this stage:** Owner OPR ingestion, reference-schedule reconciliation, a real Compliance baseline, a first commissioning pass, a canonical PunchListItem, Registry/Numbering, a canonical Risk object, First-Run Preview, and both newly-recorded future programmes. |
| Adopt Owner Baseline and Prepare Developmental Commissioning (CLAUDE-POSTCAMEL-COMM-I2) — ingesting the Product-Owner-adopted Gemini OPR as the real project's Governing Source, registering its 34 Owner Requirements, reconciling the baseline, and recording developmental commissioning doctrine; not a Compliance pass, not a first commissioning tranche | **OWNER BASELINE ESTABLISHED, DEVELOPMENTAL COMMISSIONING READY** — authorized following COMM-I1's RFP-independent preparation (`86e63dc`) and the Product Owner's adoption of **GEMINI-ARCHIOSK-RFP-02B — Revision 0.2A — Product Owner Adoption Copy**. The adopted document was added to the real, persistent commissioning specimen (`0b743d80-13b0-4253-b411-9fa17ff11927`) as a Governing Source via the ordinary, extraction-free "+ Add Documents" path COMM-I1 already identified — confirmed live to still trigger zero automatic extraction. All 34 Owner Requirements (OPR-1.1–OPR-7.5) were registered through the real "+ Register a Requirement" route, `original_requirement_identifier`/`text_reference` preserved byte-for-byte verbatim against the adopted document (verified by direct comparison, zero mismatches); the pre-existing 13 founding-charter extraction candidates remain untouched and visually distinct. **New procedural record, not an OPR requirement:** the developmental commissioning doctrine — present-state conformance ("does it work today") kept explicit and separate from developmental/conception-point conformance ("when should this Requirement first have governed design, was that window met or missed, what did a missed window cost") — with an eleven-field recording structure and a three-tier evidence standard (directly evidenced / reasonable inference / insufficient evidence), plus an explicit two-tier applicability note (deep ARCHIOSK self-commissioning vs. lighter ordinary-project use). A bounded, non-exhaustive conception-window pass over six of the 34 Requirements found three directly-evidenced timing-critical cases (OPR-3.4 Contextual Operations, OPR-1.4 Lifecycle Management, OPR-7.4 Deficiency Close-Out — each traceable to the adopted OPR's own Section A/F amendment notes) and one directly-evidenced early-and-sound contrast case (OPR-3.5 Canonical Ownership, per ROOT-A1's own STATUS.md record). Live-browser-verified against the real project: Requirements Listings shows 34 governed / 13 unpromoted, ROOT-I2's Compliance rollup correctly renders "34 awaiting review... not a finding of non-compliance" against the real Owner filename, an Adjudicate control renders but was **not exercised**. See `current/comm-i2-owner-baseline-and-developmental-commissioning.md` for the full record, the eleven-field structure, and the honestly-reported registration-form metadata gap (Section C's Rationale/Priority/Verification Method/Durable Record Source columns have no first-class per-Requirement field yet — preserved only at the whole-Source level). **Deliberately NOT done this stage:** Compliance assessment or adjudication of any of the 34 Requirements; conception-window analysis of the other 28; `PunchListItem` or any Punch List UI; FPR-12/FPR-13 implementation; Registry/Numbering, canonical Risk, First-Run Preview, PowerPoint/Terminal-Eye/Surface-Trust reopening. This is not a claim that ARCHIOSK is commissioned against this baseline — only that the baseline now exists, is verified accurate, and is ready for a first bounded commissioning tranche. |
| First Developmental Commissioning Tranche (CLAUDE-POSTCAMEL-COMM-I3) — real present-state adjudication plus developmental/conception-window commissioning for four calibration Requirements (OPR-3.4, OPR-1.4, OPR-7.4, OPR-3.5); a method-calibration exercise, not the start of unrestricted commissioning | **FIRST TRANCHE COMPLETE, METHOD CALIBRATED** — authorized following COMM-I2's acceptance (`1b9cbe8`). All four Requirements were adjudicated `Satisfied` through the real, unmodified `adjudicate_requirement` route (existing outcome vocabulary only, no new status introduced); live-verified the Compliance rollup correctly re-computing to "4 SATISFIED, 30 NOT YET ASSESSED." Developmental commissioning applied the COMM-I2 eleven-field/three-tier-evidence method to each: OPR-3.4 (Contextual Operations) — a genuine **implementation-side** correction, directly evidenced by the adopted OPR's own Historical Evolution register (rigid four-chamber layout superseded by the flexible workspace grammar), classified timely correction. OPR-1.4 (Lifecycle Management) and OPR-7.4 (Deficiency Close-Out) — both **requirements-drafting-side** corrections (the Rev 0.2 OPR text itself assumed capabilities — whole-project archive/restore, a dedicated Punch List architecture — the product never had or needed; the product was never wrong), both caught and fixed by COMM-A1's repository-grounded audit before Rev 0.2A adoption, both classified timely correction. OPR-3.5 (Canonical Ownership) — directly evidenced early-and-sound conception (ROOT-A1's own record: the domain layer was already disciplined about canonical ownership before that audit examined it), offered as a deliberate contrast case. **Cross-case finding:** no developmental claim in this repository can currently be cost/schedule-quantified — commits and governance records capture *what* changed and *why*, never effort/cost data — recorded as an evidence-tier limitation (INSUFFICIENT EVIDENCE), not estimated. **One real, live, present-state residual found and self-referentially resolved:** OPR-7.4 itself has no assembled cross-Requirement "deficiency close-out" projection view yet (unlike the proven `needs_attention_view`/Compliance-rollup precedents) — explicitly accepted as a residual per OPR-7.4's own permissive "may be projected" text, not built, and not a `PunchListItem`. Each per-Requirement finding also carries a short "ordinary-project/RFP translation" note (a lighter prospective question a normal client engagement should ask), procedural learning only, no new RFP capability. See `current/comm-i3-first-developmental-commissioning-tranche.md` for the full per-Requirement, per-field, evidence-tagged record. **Deliberately NOT done this stage:** adjudication or developmental analysis of the other 30 Requirements (explicitly bounded, not automatically expanded); any new `RequirementAdjudication` outcome value; any `PunchListItem` or Punch List UI; FPR-12/FPR-13; Registry/Numbering, canonical Risk, First-Run Preview, PowerPoint/Terminal-Eye/Surface-Trust reopening. Not a claim of Substantial or Final Completion. |
| Human Authority and Residual Governance Seal (CLAUDE-POSTCAMEL-COMM-I3A) — a narrow governance-reconciliation audit of COMM-I3's own authority claims, not a continuation of commissioning | **METHOD SEALED, PRODUCT OWNER RESIDUAL DECISION PENDING** — authorized following COMM-I3's acceptance (`678c692`). Corrects two overstated authority claims in COMM-I3's own text, documentation-only, with no product record altered and all four of COMM-I3's evidence-based conclusions preserved unchanged. **(1) OPR-7.4 residual:** COMM-I3 had described the missing assembled cross-Requirement deficiency-close-out view as "explicitly accepted" — a direct repository check found no `AcceptedResidual`-style object or field exists anywhere, and no Product Owner ever made that acceptance decision; corrected to **"Residual identified — Product Owner decision pending,"** with a full Product Owner decision package now recorded (what exists/doesn't, why OPR-7.4 stands Satisfied without it, that no operational deficiency was demonstrated, Claude's Accept-Residual recommendation stated as a recommendation only, and the consequence of each of the three possible Product Owner choices). **(2) The four `Satisfied` RequirementAdjudication records:** honestly and accurately stored (the real `archiosk_commissioning` operator account, no impersonation) and performed under a real, specific, contemporaneous Product Owner authorization (COMM-I3's own governing prompt) — but `RequirementAdjudication`'s own class docstring defines the object as recording **"a human's answer,"** and no human personally reviewed each of the four specific conclusions before persistence. Identified as a genuine governance deficiency (evidenced by the object's own docstring against its current field set — no content-provenance field exists on `RequirementAdjudication` the way `content_class` already exists on `WorkProduct`, MM8), not a code defect. **Smallest safe correction, zero code changed:** the four records remain stored exactly as-is (required by `RequirementAdjudication`'s own append-only, never-overwrite, "human-adjudication-as-evidence" design — deleting or editing them would itself violate that principle) with a corrected governance description, plus a standing invitation for the Product Owner or any designated human reviewer to personally re-adjudicate any of the four at any time through the existing, unmodified Adjudicate control — `latest_requirement_adjudication_for`'s own `records[-1]` resolution means a fresh human adjudication automatically supersedes the agent's one in effect while preserving it as evidence, requiring no new field, status, or object. A future content-provenance field on `RequirementAdjudication` is named as a possible future enhancement, explicitly not implemented this stage. See `current/comm-i3a-human-authority-and-residual-governance-seal.md` for the full record; `current/comm-i3-first-developmental-commissioning-tranche.md` now carries a correction notice at its top pointing here rather than being silently edited. **Deliberately NOT done this stage:** commissioning the remaining 30 Requirements; implementing an assembled deficiency view, `PunchListItem`, or any new `RequirementAdjudication` field/status; changing the adopted OPR; FPR-12/FPR-13 or any other Future Programme reopening. The Product Owner's own residual decision was not made on the Product Owner's behalf. |
| Product Owner Authority Seal and Scale-Readiness (CLAUDE-POSTCAMEL-COMM-I3B) — genuine Product Owner decisions closing COMM-I3A's open items, a standing human-authority rule, lifecycle terminology clarification, and the Bug Eye future-concept preservation; not a commissioning-expansion stage | **PRODUCT OWNER AUTHORITY SEALED** — authorized following COMM-I3A's decision package (`aa52bd2`). **OPR-7.4 residual:** the Product Owner personally stated, in their own words quoted in the durable record, "I ACCEPT THE RESIDUAL" concerning the missing assembled cross-Requirement deficiency-close-out view — a genuine Product Owner decision, not a commissioning-agent recommendation being relabeled, available for future reconsideration if operational evidence later demonstrates a real need. **Four COMM-I3 conclusions:** personally confirmed unchanged by the Product Owner. **Real product limitation found and honestly reported rather than worked around:** this session cannot produce a `RequirementAdjudication` whose `adjudicator` field truthfully reads as the real Product Owner, since doing so would require authenticating as that person, which this session structurally cannot and must not do (`adjudicate_requirement`'s `adjudicator` field is populated only from real session identity, with no separate "confirmed by" field) — the confirmation is recorded in governance only, per this stage's own explicit fallback instruction, and the four original `archiosk_commissioning` records remain untouched and honestly attributed. **New standing rule for all future commissioning:** the AI/commissioning agent may investigate, assess, classify, and recommend, but where a governed object is explicitly defined as recording "a human answer" (as `RequirementAdjudication`'s own docstring already says of itself), bounded autonomy to perform routine work never transfers Product Owner authority the governance model specifically reserves to a human. **Lifecycle terminology clarified, OPR-1.4 not reopened:** Remove/Restore (`remove_source`/`restore_source`, recoverable) vs. Permanent Delete (`delete_project`, destructive) vs. Archive (not used to imply whole-project archive/restore without proof); external OS-level file deletion is procedurally an integrity/recovery condition, never treated as an intentional ARCHIOSK action. **Bounded Source-identity finding:** canonical Source identity is already id-based, not path-based, confirmed by direct code search (every lookup resolves through the shared `_find`-by-`id` helper, none by name/file_path) — no current architectural dependency on fragile paths was found; the "changed location is not changed identity" principle is already true of everything built today and is preserved only for the *future* scenario of an external Data Room connector, which does not exist yet (`origin_type`'s already-named, unwired `external_connector` value is the real future extension point). **New future-programme concept recorded:** Bug Eye — Data Room Source Continuity, a watcher over document territory and governed Source relationships (not users) for confident/probable/ambiguous relocation matching, unavailable-Source recovery marking, and superseding-document revision — concept preservation only, mirroring the Adaptive Attention/Trust Exchange precedent exactly; see `specified-unbuilt/bug-eye-data-room-source-continuity.md`. See `current/comm-i3b-product-owner-authority-seal-and-scale-readiness.md` for the full record. **Deliberately NOT done this stage:** commissioning the remaining 30 Requirements; implementing Bug Eye, an assembled deficiency view, or `PunchListItem`; altering `RequirementAdjudication` schema; modifying the adopted OPR; reopening FPR-12/FPR-13 or Archive architecture. Scaling to the remaining 30 Requirements requires its own separate authorization, not automatic continuation from this stage. |
| Foundational Integrity Developmental Commissioning (CLAUDE-POSTCAMEL-COMM-I4) — agent commissioning assessment (not governed adjudication) of eight Current Baseline Requirements (OPR-1.1, 1.2, 1.3, 2.1, 2.2, 2.4, 2.5, 5.4) selected because errors in their formative conception could propagate widely | **ASSESSED, PRODUCT OWNER REVIEW REQUIRED** — authorized following COMM-I3B's sealed authority model (`1bcab22`). **No new `RequirementAdjudication` was persisted for any of the eight**, per this stage's own explicit instruction and COMM-I3B's own standing human-authority rule — every outcome is an agent assessment awaiting Product Owner review. Seven of eight assessed Satisfied, each Early-and-Sound Conception, DIRECTLY EVIDENCED: OPR-1.1 (`project_id` always freshly generated, live-confirmed via 5 distinct real UUID projects), OPR-1.3 (a real live cross-project switch this stage, zero state loss), OPR-2.1 (the already-committed MM1–MM9 record), OPR-2.2 (`Source.id` immutable by construction; no rename mutator exists at all), OPR-2.4 (MM1's evidence contract, with the pre-MM1 "Refines OPR-4.2 (Rev 0)" lineage still INSUFFICIENT EVIDENCE, carried forward unchanged), OPR-5.4 (`answer_project_question`'s narrow signature has no cross-project fetch primitive to misuse; no shared embedding index exists to leak from). **OPR-1.2 (Project Isolation) required special scrutiny and produced a genuinely mixed finding, not a single verdict:** data-level partitioning is Early-and-Sound (`project_id` required on every governed dataclass, one physical file per project since `CaseWorkspaceStore.get`'s earliest form, live-confirmed by a real cross-project switch showing zero leakage) — but access-level authorization (who may open a project at all) was a real, later-corrected gap, closed only at CLAUDE-P32 per that row's own text ("closes the P27-review-flagged gap that any authenticated user could open any project by knowing/guessing its `project_id`"), classified Timely Correction (DIRECT for the gap, REASONABLE INFERENCE for its timeliness, no elapsed-time record exists). Also notes, harmlessly: OPR-1.2's own text names "vector embeddings" requiring isolation, but no such capability exists anywhere in this codebase (`services/learning_governance.py`'s own docstring: "no embedding index shared across projects... confirmed by repository inspection") — the third instance this commissioning sequence has found of an OPR clause naming a broader capability than the product actually has (after OPR-1.4 and OPR-7.4 in COMM-I3), now recognized as a recurring, so-far-harmless pattern rather than a one-off. **OPR-2.5 (Revision Tracking) is this tranche's one genuine, substantive finding: Partially Satisfied, classified Unresolved Developmental Deficiency [DIRECT].** `CaseWorkspaceStore.register_source_revision` — the only call site anywhere that ever sets `supersedes_source_id`, confirmed by direct repository-wide search — is hardcoded to `kind=SOURCE_KIND_DRAWING`; no product path exists to formally register a revision/addendum for any non-drawing Source (a PDF, a text record, or a future revision of the adopted OPR document itself). Not a new discovery — MM2's own `STATUS.md` row already self-disclosed this exact limitation at ship time ("only via the existing drawings-scoped `register_source_revision`, not a general PDF/document revision path") — but no stage had assessed it against its governing OPR requirement until now. Real, live downstream exposure: the adopted OPR, the founding charter, and both curated text records all have no available supersession path today. **Not fixed this stage** (explicitly out of scope — no new features built merely to make a Requirement pass); recorded as a genuine Product Owner decision item (Accept Residual / Do Not Accept / Insufficient Evidence), structurally identical in shape to OPR-7.4's own residual in COMM-I3/I3A/I3B. See `current/comm-i4-foundational-integrity-commissioning-assessment.md` for the full per-Requirement record, the ten-point developmental analysis, the RFP-translation questions, and the complete Product Owner decision table. **Deliberately NOT done this stage:** any `RequirementAdjudication`; commissioning the remaining 22 Requirements; extending `register_source_revision`; implementing Bug Eye, FPR-12, or FPR-13; redesigning lifecycle/archive controls; creating `PunchListItem`; any new `RequirementAdjudication` provenance field; any login/gateway UI change; reopening Surface Trust. |
| OPR-2.5 Revision Continuity Corrective Tranche (CLAUDE-POSTCAMEL-COMM-I4A) — generalizing Source revision/supersession beyond drawings, the first COMM-stage to change application code | **CORRECTED, PRODUCT OWNER CONFIRMED SATISFIED** (confirmed at the opening of CLAUDE-POSTCAMEL-COMM-I5) — authorized by the Product Owner's own explicit Do-Not-Accept decision on COMM-I4's OPR-2.5 finding (`6289b12`: "formal Source revision/supersession is presently limited to drawings, while the adopted Current-Baseline requirement applies to document supersession and revision/addenda generally... This is a Current-Baseline correction, not a Future Programme"). `CaseWorkspaceStore.register_source_revision` — previously the only call site anywhere that ever set `supersedes_source_id`, hardcoded to `kind=SOURCE_KIND_DRAWING` with required `width`/`height` — now defaults `kind` to the OLD Source's own kind (a text_record revision stays a text_record; the pre-existing drawing path is unaffected either way) and makes `width`/`height` `Optional`; a new guard rejects revising an already-superseded Source (prevents an accidental fork). No second canonical store, no Source-identity redesign — the same `supersedes_source_id`/`superseded_by_source_id`/`Supersession` mechanism is reused exactly as-is. A new, non-Case-scoped route (`revise_document_source`) and a matching "+ Register a Document Revision" Project Tools control expose this for the three Source kinds `revise_source` (drawing-only) could never serve — `project_document`, `rfq_rfp_document`, `text_record` — mirroring the existing "+ Add Documents" control's own pattern, no broader UI redesign. **Live-demonstrated end to end** against a real, disposable throwaway project (never the adopted OPR commissioning specimen, permanently deleted afterward): a real "Owner Specification, Revision A" Source, a real Requirement citing it, then a real "Revision B (Addendum 1)" registered through the new route — confirmed live that Revision A's file/name/content are untouched, the Requirement still cites Revision A (never auto-migrated to B), both files remain distinct and inspectable, and the UI's own eligible-sources list correctly excludes the now-superseded Revision A. Nine new regression tests (`tests/test_comm_i4a_source_revision_generalization.py`) plus the one pre-existing drawing-revision test re-confirmed unaffected. Full suite caught one real gap on first run (two new `data-ui-ref` values missing their `UI_REFERENCE_MAP.md` rows — fixed) then passed clean: **2978 passed, 0 failed, 65 subtests** (30m09s). **Developmental classification, preserving both the original finding and this correction as distinct dated facts:** Late Correction, not Timely — the gap was self-disclosed by MM2's own STATUS.md row at ship time and remained open through COMM-I4 before being fixed here. **Agent reassessment offered, no `RequirementAdjudication` persisted on the Product Owner's behalf:** OPR-2.5 — Satisfied, per COMM-I3B's own standing human-authority rule. See `current/comm-i4a-opr-2-5-revision-continuity-correction.md` for the full record. **Deliberately NOT done this stage:** any `RequirementAdjudication`; commissioning the remaining 22 Requirements or reopening the seven confirmed COMM-I4 Requirements; modifying the adopted OPR; Bug Eye, FPR-12, FPR-13; whole-project Archive/Restore; login/gateway redesign; a new Punch List architecture; general Source-model refactoring beyond what OPR-2.5 required; a dedicated text-entry revision form for text_records (file upload already covers it, honestly reported as a minor UI-parity gap, not a capability gap). |
| Governed Workflows and AI Assistance Commissioning (CLAUDE-POSTCAMEL-COMM-I5) — agent commissioning assessment of fourteen Current Baseline Requirements spanning workspace navigation (3.x), governed work products (4.x), and AI assistance (5.1-5.3) | **ASSESSED, PRODUCT OWNER REVIEW REQUIRED** — authorized following the Product Owner's confirmation of OPR-2.5 (`6120042`). **No new `RequirementAdjudication` was persisted for any of the fourteen**, per COMM-I3B's standing human-authority rule. Thirteen assessed Satisfied: OPR-3.1 (Listings) and OPR-4.3 (Compliance Rollup) both classified **Timely Correction** — each was a real gap a dedicated audit found (ROOT-A1, ROOT-A2 respectively), closed in the very next authorized stage (ROOT-I1, ROOT-I2) within one continuous programme; OPR-3.2/3.3/3.7/4.1/4.2/4.4/4.5/4.6/5.1/5.2 all Early-and-Sound Conception. OPR-3.6 (Persistence) Satisfied with an honestly-noted nuance: project data is server-side and durable (live-reconfirmed via a real cross-project switch), but Display Layout/Appearance/panel-visibility preferences are `localStorage`-scoped (device/browser-bound), not account-bound — a design characteristic, not a clear deviation. **OPR-5.1/5.2 (AI reasoning/evidence grounding) freshly live-verified, not only cited from code:** a real question against the commissioning project returned a correct, grounded answer with an expandable "Source grounding" citation matching the real governed OPR-3.5 Requirement verbatim; a control question naming a genuine, different, pre-existing project in the same deployment ("Nipigon Ramp") produced zero evidence of cross-project awareness — the two forms of isolation evidence (COMM-I4's structural code-shape finding and this stage's live behavioral proof) now both exist and agree. **OPR-4.6 (RFI Generation) verified structurally, not assumed:** `create_rfi_draft` cannot be called without a `finding_id` whose Finding already has a `ReviewerValidation` on record, and every RFI-mutation route calls `_require_capability(workspace, "rfi_originate", ...)` server-side — grounding and operating-environment directionality are both enforced by construction. **OPR-4.5 (Findings & Decisions) found to already solve, for `Finding`, exactly the problem OPR-5.3 has not solved for `RequirementAdjudication`:** machine provenance lives on `AnalysisRun` (`engine_name`/`engine_version`), human judgment lives separately on `ReviewerValidation`/`Disposition` (`reviewer` field) — "full attribution" genuinely present, correctly distributed across three objects rather than collapsed onto one. **OPR-5.3 (Human Authority) is this tranche's one genuine finding: Partially Satisfied, mixed developmental classification.** `WorkProduct` (MM8's `content_class`/`accepted_by` enforcement), `Claim` (`propose_ai_assisted_claim` not route-wired — no live AI-write path), and candidate-Requirement promotion (never auto-promoted) are each Early-and-Sound. `RequirementAdjudication` is **Unresolved Developmental Deficiency**: its own class docstring calls it "the first-class record of **a human's** answer," but its field set (`id`/`project_id`/`requirement_id`/`outcome`/`adjudicator`/`adjudicated_at`/`reasoning`/evidence lists) has no content-provenance field at all — confirmed by direct inspection, not assumed. Named honestly, per this stage's own explicit instruction: nothing in the product would look any different had the four real `RequirementAdjudication` records this commissioning sequence created (COMM-I3, legitimately authorized) been misrepresented as personal human judgments instead of agent assessments — only this session's own external procedural honesty prevented that, never a product-level control; no account in this system carries any human-vs-agent operator distinction at all. **Candidate smallest bounded correction named, explicitly NOT implemented this stage:** a `content_class`-style field on `RequirementAdjudication` mirroring `WorkProduct`'s own already-proven vocabulary — the same shape of fix COMM-I4→COMM-I4A already proved out once for `register_source_revision`, requiring its own Product Owner authorization before any code changes, exactly the two-step pattern OPR-2.5 already went through. See `current/comm-i5-governed-workflows-and-ai-assistance-commissioning.md` for the full record, per-Requirement developmental findings, RFP-translation questions, and Product Owner decision table. **No application code was modified this stage** — investigation and live verification only; full suite not re-run (nothing it covers changed). **Deliberately NOT done this stage:** any `RequirementAdjudication`; commissioning OPR-6.x/OPR-7.x; reopening OPR-1.x/OPR-2.x beyond citing them as evidence; implementing the named `RequirementAdjudication` provenance field; Bug Eye, FPR-12, FPR-13; login/gateway redesign; `PunchListItem`. |
| OPR-5.3 Human Authority Provenance Corrective Tranche (CLAUDE-POSTCAMEL-COMM-I5A) — the second COMM-stage to change application code, after COMM-I4A | **CORRECTED, PRODUCT OWNER CONFIRMED SATISFIED** (confirmed 2026-08-09, developmental classification Late Correction unchanged) — authorized by the Product Owner's explicit Do-Not-Accept decision on COMM-I5's OPR-5.3 finding (`bb6fb5f`). Found, before writing any fix, that the gap was worse than silent absence: `record_requirement_adjudication`'s own governance-log call hardcoded `role="human"` **unconditionally** for every adjudication ever recorded — an affirmatively false claim for every one of the four real COMM-I3 agent-entered adjudications, not merely an ambiguous one. Audited the three existing provenance patterns COMM-I5 named (`Finding`/`AnalysisRun` machine-origin split, `ReviewerValidation`/`Disposition` human attribution, `WorkProduct.content_class`) and extended the closest, directly-reusable one: `RequirementAdjudication` gains `attribution: Optional[str]` (`human_reviewed`/`agent_assessment`, `KNOWN_ADJUDICATION_ATTRIBUTIONS`), optional at the store layer (six pre-existing direct callers stay backward-compatible) but **mandatory and explicit at the real product route** — `adjudicate_requirement` now rejects any submission missing `attribution` or supplying a value outside the two real closed choices, with no silent default to either. The false `role="human"` hardcode is replaced with an honest derivation from the real declared value. **Audited and explicitly rejected** literally reusing the existing Approval Gate (`_require_approval`) for this: every prior use of that gate commits already-persisted content with no new form data, and its own confirm-page round-trip carries only `confirm=once/session/no` — reusing it here would have silently dropped the outcome/reasoning text on the most common case (a first-time-per-session human adjudication), a genuine new regression this codebase's own design has always avoided. Built instead: a mandatory, explicit, first-person self-attestation in the same request as the outcome/reasoning, honestly reported as **not** a cryptographic proof of humanity (no such mechanism exists in today's account model, and building one is explicitly out of scope) but a real, non-trivial, honestly-logged declaration ending the prior silent/false default. **The four historical COMM-I3 records were never mutated** — per ADR-032-R06's append-only principle, applied here to the new field too — instead resolved truthfully by reference via a small named constant (`LEGACY_AGENT_ATTRIBUTED_ADJUDICATION_IDS`, the exact four real ids) and a read-time-only resolver (`resolve_requirement_adjudication_attribution`), never a second truth store. **Live-verified against the real commissioning specimen after a full app restart:** all four historical records now render **"Satisfied by archiosk_commissioning AGENT ASSESSMENT — [original reasoning, unchanged]"**, directly confirming they can no longer be mistaken for personal human judgments; every un-adjudicated Requirement's Adjudicate form shows the two required, unchecked attribution radios. **Real regressions found and fixed across two full-suite passes** (fifteen pre-existing tests across nine files POSTing to the real route without the new mandatory field, plus one asserting the old false `role="human"` value) — each updated to reflect the corrected, honest behavior, not reverted. Full suite, run genuinely fresh: **2989 passed, 0 failed, 65 subtests** (19m37s). **Developmental classification: Late Correction** — the gap existed since Foundation Batch K's own original implementation, exposed in real operation by COMM-I3, formally identified against OPR-5.3 by COMM-I5, corrected only here. **Agent reassessment offered, no `RequirementAdjudication` persisted on the Product Owner's behalf:** OPR-5.3 — Satisfied. See `current/comm-i5a-opr-5-3-human-authority-provenance-correction.md` for the full record. **Deliberately NOT done this stage:** any `RequirementAdjudication`; commissioning OPR-6.x/OPR-7.x; broad authentication/account/tenancy redesign; a new identity provider or agent-account taxonomy; Bug Eye, FPR-12, FPR-13; login/gateway redesign; `PunchListItem`; reopening OPR-2.5; modifying `Disposition`/`ReviewerValidation` or any other governed object merely for symmetry. |
| Final Current-Baseline Commissioning Tranche (CLAUDE-POSTCAMEL-COMM-I6) — agent commissioning assessment of the final eight Current Baseline Requirements (OPR-6.1–6.3, OPR-7.1–7.5), completing agent-level assessment of all 34 adopted Requirements | **COMPLETE, PRODUCT OWNER COMPLETION REVIEW REQUIRED** — authorized following the Product Owner's confirmation of OPR-5.3 (`0b82c86`). **No new `RequirementAdjudication` was persisted for any of the eight**, per COMM-I3B's standing human-authority rule. All eight assessed Satisfied. OPR-6.1 (Human vs AI): a real 3-way `DerivedObservation.author_type` vocabulary (`human`/`deterministic_process`/`ai`) present since MM1's own original design — the fourth instance of an OPR clause naming a capability ("vector" understanding) the product never built, harmlessly. OPR-6.2 (Cross-document intelligence): MM6's endpoint validation and falsification-tested cross-project guard confirmed directly — but special scrutiny found and named a real dormant finding: the older, general `record_relationship` primitive `record_evidence_relationship` wraps has no endpoint or cross-project guard of its own, confirmed unused directly by any route today, recorded as a Latent-Regression/Dormant-Risk candidate, not fixed. OPR-6.3 (Structured export): confirmed directly that export has no lifecycle-status gate (correctly keeping Export separate from Issue), and that both export renderers (`WorkProduct`, RFI) embed an honest DRAFT/ISSUED/ANSWERED status banner directly into the exported file — real traceability surviving the file leaving governed state; "Distribute" is absent as a governed concept, honestly noted as never promised by the adopted text, not a gap. **OPR-7.1/7.2 explicitly distinguish three testing tiers, per this stage's own instruction:** software regression testing (2989 tests), developmental commissioning (this whole COMM-A1→COMM-I6 sequence), and independent final commissioning (**not yet performed**, reserved for a future, genuinely independent authority distinct from this Builder). **OPR-7.2 carries an honest new methodological caveat**: every recorded Zero-Founder walkthrough (POSTCAMEL-P01 and earlier) was performed by this same Builder simulating a first-time user via the browser tool, never by a genuinely independent human tester — the workflow-completability claim is real and evidenced, the "representative human" claim is not, and this is exactly the gap the future Independent Commissioning Authority arrangement exists to close. **OPR-7.3 (Substantial Completion) is assessed as "the mechanism for determining completion exists and works" — explicitly not a declaration that completion has been reached; that decision remains the Product Owner's alone.** OPR-7.4/7.5 re-confirmed unchanged, with OPR-7.5 directly demonstrated (not merely theoretical) by OPR-7.4's own real, quoted Product Owner residual-acceptance chain. **Whole-baseline summary: all 34 Requirements now carry an agent assessment of Satisfied** (32 already Product-Owner-confirmed across COMM-I3 through COMM-I5A, the final 8 awaiting review here); zero unresolved deficiencies remain (both ever found — OPR-2.5, OPR-5.3 — were corrected and confirmed); one standing Product Owner Accepted Residual (OPR-7.4's assembled view). **Semantic-integrity observations recorded, none corrected:** `GovernanceLog.role` now means different things across event types (a naming debt this session's own COMM-I5A change introduced); "Adjudication"/"Compliance" carry different meanings internally versus in external AEC/legal usage. **Future-Prompt Watch recorded six items** (two Latent-Regression/Dormant-Risk candidates, two Back-Burner items, two Existing-Future-Programme cross-references) — see the full record for the table; none implemented. See `current/comm-i6-final-current-baseline-commissioning-tranche.md` for the complete record, the full Product Owner decision table, and the whole-baseline/developmental summaries. **No application code was modified this stage**; full suite not re-run (nothing it covers changed). **Deliberately NOT done this stage:** any `RequirementAdjudication`; declaring Substantial or Final Completion; preparing or beginning independent commissioning; modifying the adopted OPR; Bug Eye, Semantic Integrity architecture, Sovereign AI architecture, FPR-12, FPR-13; a latent-regression archaeology programme; login/gateway redesign; Surface Trust; new multimodal capabilities; `PunchListItem`. |
| Repository migration/consolidation | **FROZEN** — both repos remain physically separate |

If any future session is tempted to build something from `specified-unbuilt/` or `deferred-reserved/` without a fresh, explicit authorization, that is a mistake this document exists to prevent.

---

## Where to go next

- The rule that never changes: `constitutional-invariants.md`
- What's real in code today: `current/kernel-object-model.md`
- What's designed but not built: `specified-unbuilt/*.md`
- What's acknowledged but not designed: `deferred-reserved/reservations.md`
- How this baseline itself may change: `governance-of-governance/amendment-and-ratification.md`
- What exists in the historic Explorer corpus and where it maps: `history-mapping.md`
