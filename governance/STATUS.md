# BEEHIVE Governance Baseline — STATUS

**This is the mandatory first read for any future session, human or AI, working on ARCHIOSK/BEEHIVE.**

**Placement notice:** this baseline was authored in a staging area and, on explicit authorization, committed here at `governance/` in this repository (`C:\Archiosk\Research\archiosk`, the backend repo) as a documentation-only commit. This is its interim live home, not necessarily its final one — the ratified target architecture envisions a single consolidated repository (`backend/` + `explorer/` + `governance/`), and repository consolidation itself remains separately unauthorized and frozen (see the authorization table below). Nothing in `C:\Archiosk\App\archiosk-explorer` was touched, moved, or merged to produce this placement — its historic governance corpus remains exactly where it is; see `history-mapping.md` for the full inventory and mapping.

---

## What BEEHIVE is

BEEHIVE is a governed project-development intelligence environment that accompanies a construction/infrastructure project through its lifecycle — from earliest conception through procurement, contractual commitment, design, construction, and, where the delivery model requires it, operations and handback — while preserving provenance, authority, temporal state, project isolation, and human decision rights at every stage. It is not inherently an Owner application or a Proponent application.

A particular project's governed environment emerges from: **BEEHIVE Constitutional Kernel + Perspective + Contract/Delivery DNA + Project-Specific Governed Sources.**

**BEEHIVE is a user-facing project application, not a governance-document interface.** An ordinary authorized user creates a project, works in it, saves, and reopens it later — conceptually comparable to how modern AI applications let a user create and reopen a persistent Project — with all of the governance/provenance/authority/lifecycle machinery described in this baseline operating underneath that experience, never as something a user must read, understand, or manually manipulate to do ordinary project work. See `specified-unbuilt/perspective-and-contract-dna.md` for the Create-Project/Pursuit UX flow this baseline already supports.

Everything actually implemented today is a strict, uncontradicted subset of this thesis. Nothing built needs to be reworked as the remaining pieces are designed and built. See `constitutional-invariants.md` for the rules that never change, and `current/kernel-object-model.md` for what exists in code today.

---

## What's actually implemented (real, tested, running code)

`services/case_workspace.py` (backend repo), 10 Foundation Batches plus the `promote_requirement_item()` bridge, Case-visibility (Private/Shared), the collaboration-threshold/retraction tranche, the Archive tranche, the Case-lineage/Derive tranche, the Selective Adopt/Carry-Forward tranche, the Camel MM1 Multimodal Foundation and Evidence Contract (`StructuralUnit`/`AddressableRegion`/`EvidenceItem`/`DerivedObservation`), the Camel MM2 PDF and Document Intelligence stage (`register_pdf_page_structure`, `services/pdf_intelligence.py`), the Camel MM3 Spreadsheet and Structured-Data Intelligence stage (`register_spreadsheet_structure`, `services/spreadsheet_intelligence.py`), the Camel MM4 Drawing Intelligence and Orientation-Normalized Comparison stage (`register_drawing_sheet_structure`, `create_addressable_drawing_region`, `build_evidence_sachet`, `services/drawing_intelligence.py`), and the Camel MM5 Image, Screenshot, and Camera Evidence stage (`create_addressable_marker_region`, `services/image_intelligence.py`, the real governed Eye pane), 324 passing `unittest` tests as of the tranche this sentence was last updated for (see `current/kernel-object-model.md` for the current real count). Full detail and code anchors: `current/kernel-object-model.md`. In one sentence: `Source`, `Requirement`, `Finding`, `Relationship`, `Case`, `ReviewerValidation`, `Disposition`, `RequirementAdjudication`, `Snapshot`, `Supersession`, `TemporalObligation`, `AnalysisRun`/`AnalysisTrigger`, `GovernanceLog`, the `promote_requirement_item()` bridge, Case visibility (`CASE_VISIBILITY_PRIVATE`/`CASE_VISIBILITY_SHARED`, `visible_cases_for`, `share_case`), the Collaborative state/threshold (`CASE_VISIBILITY_COLLABORATIVE`, `retract_case_to_private`), terminal Case Archive (`CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED`, `archive_case`, the centralized `_require_case_not_archived` frozen-state guard), Derive (`derive_case_from_archive`, `derived_cases_of`, `RELATIONSHIP_TYPE_DERIVED_FROM`), and Selective Adopt/Carry-Forward for Finding + ReviewMessage (`adopt_finding_into_case`, `adopt_review_message_into_case`, `carried_forward_adoptions_for_case`, the new `CarriedForwardAdoption` primitive) all exist, are tested, and are the ground truth for everything else in this baseline. `RequirementAdjudication`, the promotion bridge, and the full Private→Shared→Collaborative→Archived→Derived→Adopted Case lifecycle are also now reachable through minimal route wiring in `routes/workspace.py` (`promote_requirement_item_route`, `adjudicate_requirement`, `share_case`, `retract_case`, `archive_case`, `derive_case`, `adopt_finding`, `adopt_review_message`).

## What's specified but unbuilt (fully designed, zero code)

Scenario/Scenario-Delta, `ViabilityAssessment`, `Template`, `ReferenceStandard`, Contract/Delivery DNA, Perspective (application layer), the graduated Contract Commitment event sequence, authoritative project metamorphosis (composes from existing primitives, no new object), Owner/Proponent publication (Published Procurement Instrument), the Neutral/Adjudication Workspace, the Experience Corpus (all forms), Project Security Policy (as governed `Requirement` content), the remainder of the investigation lifecycle extensions **beyond Case visibility/collaboration/Archive/Derive/Adopt(Finding+ReviewMessage) itself** — `CaseLock`, general Copy/Adopt (carry-forward of any object type beyond Finding/ReviewMessage), automatic predecessor-issue applicability analysis, publication anchoring — Deficiency/completion-resolution, dormancy/restart. Full detail in `specified-unbuilt/`. (Case visibility — Private/Shared/Collaborative, the explicit Share transition, the collaboration threshold, pre-collaboration Shared→Private retraction, terminal Archive from any visibility state, Derive (Archive → new active Case with minimal working context), and Selective Adopt/Carry-Forward for Finding + ReviewMessage — is now implemented; see the authorization table below and `current/kernel-object-model.md`.)

**The Camel Multimodal Programme (MM1–MM9)** — recorded `CLAUDE-P40-VW9A` (2026-08-05), closing a continuity gap: this programme was maintained outside the repository and could not be located during the CLAUDE-P40-VW9 final report. Multimodal foundation/evidence contract, PDF, spreadsheet, drawing, image/camera intelligence, cross-document/cross-modal relationships, governed multimodal analytics, routine in-app creation/editing, and consolidated end-to-end validation — plus the cross-cutting Design-Manager-as-expert-integrator requirement (canonical case: a Progressive Design-Build Excel risk-register/Monte Carlo workflow). **MM1 (Multimodal Foundation and Evidence Contract) is now IMPLEMENTED** — `CLAUDE-MM1` (2026-08-05), authorized following the accepted cockpit gate (`CLAUDE-CGP-02`); see the authorization table below and `current/kernel-object-model.md`. **MM2 (PDF and Document Intelligence) is now IMPLEMENTED, bounded** — `CLAUDE-MM2` (2026-08-05), authorized following the accepted MM1 seal; text-native/image-only/mixed PDF classification, page-level structure, and paragraph-level evidence/citation, deliberately not OCR, PDF rendering, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM3 (Spreadsheet and Structured-Data Intelligence) is now IMPLEMENTED, bounded** — `CLAUDE-MM3` (2026-08-05), authorized following the accepted MM2 seal; worksheet/row structure, formula-preserving evidence, and one bounded single-cell edit with a real, live-verified export/reopen round trip, deliberately not Monte Carlo, a spreadsheet grid UI, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM4 (Drawing Intelligence and Orientation-Normalized Comparison) is now IMPLEMENTED, bounded** — `CLAUDE-MM4` (2026-08-06), authorized following the accepted MM3 seal; drawing-sheet structure, on-demand rectangular regions, reversible mirror/rotate view transforms with exact original-coordinate round-tripping, real independent-Display comparison (two image drawings), and the Governed Evidence Sachet, deliberately not native CAD/BIM parsing, automatic symbol/dimension recognition, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM5 (Image, Screenshot, and Camera Evidence) is now IMPLEMENTED, bounded** — `CLAUDE-MM5` (2026-08-06), authorized following the accepted MM4 seal; a real governed Eye pane (paste/drop/preview/rotate/mirror/save), `unit_type="image"` structural units reusing MM4's own region/citation machinery, point-marker annotations, and EXIF-free derivative crop export with a GPS-value-never-extracted privacy boundary, deliberately not a photo editor, automatic object/defect/facial recognition, or any other later-modality engine; see the authorization table below and `current/kernel-object-model.md`. **MM6–MM9 remain NOT AUTHORIZED** for implementation — still a staged intent record for those stages, not a build order. Full detail: `specified-unbuilt/camel-multimodal-programme.md`.

**External Intelligence Airlock and Constructive Boundary Response** — recorded `CLAUDE-CGP-02` (2026-08-05) as a planning-level record ahead of the Camel programme's own MM7 (governed analytics) and any future outbound-AI capability: a controlled outbound/inbound vestibule for questions requiring public or external knowledge (minimum-necessary mission packets, allow-listing over after-the-fact redaction, disposable aliases/expiry, untrusted-inbound handling, prompt-injection detection on returned content, no silent AI-to-authoritative promotion of external evidence) and a Constructive Boundary Response pattern for legitimate users reaching a protected boundary (redirect without disclosing internal security detail, log internally, escalate only for repeated/hostile probing). Composes with, and does not duplicate, the existing `ACTION_EXTERNAL_AI_REQUEST` gate (`specified-unbuilt/organizational-security-department.md`) and `services/project_access.py`'s existing non-disclosure precedent. **NOT AUTHORIZED** for implementation — a planning-level intent record, not a build order. Full detail: `specified-unbuilt/external-intelligence-airlock.md`.

## What's deliberately deferred (acknowledged, not designed)

Machine Model / Installed Asset Identity; infrastructure/tenant hosting isolation; recurring `TemporalObligation` implementation; Experience Corpus partition mechanics; multi-decade storage strategy; template-library staleness governance; detailed workspace-module/application-surface structure; a future governed legal/dispute context (formal termination, default, insolvency, negotiated termination, court/arbitral intervention). Full list: `deferred-reserved/reservations.md`.

---

## Implementation authorization status — verbatim, do not silently change

| Item | Status |
|---|---|
| Architecture (the nine-round review this baseline documents) | **RATIFIED** |
| Application implementation, broadly | **STILL FROZEN** |
| Foundation Batch K (`RequirementAdjudication`) | Already implemented in code; **further feature expansion NOT AUTHORIZED** |
| `promote_requirement_item()` design | READY (superseded by implementation below) |
| `promote_requirement_item()` implementation | **IMPLEMENTED** — `services/case_workspace.py` (`CaseWorkspaceStore.promote_requirement_item`), 14 tests in `tests/test_requirement_promotion.py::PromoteRequirementItemTests`. Further feature expansion beyond this narrow tranche remains **NOT AUTHORIZED**. |
| `RequirementAdjudication` route wiring (`routes/workspace.py`) | **IMPLEMENTED** — `promote_requirement_item_route` and `adjudicate_requirement`, 5 tests in `tests/test_requirement_promotion.py::RequirementRouteWiringTests`. No broader UI/route architecture authorized beyond these two routes. |
| Case visibility — PRIVATE / SHARED / COLLABORATIVE states | **IMPLEMENTED** — `CaseRecord.visibility`/`created_by`/`shared_by`/`shared_at`/`collaboration_established_by`/`collaboration_established_at`/`collaboration_contribution_type`/`collaboration_contribution_id`/`retracted_by`/`retracted_at`, `CaseWorkspaceStore.visible_cases_for` (the real enforcement point — every case listing/switching/default-selection query in `routes/workspace.py` goes through it), plus indirect-identifier guards on `artifact_image` and `preview_finding_id`. 50 tests total across `tests/test_case_privacy.py` (24) and `tests/test_case_collaboration.py` (26). |
| Explicit Case PRIVATE → SHARED transition | **IMPLEMENTED** — `CaseWorkspaceStore.share_case` (owner-only authority), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/share`. |
| Collaboration threshold (SHARED → COLLABORATIVE) | **IMPLEMENTED** — `CaseWorkspaceStore._cross_collaboration_threshold_if_qualifying`, invoked from the six qualifying write paths (`record_reviewer_validation`, `record_disposition`, `add_review_message` [human origin only], `request_attention`, `confirm_relationship`, `record_analysis` [`ANALYSIS_TRIGGER_USER_INITIATED` only]) atomically alongside each write's own `save()`. `Activity` and `RequirementAdjudication` are deliberately excluded, reasoned and documented in code, not silently omitted — see `current/kernel-object-model.md`. |
| Pre-collaboration Case SHARED → PRIVATE retraction | **IMPLEMENTED** — `CaseWorkspaceStore.retract_case_to_private` (owner-only, rejects outright once Collaborative), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/retract`. |
| Post-collaboration privacy-reversion prohibition (Constitutional Invariant 12) | **IMPLEMENTED** — enforced at the single validation gate in `retract_case_to_private`; no other method mutates `visibility`, so there is exactly one place irreversibility could fail, and it is tested directly. |
| Case Archive — terminal/frozen lifecycle status | **IMPLEMENTED** — `CaseRecord.status` (`CASE_STATUS_OPEN`/`CASE_STATUS_ARCHIVED`), confirmed as a **separate axis from `visibility`, not a fourth visibility value** — see `current/kernel-object-model.md`'s "Case Archive" entry for the two-axis rationale. |
| Authorized Archive transition (owner-or-admin) | **IMPLEMENTED** — `CaseWorkspaceStore.archive_case`, permitted uniformly from PRIVATE/SHARED/COLLABORATIVE, atomic single-`save()`, route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/archive`. Machine agents cannot perform this — no automated/triggered call site exists anywhere in the codebase. |
| Archived-Case mutation prohibition (centralized frozen-state enforcement) | **IMPLEMENTED** — `CaseWorkspaceStore._require_case_not_archived`, called at the top of every identified Case-mutating write path (content and visibility-transition alike), not scattered per-route checks. Unresolved comments/Findings are explicitly **not** required to be resolved before archiving — archive preserves the governed record exactly as-is, including unresolved items, rather than forcing resolution or discarding them. 29 tests in `tests/test_case_archive.py`. |
| Archive → Derived Active Case | **IMPLEMENTED** — `CaseWorkspaceStore.derive_case_from_archive`: source must be `CASE_STATUS_ARCHIVED`, owner-or-admin authority (same narrow pattern as `archive_case`), new Case gets a new id, `status=OPEN`, `visibility=PRIVATE` unconditionally (Constitutional Invariant 11 — deriving from a Shared/Collaborative archive never silently publishes the new Case), route wiring at `POST /projects/<project_id>/workspace/cases/<case_id>/derive`. Not Supersession, not Copy/Adopt. |
| Immutable derivation lineage | **IMPLEMENTED** — `CaseRecord.derived_from_case_id` (denormalized forward pointer) plus an authoritative `Relationship` (`relationship_type=RELATIONSHIP_TYPE_DERIVED_FROM`, `provisional=False`), structurally distinct from `Supersession`. Forward lookup (derived→archive) and reverse lookup (archive→every Case derived from it, via `CaseWorkspaceStore.derived_cases_of`) both implemented and tested. The archived source is never mutated by derivation. |
| Clean working-surface semantics | **IMPLEMENTED** — only `title`/`objective` (overridable) and `source_ids` (a reference-list copy, never the underlying `Source` documents) carry onto the derived Case; all historical contributions (comments, Findings, `ReviewerValidation`, `Disposition`, `Attention`, analysis records, collaboration/archive events) remain exclusively attached to the archived Case, never cloned. 29 tests in `tests/test_case_derivation.py`. |
| Selective Adopt/Carry-Forward — Finding only | **IMPLEMENTED** — `CaseWorkspaceStore.adopt_finding_into_case`: source Finding's Case must be archived, target Case must be that Case's own recorded derivation, owner-or-admin authority on the TARGET Case. Creates a genuinely new `Finding` (`claim_status=FINDING_STATUS_PROVISIONAL` — never authoritative merely by adoption; renewed `ReviewerValidation`/`Disposition` still required) plus an honestly-labeled accompanying `AnalysisRun`, atomic single-`save()`. General carry-forward of any other object type remains **NOT AUTHORIZED**. |
| Selective Adopt/Carry-Forward — ReviewMessage/comment only | **IMPLEMENTED** — `CaseWorkspaceStore.adopt_review_message_into_case`: same source/target/authority rule as above. Creates a new `ReviewThread`+`ReviewMessage` in the target Case authored by the **adopting** actor, never the original commenter (original message/thread untouched, unresolved comments carry forward exactly as archived); original voice preserved via a verbatim quote plus `related_object_type`/`related_object_id`, not by misattributing authorship. Atomic single-`save()`. |
| `CarriedForwardAdoption` lineage | **IMPLEMENTED** — `workspace.carried_forward_adoptions`, `CaseWorkspaceStore.carried_forward_adoptions_for_case`: one uniform, directly-filterable governed record per adopted item (source Case, target Case, object type, source object id, successor object id, adopting actor, timestamp) — deliberately not folded into the `Relationship` substrate; see `current/kernel-object-model.md` for why. 31 tests in `tests/test_case_adoption.py`. |
| `CaseLock`, general Copy/Adopt (carry-forward beyond Finding/ReviewMessage), automatic predecessor-issue applicability analysis, publication anchoring | **NOT AUTHORIZED** — specified only; explicitly *not* implied by the rows above. `Disposition`, `ReviewerValidation`, `RequirementAdjudication`, `AnalysisRun` (as a directly-adoptable object), and `Attention` are explicitly excluded from carry-forward, each reasoned in `specified-unbuilt/investigation-lifecycle-extensions.md`. See that document for full detail. |
| Scenario, ViabilityAssessment, Perspective, Contract DNA, Published Procurement Instrument, Neutral Workspace, Experience Corpus | **NOT AUTHORIZED** — specified only |
| Project Operating Environment (CLAUDE-P29) — locked Client/Owner vs. Design-Builder/Proponent classification | **IMPLEMENTED, bounded** — `ProjectWorkspace.operating_environment`, `CaseWorkspaceStore.set_operating_environment` (single write gate, `OperatingEnvironmentAlreadySetError` on any second call), `services/environment_capabilities.py`. **This is a distinct, narrower authorization from the still-`NOT AUTHORIZED` Perspective/Contract-DNA row above — do not read this row as reopening that one.** Perspective (per `specified-unbuilt/perspective-and-contract-dna.md`) is a mutable, per-reviewer UI lens over a shared project; Operating Environment is an immutable, project-wide, creation-time classification that gates which `Participant.role_type` values are selectable. Required at creation for every new project (`services/ingestion.py`'s `ingest_upload`, no default); a pre-P29 project loads `operating_environment=None` and may be classified exactly once via the admin-only `classify_operating_environment` route — never inferred, never backfilled automatically. Further feature expansion (additional environment-gated behavior beyond participant-role selection, additional environment values beyond the two above) remains **NOT AUTHORIZED** by this row and needs its own fresh authorization. 29 tests in `tests/test_operating_environment.py`. See `current/kernel-object-model.md`'s "Project Operating Environment" entry for full detail. |
| Environment Capability Architecture (CLAUDE-P30) — capability grammar, RFI/clarification directionality, Go/No-Go decision family | **IMPLEMENTED, bounded** — `services/environment_capabilities.py`'s `CAPABILITY_REGISTRY`/`capability_availability`/`capability_denial_reason` (the single resolution point routes/templates/exports call into, explicitly not a plugin framework); `rfi_originate` (Design-Builder/Proponent only)/`rfi_respond` (Client/Owner only, new `RFIDraft.response_text`/`RFI_STATUS_ANSWERED`) as a `CAPABILITY_COUNTERPART` pair; `go_no_go` as a `CAPABILITY_PARALLEL` pair (new `GoNoGoAssessment` primitive, one shared record shape validated against `CLIENT_OWNER_DECISION_STAGES`/`DESIGN_BUILDER_PROPONENT_DECISION_STAGES` — genuinely different vocabularies, no fallback for a legacy/unclassified project). Builds on, and does not reopen, the CLAUDE-P29 row above — this is the "natural next step" the P29 row's own "further feature expansion... needs its own fresh authorization" clause anticipated, authorized narrowly to these two representative workflows. **Deliberately not done this stage, remaining NOT AUTHORIZED:** any AI-prompt/`services/bhive_parser.py` environment-awareness (documented as a deliberate deferral in `environment_capabilities.py`'s own module docstring, not an oversight); any `CAPABILITY_CLIENT_ONLY`/`CAPABILITY_PROPONENT_ONLY` registry entries with no counterpart (none were found to be genuinely exclusive on inspection — see the CLAUDE-P30 final report's Part XI critique); any third Project Operating Environment value; multi-organization tenancy; the organizational security-team/policy-ingestion architecture reserved for a separate future stage. 39 tests in `tests/test_capability_architecture.py`. See `current/kernel-object-model.md`'s "Environment Capability Architecture" entry for full detail. |
| Organizational Security and Information Governance (CLAUDE-P31) — mandatory floor, single-deployment security baseline, project security profiles, written-policy ingestion, governed Q&A, centralized action-decision engine, assurance, self-check | **IMPLEMENTED, bounded** — this is an infrastructure/application-layer authorization (like CLAUDE-P27-B's tenancy/CSRF/rate-limiting work), not a domain-model concept, and explicitly distinct from `specified-unbuilt/security-policy.md`'s "Project Security Policy" (a customer's own security requirements as governed `Requirement` content — that document is untouched by this row). `services/security_policy.py`'s `evaluate_action` (floor → baseline → profile → exception, most-restrictive-wins, exception ceiling capped at `DECISION_ALLOW`) is the single resolver; `services/security_governance.py`'s `SecurityGovernanceStore` manages ONE global, deployment-wide record (no multi-organization tenancy exists — see honesty boundary in that module's own docstring). Real enforcement wired at two representative points: `services/ingestion.py` gates `ACTION_EXTERNAL_AI_REQUEST` via `BHiveParser`'s pre-existing `ai_calls_disabled` kill switch (CLAUDE-P27-B) with zero changes inside `bhive_parser.py` itself; `routes/workspace.py` gates `ACTION_EXPORT` on both RFI export routes. `services/learning_governance.py`'s `LearningContributionRequest` is approval-tracking only — no shared-learning/training pipeline exists in this repository to move data into, and this row makes no claim otherwise. **Deliberately NOT built, remaining NOT AUTHORIZED:** AI-assisted policy-clause extraction (statements/proposals are human-entered only — no new prompt was written against `bhive_parser.py` or anywhere else); real learning-zone data movement; multi-organization tenant isolation; a content-level assurance viewer; cryptographic audit integrity; a dedicated Security Officer role distinct from `admin`. See `specified-unbuilt/organizational-security-department.md` for the full target architecture and `current/kernel-object-model.md`'s matching entry. 80 tests across `tests/test_security_policy_engine.py`, `test_security_governance.py`, `test_learning_governance.py`, `test_security_enforcement.py`, `test_security_assurance.py`. |
| Project-Level Access Control (CLAUDE-P32) — which authenticated accounts may open a given project at all | **IMPLEMENTED, bounded** — closes the P27-review-flagged gap that any authenticated user could open any project by knowing/guessing its `project_id`. `services/project_access.py`'s `can_access_project` (the single centralized decision: owner, explicit `access_allow_list`, or admin — `owner is None` fails CLOSED, admin-only, the deliberate opposite of every other None-means-ungated field in this codebase) is enforced at `routes/workspace.py`'s `_load_workspace_or_404` (all 50 routes, including `export_rfi`, refactored this stage to use it), `routes/api.py`'s `_load_authorized_project_or_404` (6 routes) plus a filtered `GET /documents`, and `routes/portal.py`'s `_accessible_documents`/`_require_project_access_or_404` (`/`, `/projects`, `/search`, `/dashboard/<id>`, `/projects/<id>/delete`) — including `app.py`'s global nav-sidebar context processor, discovered during this stage's own bypass inventory to be leaking project names on every authenticated page (including error pages) regardless of access. Denial is a generic 404 everywhere, identical whether a project doesn't exist or the caller isn't authorized for it. `ProjectWorkspace.owner`/`access_allow_list` (`services/case_workspace.py`) — a new project's `owner` is locked at creation from the real authenticated session identity (`services/ingestion.py`'s `owner` parameter, deliberately never the free-text `actor` governance field); a pre-P32 project is backfilled only by deterministic exact-match against a real `models.User.username` recorded in its own `document_ingested` governance event, else stays unowned (admin-only) rather than guessed. **This is project-level access control within one deployment, not multi-organization tenancy** — `governance/specified-unbuilt/tenancy-and-project-authorization.md` remains unimplemented, and this row makes no isolation claim beyond a single deployment's own accounts; see `services/security_policy.py`'s `SECURITY_CLAIMS_REGISTRY` for the exact claim boundary. **Deliberately NOT built this stage:** separate read/write project-level permissions (the existing admin/read_only role axis is unchanged and still governs in-project actions); any case-workspace decomposition (explicitly deferred, see `current/kernel-object-model.md`'s own note); ownership-transfer UI beyond the existing admin reassignment route. 41 tests in `tests/test_project_access_control.py`, plus fixture updates to two ratified governance test suites (`tests/test_case_privacy.py`, `tests/test_route_authorization_hardening.py`) to grant both their test sessions project access as a new precondition, preserving what each already verified (Case-level privacy) unchanged. See `current/kernel-object-model.md`'s "Project-Level Access Control" entry for full detail. |
| Project-Scoped Conversation Tags and Tasks (CLAUDE-P40-VW7) — a OneNote-style selection toolbar on Project Conversation text, built-in Important/Question/Highlight tags, a small custom-tag color picker, real persisted Tasks, and two new Lists branches (Tasks/Tags) | **IMPLEMENTED, bounded** — an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to exactly this capability. `services/case_workspace.py`'s `ConversationSourceAnchor`/`Tag`/`TagOccurrence`/`Task` dataclasses, `BUILT_IN_TAGS` (Important/Question/Highlight — fixed code-level identities, never stored per-project), and the `create_custom_tag`/`add_tag_occurrence`/`remove_tag_occurrence`/`create_task`/`complete_task`/`reopen_task`/`resolve_conversation_anchor` methods (all additive `ProjectWorkspace` fields — `tags`/`tag_occurrences`/`tasks` — legacy records simply lack the key and load with the empty-list default). Anchoring is a text-quote-selector (scope + case/message/guidance identity + start/end offsets + exact quotation + limited prefix/suffix), not DOM coordinates; `_validate_source_anchor` performs a real existence check against the current workspace before persisting, and `resolve_conversation_anchor` decides "Source unavailable" at read time rather than assuming a record's target still exists. Route authorization is exactly `_load_workspace_or_404` — the same owner/allow-list/admin rule already governing the source conversation, no separate Tag/Task-specific gate. `routes/workspace.py`'s `add_tag_occurrence_route`/`remove_tag_occurrence_route`/`create_task_route` return JSON for live, no-reload Lists updates (this repository's first use of `fetch()`, confirmed architecturally compatible via `tools/dependency_fit.py` before use); `complete_task_route`/`reopen_task_route` stay classic form-POST + redirect. 54 tests in `tests/test_p40vw7_conversation_tags_and_tasks.py`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** cross-project intelligence or any tag/task view spanning more than one project; machine-generated tag/task suggestions; organization-wide task management, user assignment, due dates, notifications, calendar integration, or external task services; permanent inline in-message highlight rendering (Tag/Highlight occurrences are discoverable via the Lists panel and a scroll-and-flash navigation, not a per-character `<mark>` wrap reconciled against the existing `hotlinks` filter — a deliberate, documented scope simplification, not a silent corner-cut). |
| Governed Files Display and Project File Architecture (CLAUDE-P40-VW9) — a Files Display surface with two governed sibling roots (Data Room, Design-Builder Workspace) and a `Folder` domain model for Design-Builder Workspace only | **IMPLEMENTED, bounded** — an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to exactly this capability, following the same pattern CLAUDE-P40-VW7's Tag/Task addition already established. `services/case_workspace.py`'s `FOLDER_ROOT_DATA_ROOM`/`FOLDER_ROOT_DESIGN_BUILDER` (a closed, two-value vocabulary — different governance spaces, not open-world), `Folder` dataclass (`id`/`project_id`/`root`/`name`/`parent_folder_id`/`removed_at`/`removed_by` — canonical identity is `id`, path always derived at read time via `CaseWorkspaceStore._folder_path`, never stored), and `create_folder`/`rename_folder`/`move_folder`/`delete_folder` (all additive `ProjectWorkspace.folders` field — legacy records simply lack the key and load with the empty-list default, identical shape to VW7's own `tags`/`tasks`). Both roots are GOVERNED VIRTUAL roots — neither is a persisted `Folder` row; `create_folder` has no `root` parameter at all and always writes `FOLDER_ROOT_DESIGN_BUILDER`, so there is structurally no route or method that can create a Data Room folder this stage. `routes/workspace.py`'s new `STABLE_DIRECTORY_KINDS["files"]` entry (the second real entry in the CLAUDE-P40-VW8-QA1 extension point) and `/projects/<id>/workspace/folders...` routes — route authorization is exactly `_load_workspace_or_404`, the same rule already governing every other Project-scoped view, no separate Files/folder-specific gate; no owner/admin gate on folder mutations (mirrors `create_task`/`create_custom_tag`'s own precedent — Design-Builder Workspace is collaborative team structure, not owner-locked evidence). 40 tests in `tests/test_p40vw9_files_display_and_folder_architecture.py`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** the final government/client-issued Data Room hierarchy or any invented folder-name standard; bulk/ZIP import; external cloud-drive integration or remote retrieval; assigning existing Documents into a folder (no `folder_id` field exists on `Source`); the full `linked → retrieved → ingested → used → preserved` lifecycle (named/documented as a future vocabulary only — `ingested` is the only state genuinely real today, unchanged by this stage); addenda reconciliation; supersession/version comparison; checksum-based evidence preservation beyond `Source.file_hash`'s own pre-existing, unenforced field; a Files restore-UI (the soft-delete data is recoverable, but no "Removed Folders" surface was built); cross-Project access; global search; P41. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM1 — Multimodal Foundation and Evidence Contract (CLAUDE-MM1) — `StructuralUnit`/`AddressableRegion`/`EvidenceItem`/`DerivedObservation`, reusing `Relationship` for evidence relationships and a read-time citation resolver | **IMPLEMENTED, bounded** — authorized following the accepted cockpit gate (`CLAUDE-CGP-02`, GO recommendation), an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM1 only — MM2–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations (see the Camel Multimodal Programme row above). Deliberately generalizes rather than duplicates `Table`/`TableRow` (Batch J) — those remain the real tabular-specific realization, untouched by this row. `evidence_class` (`KNOWN_EVIDENCE_CLASSES`) and `author_type` (`KNOWN_OBSERVATION_AUTHOR_TYPES`) are the two closed, validated vocabularies; every other new field is additive (`ProjectWorkspace.structural_units`/`addressable_regions`/`evidence_items`/`derived_observations`, four new `Optional` `Source` fields, new `OBJECT_KIND_*`/`RELATIONSHIP_TYPE_*` constants) — legacy records simply lack the new keys and load with the empty-list/`None` defaults, the same backward-compatible shape every prior addition uses. `DerivedObservation` is deliberately NOT `Finding` (which requires a `case_id`) and NOT merged with it — flagged as an open future design-review question, not silently resolved. Retrieval: three read-only routes on the existing `/api/v1` surface (`.../structural-units`, `.../evidence`, `.../citations/<region_id>`) — no new UI built. 28 tests (25 in `tests/test_mm1_evidence_contract.py`, 3 added to `tests/test_api_authentication.py`'s route-auth matrix), including falsified cross-project-guard and broken-citation-anchor cases. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** any real extraction engine (OCR, PDF rendering, spreadsheet parsing, drawing/image intelligence — MM2–MM5); cross-document/cross-modal analysis (MM6/MM7); the Monte Carlo/Design-Manager engine itself (MM3/MM7, though the model is shown compatible); any External Intelligence Airlock connector (planning-level compatibility only); full source-version-aware citation staleness detection; wiring structural-unit/region/evidence extraction into the live ingestion pipeline. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM2 — PDF and Document Intelligence (CLAUDE-MM2) — text-native/image-only/mixed PDF classification, page-level `StructuralUnit`s, paragraph-level `AddressableRegion`/`EvidenceItem`s, and a strengthened citation resolver | **IMPLEMENTED, bounded** — authorized following the accepted MM1 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM2 only — MM3–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. `services/case_workspace.py`'s `register_pdf_page_structure` mirrors `register_table_evidence`'s own already-parsed-input shape (takes `pages: list[str]`, never raw bytes — this module still does not import `bhive_parser`); the actual pypdf read and encrypted/malformed classification live in the new `services/pdf_intelligence.py`, a thin orchestration layer, not a second parser. `resolve_region_citation` gained source-version staleness awareness (a region anchored to a superseded `Source` now resolves `"status": "stale"`, label still preserved) reusing the existing `Source.superseded_by_source_id` pointer — no new version mechanism; this closes part of the MM1 row's own "full source-version-aware citation staleness detection" deferral above, though only via the existing drawings-scoped `register_source_revision`, not a general PDF/document revision path. Re-confirmed, not re-investigated, that OCR and PDF-to-image rendering remain unavailable in this environment (`services/drawing_intake.py`'s own CLAUDE-P40-VW8-QA-R2A audit) — an image-only PDF is classified honestly, never rendered or OCR'd. One new admin-gated write route (`POST /api/v1/documents/<project_id>/sources/<source_id>/pdf-structure`); the existing PDF.js-backed viewer (`static/js/pdf_viewer.js`) was reused as-is for page navigation, real-browser-verified against a real multi-page PDF with zero code changes to it. 48 tests (23 in `tests/test_mm2_pdf_document_intelligence.py`, 2 new tests + 1 new route added to `tests/test_api_authentication.py`'s route-auth matrix), including a deliberate real-PDF-file end-to-end test (a hand-built, valid, minimal PDF — no reportlab/fpdf dependency added) alongside the established mocked-`extract_pdf_pages` pattern for the rest. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** OCR, PDF-to-image rendering, handwritten recognition, advanced table extraction, PDF annotation editing, redaction, digital-signature validation, form filling, embedded-file extraction, a new region-selection UI, full semantic redline/version comparison, and every other MM3–MM9 modality engine. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM3 — Spreadsheet and Structured-Data Intelligence (CLAUDE-MM3) — worksheet/row `StructuralUnit`/`AddressableRegion`/`EvidenceItem`s, workbook classification, and one bounded single-cell edit | **IMPLEMENTED, bounded** — authorized following the accepted MM2 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM3 only — MM4–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **New dependency `openpyxl==3.1.5`** — checked against `tools/dependency_fit.py` (clean PASS) before adding, directly analogous to the already-accepted `pypdf`/`python-docx`; legacy `.xls` deliberately not supported (would need a second dependency). `.xlsx` is an add-to-existing-project Source format only (`routes/workspace.py`'s `ALLOWED_DOCUMENT_EXTENSIONS`), deliberately not a project-creation format (`services/ingestion.py`'s `ALLOWED_UPLOAD_EXTENSIONS` untouched) — avoids forcing a spreadsheet through `BHiveParser`'s fragile requirement-classification pipeline. `services/case_workspace.py`'s `register_spreadsheet_structure` reuses MM1/MM2's own primitives with zero new domain objects (a worksheet is a `StructuralUnit`, a row is an `AddressableRegion` carrying real structured cell data in its own `address` dict); citation rendering needed zero new code (MM1's existing `"cell"/"row"` label branch already covers it). New `services/spreadsheet_intelligence.py`: two-pass openpyxl read distinguishing formula/cached-value/entered-value; content-based macro detection (`xl/vbaProject.bin` inside the zip, regardless of claimed extension); OLE2-magic-number encrypted/legacy detection; decompression-bomb and row/sheet-count bounds; `apply_bounded_cell_edit` refuses formula cells, checks an optional `expected_file_hash` against a real file-level race, backs up the pre-edit original before overwriting, and preserves the edited value's original data type (no leading-zero coercion). "Export a revised workbook" needed zero new code — the pre-existing generic Source-download route already serves the post-edit bytes. Two new admin-gated write routes (`POST .../spreadsheet-structure`, `POST .../spreadsheet-cell`). **Real-browser-verified defect found and fixed during this stage's own verification:** `templates/base.html`'s Add-Documents file input had a stale hardcoded `accept` attribute silently filtering `.xlsx` out of the browser's own FileList before submission even though the server-side check was already correct - fixed. 81 tests (33 in `tests/test_mm3_spreadsheet_intelligence.py` including a content-based macro-detection proof and a falsified concurrency-guard test, 4 new tests + 2 new routes in `tests/test_api_authentication.py`'s route-auth matrix), full end-to-end live verification (real upload, real registration, real edit, real re-download confirming the edit persisted and every formula/untouched cell was byte-for-byte unaffected). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** Monte Carlo simulation, a spreadsheet grid/editor UI, full Excel recalculation, VBA/macro execution, Power Query, pivot-table/chart editing, arbitrary formula authoring, legacy `.xls` support, full workbook-wide semantic comparison, and every other MM4–MM9 modality engine. A permanent risk-record schema was deliberately NOT created — a risk-register row is already representable via the general row-region mechanism, per this stage's own "do not force a permanent risk schema prematurely" instruction. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM4 — Drawing Intelligence and Orientation-Normalized Comparison (CLAUDE-MM4) — drawing-sheet `StructuralUnit`s, on-demand rectangular `AddressableRegion`s, reversible mirror/rotate view transforms, real independent-Display comparison, and the Governed Evidence Sachet | **IMPLEMENTED, bounded** — authorized following the accepted MM3 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM4 only — MM5–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency** — `pypdf` (MM2) and `Pillow` (pre-MM1) already cover PDF/raster reads; Pillow's own built-in `DecompressionBombError` guard is reused for free. `services/case_workspace.py`'s `register_drawing_sheet_structure` mirrors MM2/MM3's own already-parsed-input shape (a drawing sheet is a `StructuralUnit`, `unit_type="sheet"`); unlike MM2/MM3, no region is auto-created — `create_addressable_drawing_region` (`region_type="rectangular"`, bounds-validated, sequential `region_index`) is reviewer-driven, on demand. New `services/drawing_intelligence.py` owns the one genuinely new piece of math this stage needs: `transform_point_to_display`/`transform_point_to_original` (mirror-then-rotate, exact round-trip, deterministically tested for every rotation × mirror combination) — the required visible "Rotated/Mirrored — source unchanged" status, with every region always stored in the sheet's ORIGINAL frame regardless of the reviewer's current view transform. `static/js/pdf_viewer.js` gained mirror-H/mirror-V/reset plus a new persisted "region" tool (reusing its own pre-existing VW7A-QA2 annotation-overlay coordinate machinery); a new `static/js/drawing_image_viewer.js` gives a standalone raster drawing (previously a bare, uncontrolled `<img>`) real zoom/pan/rotate/mirror/region-select for the first time, mounted per-element (not a page singleton) so it works identically in the primary document pane AND inside a comparison Display division — `static/js/case_workspace.js`'s `populateDivision` now mounts it there too, the one deliberate extension to the existing multi-Display mechanism this stage makes (a `source`-kind division is a direct DOM insertion, not an `<iframe>` — confirmed, not assumed, before this change). Governed Evidence Sachet (`CaseWorkspaceStore.build_evidence_sachet`) implemented for the first time — a read-time-assembled, allow-listed evidence packet for one region plus its sheet/siblings/citation, explicitly excluding every other sheet and Source in the project; exposed read-only via `GET .../regions/<region_id>/evidence-sachet`; no external connector wired to it. Title-block metadata (sheet number/title/discipline/revision/scale) is extracted per PAGE by reusing `services/drawing_intake.py`'s own field-pattern vocabulary, each field tagged with an honest reliability tier (directly-extracted/inferred/unavailable) — a standalone image, with no text layer to mine, honestly reports every field unavailable. Scale/measurement is recorded, never authoritative — a visible "measurements are not reliable" warning ships in the drawing-image viewer itself. Three new routes (`POST .../drawing-structure`, `POST .../drawing-regions` both admin-gated; `GET .../regions/<id>/evidence-sachet` not admin-gated, matching the existing citation route's own authority level). 48 tests (42 in `tests/test_mm4_drawing_intelligence.py` including a composition-order falsification, real-pypdf/real-Pillow end-to-end orchestration, and functional API-route tests, 6 new tests + 3 new routes in `tests/test_api_authentication.py`'s route-auth matrix), plus one pre-existing test (`test_p40vw7a_qa2_thumbnails_annotations_layout.py`) updated to its real, narrower invariant now that `pdf_viewer.js` legitimately calls `fetch()`. **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** native DWG/DXF/RVT/IFC parsing, full BIM navigation, automatic symbol/room/dimension recognition, authoritative takeoff, full drawing-overlay registration/clash detection, sophisticated redline comparison, handwritten-markup recognition, a full annotation editor, synchronized pan/zoom/opacity comparison controls, a unified top-menu drawing toolbar, and every other MM5–MM9 modality engine. A drawing-oriented PDF's own comparison-Display behavior is unchanged (still the pre-existing plain `<iframe>`, no Archiosk controls) — an explicit, documented, bounded deferral; the independent-orientation comparison proof itself uses two image drawings, which is real and fully independent. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Camel MM5 — Image, Screenshot, and Camera Evidence (CLAUDE-MM5) — a real governed Eye pane (paste/drop/preview/rotate/mirror/save), `unit_type="image"` structural units, point-marker annotations, and EXIF-free derivative crop export | **IMPLEMENTED, bounded** — authorized following the accepted MM4 seal, an explicit, narrow lift of the "Application implementation, broadly: STILL FROZEN" row above, scoped to MM5 only — MM6–MM9 remain their own separate, still-**NOT AUTHORIZED** authorizations. **No new dependency** — `Pillow` (pre-MM1) already covers PNG/JPEG/WebP decode and EXIF extraction. `register_drawing_sheet_structure` (MM4) gained one backward-compatible `unit_type` parameter (default `"sheet"`, unchanged for every existing MM4 caller); MM5 is the first caller to pass `unit_type="image"`, reusing MM4's own `AddressableRegion`/`EvidenceItem`/citation/evidence-sachet machinery rather than a parallel path. New `services/image_intelligence.py` (deliberately separate from `services/drawing_intelligence.py` — a different metadata vocabulary, EXIF camera facts vs. drawing title-blocks): `register_eye_capture` (content-sniffed format detection, not extension-gated alone — Section 17), `extract_bounded_crop` (a real derivative PNG, EXIF-free by construction, `origin_type=derivative_crop`/`origin_reference=<region_id>`), `create_marker_and_evidence` (`region_type="marker"`, a point annotation, `EVIDENCE_CLASS_USER_ENTERED`). **Privacy boundary, the one genuinely new policy decision this stage makes:** GPS coordinates are DETECTED (`gps_present: true/false`) but their VALUES are never read into memory at all — falsification-tested, not merely asserted. `templates/base.html`'s Eye pane is now the real governed visual-evidence surface Section 7 describes: view-only rotate/mirror/reset on the unsaved preview, an explicit temporary-vs-saved status, and a real "Save to project" action that hands off to MM4's own `drawing_image_viewer.js` (now loaded from `base.html` directly, not only `case_workspace.html`) rather than reimplementing rotate/mirror/region-select/citation a second time. Two new bounded tools added to that shared viewer: a marker tool and an "Export crop" action. Three new routes (`POST .../eye-capture`, `POST .../sources/<id>/markers`, `POST .../sources/<id>/derivative-crop`, all admin-gated). Comparison with an MM4 drawing needed zero new code — an Eye-saved photo is a `Source(kind="drawing")` like any MM4 drawing image, so it already works in a comparison Display division. 74 tests (26 in `tests/test_mm5_image_intelligence.py` including a GPS-value-never-extracted falsification and `unit_type` backward-compatibility proof, 6 new tests + 3 new routes in `tests/test_api_authentication.py`'s route-auth matrix), plus three pre-existing EYE1-era tests updated to their real, MM5-superseded invariants (the "no persist" scope guard narrowed to what MM5 itself still defers; the "never sent anywhere" guard narrowed to "not sent until the reviewer explicitly saves"). **Deliberately NOT built this stage, remaining NOT AUTHORIZED:** a full photo editor, filters/artistic effects, automatic object/defect/facial recognition, OCR over arbitrary images, panorama stitching, a direct mobile application, continuous camera streaming, a full annotation suite beyond the one point-marker type, full overlay registration, external AI/image services, and every other MM6–MM9 modality engine. EXIF `orientation` is captured/reported but not auto-applied to rotate the displayed image — a documented, deliberate limitation. See `current/kernel-object-model.md`'s matching entry for full detail. |
| Repository migration/consolidation | **FROZEN** — both repos remain physically separate |

If any future session is tempted to build something from `specified-unbuilt/` or `deferred-reserved/` without a fresh, explicit authorization, that is a mistake this document exists to prevent.

---

## Where to go next

- The rule that never changes: `constitutional-invariants.md`
- What's real in code today: `current/kernel-object-model.md`
- What's designed but not built: `specified-unbuilt/*.md`
- What's acknowledged but not designed: `deferred-reserved/reservations.md`
- How this baseline itself may change: `governance-of-governance/amendment-and-ratification.md`
- What exists in the historic Explorer corpus and where it maps: `history-mapping.md`
