# MANIFEST

The standalone spatial viewport uses vendored Three.js r160 modules at
`static/js/vendor/three/three.module.js` and `OrbitControls.js`. They are
MIT-licensed, pinned to the upstream r160 release, and documented with
SHA-256 provenance in `static/js/vendor/three/README.md`; runtime imports are
local and have no CDN fallback.

A structured catalog of every tracked file in this repository: what it
is, what it does, and what it connects to. Generated as a navigation
aid — regenerate/update it by hand when files are added, removed, or
significantly repurposed; nothing here is auto-synced from the code.

Scope: tracked application files only (`git ls-files`). `venv/`,
`__pycache__/`, `instance/`, and other git-ignored/generated paths are
excluded — see `.gitignore`. Individual `tests/*.py` files and
`governance/*.md` documents are deliberately not catalogued here —
tests have no separate index (there are 65+ of them; read the test
file itself), and `governance/` is its own system with its own index
(`governance/STATUS.md`, `governance/history-mapping.md`) per
`CLAUDE.md`'s "does not contain" list. Likewise, this manifest doesn't
self-catalog `CLAUDE.md`/`CONTINUATION_CHECKPOINT.md`/its own file —
add `UI_REFERENCE_MAP.md` (CLAUDE-P40-VW7A) to that same group: a
top-level durable record with its own index (every `data-ui-ref` id used
across `templates/base.html`/`case_workspace.html`/`_macros.html`),
not a file-layout entry this document's own rows describe. Same group
again for `TEST_LANES.md` (CLAUDE-TEST-ACCEL-01, new) — a hand-
maintained mapping from "what changed" to "which smaller, file-list-
based test set gives fast feedback before the full 3,594-test suite,"
with measured proof for two worked examples (SPIN-00A, Requirements);
it does not introduce pytest markers or reorganize `tests/` itself, and
the full suite remains the only real pre-commit/pre-deploy gate.
Same group once more for `CROSS_LAYER_VERIFICATION.md`
(CLAUDE-CH-AO-FIREWALL-AUDIT-01, new) — a development convention
recording how an observation made while exercising one part of the
system becomes a diagnosis about another part without becoming
permission to change it, plus the four test purposes mapped onto
`TEST_LANES.md`'s existing assurance tiers; it carries no product
authority, adds no lane, and names no runtime component.

**Known gap, not yet fixed:** this manifest predates the multi-user
`User`/`PasswordResetToken` auth system (`models.py`,
`services/auth.py`'s real shape, `services/password_reset.py`,
`services/email.py`), the entire Case Workspace subsystem
(`routes/workspace.py`, `services/case_workspace.py`,
`templates/case_workspace.html`), and more — several entries below
(marked inline) describe an earlier, superseded state. Treat any
specific behavioral claim below with suspicion and verify against the
actual file before relying on it; a full refresh is real, separate
future work, not attempted here (CLAUDE-P27-D only added entries for
files it introduced and corrected one claim that had become
factually false).

## Quick map

```
wsgi.py / app.py (entrypoints)
        |
        v
   config.py  <---- .env / .env.example
        |
        v
routes/portal.py --+--> services/ingestion.py --+--> services/bhive_parser.py
routes/api.py    ---'                            '--> services/requirements_registry.py
                                                  '--> services/rfi_export.py (consistency flags -> .docx)
                                                  '--> services/governance.py (append-only audit trail, .jsonl per project)
        |
        v
templates/*.html (Jinja, extend base.html)
        |
        v
static/css/main.css, static/js/case_workspace.js

deploy/*  (nginx, gunicorn) — infra wrapping wsgi:app, not imported by Python
```

---

## 1. Application entrypoints

| File | Purpose | Connects to |
|---|---|---|
| `app.py` | Flask application factory (`create_app`). Registers blueprints (`portal_bp`/`api_bp`/`workspace_bp`/`security_bp`), error handlers (404/500/403), and a context processor injecting `current_year`, `static_version`, `authenticated`, `is_admin`, `nav_recent_projects` (the side-rail's project list — CLAUDE-P32: `_nav_recent_projects` now filters to `services.project_access.can_access_project`-accessible projects only, before the display cap, and fails closed [excludes, does not crash] on a workspace file it can't parse), and (CLAUDE-CA1D-CSP-INLINE-SCRIPT-FIX-01) `csp_nonce`. `get_csp_nonce()`/`_register_security_headers` own the app's `Content-Security-Policy` response header (moved here from `deploy/nginx.conf`, which can't vary a header per request): a fresh nonce is minted once per request (`flask.g`) and matched into every template's inline `<script nonce="{{ csp_nonce }}">` tag — nginx's own static CSP header used to silently block every one of them in production with no console error, since it had no `'unsafe-inline'`/nonce for a real, load-bearing set of inline scripts across the app. Local dev entrypoint via `python app.py`. | Imports `config.get_config`; imports `routes.portal.portal_bp`, `routes.api.api_bp`, `routes.workspace.workspace_bp`, `routes.security.security_bp` inside `_register_blueprints`; imports `services.auth.is_authenticated`/`is_admin`, `services.project_access`; renders `templates/errors/error.html`. Unit/integration-tested by `tests/test_ca1d_csp_inline_script_fix_01.py`. |
| `wsgi.py` | Production WSGI entrypoint (`gunicorn -c deploy/gunicorn.conf.py wsgi:app`). Builds the app via `create_app()` and logs a warning (not a crash) if required env vars are missing. | Imports `app.create_app` and `config.BaseConfig`. Consumed by `deploy/gunicorn.service`'s `ExecStart`. |

## 2. Configuration

| File | Purpose | Connects to |
|---|---|---|
| `config.py` | Calls `load_dotenv(BASE_DIR / ".env")` (explicit path so it works regardless of cwd; never overrides a real env var already set) before defining env-driven settings: `BaseConfig`/`DevelopmentConfig`/`ProductionConfig`/`TestingConfig`, `get_config(name)` resolver, `BaseConfig.validate()` (returns missing required env vars). Defines `SECRET_KEY`, `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, `DATABASE_URL`, `REGISTRY_STORE_PATH`, `MAX_CONTENT_LENGTH`, `ALLOWED_UPLOAD_EXTENSIONS`, `STATIC_VERSION`, `AUTH_USERNAME`/`AUTH_PASSWORD_HASH` (unset means the login gate fails closed — see `services/auth.py`), and the session cookie flags (`SESSION_COOKIE_SECURE` off in dev/testing since local dev is plain HTTP). | Read by `app.py` (`create_app`) and `wsgi.py` (`validate()`). Values consumed downstream via `current_app.config` in `routes/api.py`, `routes/portal.py`, `services/ingestion.py`, `services/auth.py`; `STATIC_VERSION` specifically via `app.py`'s context processor into `templates/base.html`/`dashboard.html`. |
| `.env.example` | Template for local `.env` — documents every env var `config.py` reads (`FLASK_SECRET_KEY`, `PORT`, `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, `ANTHROPIC_TIMEOUT_SECONDS`, `DATABASE_URL`, `REGISTRY_STORE_PATH`, `MAX_UPLOAD_MB`). | Mirrors `config.py` and `services/bhive_parser.py`'s env reads. Never committed with real values — copy to `.env`. |
| `.gitignore` | Excludes secrets (`.env`), Python artifacts, `venv/`, the `instance/` data folder (sqlite db + JSON registry store), logs, editor dirs. | Keeps `REGISTRY_STORE_PATH` (default `instance/registry`) and `DATABASE_URL` sqlite file out of version control. |
| `requirements.txt` | Pinned Python dependencies, by category rather than an exact list here (an exact duplicate list drifts — see the "Known gap" note above for exactly this failure mode; read `requirements.txt` directly for current pins): web framework + ORM (`Flask`, `Flask-SQLAlchemy`, `SQLAlchemy`), WSGI server (`gunicorn`), security/hardening (`Flask-Limiter`, `Flask-WTF`, `Flask-Migrate` — all added CLAUDE-P27-B), AI client (`anthropic`, `httpx` pinned to `0.27.2` — `0.28+` breaks `anthropic==0.34.2`'s client construction), document parsing (`python-docx`, `pypdf`, `Pillow`), config (`python-dotenv`). | Installed into `venv/`. `anthropic`/`httpx` versions are load-bearing for `services/bhive_parser.py`. |

## 3. Documentation

| File | Purpose | Connects to |
|---|---|---|
| `README.md` | Project overview, layout diagram, local setup, VPS deployment steps, security notes. | Describes the same file layout this manifest catalogs, from a "how do I run this" angle rather than a "what connects to what" angle. |

## 4. Backend routes (Flask blueprints)

| File | Purpose | Connects to |
|---|---|---|
| `routes/__init__.py` | Marks `routes/` as a package. Empty. | — |
| `routes/portal.py` | HTML page blueprint (`portal_bp`, no URL prefix). Routes: `/` (CLAUDE-P40-VW5: an unauthenticated visit now redirects straight to `/login` rather than rendering an intermediate marketing page; an authenticated visitor still gets a small, restrained recent-projects entry point, access-filtered per CLAUDE-P32), `/health` (liveness/readiness probe, public), `/login` (GET form + POST handler, generic error message, same-site-only `?next=` redirect — defaults to `/gateway` when no `next` is given), `/logout`, `/gateway` (`@login_required` — post-login landing, one action card: open an existing project; admins also see the two project-creation cards; CLAUDE-P40-VW5: renders `gateway.html` via the standalone `gateway_shell.html` chain, no Lists panel/app-shell — see that template's own comment for the root cause it fixes), `/upload` (`@admin_required` — GET form + POST handler, requires `operating_environment` [CLAUDE-P29] and now passes `owner=session['username']` [CLAUDE-P32, never the free-text `actor` field] to `ingest_upload`; `?environment=` pre-selects the environment radio from `gateway.html`'s two-entrance cards, accepts optional `actor`/`role` form fields, redirects into Case Workspace on success), `/dashboard` and `/dashboard/<project_id>` (`@login_required` — retired as a page, redirects into `workspace.show_workspace` / `portal.projects_list`; CLAUDE-P32: `/dashboard/<id>` now also runs the project-access gate directly rather than relying solely on the redirect target's own gate), `/projects` and `/search` (CLAUDE-P32: both now filter results to accessible projects via the shared `_accessible_documents` helper), `/projects/<id>/delete` (`@admin_required`, also gated for defense-in-depth even though admin already bypasses it — permanent, unrelated to CLAUDE-P40-E2's Remove below), `/removed-projects` (`@login_required`, CLAUDE-P40-E2 — lists recoverably-removed Projects for restoration, via `_accessible_documents(..., include_removed=True)`), `/admin/reset-project-data` (`@admin_required`, CLAUDE-P40-E2 — GET shows exact inventory + typed-confirmation form, POST wipes `REGISTRY_STORE_PATH` except `security_governance/` after taking a checksummed, manifest-bearing snapshot via `_create_snapshot`; `instance/bhive.db`/`.env`/config are never touched since this only acts inside `REGISTRY_STORE_PATH`), `/admin/reset-project-data/snapshots` (`@admin_required`, CLAUDE-P40-E2A — lists every snapshot `_create_snapshot` has written, by timestamp/actor/kind/inventory), `/admin/reset-project-data/snapshots/<snapshot_id>/restore` (`@admin_required`, CLAUDE-P40-E2A — GET re-verifies the snapshot's own checksums and previews current-vs-restored inventory; POST re-verifies again, takes its own `pre_restore_safety` snapshot of the CURRENT live state, then atomically swaps a staged rebuild into place via two `os.rename` calls — `security_governance/` is always taken from the CURRENT live store, never the snapshot, so accounts/security state is never reverted). Both `/admin/reset-project-data` and the restore route share one `os.O_EXCL` lock file beside `REGISTRY_STORE_PATH` (`.reset_project_data.lock`), so a reset and a restore against the same store can never run concurrently, and a stale lock is surfaced with its age rather than silently blocking forever. | Calls `services.auth.check_credentials`/`log_in`/`log_out`/`login_required`/`is_admin`; `services.ingestion.ingest_upload` / `get_registry` / `get_governance_log`; `services.project_access.can_access_project`/`ensure_owner_backfilled`/`known_usernames` (CLAUDE-P32); renders `templates/index.html`, `login.html`, `gateway.html`, `upload.html`, `removed_projects.html`, `reset_project_data.html`, `reset_snapshots.html`, `restore_snapshot.html`; registered by `app.py`. |
| `routes/api.py` | JSON API blueprint (`api_bp`, mounted at `/api/v1`). All routes require the same session-cookie login as `routes/portal.py`, enforced blueprint-wide via a `before_request` hook (not per-route decorators) — `POST /documents/ingest` additionally requires the admin role, mirroring `/upload`'s `@admin_required`, and now passes `owner=session['username']` to `ingest_upload` (CLAUDE-P32, same reasoning as `routes/portal.py`'s `upload()`). Routes: `POST /documents/ingest`, `GET /documents` (CLAUDE-P32: filtered to accessible projects only, was previously every project_id in the deployment), `GET /documents/<id>`, `GET /documents/<id>/requirements` (optional `?category=`), `GET /documents/<id>/milestones`, `GET /documents/<id>/consistency`, `GET /documents/<id>/governance`, `GET /documents/<id>/rfi` (downloads a `.docx`, 409 if nothing to export; stamps the operating-environment label) — these six now all go through the shared `_load_authorized_project_or_404` helper (CLAUDE-P32), returning the identical generic 404 whether a project doesn't exist or the caller isn't authorized for it, never distinguishing the two. `GET /categories`. Handles `RequestEntityTooLarge` (413) for oversized uploads. | Calls `services.auth.is_authenticated`/`is_admin`; `services.ingestion.ingest_upload` / `get_registry` / `get_governance_log`; calls `services.rfi_export.build_rfi_docx`; reads `services.bhive_parser.REQUIREMENT_CATEGORIES`; `services.case_workspace.CaseWorkspaceStore`, `services.environment_capabilities.OPERATING_ENVIRONMENT_LABELS`, `services.project_access.load_authorized_project_or_none`/`can_access_project` (CLAUDE-P32); registered by `app.py`. |

| `routes/security.py` | (CLAUDE-P31) Security Department blueprint (`security_bp`, mounted at `/security`), `@admin_required` throughout — no dedicated Security Officer role exists yet. Routes: source-policy ingestion (`POST /security/policies`, optional file upload, SHA-256 fingerprinted), policy-statement recording, control proposals, governed Q&A (record/approve), baseline lifecycle (create/add-control/acknowledge-impact/activate/withdraw), exception grant/revoke, per-project security-profile assignment, learning-contribution request/advance/decide, and `GET /security/` (the workspace itself: policy/Q&A/baseline/exception/project/learning/assurance sections). | Calls `services.security_governance.SecurityGovernanceStore`, `services.security_assurance.aggregate_security_activity`/`run_security_self_check`, `services.case_workspace.CaseWorkspaceStore`; renders `templates/security_department.html`; registered by `app.py`. |
| `routes/operations.py` | (CLAUDE-CA1D-INSTRUMENT-RAIL-01) Operations blueprint (`operations_bp`, mounted at `/operations`), `@admin_required`. Smallest proof slice of the Instrument Rail Plan-Mode report's four-part spatial model, Section D.1 (persistent, deployment-wide admin machinery gets a page at the perimeter, reached exactly the same way Security Department already is — not a new rail column). `GET /operations/` — the one route: wires `services.diagnostics.build_technical_telemetry` (built CLAUDE-P31, never previously rendered anywhere) into a real page, plus the live `AI_CALLS_DISABLED` kill-switch state as a second small accordion. Deliberately excludes repository/git diagnostics, subagent orchestration detail, and terminal integration this tranche — see the route's own docstring and `CONTINUATION_CHECKPOINT.md`. | Calls `services.diagnostics.build_technical_telemetry`; renders `templates/operations.html`; registered by `app.py`. `templates/base.html`'s Lists admin branch links to it (`is_admin` only, beside the Security leaf). |

## 5. Parsing & business logic (services)

| File | Purpose | Connects to |
|---|---|---|
| `services/__init__.py` | Marks `services/` as a package. Empty. | — |
| `services/bhive_parser.py` | The B-Hive core chassis: `BHiveParser` runs extract → segment → classify → consistency-check → assemble on an uploaded RFP/RFQ. Extract supports `.txt`/`.csv`/`.md` (direct decode), `.docx` (`python-docx`, skips heading/title-styled paragraphs), `.pdf` (`pypdf`). Segment is table-aware (Batch H): `extract_markdown_tables()` (a minimal GFM pipe-table parser — module-level, pure) finds tables first; their raw lines are excluded from the naive per-line pass (each data row instead becomes its own header-labeled chunk, e.g. `"Functional Group: ...; Room / Space: ..."`), and markdown ATX headings (`## ...`) are excluded the same way `.docx` already excludes heading-styled paragraphs. Classify uses the Anthropic API (batches of 25 chunks, per-batch timeout via `ANTHROPIC_TIMEOUT_SECONDS`, an overall `ANTHROPIC_CLASSIFY_BUDGET_SECONDS` budget across all batches, falls back to rule-based classification on timeout/budget-exceeded/any other exception) or a deterministic keyword classifier when no API key is set. Consistency-check is a single Anthropic call reviewing all classified requirements together for cross-requirement contradictions (`ANTHROPIC_CONSISTENCY_TIMEOUT_SECONDS`, capped at 150 requirements) — requires an API key, no rule-based fallback, best-effort/never blocks ingestion. Assemble derives a milestone list from `schedule_milestone`-category requirements. Defines `REQUIREMENT_CATEGORIES` and the `ParsedDocument`/`RequirementItem`/`ConsistencyFlag` dataclasses; `ParsedDocument.tables` (Batch H) carries the raw structured tables (headers + rows) separately from `requirements`, for a future reconciliation/arithmetic-check capability that isn't built yet. | Used by `services/ingestion.py`. `ParsedDocument`/`RequirementItem`/`ConsistencyFlag` are consumed by `services/requirements_registry.py` for (de)serialization and by `services/rfi_export.py` for `.docx` generation. `REQUIREMENT_CATEGORIES` is imported by both route blueprints. |
| `services/ingestion.py` | Shared upload-handling glue between the API and HTML upload form: `ingest_upload(file_storage, app, operating_environment, owner, actor=None, role=None, project_name=None)` — `operating_environment` (CLAUDE-P29) and `owner` (CLAUDE-P32) are both required, no default, validated before any file/parsing work begins. `owner` must be the caller's real, already-authenticated session identity, deliberately separate from the free-text `actor` governance-log field — see the function's own docstring. Also evaluates `ACTION_EXTERNAL_AI_REQUEST` (CLAUDE-P31) before classification. Validates the extension against `ALLOWED_UPLOAD_EXTENSIONS`, runs `BHiveParser.parse`, saves via the registry, always creates the project's `ProjectWorkspace` eagerly and locks its operating environment (`set_operating_environment`) and owner (`set_project_owner`), records a `document_ingested` governance event, and raises `UploadError` on bad input, including a missing/invalid `operating_environment` or `owner`. `get_registry(app)` builds a `RequirementsRegistry`; `get_governance_log(app)` builds a `GovernanceLog` — both from `app.config['REGISTRY_STORE_PATH']`. | Imported by both `routes/api.py` and `routes/portal.py` so the validate→parse→save→log sequence isn't duplicated across the JSON and HTML paths. Wraps `services.bhive_parser.BHiveParser`, `services.requirements_registry.RequirementsRegistry`, `services.governance.GovernanceLog`, `services.case_workspace.CaseWorkspaceStore`/`services.environment_capabilities`, and `services.security_governance`/`services.security_policy` (CLAUDE-P31). |
| `services/draft_assist.py` | (CLAUDE-COMPOSER-DRAFT-ASSIST-01, new) The Composer pen's own service: a CLOSED vocabulary of eleven draft actions (rewrite clearly / shorter / longer / more formal / more direct / RFI wording / site observation / meeting note / email / grammar only / check ambiguity) and one system prompt whose FIRST instruction is an absolute rule - improve wording, never introduce a fact the original does not contain, and preserve the author's level of certainty in both directions. The vocabulary is closed because the action arrives from a form field; an open one would let a caller pass arbitrary instructions to the model. `check ambiguity` carries `rewrites=False` so the client cannot offer Replace for it - answering "what is ambiguous here?" with silently resolved prose would hide the thing that was asked about. Returns a proposal as DATA; it cannot write to a draft. | Calls `services/llm_gateway.py:call_llm_json`; called by `routes/workspace.py:composer_draft_assist` (policy-gated through the same `_external_ai_status`/`ACTION_EXTERNAL_AI_REQUEST` resolver as every other external call). Tested by `tests/test_composer_draft_assist_01.py`. |
| `services/diagnostic_report.py` | (CLAUDE-DIAGNOSTIC-BRIDGE-01, new) Capture a live product problem where it happened, and hand a finished investigation back by email. Five acts kept separate: capture (`record`) -> investigate -> `complete` -> `email_report` -> code/deploy, which is NOT representable here at all. **Not a transport**: ARCHIOSK cannot call a development agent, which runs on an operator's own machine when a human starts it, so `record()` writes an inert row a Claude Code session reads later. `deployed_build_sha()` returns None rather than guessing on the server, where the app directory is not a git repository. `format_email` prints "not established" for a missing field rather than omitting it, because a silently absent section reads as an answer of "nothing", and `email_report` refuses to send an uninvestigated report for the same reason. | Reuses `services/email.py:send_email` unchanged (no second mail subsystem); persists `models.DiagnosticReport`; driven by `routes/portal.py` and `tools/diagnostic_report.py`. Tested by `tests/test_diagnostic_bridge_01.py`. |
| `services/drawing_intake.py` | (CLAUDE-P40-VW8-QA-R2A) Drawing-first Project qualification — a STAGING-time-only analysis step, deliberately never the full `BHiveParser.parse` pipeline (no external-AI call of any kind; see the module's own header for the full repository-grounded capability audit: native PDF/DOCX text extraction is real, PDF-page rendering and local OCR are NOT available in this environment). `analyze_upload(raw_bytes, filename)` dispatches to `extract_candidates_from_pdf`/`_docx` (LABEL: VALUE pattern matching against `CANDIDATE_FIELDS` — project_name/project_number/project_address/owner_client/drawing_title/sheet_number/discipline/consultant/issue_date/revision/scale — same technique as `bhive_parser.py`'s own `_DOCUMENT_METADATA_LABEL_PATTERN`), returning a `DrawingIntakeResult` (candidates + `text_extraction_status`). Each `CandidateField` retains source page, evidence snippet, extraction method, confidence tier, and confirmed/corrected/unconfirmed status. `PendingUploadStore` (flat-JSON + sibling raw-bytes file under `REGISTRY_STORE_PATH/pending_uploads/`, same pattern as `workspace_sources/`) holds a staged upload between the initial POST and the reviewer's confirm/correct step — never a database table. | Used by `routes/portal.py`'s `upload`/`upload_confirm`/`upload_confirm_discard` routes. Reuses `services.bhive_parser.BHiveParser.extract_pdf_pages` (the one real `pypdf` call site) rather than a second, parallel PDF-reading implementation; the confirm step calls the existing, unchanged `services.ingestion.ingest_upload` to actually create the Project. |
| `services/project_access.py` | (CLAUDE-P32) Project-level access control: `can_access_project(workspace, username, is_admin)` (the one centralized decision -- `owner is None` fails CLOSED, admin-only, unlike every other None-means-ungated field elsewhere in this codebase), `infer_owner_from_ingestion_actor`/`ensure_owner_backfilled` (deterministic, exact-match-only legacy-project backfill against a real `models.User.username` -- never a guess), `known_usernames`, `load_authorized_project_or_none` (the shared routine every blueprint's own thin loader wraps). Deliberately NOT imported by `services/case_workspace.py` itself (which still does not import `models`, preserving its existing framework-decoupling). | Imports `models.User`. Used by `routes/workspace.py`'s `_load_workspace_or_404`, `routes/api.py`'s `_load_authorized_project_or_404`, `routes/portal.py`'s `_accessible_documents`/`_require_project_access_or_404`, and `app.py`'s `_nav_recent_projects`. |
| `services/environment_capabilities.py` | (CLAUDE-P29) Project Operating Environment closed enum + strict validator + one concrete capability mapping: `CLIENT_OWNER`/`DESIGN_BUILDER_PROPONENT`, `OPERATING_ENVIRONMENTS`, `OPERATING_ENVIRONMENT_LABELS`, `is_valid_operating_environment`, `allowed_participant_roles(operating_environment)` (returns `None` for a legacy/unset environment, meaning "no gating"). (CLAUDE-P30) Extended with a capability grammar (`CAPABILITY_REGISTRY`, `CapabilityDefinition`, `capability_availability`/`capability_variant_label`/`capability_denial_reason` — the single resolution point every route/template/export calls into) and `CLIENT_OWNER_DECISION_STAGES`/`DESIGN_BUILDER_PROPONENT_DECISION_STAGES`/`decision_stages_for_environment` for the Go/No-Go decision family. Deliberately not a plugin/registry framework. | Imported by `services/ingestion.py`, `services/case_workspace.py` (inside `set_operating_environment`/`record_go_no_go_decision`, to avoid a module-level import cycle), and `routes/portal.py`/`routes/workspace.py`/`routes/api.py`. |
| `services/governance.py` | Append-only audit-trail log: `GovernanceEvent` dataclass and `GovernanceLog.append(project_id, event_type, actor, role, payload=None, predecessor_id=None)` / `.read(project_id)`. One `.jsonl` file per project (`<project_id>.governance.jsonl`, always opened in append mode — never read-modify-rewritten), so history can't be silently altered. Corrections are new events with `predecessor_id` pointing back at what they correct, never edits to the original line. Raises `GovernanceError` if `actor`/`role` are empty/whitespace-only. No real authentication backs `actor`/`role` — they're free-text fields recorded as given, not verified identity (unrelated to `services/auth.py`'s login gate — this is an audit label, not a permission check). | Used by `services/ingestion.py`. Files live alongside the registry's own JSON files under `instance/registry/` (git-ignored). |
| `services/auth.py` | **Stale entry, superseded shape** — this row still describes the original single-shared-credential design. The real, current mechanism is a `User`-table-backed session login (`models.py`, multi-user, `admin`/`read_only` roles, provisioned via `tools/create_credentials.py`) — `check_credentials`, `is_authenticated()`, `log_in`/`log_out`, `login_required`/`admin_required` decorators. **`routes/api.py` now also requires this same session login**, enforced blueprint-wide (CLAUDE-P27-B) — the "does not touch routes/api.py" claim this row previously made is false as of commit `c2db13f`. Full current behavior not re-documented here; read `services/auth.py` directly. | Used by `routes/portal.py`, `routes/api.py` (session auth added CLAUDE-P27-B), `routes/workspace.py`, and `app.py`'s context processor. |
| `services/rate_limit.py` | (CLAUDE-P27-B) Module-level Flask-Limiter singleton (`limiter = Limiter(key_func=get_remote_address)`), created unbound like `models.py`'s `db = SQLAlchemy()` and bound via `limiter.init_app(app)` in `app.py`. In-memory storage — under multiple Gunicorn workers each worker holds its own counter, so the effective ceiling is (configured limit × worker count), not one shared limit. Disabled under `TestingConfig` (`RATELIMIT_ENABLED=False`). | Imported by `routes/portal.py` (`/login`, `/forgot-password`, `/reset-password`, `/upload`) and `routes/api.py` (`/documents/ingest`) to decorate specific routes with `@limiter.limit(...)`. |
| `services/security_policy.py` | (CLAUDE-P31) `GOVERNED_ACTIONS` (8), `MANDATORY_FLOOR_DEFAULTS`, `evaluate_action` (floor → baseline → profile → exception resolver, most-restrictive-wins, exception capped at `DECISION_ALLOW`), `INFORMATION_CLASSIFICATIONS` + `CLASSIFICATION_PROFILE_DECISIONS`, `SECURITY_CLAIMS_REGISTRY`. Distinct from `governance/specified-unbuilt/security-policy.md`'s "Project Security Policy" — see this module's own docstring. | Imported by `services/ingestion.py`, `routes/workspace.py`, `services/diagnostics.py`, `services/security_governance.py` (inside methods, to avoid an import cycle), `routes/security.py`. |
| `services/security_governance.py` | (CLAUDE-P31) `SecurityGovernanceStore` — ONE global, deployment-wide flat-JSON record (`<REGISTRY_STORE_PATH>/security_governance/security_governance.json`, deliberately a subdirectory so `RequirementsRegistry.list_ids()`'s `*.json` glob never mistakes it for a project) holding `SourcePolicy`/`PolicyStatement`/`ProposedControl`/`QAEntry`/`BaselineVersion`/`SecurityException`/`LearningContributionRequest`. Full baseline draft→under_review→approved→active→superseded/withdrawn lifecycle. | Used by `routes/security.py`, `services/ingestion.py`, `routes/workspace.py`. Wraps `services.learning_governance`'s pure lifecycle functions for the learning-contribution methods. |
| `services/case_workspace.py` (P31/P32 additions only — see that file's own P30/P29/etc. history for everything else) | `ProjectWorkspace.security_profile`/`_set_by`/`_set_at` (CLAUDE-P31, re-settable, unlike `operating_environment` — policy changes over a project's life), `CaseWorkspaceStore.set_project_security_profile`. `ProjectWorkspace.owner`/`_set_by`/`_set_at`/`access_allow_list` (CLAUDE-P32 — `owner is None` fails closed, admin-only, the deliberate opposite of every other None-means-ungated field in this module), `CaseWorkspaceStore.set_project_owner` (re-settable, admin-only at the route layer, unlike `operating_environment`'s hard lock), `grant_project_access`/`revoke_project_access` (owner-or-admin authority, the same pattern `archive_case`/`derive_case` already established — `actor_role` passed through as a plain string, this module still does not import `models`/`services.auth`). | `security_profile` validated against `services.security_policy.is_valid_classification`. `owner`/`access_allow_list` write methods take no dependency on `services.project_access` (that module depends on this one, not the reverse) — real-username validation against `models.User` happens at the route layer in `routes/workspace.py`, not here. |
| `services/case_workspace.py` (P40-E2 additions only) | `Source.removed_at`/`removed_by`/`removal_reason`, `ProjectWorkspace.removed_at`/`removed_by`/`removal_reason` — recoverable "Remove Document"/"Remove Project", never a deletion; `removed_at is None` is the only thing that means "active" anywhere either field is read. `CaseWorkspaceStore.active_sources`/`removed_sources` (the filter every display/AI-context/search read of `workspace.sources` should use going forward — a provenance lookup that must keep resolving a removed Source, e.g. a Finding's own citation, calls `_find` directly instead, never this filter). `remove_source`/`restore_source`/`remove_project`/`restore_project` — owner-or-admin authority (same pattern as `grant_project_access`/`archive_case`), raise `CaseWorkspaceError` if already in the target state. (CLAUDE-P40-E2A) `register_requirement`/`promote_requirement_item` now also raise `CaseWorkspaceError` if `source_id` names a removed Source — NEW analysis work must not silently re-target removed content, unlike an EXISTING Requirement/Finding that already cites one, which keeps resolving it via `_find` unfiltered (an honest "removed" state, never a broken reference). | `active_sources`/`removed_sources` used by `routes/workspace.py`'s `show_workspace` (feeds `case_workspace.html`'s Toolbox and Sources listing), `routes/portal.py`'s `_accessible_documents`/`_nav_recent_projects` (project-level `removed_at` only), and `services/conversation_interpreter.py`'s drawing-Source detection (CLAUDE-P40-E2A — "Analyze this drawing" no longer considers a removed drawing Source attached to a Case). Routes for the four write methods live in `routes/workspace.py` (`remove_document_route`/`restore_document_route`/`remove_project_route`/`restore_project_route`), using the same `confirm=yes/no` vocabulary as `routes/portal.py`'s pre-existing (unrelated, permanent) `delete_project` — see `templates/confirm_remove_document.html`/`confirm_remove_project.html`. CLAUDE-P40-E2A also added `allow_removed` to `routes/workspace.py`'s `_load_workspace_or_404` — the near-universal choke point every route in that blueprint reaches project data through — so a removed Project's ordinary routes redirect to `templates/project_removed.html`'s tombstone instead of proceeding, with no per-route change needed; `source_file`/`revise_source` additionally check the specific Source's own `removed_at`. |
| `services/case_workspace.py` (CLAUDE-CA1D-ATTENTION-STATE-02 additions only) | `ProjectWorkspace.item_reviewed_at` — per-reviewer, per-item "meaningfully reviewed at" marker (`username -> object_id -> ISO timestamp`), personal/display-only like `last_viewed_by`/`starred` above, no governance meaning, no event of its own. `CaseWorkspaceStore.record_item_reviewed` (mirrors `record_last_viewed`'s exact raw-JSON-patch-under-`_save_lock` write pattern, never `save()`'s versioned path). `CaseWorkspaceStore.has_unreviewed_change` — derived, never stored (same discipline `review_state_for_finding` already uses): compares `item_reviewed_at` against the existing, already-governed `finding_reviewed` `GovernanceLog` events for a Finding, never a second competing history of "what changed." First proof entity only (Findings); conservative by construction (missing/malformed persisted state always reads as unreviewed, never a guessed richer state). Caller-ordering is load-bearing for correctness — `record_item_reviewed` must run AFTER the `finding_reviewed` event its own triggering action produced, never before (see that method's own docstring). | `has_unreviewed_change` reads `services.governance.GovernanceLog`. `record_item_reviewed` called by `routes/workspace.py`'s `validate_finding`/`set_disposition`, immediately after their existing `finding_reviewed` log calls. Unit- and route-tested by `tests/test_ca1d_attention_state_02.py`. Deliberately does not touch, extend, or repurpose `ReviewThread`/`Attention` (a separate, explicit human-directed-escalation concept — see `governance/specified-unbuilt/per-item-attention-review-state.md`'s own A0 section for why). |
| `services/case_workspace.py` (P40-VW9 additions only) | Governed Files Display and Project File Architecture. `FOLDER_ROOT_DATA_ROOM`/`FOLDER_ROOT_DESIGN_BUILDER`/`KNOWN_FOLDER_ROOTS` — a closed, two-value vocabulary naming the two governed sibling roots (different governance spaces, not open-world like `Source.kind`). Both roots are GOVERNED VIRTUAL roots this stage — neither is a persisted `Folder` row of its own; `create_folder` has no `root` parameter at all and always writes `FOLDER_ROOT_DESIGN_BUILDER`, so there is structurally no way to create a Data Room folder record this stage. `Folder` dataclass (`id`/`project_id`/`root`/`name`/`created_at`/`created_by`/`parent_folder_id`/`removed_at`/`removed_by`) — canonical identity is `id`, mirroring `Source`'s own "folder locations and filenames are external representations only" principle; a folder's full path is always DERIVED at read time (`CaseWorkspaceStore._folder_path`, walks `parent_folder_id`), never stored as a string. `ProjectWorkspace.folders` — purely additive (`list[dict]`, empty-list default, same backward-compatible pattern as `tags`/`tasks`). `CaseWorkspaceStore.create_folder`/`rename_folder`/`move_folder`/`delete_folder` — no owner/admin gate (matching `create_task`/`create_custom_tag`'s own precedent, not `remove_source`'s — a Design-Builder folder is collaborative team structure, not owner-locked evidence); sibling-name uniqueness (`_reject_if_sibling_folder_name_taken`, scoped to project+root+parent, mirroring `ingestion.py`'s own `_reject_if_name_taken` style); `move_folder` prevents cycles via `_folder_descendant_ids` and rejects a foreign/removed/Data-Room destination; `delete_folder` only ever operates on an EMPTY folder and is a recoverable soft-delete (`removed_at`/`removed_by`, same tombstone convention as `Source`/`Project` — no restore UI built this stage, data stays recoverable). **Superseded by CLAUDE-RFP27-TERRITORY-01 below**: this stage's own "no `folder_id` field was added to `Source`" claim no longer holds — see that row. | `routes/workspace.py`'s new `create_folder_route`/`rename_folder_route`/`move_folder_route`/`delete_folder_route` (`/projects/<id>/workspace/folders...`), and `show_workspace`'s own Files-specific resolution block (`open_folder`/`folder_ancestors`/`design_builder_children`/`design_builder_move_targets`/`data_room_sources`, only computed when `directory_view == "files"`). Rendered by `templates/case_workspace.html`'s own `directory_view == 'files'` branch and `templates/confirm_delete_folder.html`. |
| `services/case_workspace.py` (CLAUDE-RFP27-TERRITORY-01 additions only) | Data Room corpus/territory completeness. `Source.folder_id` (Optional, default `None`, additive/backward-compatible) — a Document CAN now belong to a Folder, superseding P40-VW9's own "not this stage" note above; never inferred from filename, only ever set via `set_source_folder`/`reconcile_data_room_upload`/`add_source(folder_id=...)`. `CaseWorkspaceStore.ensure_folder_path(workspace, root, relative_path, actor, governance_log)` — the ONE place (besides `create_folder`) allowed to construct a `root=FOLDER_ROOT_DATA_ROOM` Folder; idempotent path-segment walker, reuses `create_folder`'s sibling-uniqueness scoping per segment. `CaseWorkspaceStore.set_source_folder(workspace, source_id, folder_id, actor, governance_log)` — relinks an existing Source's `folder_id` without touching identity/content, logs `source_relinked_to_folder`. | `ensure_folder_path` called by `routes/workspace.py`'s `register_folder_paths_route` and `services/ingestion.py`'s `reconcile_data_room_upload` (per real directory segment). `set_source_folder` called by `reconcile_data_room_upload` (hash-dedup relink path). `services/conversation_interpreter.py`'s `_resolve_mentioned_folder`/`_handle_folder_reference` read `folder_id` to report a real Source count per Folder. |
| `services/ingestion.py` (CLAUDE-RFP27-TERRITORY-01 addition only) | `reconcile_data_room_upload(files, relative_paths, project_id, app, actor=None, role=None)` — the Data Room discovery/reconciliation mechanism (Part 4): same `webkitdirectory` files/relative_paths shape as `ingest_folder_upload`, but reconciles against an EXISTING project rather than creating one. Every directory segment becomes a real Folder via `ensure_folder_path`; a file whose SHA-256 exactly matches an already-registered Source is relinked (`set_source_folder`), never duplicated; a genuinely new eligible file is registered exactly like `ingest_folder_upload`'s own non-founding files (`add_source`, real `BHiveParser._extract`, `register_plain_text_structure`). Deliberately additive/read-only-of-canonical-state — a file present in a prior scan but absent from this one is simply not mentioned, never auto-deleted. Returns one `{"filename", "relative_path", "status": "added"\|"relinked"\|"skipped", "reason", "source_id"}` dict per file. | Called by `routes/workspace.py`'s `reconcile_data_room_route`. |
| `routes/workspace.py` (CLAUDE-RFP27-TERRITORY-01 additions only) | `register_folder_paths_route` (`POST /projects/<id>/workspace/folders/register-paths`, admin-gated) — explicit empty-folder registration, one relative path per line, explicit required `root` choice (`KNOWN_FOLDER_ROOTS`, never inferred); the one case a file upload structurally can never report (browsers never emit a `File` for an empty directory). `reconcile_data_room_route` (`POST /projects/<id>/workspace/data-room/reconcile`, admin-gated) — **superseded by CLAUDE-DATA-ROOM-RECONCILE-01 below, see that row** — this route no longer registers anything itself. `show_workspace`'s Files block gained Data-Room-side folder resolution (`open_data_room_folder`/`data_room_folder_ancestors`/`data_room_children`/`data_room_folder_sources`/`data_room_unfiled_sources`, keyed off a separate `?data_room_folder=` param, distinct from Design-Builder's own `?folder=`) mirroring the pre-existing Design-Builder resolution pattern. | Rendered by `templates/case_workspace.html`'s rewritten Data Room section (breadcrumb/folder-list/folder-row/folder-empty/admin-actions — see `UI_REFERENCE_MAP.md`). |
| `services/ingestion.py` (CLAUDE-DATA-ROOM-RECONCILE-01 additions only) | **Supersedes `reconcile_data_room_route`'s prior one-shot behavior** — "analyze and report before modifying governed project evidence" was a real Product Owner requirement the old immediate-mutation behavior violated. `preview_data_room_reconcile(files, relative_paths, project_id, app)` — read-only; classifies every scanned file as `unchanged`/`new`/`modified`/`missing`/`renamed`/`ineligible`/`ambiguous` by comparing content hash and `origin_reference` against `workspace.sources` (active only, `removed_at is None`); never calls `add_source`/`set_source_folder`/`ensure_folder_path`. Returns `(report, new_eligible_files)` — `report["by_status"]` deliberately not `"items"` (a dict already has an `.items()` method, which Jinja's dot-access would silently shadow — a real bug this stage's own test suite caught). `PendingReconcileStore` — same flat-JSON + sibling-raw-bytes-file staging pattern as `services/drawing_intake.py`'s `PendingUploadStore`, pluralized (a preview may stage zero to many new-eligible files' raw bytes), 24h TTL, swept opportunistically on `create()`. `reconcile_data_room_upload` (CLAUDE-RFP27-TERRITORY-01) is UNCHANGED and still does all real registration — called again, unmodified, only from the new confirm route below, never duplicated. | Called by `routes/workspace.py`'s `reconcile_data_room_route` (preview) and `reconcile_data_room_confirm_route` (registration, via the unchanged `reconcile_data_room_upload`). |
| `routes/workspace.py` (CLAUDE-DATA-ROOM-RECONCILE-01 additions only) | `reconcile_data_room_route` (`POST /projects/<id>/workspace/data-room/reconcile`) now only previews and stages — redirects to `?view=files&reconcile_preview=<staging_id>`. `reconcile_data_room_confirm_route` (`POST .../data-room/reconcile/<staging_id>/confirm`, admin-gated) — the one human-approved action a preview can trigger ("Add N new document(s)"), re-validates the staging record belongs to this project before acting (project isolation), reuses `reconcile_data_room_upload` unchanged. `reconcile_data_room_discard_route` (`POST .../data-room/reconcile/<staging_id>/discard`, admin-gated) — abandons a preview without registering anything. `show_workspace`'s Files block resolves `?reconcile_preview=<staging_id>` into a `reconcile_preview` template variable (re-validated against this project's own id, degrades to `None` on a missing/expired/foreign staging_id, never an error). | Rendered inline in `templates/case_workspace.html`'s existing `display.files.data-room.admin-actions` disclosure — never a second, competing Reconcile surface — see `UI_REFERENCE_MAP.md`. |
| `services/conversation_interpreter.py` (CLAUDE-RFP27-TERRITORY-01 addition only) | `_resolve_mentioned_folder(workspace, text)`/`_handle_folder_reference(workspace, folder)` — dispatched inside `interpret_message`, checked after the orientation branch and before the generic project-question branch. Deterministic, no model call: case-insensitive whole-name match (≥4 chars) of any real, non-removed Folder against the message text (longest name wins on overlap), then an honest reply distinguishing "exists and is currently empty" from a real Source/subfolder count — Part 3's own required proof that GO never fabricates content for an empty folder nor wrongly implies it doesn't exist. | Unit-tested directly (`case=None`, no route) by `tests/test_p40vw9_files_display_and_folder_architecture.py::GoFolderReferenceTests`, same pattern as `test_conversation_apertures.py::InterpretMessageWithoutACaseTests`. |
| `config.py`, `services/ingestion.py` (CLAUDE-SPREADSHEET-SOURCE-ELIGIBILITY-01 additions only) | `.xlsx` added to `ALLOWED_UPLOAD_EXTENSIONS` — a real, already-hardened extraction pipeline for it already existed (`services/spreadsheet_intelligence.py`, CLAUDE-MM3) and was already reachable for adding a spreadsheet to an EXISTING project (`routes/api.py`'s `/spreadsheet-structure`), but this list (new-project/folder-upload/Data-Room-Reconcile eligibility) had never been revisited — a legacy scope gap, not a security boundary. New `_register_source_content(store, workspace, source, raw_bytes, filename, parser, actor, governance_log)` — shared by `ingest_folder_upload` and `reconcile_data_room_upload` (previously two near-identical inline blocks), routes `.xlsx` through `spreadsheet_intelligence.inspect_workbook` + `CaseWorkspaceStore.register_spreadsheet_structure` (real sheet/row/cell evidence, never BHiveParser, which has no `.xlsx` branch); every other extension is byte-for-byte the same `BHiveParser._extract`/`register_plain_text_structure` path as before. `ingest_upload` gained an explicit guard refusing `.xlsx` as a project's FOUNDING document (its structure isn't prose suitable for `classify()`/`_check_consistency()`) with a constructive redirect to folder-upload/Reconcile instead of an opaque parser failure. `preview_data_room_reconcile` needed no change — its hash/`origin_reference` classification is already extension-agnostic, so it classifies a new `.xlsx` as `new`/eligible automatically once the extension list allows it. | `templates/upload.html`'s folder-picker JS split its one `ELIGIBLE_EXTENSIONS` list into `REGISTERABLE_EXTENSIONS` (drives the "N eligible" summary, now includes `.xlsx`) and `FOUNDING_ELIGIBLE_EXTENSIONS` (drives founding-candidate selection, unchanged, excludes `.xlsx`) — the two diverged for the first time this stage; one shared list would have let a spreadsheet-only top-level folder falsely reach the founding picker. Tested by `tests/test_spreadsheet_source_eligibility_01.py` (integration surface only — `tests/test_mm3_spreadsheet_intelligence.py` already exhaustively covers `spreadsheet_intelligence.py`'s own parsing/security internals, reused unchanged). |
| `templates/removed_projects.html`, `templates/confirm_remove_document.html`, `templates/confirm_remove_project.html`, `templates/reset_project_data.html` | (CLAUDE-P40-E2) `removed_projects.html` — "Removed Projects" listing (`/removed-projects`) with per-row Restore; reuses `_accessible_documents(..., include_removed=True)` so an unauthorized user never sees a removed project's name. The two `confirm_remove_*.html` files extend `confirm_base.html` like `confirm_delete_project.html` does, but explain recoverability (opposite framing from `confirm_delete_project.html`'s permanent-erasure wording). `reset_project_data.html` — administrator-only "clean test reset": exact inventory + typed-confirmation-phrase form; extends `base.html` directly (not `confirm_base.html`, which has no text-input slot). | Rendered by `routes/portal.py`'s `removed_projects`/`reset_project_data` and `routes/workspace.py`'s `remove_document_route`/`remove_project_route`. |
| `templates/project_removed.html` | (CLAUDE-P40-E2A, Section A) The removed-Project tombstone — display name, removed-at/-by, and the one Restore Project action; no active-Workspace content (no Toolbox, no Sources/Requirements/Findings). Authorization and lifecycle are different checks: an authorized-but-not-yet-restored owner/admin sees this; an unauthorized caller still gets the pre-existing fail-closed 404 (this template never renders for them at all). | Rendered by `routes/workspace.py`'s `show_workspace` when `workspace.removed_at` is set — the one `allow_removed=True` exception in `_load_workspace_or_404` besides `restore_project_route` itself; every other route in that blueprint redirects here instead of proceeding, so all child Document/Investigation routes inherit the removed state automatically, with no per-route change. |
| `templates/confirm_delete_folder.html` | (CLAUDE-P40-VW9) Extends `confirm_base.html`, same family/shape as `confirm_remove_document.html` — the lighter `confirm=yes/no` gate, not the Approval Gate's `confirm=once/session/no` (CLAUDE.md's own "two different confirm vocabularies" note places folder deletion in the same category as Remove Document/Remove Project). Explains the folder-deletion record is preserved, never erased, matching the recoverable-removal framing its siblings use. **CLAUDE-P40-VW9A addendum:** `confirm_back_url` now returns to the folder's own real `parent_folder_id` (root-level folders still land on the bare Files root) instead of always the bare Files root — `parent_folder_id` is server-derived from the already-loaded, project-scoped folder record in `delete_folder_route`, never raw request input, matching `confirm_action.html`'s own `case_id` back-link precedent. | Rendered by `routes/workspace.py`'s `delete_folder_route` when `confirm` is absent/not `yes`. |
| `templates/reset_snapshots.html`, `templates/restore_snapshot.html` | (CLAUDE-P40-E2A, Section B) `reset_snapshots.html` — every checksummed snapshot `_create_snapshot` has written (a Reset's own, or a Restore's automatic `pre_restore_safety` copy), newest first, each linking to its own restore preview. `restore_snapshot.html` — re-verifies integrity live (never trusts a stale GET), previews current-vs-restored inventory, and only offers the typed-confirmation form when verification passed. | Rendered by `routes/portal.py`'s `list_reset_snapshots`/`restore_reset_snapshot`. |
| `services/diagnostics.py` | (CLAUDE-P31) `TechnicalTelemetry` (structurally content-free dataclass, never gated) vs. `SupportPackage` (`build_support_package` requires an already-favorable `SecurityDecision`, raises `SupportPackageDeniedError` otherwise). No existing telemetry/support mechanism existed in this repository before this module. `build_technical_telemetry` is now actually rendered somewhere (CLAUDE-CA1D-INSTRUMENT-RAIL-01, `routes/operations.py`) — previously built but unwired to any UI. | Consumes `services.security_policy.SecurityDecision`/`DECISION_ALLOW`/`DECISION_ALLOW_APPROVED_ROUTE`; reads `services.bhive_parser.BHIVE_PARSER_VERSION`. `build_technical_telemetry` called by `routes/operations.py`. |
| `services/dev_session_status.py` | (CLAUDE-CA1D-LIVE-BRIDGE-01) Read side of the read-only Claude/agent session-state bridge. `DevSessionStatus` — a closed, content-free dataclass, same discipline as `TechnicalTelemetry` above — and `read_dev_session_status()`, a synchronous, never-raising reader: missing file, malformed JSON, a non-dict payload, an unrecognized `state` value, a non-integer `pid`, or a `pid` no longer belonging to a live process (mirrors `routes/portal.py`'s own `_pid_is_alive` idiom exactly, duplicated rather than cross-layer-imported) all degrade to `state="unknown"` — never a guessed richer state. Not wired to any route/UI yet — this stage proved only that the bridge itself produces reliable, content-zero structural state. | Reads `instance/dev_session_status.json` (git-ignored), written by `tools/write_dev_session_status.py`. Unit-tested by `tests/test_ca1d_live_bridge_01.py` against synthetic fixtures. |
| `services/learning_governance.py` | (CLAUDE-P31) `LearningContributionRequest`: `ZONE_PROJECT_PRIVATE`/`ZONE_ORGANIZATION_PRIVATE`/`ZONE_SHARED_ARCHIOSK_IMPROVEMENT`, a 5-stage required review sequence before `contribution_approved`, self-approval prohibited for shared-improvement targets. Governs intent/approval state ONLY — no shared-learning pipeline exists in this repository to move any data into. | No import edge to/from `services.case_workspace` in either direction (structurally enforces "a quality rating never implies training consent"). Persisted via `services.security_governance.SecurityGovernanceStore`. |
| `services/security_assurance.py` | (CLAUDE-P31) `aggregate_security_activity` — pure read-side aggregation over every project's existing `GovernanceLog`, filtered to `SECURITY_RELEVANT_EVENT_TYPES`, activity-level fields only (never project content). `run_security_self_check` — 5 representative independent-verification checks (valid environment/profile values, control-decision provenance, single active baseline, no stale-but-active exception). | Reads via `services.requirements_registry.RequirementsRegistry.list_ids()` (the only cross-project enumeration this codebase has) and `services.governance.GovernanceLog`; used by `routes/security.py`. |
| `services/requirements_registry.py` | Flat-file JSON persistence for `ParsedDocument` records: `save()`, `get(project_id)`, `list_ids()`. Storage-agnostic at the call site by design — swappable for a DB-backed implementation later without touching the routes. | Reads/writes `ParsedDocument`/`RequirementItem`/`ConsistencyFlag` from `services/bhive_parser.py`. Instantiated by `services/ingestion.get_registry`; store path comes from `config.py`'s `REGISTRY_STORE_PATH`. Backing files live under `instance/registry/` (git-ignored). |
| `services/project_qa.py` | (CLAUDE-P38, OBS-01) `answer_project_question(question, document_filename, candidate_requirements, governed_requirements, milestones, ...)` — real, grounded, read-only project-level Q&A. Mirrors `services/requirement_investigation.py`'s Anthropic integration pattern exactly (lazy import, same env vars, honest degrade on no-key/timeout/malformed-output). Grounded only in already-extracted evidence, never the model's own world knowledge or the Source's full original text. Creates no governed record of any kind — the question and reply are already persisted as ordinary `ConversationMessage`s by the caller. **CLAUDE-GO-GROUNDING-EVIDENCE-SELECTION-01**: `_build_prompt`'s `additional_document_evidence` section no longer takes a plain `[:15]` slice in Source-registration order (a document registered after the first 15 could never reach grounding regardless of relevance — reproduced live on North Bayview) — now calls `services.conversational_turn.select_relevant_document_evidence` (question-aware scoring: explicit name match > currently-open Source > keyword relevance > authority/recency tiebreakers) and additionally lists every document's own NAME regardless of selection, so GO can honestly distinguish "exists but insufficient content" from "doesn't exist." `BEHAVIORAL_CONTRACT` gained one explicit rule: evidence selection and evidence authority are different questions — non-binding/reference/proposed content must never be presented as binding merely because it was selected. | Called by `services/conversation_interpreter.py`'s `_handle_project_question`, gated through the same `ACTION_EXTERNAL_AI_REQUEST` policy check (`_evaluate_external_ai_policy`) that module already uses for Requirement investigation. |
| `services/conversational_turn.py` (CLAUDE-GO-GROUNDING-EVIDENCE-SELECTION-01 additions) | `select_relevant_document_evidence(additional_document_evidence, question, selected_source_id=None, selected_source_name=None, max_documents=15, max_excerpts_per_document=8)` — the ONE relevance-scoring implementation both `project_qa.py`'s `_build_prompt` (live) and this module's own `_build_conversational_turn_prompt` (CLAUDE-CA1D-COMPOSER-SPINE-01 Stage 2, not yet wired into `interpret_message` — fixed now so Stage 3 doesn't reintroduce the same defect) call, instead of each keeping its own duplicate cap/slice. Scoring tiers (additive, not exclusive): explicit name match (`_is_explicit_name_match`, a ratio+count threshold against the question's own significant words) > currently-open Source (id or name) > keyword overlap with filename+excerpts > `document_authority` (when populated — usually isn't yet, see the field's own honest-absence note) > `Source.added_at` recency (smallest weight, pure tiebreaker, never dominant). A document scoring as an explicit match or the current document also gets a much larger excerpt allowance (80, not 8) — tabular/spreadsheet rows have no reliable per-row keyword signal of their own (a data row rarely repeats its own sheet's title/header words), so keyword-filtering individual rows out of a document the reviewer specifically named would silently drop the very rows that answer the question. `gather_project_evidence`'s own `additional_document_evidence` dicts gained three additive keys (`source_id`, `added_at`, `document_authority`) to support this scoring — backward-compatible, no existing reader broken. | Imported by `services/project_qa.py` (`from services.conversational_turn import build_bounded_history, select_relevant_document_evidence`, alongside the already-established `build_bounded_history` import). Tested by `tests/test_go_grounding_evidence_selection_01.py` (unit-level scoring + the governing prompt's own North Bayview proof games A-H) plus a real end-to-end pipeline smoke check against the actual local North Bayview specimen (scratchpad-only, not committed — see that session's own report). |
| `services/document_context_intelligence.py` | (CLAUDE-GO-QAC-01) `draft_document_context_claims(source_name, evidence_text, api_key=None, timeout=None)` — GO's own first-pass Document Context claims for Admin Document Mode, grounded only in a Source's already-extracted `EvidenceItem` text (never the model's own world knowledge). Mirrors `services/requirement_investigation.py`/`services/project_qa.py`'s Anthropic pattern via the shared `services.llm_gateway.call_llm_json` (lazy import, same env vars, honest degrade on no-key/timeout/malformed-output — never a fabricated claim). Returns `{"ran","claims":[{"field_kind","statement","directly_evidenced"}],"skipped_reason"}`. | Called by `routes/workspace.py`'s `draft_document_context_claims_route`. Drafted claims persisted via `services.case_workspace.CaseWorkspaceStore.draft_document_context_claim`. |
| `services/quantitative_investigation.py` | (CLAUDE-P40-VW8-QA-R6) A general (not scenario-hardcoded) evidence-guided distance/elevation-difference/slope/clearance feasibility pattern. `looks_like_quantitative_feasibility_question`/`extract_values_from_text` (LABEL-adjacent regex over conversation text — entrance/basement grade elevation, available travel length, driveway width, longitudinal slope — every value keeps its own exact matched quote, `extraction_method="conversation_stated"`, `status="user_provided"`; never a drawing citation, since no OCR/rendering exists in this environment — see this module's own header, same conclusion as `services/drawing_intake.py`'s R2A audit). `compute_feasibility` — the exact vertical-drop/basic-sloped-run/total-required-travel/margin formula, structurally unable to accept a width parameter (never insertable into the slope arithmetic). `build_progress_reply`/`build_calculation_reply`/`build_finding_statement`/`suggested_title` — all reply/Finding text construction. No external-AI call anywhere in this file. | Called by `services/conversation_interpreter.py`'s new `_handle_quantitative_investigation` (dispatch) and its `needs_case` escalation-offer branch (`suggested_title`). Finding creation reuses `CaseWorkspaceStore.record_analysis` unchanged — no new business object. |
| `services/project_briefing.py` | (CLAUDE-P38-B, semantic filters/labels CLAUDE-P38-C, CLAUDE-P38-D2) Narrative-first project opening. `deterministic_sections(candidate_requirements, milestones)` — no-AI grouping of already-extracted `RequirementItem`s by category (Technical/Financial Submission, evaluation, scope) plus `document.milestones`, into a Suggested Reading Path; filtered through `_qualifies_as_technical`/`_qualifies_as_financial`/`_qualifies_as_key_date` (bounded exclusion-always-wins signal patterns, not a classifier rewrite — see the module's own P38-D2 comments for the heading-prefix/background-prose/tightened-inclusion redesign); always available, never fabricates a section the extraction doesn't support. `generate_project_briefing(...)` — real, grounded Anthropic synthesis (Executive Summary/Objectives/Project Brief/Procurement Route/Matters Requiring Early Attention/`grounded_in` verbatim-quoted source excerpts), mirrors `services/requirement_investigation.py`'s/`services/project_qa.py`'s pattern exactly; every field empty rather than guessed when unsupported. | Called by `routes/workspace.py`'s `generate_project_briefing_route`, `preparing_project_briefing` (CLAUDE-P38-D2 automatic-generation interstitial), and `show_workspace`, gated through the same `ACTION_EXTERNAL_AI_REQUEST` policy check (`_evaluate_security_action`/`_project_briefing_ai_status`) as every other real external-AI call site. Result cached on `ProjectWorkspace.project_briefing` via `CaseWorkspaceStore.set_project_briefing`/`source_signature_for` (one prior version preserved as `project_briefing_previous`, not overwritten). |
| `services/rfi_export.py` | `build_rfi_docx(document, operating_environment_label=None)` turns a `ParsedDocument`'s flagged `ConsistencyFlag`s into a real Request for Information `.docx` via `python-docx` — one `RFI-NNN` section per flag with both requirement excerpts and the explanation; stamps the optional CLAUDE-P29 `operating_environment_label` into the metadata block when given. `build_rfi_draft_docx(draft, operating_environment_label=None)` does the equivalent per-Finding governed export; CLAUDE-P30 added a "Workflow Direction" line (question always originated Design-Builder/Proponent-side) and, once `draft.status == "answered"`, a "Response" section (`draft.response_text`/`responded_by`/`responded_at`). Raises `RFIExportError` (message distinguishes "checked, nothing flagged" from "never checked") when there's nothing to export. | Called by `routes/api.py`'s `GET /documents/<id>/rfi` (stamped as of CLAUDE-P30) and `routes/workspace.py`'s `export_rfi`/`export_rfi_draft` (stamped). Consumes `ParsedDocument`/`ConsistencyFlag` from `services/bhive_parser.py`. |
| `services/procurement_publication.py` | (CLAUDE-RFP-BOUNDARY-01) `build_published_package_zip(sources)` — turns the already-selected, already-governed Source dicts `CaseWorkspaceStore.publish_procurement_package` returns into a plain stdlib-`zipfile` export artifact (the Published Procurement Instrument), no manifest file inside it (provenance lives in the publish governance-log event instead, never re-ingestable as a Source). No new persistent storage — streams already-stored files verbatim. Raises `PublicationExportError` when a selected file is missing on disk. | Called by `routes/workspace.py`'s `publish_procurement_package_route`. Consumes Source dicts from `services.case_workspace.CaseWorkspaceStore.publish_procurement_package`. |
| `engine/__init__.py`, `engine/pdf_extractor.py` | (CLAUDE-DRAWING-REFS-01) Headless extraction of a PDF's vector primitives and NATIVE text spans, each with `bbox_points`/`centroid_points`/`font_size_points`/`rotation_degrees` — the coordinates are the point, and are what `pypdf` (text-only) cannot give. `PDFVectorExtractor.stream_pages()` yields one dict per page (page-at-a-time, so a large sheet set is never held whole in memory); `extract_document()` collects them. `schema_version = "pdf_geometry_semantics_v1"` is stamped on the output so a re-extraction under a changed extractor is identifiable rather than silently mixed with older output. Degrades explicitly: `pymupdf` missing raises `RuntimeError` at call time rather than at import, so the app still starts without it. | Deliberately a SEPARATE top-level package, not a `services/` module: it imports nothing from `services/`/`routes/`/`models.py` and knows nothing about `ProjectWorkspace` — geometry extraction is not business logic, and keeping the boundary means the extractor can be tested (and replaced) without the app. Its positioned text spans are what `services/case_workspace.py`'s drawing-token references resolve against. Tested by `tests/test_pdf_extractor.py`. |
| `engine/ifc_volume_validator.py` | (CLAUDE-SPATIAL-COMPILER-01) `IFCVolumeValidator.validate(model)` / `.export(model)` / `.validate_and_export(model)` — checks a parametric model and serialises it to IFC 4X3 STEP text. Validation is the useful half: a space boundary must be CLOSED, have at least four points and NOT self-intersect; heights and wall thicknesses must be positive; an opening must fit inside its host wall's length and height; a space must name a level that exists. `IFCValidationError` (a `ValueError`) on any breach. Emits IFCPROJECT/IFCSITE/IFCBUILDING/IFCBUILDINGSTOREY, IFCSPACE as an IFCEXTRUDEDAREASOLID over an IFCARBITRARYCLOSEDPROFILEDEF, IFCWALLSTANDARDCASE, and door voids as IFCOPENINGELEMENT + IFCRELVOIDSELEMENT. | Pure geometry and text; imports nothing from `services/`/`routes/`/`models.py` and not even from `engine/pdf_extractor.py`. Consumes the model dict `engine/spatial_compiler.py` produces. Tested by `tests/test_ifc_volume_validator.py`. |
| `engine/spatial_compiler.py` | (CLAUDE-SPATIAL-COMPILER-01) The middle stage of the metabolic bridge — `PDFVectorExtractor` -> `SpatialCompiler` -> `IFCVolumeValidator`. `SpatialCompiler().compile(document, schedule=..., project_name=...)` returns the model dict; `compile_to_ifc(document, ...)` runs the whole bridge and returns `(model, ifc_text)`. Public geometry helpers are importable and separately tested: `assemble_loops` (chains raw strokes into closed loops by endpoint adjacency and DISCARDS open chains — sealing one would invent a room the drawing never enclosed), `point_in_polygon` (ray casting, so an L-shaped room binds its label correctly where a bounding-box test would not), `signed_area`/`ensure_ccw` (IFC profile winding), `derive_points_per_foot` (scale by MAJORITY agreement among datum spacings, never the title block's declared scale — on the proving corpus those disagree 18.0 vs 12.0 pt/ft and every derived dimension would inherit the error). Flips Y about page height because the extractor reports `top_left_origin_y_down` and extruding that frame directly yields a MIRRORED building that is still closed, simple, positive-area and exports cleanly — no validator here would catch it. Refuses rather than guesses: `SpatialCompilationError` when no section sheet carries level datums, because there is then no evidence of storey height. Values not dimensioned in the source (wall thickness, fallback door width) are parameters echoed into the model's `assumptions`, and every contradiction, fallback or clamped door position is recorded in `warnings` rather than silently resolved. | Imports `engine.ifc_volume_validator` lazily inside `compile_to_ifc` only. Nothing in `services/`/`routes/` imports `engine/` — it is deliberately a separate top-level package (geometry is not business logic). Tested by `tests/test_spatial_compiler.py`; the drawing-set corpus it compiles is `tests/fixtures/metabolic_bridge/builder_corpus/`. |
| `services/case_workspace.py` (CLAUDE-DRAWING-REFS-01 additions only) | Drawing-token references — a tag on a plan citing a row in a schedule is the same act as a clause citing a Section, so this is new PATTERNS and new TARGETS for the EXISTING `SourceReference` machinery, not a second resolver. `REFERENCE_TYPE_SHEET`/`_DETAIL_CALLOUT`/`_SCHEDULE_MARK`/`_GRID_INTERSECTION` join the open-world `KNOWN_REFERENCE_TYPES`; the closed, evaluator-owned `RESOLUTION_STATUS_*` vocabulary is deliberately NOT extended (an orphan tag is already `target_not_found`). `parse_source_reference_text(text, include_drawing_tokens=False, known_sheets=None)` reads keyword-led forms ("Sheet A-501", "Door D-101", "Grid A/1") and the compound "3/A-501" callout always; a BARE mark ("D-101" alone, how a plan actually annotates) only when the caller opts in, because a sheet id and a schedule mark are the same shape and a specification saying "clause D-101" is not a door. Ordered most-specific-first behind the pre-existing `consumed_spans` guard, so a callout is never split into a bare sheet. TWO drawing findings are deliberately queries over registered data rather than statuses — a duplicate mark (invisible to the resolver: `known_targets` values are SETS, so two rows sharing a mark collapse and resolve `resolved_exact`) and an unreferenced schedule row. | `extract_and_register_source_references(..., include_drawing_tokens=False)` threads the flag through and DERIVES `known_sheets` from `known_targets["sheet"]` itself, so no caller has to repeat what it has already declared. **CLAUDE-DRAWING-REFS-02** added that `known_sheets` precedence rule after a blind audit of a real three-sheet set: each sheet's own number in its title block was read as a door tag and reported as an orphan, six false against one real. A bare token matching a known sheet is now consumed WITHOUT being emitted - a sheet naming itself is not a citation - while keyword-led readings ("Sheet A-101", "3/A-501") are untouched. No status was added; the fix is precedence, because `target_not_found` cannot distinguish a self-naming sheet from a genuine undefined door once the candidate is already typed. Every pre-existing caller passes nothing, so no existing reading moves. Tested by `tests/test_drawing_reference_binding_01.py`. |

## 6. Frontend templates (Jinja2)

| File | Purpose | Connects to |
|---|---|---|
| `templates/_macros.html` | Shared page-geometry macros: `page_header` (title-left/controls-right page header), `accordion` (top-level `<details class="accordion-section">` disclosure), `subdisclosure` (nested "+ Add X" / "View Y" disclosure), `tool_pane` (CLAUDE-SPIN-00A, new — the `<section class="workspace-pane">` + `<h3>` boilerplate every Toolbox pane hand-wrote, extracted for Spin's own use; existing panes left as-is, no forced migration). Introduced to stop these geometries from being hand-copied per template with accidental drift each time. (CLAUDE-P40-BRAND1, corrected) Also `archiosk_mark(size, css_class)` - the ONE shared inline-SVG source for the ARCHIOSK mark: two mirrored, asymmetrical straight-line open angles (short arm, longer leaning leg) whose inner vertices deliberately don't touch, plus one small filled dot below that gap - no curves, no crossbar, `stroke`/`fill="currentColor"` so color is entirely CSS-driven, always `aria-hidden`/decorative. | Imported (`{% import "_macros.html" as macros %}`) by `projects.html`, `case_workspace.html`, `base.html` (the latter for `archiosk_mark` in the top-left header brand link), and `templates/_spin_prototype.html`. Not a page on its own - never rendered directly. |
| `templates/_app_menu.html` | (CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01, Disposition 2/3, new — extracted verbatim from `base.html`; identity/activity added CLAUDE-ARCHIOSK-IDENTITY-ACTIVITY-INDICATOR-01) The application menu bar + breadcrumb/side-indicator region — `.workspace-topbar-identity` (now leading with the stationary `macros.archiosk_mark()` identity mark and the hidden-by-default three-dot working indicator, `#workspace-app-activity`, BOTH structurally outside and distinct from the Archiosk menu item itself), `<nav class="workspace-menubar">` (Archiosk \| File \| Edit \| View \| Document \| Tools \| Window \| Help), the Keyboard Shortcuts panel, and (only within an open Workspace) the breadcrumb/side-indicator. Self-contained `{% import "_macros.html" as macros %}` (never relies on the including template having already imported it — `gateway_shell.html` doesn't). `{% include %}`d verbatim by BOTH `base.html` and `gateway_shell.html` — one canonical markup source, not two menu implementations; Jinja's `{% include %}` shares the caller's own context, so every `is defined`/`is_admin`/`can_publish_procurement_package`/etc. guard already inside it renders honestly for whichever shell includes it (Gateway simply never sets `project_id`/`workspace`/`selected_source`, so the Workspace-only regions and commands correctly render nothing or an honest disabled state there — no Gateway-specific branch was added). | `{% include "_app_menu.html" %}`d by `templates/base.html` and `templates/gateway_shell.html`. Requires `static/js/app_menu.js` loaded by the including page; the activity indicator is driven by `static/js/case_workspace.js`'s own Composer submit handler where that script also loads (case_workspace.html pages only — Gateway's own separate rule-based Ask responder never triggers it, correctly, since it isn't real GO/background work). |
| `templates/base.html` | (CLAUDE-P40-E3A, restructured CLAUDE-APP-MENU-01, menu bar extracted to `_app_menu.html` by CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01) Master layout and application shell. Blueprint-grid backdrop; when `authenticated`: a full-width top bar led by the shared application menu bar (see `templates/_app_menu.html`, above — Archiosk \| File \| Edit \| View \| Document \| Tools \| Window \| Help, every authenticated page, top-left; retires the earlier icon+wordmark brand link, CLAUDE-P40-BRAND1 — Archiosk is now a plain menu entry, styled like its neighbors). The Archiosk menu holds Home/About, the Admin submenu (relocated here from the Account menu, `is_admin` only), the Appearance submenu (relocated here, un-gated — reachable on every authenticated page, not just inside an open Workspace), the Display-Layout submenu (relocated here, still gated to an open Workspace — it drives that Project's own Display grid), and Exit. The remaining seven menus reuse existing controls/routes by id (`data-reuse-control`) or a small named action set (`data-action`, `static/js/app_menu.js`) — never a second command implementation. Breadcrumb/side-indicator sit beside the bar (only within an open Workspace), then `.app-shell-body` holding the one reviewer-wide recursive Lists panel split vertically into `.lists-pane` above a permanent `.thumbnails-pane` (CLAUDE-P40-VW7A-QA2, corrected CLAUDE-P40-LTH1 - Thumbnails is never `[hidden]`, matching Eye's own "permanent pane" treatment relative to Toolbox), `.app-main` (`{% block content %}`), and — only when `project_id`/`workspace` are both defined — the Toolbox aside (`{% block toolbox %}`); a full-width Chat row (`{% block chat %}`, same `project_id`/`workspace` gate) sits beneath. Panel-divider buttons (not top-bar toggle buttons) collapse Lists/Toolbox. Blocks: `title`, `extra_head`, `content`, `toolbox`, `chat`, `extra_scripts`; only `case_workspace.html` fills `toolbox`/`chat`, every other template leaves them absent. An EARLIER, unrelated stage also tagged CLAUDE-P40-VW7B ("generalize active-Display projection; relocate a misplaced admin control" — git `a61a7b8`/`9a5c11b`) added the OWN separate inline click-interceptor script that routes a Document/Investigation/Overview Lists-leaf click into whichever Display division is the active target (`window.ArchioskDisplay`) — unrelated to, and untouched by, the CURRENT CLAUDE-P40-VW7B stage ("Foreground Project Vestibule and Four-Position Investigation Attention Model") described next: `nav_recent_projects` and the whole portfolio "Projects" root/"+ New Project"/"Removed Projects" branch now render ONLY when no Project is open (`{% else %}` of a new `project_id is defined and workspace is defined` check) — an OPEN Project's own Lists shows ONLY that Project's own family branch, composed server-side, never CSS-hidden. The Foreground Project's own breadcrumb segment (`menu.context.switch-project`) is now a real link into the Project Vestibule (`portal.choose_project?current=<id>`, see `templates/project_chooser.html` below). The old CLAUDE-P40-VW8 Project-switching interruption dialog (`#project-switch-dialog`) and its click-interceptor are removed outright — dead code once its only trigger (`lists.projects.leaf` inside an open Project) stopped rendering; its CSS classes were renamed (`.attention-capacity-dialog*`) and reused for the new fifth-Investigation capacity dialog instead of being duplicated (see `static/js/investigation_attention.js` below). | Extended by every other template EXCEPT `case_workspace.html` when rendered with `panel_only=True` (see `templates/panel_shell.html` below). Links use `url_for('portal.index'\|'logout'\|'portal.upload'\|'portal.removed_projects'\|'security.department_home'\|'portal.choose_project')`. Pulls in `static/css/main.css`, `static/js/case_workspace.js` (only on pages that set it via `extra_scripts`). |
| `templates/panel_shell.html` | (CLAUDE-P40-VW7B) A minimal standalone document — CSS links, CSRF meta + auto-inject, a `.app-main`-scoped Appearance-mode script, `{% block content %}`/`{% block extra_scripts %}` — with no Menu/Lists/Toolbox/Chat. `case_workspace.html` extends this instead of `base.html` when `routes/workspace.py`'s `show_workspace` sets `panel_only=True` (a `?panel=1` request), so an Investigation/Overview's Division-0 content can be embedded via a plain `<iframe>` into a non-zero Display division client-side (`window.ArchioskDisplay.populateDivision`) — the same "project into the active Display" outcome a Document already got (a real file, `<iframe src=file_url>`/`<img>`, unaffected), extended to a record that isn't a static file. | Extended by `case_workspace.html` only when `panel_only` is true. See `UI_REFERENCE_MAP.md`'s own Display section for the full mechanism. |
| `templates/index.html` | Authenticated-only project entry point (CLAUDE-P40-VW5: `routes/portal.py:index` now redirects an unauthenticated visit to `/login` before this template is ever reached — its own former anonymous "Sign in to get started" branch is gone): one "Open Project" action. | Extends `base.html`. Rendered by `routes/portal.py:index`. |
| `templates/login.html`, `templates/forgot_password.html`, `templates/reset_password.html` | Sign-in form (username/password, generic invalid-credentials message) and the password-reset flow, each styled as a centered `.gateway-card.gateway-card-compact` — logo/brand lockup, section label, geometry and turquoise background matched to the archiosk-explorer (port 5173) welcome card. | Extend `templates/auth_shell.html` (CLAUDE-P40-D1 — a genuinely standalone shell, never `base.html`/`gateway_base.html`; see that file's own comment). Rendered by `routes/portal.py:login`/`forgot_password`/`reset_password`. |
| `templates/gateway_shell.html` | (CLAUDE-P40-VW5, menu bar added CLAUDE-UI-ACTION-REDUNDANCY-REVIEW-01) Standalone Project Gateway shell — own `<!doctype html>`, the SAME shared application menu bar every authenticated page gets (`{% include "_app_menu.html" %}`, replacing the old standalone `.workspace-topbar-brand` text link outright — Disposition 3) + an account menu (Sign out, Removed Projects — admin-only Security relocated out, now covered by the shared menu's own Archiosk > Admin > Security), no Lists panel/Toolbox/Display/Chat. Fixes the defect where Gateway inherited `base.html`'s Lists panel (gated only on bare `authenticated`, unlike Toolbox/Chat/Display-Layout). `app.py`'s `inject_globals()` also skips the `nav_recent_projects` store query for this route specifically. Its own former `#workspace-user-menu`-specific outside-click/Escape IIFE was removed too — `static/js/app_menu.js` (now loaded here) already covers every `<details>` inside `.workspace-topbar` generically, the same redundant-duplicate cleanup applied to `base.html`'s own equivalent old script. | Extended by `gateway_base.html`. Links use `url_for('portal.index'\|'removed_projects'\|'logout'\|'security.department_home'\|'portal.about')` (the latter two via the included menu). Requires `static/js/app_menu.js`. |
| `templates/gateway.html` | (CLAUDE-GO-NEUTRAL-ENTRY-01, replacing the earlier CLAUDE-P29/CLAUDE-CA1D-PROJECT-GATEWAY-LABELS-01 two-door layout) Post-login landing page: ONE neutral entry — a single admin-only "New Project" action (`portal.upload`, no `?environment=` preset) and one unfiltered "Open Existing Project" inline reveal over every authorized project regardless of operating environment. The Client/Owner-vs-Design-Builder/Proponent front-door split is gone — `operating_environment` is unchanged as a real, governed, project-level fact, still required and locked at `/upload`'s own commissioning step, it just no longer partitions the Gateway's own project list. Extends `gateway_base.html`, which (CLAUDE-P40-VW5) now extends `gateway_shell.html`, not `base.html`. | Rendered by `routes/portal.py:gateway`. Links to `portal.upload`/`portal.choose_project` (CLAUDE-P40-VW8-QA — was `portal.projects_list`, see below). |
| `templates/about.html` | (CLAUDE-APP-MENU-01, new) The Archiosk menu's own "About" destination — honest, static facts already shown elsewhere (`gateway.html`'s own footer text: "Flat-JSON registry" / "Static build v…"), no fabricated version/build metadata, a link back to Archiosk Home. | Extends `base.html`. Rendered by `routes/portal.py:about` (`/about`, `@login_required`). |
| `templates/project_chooser.html` | (CLAUDE-P40-VW8-QA, Section 12; extended CLAUDE-P40-VW7B) Focused existing-Project picker, now ALSO the Project Vestibule (Section 4) — the least-disruptive repository-compatible choice, reused rather than a new page built. Search box + one card per accessible Project (excluding the current one), no counts/sort/Delete-forms. Extends `gateway_base.html` (same minimal shell Gateway itself uses), deliberately not `base.html` — never shows the full authenticated Lists workspace, Documents, Findings, or conversation content of any Project (Section 4's own "do not turn this into a second working dashboard"). VW7B addition: an optional `?current=<project_id>` (set only by `menu.context.switch-project`, never a new persisted "current project" concept) renders a "Current Project" section above "Available Projects," and a "Removed Projects" link out to the real, already-governed page (`portal.removed_projects`) — never duplicated inline. `projects.html`/`projects_list` are unchanged and remain the separate, still-reachable "administrative management" destination. | Rendered by `routes/portal.py:choose_project` (`/projects/choose`), reusing `_accessible_documents`/`_safe_workspace` — no new authorization surface; `current` is resolved through that SAME already-filtered list, never a second lookup. |
| `templates/upload.html` | Upload form (`multipart/form-data`, file input restricted to `.pdf/.docx/.txt/.csv/.md`), a required Project Operating Environment radio fieldset + client-side confirmation checkbox (CLAUDE-P29 — real enforcement is server-side via `is_valid_operating_environment`, this is UX-only), plus optional free-text `actor`/`role` fields for the governance audit trail. Displays `max_upload_mb` (CLAUDE-P40-VW8-QA, Upload-Capacity Correction) and an inline `error` message on rejected uploads, plus (CLAUDE-P40-VW8-QA-R2A) client-side pre-submit file-size validation. | Extends `base.html`. Rendered by `routes/portal.py:upload` (GET and the 400 error path). A POST with a drawing-like candidate detected, or no native text at all, redirects to `upload_confirm.html` below instead of ingesting directly. |
| `templates/upload_confirm.html` | (CLAUDE-P40-VW8-QA-R2A) The "machine proposes, human confirms or corrects" step — one editable field per `services/drawing_intake.py`'s `CANDIDATE_FIELDS`, each showing its evidence (confidence/page/extraction method/matched line) when a candidate was found; an explicit radio choice (never a silent default) when the user-entered and drawing-derived Project names conflict; an honest notice when no native text existed at all (image-only/scanned PDF) instead of failing the whole upload. Confirm creates the Project via the same `ingest_upload` every other path uses, using the reviewer's confirmed/corrected values; Discard removes the staged upload. | Extends `base.html`. Rendered by `routes/portal.py:upload_confirm` (`/upload/confirm/<staging_id>`) — **admin only**, same gate as `/upload`. |
| ~~`templates/dashboard.html`~~ | **Retired.** Case Workspace (`case_workspace.html`) absorbed every real piece of content this had (extracted/governed Requirements, the actual consistency-flag cards in RFI Export, audit trail in History) except the milestone lattice, which was dropped outright - confirmed non-functional for real projects (`_derive_milestones` in `bhive_parser.py` only ever produces `status="pending"`; "done"/"active" only ever appeared in this route's own hardcoded demo data). `routes/portal.py`'s `dashboard()` is now a redirect into Case Workspace, kept so old bookmarks/links still land somewhere real. | n/a |
| `templates/errors/error.html` | The one dead-end error page (403/404/500 share it - same shape, different `code`/`heading`/`message`/`action_url`/`action_label`). | Extends `base.html`. Rendered by `app.py`'s `_render_error` helper from the 403/404/500 handlers, non-API requests only. |
| `templates/confirm_base.html` | Confirm-page family base: one centered card, one question, one action set, one way back. Blocks: `confirm_title`, `confirm_label`, `confirm_heading`, `confirm_body`, `confirm_actions`, `confirm_back_url`, `confirm_back_label`. | Extends `base.html`. Extended by `confirm_action.html` (Approval Gate) and `confirm_delete_project.html`. |
| `templates/preparing_project_briefing.html` | (CLAUDE-P38-D2) The "Preparing your Project Briefing…" interstitial — a real, visible, auto-submitting page (not a hidden background job; see `services/project_briefing.py`'s manifest row and this stage's own checkpoint entry for why synchronous generation behind a visible waiting page was the only option this deployment's process model allows). Auto-submits a POST to `workspace.generate_project_briefing_route`; the same "Continue" button works with JavaScript disabled. | Extends `base.html`. Rendered by `routes/workspace.py`'s `preparing_project_briefing`, reached from `routes/portal.py`'s `upload` route. |
| `templates/security_department.html` | (CLAUDE-P31) The Security Department workspace — mandatory-floor/claims-registry display, source policies + statements + proposed controls, governed Q&A, baseline lifecycle, exceptions, per-project security profile, learning-contribution requests, assurance activity feed + self-check findings. Reuses `_macros.html`'s `accordion`/`subdisclosure`, the same plain-form-POST pattern as `case_workspace.html`. | Extends `base.html`. Rendered by `routes/security.py`'s `department_home`. `templates/base.html`'s side-rail nav links to it (`is_admin` only). |
| `templates/_spin_prototype.html` | (CLAUDE-SPIN-00A comparison prototype, grammar finalized CLAUDE-SPIN-01) Spin's one canonical container: Alternative A (Compact Control Stack) as selected by the Product Owner, plus one incorporated characteristic from Alternative B (`.spin-status-strip` — the status badge and active-set summary paired in one header block, readable without scrolling). B's physical Engaged/Not-Engaged row regrouping and C's always-visible engagement-sequence display were both explicitly not adopted; the dev-only A/B/C switcher and the two unselected variants were removed. Built from shared primitives (`macros.tool_pane`, `.tool-control-row`, `.tool-toggle`, `.review-state-badge`, `.active-set-summary`, `.gauge-slot`). Pure client-side prototype state (`static/js/spin_prototype.js`) — no real evidence filtering, no persistence, never touches `operating_environment` or any real project fact; `SPIN_PROTOTYPE_DISCIPLINES` (route-level, deliberately noncanonical) is the only data shown. | `{% include %}`d by `templates/case_workspace.html`'s `{% block toolbox %}` when `spin_mode` is true. Rendered by `routes/workspace.py:show_workspace` (`?spin=1`). |
| `templates/operations.html` | (CLAUDE-CA1D-INSTRUMENT-RAIL-01) The Operations page — two small accordions: System Telemetry (`services.diagnostics.TechnicalTelemetry` fields) and the AI Calls Kill Switch (`AI_CALLS_DISABLED` live state). Deliberately minimal — the smallest proof that a previously-unwired backend diagnostic primitive has a legitimate peripheral, admin-only home; not the full Instrument Rail programme. | Extends `base.html`. Rendered by `routes/operations.py`'s `department_home`. `templates/base.html`'s Lists admin branch links to it (`is_admin` only, beside Security); the top-bar quiet status line (see `main.css`/`base.html` below) also links here for detail. |

## 7. Static assets

| File | Purpose | Connects to |
|---|---|---|
| `static/css/main.css` | The full design system: color tokens (`--ink`, `--panel`, `--amber`, `--teal`, etc.), typography, header/nav, hero, pipeline cells, dashboard header, honeycomb lattice, registry table, chips, error page. | Linked from `templates/base.html`; every template's markup is written against these class names. |
| `static/js/pdf_viewer.js` | (CLAUDE-P40-VW7A-QA, extended CLAUDE-P40-VW7A-QA2, CLAUDE-P40-LTH1) Thin adapter driving vendored PDF.js directly (canvas render, page/zoom/rotation/search state) - the top-menu document controls' own real behavior lives here, not a bundled PDF.js UI. Auto-mounts against `#document-viewer-pdf-canvas` when present (a full-page-reload app - no client router needed to detect "the active document changed"). VW7A-QA2 added: lazy per-page thumbnail rendering (`#thumbnails-list`, `IntersectionObserver`), a real, client-side-only PDF annotation overlay (text/highlight/ink, select+delete, undo/redo - see `templates/base.html`'s own `#doc-annotate-*` controls; deliberately no Save/Export, a disclosed scope boundary, not an oversight - no PDF-writing library is vendored), and a visible load-error state (`showLoadError`) replacing a prior silent console-only failure. LTH1 added: `mountRememberedThumbnailsIfAny()` - when no Document is selected at all on the current page, populates `#thumbnails-pane` from a client-side-remembered last-viewed PDF Source for this Project+reviewer (`localStorage`, revalidated against `#workspace-active-sources-data`'s new `is_pdf` field every load, never an arbitrary first-Document guess); a thumbnail click in that mode navigates to the Document route instead of calling `goToPage()` (no live canvas exists on that page). | Loaded by `templates/case_workspace.html`; drives `templates/base.html`'s own `#workspace-document-controls` region and `#thumbnails-pane`; reads `static/js/vendor/pdfjs/`. |
| `static/js/vendor/three/` (`three.module.js`, `OrbitControls.js`, `LICENSE`, `README.md`) | (CLAUDE-SPATIAL-VIEWPORT-01) Vendored (not npm-installed — no client build step) Three.js `r160` ES modules, used as downloaded. That directory's own `README.md` records version, upstream tag, licence and a SHA-256 for each file; both were verified to match before tracking. The ONE upstream modification is in `OrbitControls.js`: its bare `three` specifier became `./three.module.js`, because a browser ES-module loader has no bare-specifier resolution without an import map — a path change, never a behavioural one. `LICENSE` was added here: `three.module.js` carries an SPDX header but `OrbitControls.js` carries no attribution at all, and MIT requires the permission notice travel with redistributed copies (same reason `vendor/pdfjs/LICENSE` exists). | Imported only by `static/js/spatial_viewport.mjs` via local relative paths. No CDN, no runtime network fallback: the three `fetch(` calls in the vendored build live in `FileLoader`/`ImageBitmapLoader`, which the viewport never instantiates. Served under nginx's `/static/` block, whose `default_type application/javascript` is what makes `.mjs` executable at all (stock `mime.types` has no `.mjs` entry — see `deploy/nginx.conf`). |
| `static/js/spatial_viewport.mjs` | (CLAUDE-SPATIAL-VIEWPORT-01) `SpatialViewport` — renders the model `engine/spatial_compiler.py` produces as an interactive 3D volume (extruded space shapes per storey, walls split around door openings with lintels above them, orbit controls). `setData(model)` accepts the compiler's own dict shape directly, including both `{x, y}` and `[x, y]` point forms, and enforces hard caps (500 spaces / 1000 walls / 10000 vertices) rather than letting a malformed model exhaust the browser. Room and level labels are rasterised through `ctx.fillText()` onto an offscreen canvas used as a `CanvasTexture` — never `innerHTML` and never a DOM node, so a room name cannot carry markup into the page. | Client-side only; imported by no template yet. When it IS wired in it must load via `<script src=...>` or carry `nonce="{{ csp_nonce }}"` — the app's CSP is `script-src 'self' 'nonce-...'`, which permits same-origin modules and their static imports but blocks an un-nonced inline module. `tests/fixtures/spatial_viewport_test.html` is a standalone fixture (inline module, opened directly, not served by Flask). |
| `static/js/vendor/pdfjs/` (`pdf.min.mjs`, `pdf.worker.min.mjs`, `LICENSE`, `README.md`) | Vendored (not npm-installed - no client build step), pre-built ES-module PDF.js distribution files, used as downloaded. CLAUDE-P40-VW7A-QA2 switched the two `.mjs` files from the modern `build/` distribution to `legacy/build/` (broader browser/JS-engine compatibility, the leading candidate fix for a real-browser report that the header controls never appeared at all). See that directory's own `README.md` for exact version/source/license and why only the low-level rendering API files were taken (not PDF.js's own bundled UI). | Loaded via dynamic `import()` from `static/js/pdf_viewer.js` only. |
| `static/js/eye_pane.js` | (CLAUDE-P40-EYE1, extended by a same-stage browser correction) The new Eye pane's own structural-scaffold interactivity - a real paste/drop target (`#eye-drop-target`): `dragover`/`drop`/`paste` events genuinely captured, an image read via `FileReader` and handed to a real responsive zoom/pan viewing canvas (`#eye-canvas` - Fit/zoom-in/zoom-out/Actual-size/Reset/Remove, focused-wheel zoom, native-scroll panning, a `ResizeObserver` that re-fits on any container resize/maximize while still in Fit mode) - never sent anywhere (no `fetch()`/`XMLHttpRequest`), an inline error for non-image input. Deliberately no editing/annotation/attach-to-Chat/AI-interpretation/persistence - EYE1's own explicit scope boundary, deferred to EYE2. | Loaded by `templates/base.html` directly (`<script src>`, not via `case_workspace.html`'s `{% block extra_scripts %}`, since the Eye pane's own markup is shell-owned, gated the same `project_id`/`workspace`-defined condition as Toolbox/Chat). |
| `static/js/spin_prototype.js` | (CLAUDE-SPIN-00A comparison prototype, simplified CLAUDE-SPIN-01) Drives `templates/_spin_prototype.html`'s one canonical grammar: per-discipline ON/OFF, All ON/All OFF/Return to Baseline, active-set summary text, Baseline-vs-Spin-Active status badge. Still privately tracks each discipline's engagement ORDER (harmless, unsurfaced — the Product Owner's own "preserve... but do not surface" instruction, kept for a future GO capability). No `fetch`/`XHR` anywhere in the file — never calls a route, never persists, never touches real project data. No-ops immediately (early return) unless `.spin-status-strip` exists in the DOM. | Loaded by `templates/base.html` (same "always present, no-op unless its own markup exists" idiom as `eye_pane.js`), immediately after `eye_pane.js`. |
| `static/js/document_structure_registration.js` | (CLAUDE-MM9, new) A lazy, per-Source "Register this document"/"Register this workbook" widget for MM2 (PDF) and MM3 (spreadsheet) Sources — mirrors `drawing_image_viewer.js`'s own MM4 registration-button pattern. Checks `GET /api/v1/documents/<project_id>/structural-units`/`.../evidence` for an existing registration; if none, POSTs to the existing, unmodified `/pdf-structure`/`/spreadsheet-structure` routes and re-renders a citable-count summary. No new backend route, no new domain object — closes a real gap where those already-tested MM2/MM3 routes had no UI trigger anywhere in the app. | Loaded by `templates/case_workspace.html`'s own `{% block extra_scripts %}`; mounts on `[data-document-structure-registration]`, one per PDF/`.xlsx` Source detail pane. |
| `static/js/files_folder_menus.js` | (CLAUDE-P40-VW9A, new) The Design-Builder Workspace's per-folder-row "..." action `<details>` menus (`.files-folder-actions`, `templates/case_workspace.html`) as an exclusive group with click-outside/Escape dismissal - adapts `document_tabs.js`'s own document-level dismissal pattern to NATIVE `<details>`/`<summary>` elements rather than rebuilding a synthetic body-appended menu (those hold real `<form>`s posting to rename/move/delete, unlike `document_tabs.js`'s own plain-button menu), so native keyboard/screen-reader semantics are never touched - only exclusivity and outside-dismissal are added. Full-page-reload architecture (no MutationObserver needed): folder rows are fixed at page-load time. | Loaded by `templates/case_workspace.html`'s own `{% block extra_scripts %}`, unconditionally (works inside a `panel_only` multi-Display division too, unlike `document_tabs.js`). |
| `static/js/document_tabs.js` | (CLAUDE-P40-DTAB1) The Document tab strip's own logic - Documents only, Division-0-only. Builds every tab from `#workspace-active-sources-data` (authorized/Project-scoped) cross-referenced against `localStorage` (pinned/hidden tabs, per-user per-Project) and `sessionStorage` (the one replaceable preview tab). Every tab is a real `<a href="?source=<id>">` - activating one is a genuine page navigation, never client-side routing. Preview-to-pinned conversion, rename/alias (with duplicate/empty validation), curated color, hide/unhide with MRU-visible-tab fallback, close/close-others, roving-tabindex keyboard tablist semantics. No new backend endpoint, no schema change - a pure client-side workspace preference. CLAUDE-P40-VW8 (Governed Display Tab System - distinct from the earlier "CLAUDE-P40-VW8"/"CLAUDE-P40-VW8-QA" stage, see this file's own note on `investigation_attention.js` below): `activateFallback`'s own final fallback (before the empty Display state) now also consults `investigation_attention.js`'s exposed `mostRecentAttended` lookup, treating this file's own Document tabs and that file's own Attention Positions as one governed close-fallback pool. | Loaded by `templates/case_workspace.html` (only outside a `panel_only` render) BEFORE `pdf_viewer.js` and `investigation_attention.js`; `pdf_viewer.js`'s own per-tab viewer-state persistence (page/zoom/rotation/scroll/search) is a separate, independently-keyed `localStorage` mechanism in that same file, not owned by this one. |
| `static/js/app_menu.js` | (CLAUDE-APP-MENU-01, new) The top-left application menu bar's own generic behavior - Escape closes the deepest open `<details>` popup, opening one closes every sibling that isn't its own ancestor, an outside click closes whatever is open (none of which native `<details>` provides on its own); `syncMenuState()` mirrors each `[data-reuse-control]` item's enabled/disabled and `aria-pressed` state from the real control it targets, re-derived every time any menu opens, never a second flag; `[data-reuse-control]` items click the real target by id, `[data-action]` items call one of a small named set of real actions (close-active-tab, open-publish-panel, open-document-context, open-admin-document-understanding, open-hidden-tabs, close-all-tabs, toggle-fullscreen, open-keyboard-shortcuts) — no parallel command implementation anywhere in this file. Exit is guarded by a plain `confirm()` only when real in-progress input is detected (non-empty Composer text, an open tab-rename input, a mid-stroke annotation tool). | Loaded by `templates/base.html` directly, outside the `project_id`/`workspace`-defined gate (the menu bar itself renders on every authenticated page); reads/writes `document_tabs.js`'s exposed `window.ArchioskDocumentTabs`. |
| `static/js/workspace_trays.js` | (CLAUDE-MOBILE-FRAME-02, new) The common panel-state API `CIC-PANEL` v1.0 listed as a KNOWN LIMITATION, and the mobile workspace frame it exists to serve: a fixed compact header, ONE active work tray, and a persistent bottom Composer. Owns exactly one new state — `data-tray-focus` on `<html>`, naming which of Lists/Display/Eye/Toolbox (NPT-002/003/005/006) holds the work area — plus the phone-side work/Composer boundary. Deliberately extends rather than replaces: COLLAPSED stays `html.launcher-hidden`/`html.toolbox-hidden` (the panel dividers in `base.html`), NORMAL stays LAY-5A in `main.css`, sizing stays `window.__chatSplitter`'s single `--chat-height` write point in `case_workspace.js`, and the Eye/Toolbox maximize buttons stay intra-column proportion controls. Also owns the phone menu drawer toggle (`html.mobile-nav-open`, revealing the same eight-menu `<nav>` rather than a duplicate mobile menu) and publishes `--kb-inset` from `visualViewport` so the Composer stays above the iOS software keyboard. Presentation state only — it has no fetch/XHR/form path at all, persists one reviewer-wide preference (`beehive:tray:focus`), and cannot reach a route; guarded by `tests/test_mobile_frame_02.py`. Exposes `window.ArchioskTrays`. | Loaded by `templates/base.html` directly, alongside `app_menu.js` and outside the `project_id`/`workspace` gate (the tray switcher renders on every authenticated page). Self-guards throughout — a switcher button whose tray is absent removes itself — so it is a safe no-op on a project-less page. Reads `window.__chatSplitter` (published later by `case_workspace.js`) lazily, so load order between them does not matter. |
| `static/js/investigation_attention.js` | (CLAUDE-P40-VW7B, new) The Investigation Attention strip's own logic - the same architecture as `document_tabs.js` above, deliberately mirrored (up to four Case ids held in attention, `localStorage`, keyed by username + Project id, revalidated on every load against `#workspace-visible-cases-data` - never an arbitrary first-Investigation guess). "Focused" is not tracked here at all - it is simply whichever Case `?case=` already names (`data-active-case-id` on `#attention-strip`), the same "no new persisted state" reasoning `templates/base.html`'s own Foreground Project comment gives one level up. Capacity enforcement (max 4) happens via POST-LOAD reconciliation on every workspace render, not a pre-click interceptor (deliberately, to avoid entangling with the UNRELATED, pre-existing Display-division click-interceptor also in `base.html` - see that file's own note on the two different "VW7B" stages) - this uniformly covers every real entry path (a click, a bookmark, Back/Forward, a refresh, `create_case`'s own redirect) rather than only the common one. When full, opens `#attention-capacity-dialog` (release one, immediate and client-side-only; or conclude one via the real, already-existing, owner-or-admin-gated `workspace.archive_case` route, extended with an optional `next_case` form field so concluding lands back on the Investigation the reviewer actually meant to open, not the one just archived; or cancel, navigating back to Overview). Releasing an Investigation from attention never deletes/archives/closes it - a pure `localStorage` membership change. CLAUDE-P40-VW8 (Governed Display Tab System - a DIFFERENT stage from the earlier, already-shipped "CLAUDE-P40-VW8"/"CLAUDE-P40-VW8-QA", flagged per this repo's tag-collision discipline): added Space-key activation to `onPositionKeydown` (parity with `document_tabs.js`'s own `onTabKeydown`) and exposed a new read-only `mostRecentAttended(excludingId)` lookup on `window.ArchioskInvestigationAttention`, consulted by `document_tabs.js`'s own close-fallback (see that file's entry above) - never a change to release's own "never navigates" guarantee. | Loaded by `templates/case_workspace.html` (only outside a `panel_only` render) alongside `document_tabs.js`; reads `#workspace-visible-cases-data` (`routes/workspace.py`'s `show_workspace`) and posts to `workspace.archive_case`. |
| `templates/sw.js` | (CLAUDE-MOBILE-PWA-01, new) The service worker, rendered through Jinja and served from the site ROOT by `routes/portal.py:service_worker` — scope is the URL path a worker is served from, so a `/static/sw.js` could only ever control `/static/*`. Written to be as close to inert as a worker can be while still making ARCHIOSK installable: the cache name carries `STATIC_VERSION` (so a new deploy cannot read the old deploy's cache), obsolete caches are deleted on `activate`, HTML is NEVER served cache-first, and the only URLs ever written to a cache are versioned `/static/` ones — every page, project, document and API call is passed straight through to the network untouched, which is why authentication/authorization are unaffected. | Rendered by `routes/portal.py:service_worker`; consumes `static_version`; messaged by `static/js/pwa.js`. Tested by `tests/test_mobile_pwa_icon_01.py`. |
| `migrations/versions/a3f1c07d92b4_diagnostic_reports.py` | (CLAUDE-DIAGNOSTIC-BRIDGE-01, new) Creates `diagnostic_reports`. Hand-written for the same reason the two revisions before it were: `app.py`'s `_register_database()` always runs `db.create_all()` first, so autogenerate never sees a diff. Records in the migration itself WHY the database rather than the flat-JSON store: Reset/Restore renames the whole registry directory away, and a diagnostic about the APPLICATION must not die because somebody reset a PROJECT. Carries no `server_default` - the model uses a Python-side default, and `tests/test_flask_migrate_baseline.py` asserts this DDL matches `db.create_all()` exactly (it caught that divergence). | Revises `d67fbff1ba5e`. |
| `templates/manifest.webmanifest` | (CLAUDE-MOBILE-PWA-01, new) What makes ARCHIOSK addable to a phone home screen — `start_url`/`scope` `/`, `display: standalone`, `orientation: portrait`, Deep Ocean `#0b1f28` background/theme, and the icon set (192/512 PNG `any`, 512 PNG `maskable`, SVG fallback). Jinja-rendered so icon URLs carry `?v=STATIC_VERSION`. | Rendered by `routes/portal.py:web_manifest`; linked by the four shells. Tested by `tests/test_mobile_pwa_icon_01.py`. |
| `templates/diagnostics.html` | (CLAUDE-DIAGNOSTIC-BRIDGE-01, new) The admin read-only view of captured problems, and Developer Mode's first visible destination - Developer Mode is a session flag toggled from Archiosk > Admin, so it had no page to land on, which is why it was hard to find. READ-ONLY completely: no form and no action that investigates, changes code or deploys, because those are separate acts and the cleanest way to keep them separate is for this surface to have no mechanism for any of them. Does not claim anything was transmitted. | Rendered by `routes/portal.py:list_diagnostics` (`@admin_required`). Tested by `tests/test_diagnostic_bridge_01.py`. |
| `static/js/pwa.js` | (CLAUDE-MOBILE-PWA-01, new) Registers the worker and, when a genuinely NEW worker takes over, OFFERS a reload — never performs one. A page that reloads itself can discard a half-typed Composer message, and on a phone the reviewer may be standing on site with one hand. Suppresses the notice on first-ever registration (no previous controller means an install, not an update), and a registration failure can never break the page. | Registers `templates/sw.js`; renders `.update-notice` (styled in `static/css/main.css`); loaded by `landing_shell.html`/`auth_shell.html`/`gateway_shell.html`/`base.html`. Tested by `tests/test_mobile_pwa_icon_01.py`. |
| `static/js/draft_assist.js` | (CLAUDE-COMPOSER-DRAFT-ASSIST-01, new) The pen beside the Composer. Revealed only while the draft has text and hidden again when emptied. **The draft is never overwritten**: the proposal renders beside the original and EXACTLY TWO lines in this file write to the textarea, both inside click handlers on buttons the reviewer pressed - a test asserts that count so a third cannot appear quietly. The comparison is the safety mechanism, not a layout preference: a model that drops a qualification or firms up a hedge produces prose that reads better and says something different, and only seeing both catches it. Replace is hidden entirely for a reporting action. Every failure path says "Your draft is untouched". | Posts to `workspace.composer_draft_assist`; renders `.composer-pen-*` (`static/css/main.css`), markup in `templates/_macros.html`. Tested by `tests/test_composer_draft_assist_01.py`. |
| `static/js/sonic_cue.js` | (CLAUDE-LANDING-SONIC-01, new — EXPERIMENTAL pending Product Owner listening) The ~1.4s ARCHIOSK startup cue, SYNTHESISED from three oscillators rather than loaded from a file: nothing to fetch, decode or cache, and the sonic identity stays reviewable in a diff as numbers. 110 Hz ground → a 196→587 Hz glide behind a filter opening 700→4200 Hz → 880 + 1318.5 Hz together (an open fifth; deliberately no third, which would make it a fanfare). Loaded ONLY by `landing_shell.html`, so it structurally cannot fire during Composer use. Plays at most once per session (`sessionStorage`), never loops, is skipped entirely under `prefers-reduced-motion`, and constructs no `AudioContext` at all while muted. On iOS a cold launch CANNOT sound — an AudioContext is created suspended and resumes only inside a real user gesture — so it tries, and if the platform refuses it arms and plays at the first real tap; there is deliberately no silent-buffer unlock trick. Touches no `speechSynthesis` and requests no microphone: it is unrelated to `static/js/voice_input.js`, and must never become the automatic spoken welcome retired in `aec1b04`. | Renders `.landing-sound-toggle` (`templates/landing.html`, styled in `static/css/landing.css`). Tested by `tests/test_landing_sonic_01.py`. |
| `static/app-icon.svg` | (CLAUDE-MOBILE-ICON-01, new — EXPERIMENTAL pending Product Owner visual acceptance) The application icon. Product Owner geometry: based on an X, **bottom portion closed**, upper-left portion shorter. A FILLED OUTLINE, not a stroked line, and GENERATED by `tools/render_app_icon.py` — do not hand-edit. The centreline walks short upper-left arm → waist → bottom-left foot → closing base → bottom-right foot → back through the waist → long upper-right arm, which is what turns four open arms into a base the form stands on and what keeps it legible when downscaled. It is offset into a real polygon in code because the Product Owner's later direction — "cut the ends with a knife, the left one horizontally and the right one vertically, and make the edges of the base angles sharp" — is not expressible as a stroke: SVG's three line caps are all PERPENDICULAR to the stroke, and both free ends here are diagonal, so no cap setting can give one a horizontal termination and the other a vertical one. Mitre joins make the feet sharp as real geometry rather than a renderer setting. Reconciles with — rather than replaces — the existing `archiosk_mark` in `templates/_macros.html`: same waist point, same two-arms-from-a-centre construction. | Source of truth for `static/icons/*.png`; used by `templates/landing.html`'s brand composition and precached by `templates/sw.js`. Geometry asserted by `tests/test_mobile_pwa_icon_01.py`. |
| `static/icons/app-icon-180.png`, `app-icon-192.png`, `app-icon-512.png`, `app-icon-maskable-512.png` | (CLAUDE-MOBILE-ICON-01, new) Raster renderings of `static/app-icon.svg` at the sizes home screens actually request. PNG is a REQUIREMENT, not a convenience: iOS ignores SVG for `apple-touch-icon` and falls back to a screenshot of the page. The maskable variant insets the mark ~10% because Android crops to its own mask. Generated by `tools/render_app_icon.py`, which emits `static/app-icon.svg` AND all four PNGs from ONE centreline definition — so the vector and the rasters cannot disagree, and a test asserts the committed SVG still matches what the generator produces. | Declared in `templates/manifest.webmanifest`; `-180` is the `apple-touch-icon` in all four shells. Tested by `tests/test_mobile_pwa_icon_01.py`. |

## 8. Deployment (infra config, not imported by Python)

| File | Purpose | Connects to |
|---|---|---|
| `deploy/nginx.conf` | Reverse-proxy site config for archiosk.com. HTTP→HTTPS redirect, TLS server block, `/static/` alias with far-future immutable caching (safe only because of `STATIC_VERSION`'s `?v=` cache-busting — see `config.py`), security headers (HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and a `Content-Security-Policy` of `default-src 'self'` with no exceptions — the app has no inline styles/scripts or external resources) on every response — repeated inside `location /static/` since nginx stops inheriting a parent's `add_header` once a location defines its own — dedicated quiet `/health` location (tight timeouts, no access log) for external monitors, upstream passive health check (`max_fails=3 fail_timeout=30s`) against the single gunicorn backend, and `location /`'s `proxy_read_timeout`/`proxy_send_timeout`/`client_body_timeout` (150s/120s/120s) sized to the classify + consistency-check budget. | Proxies to `127.0.0.1:8000`, matching `deploy/gunicorn.conf.py`'s default `bind`. The `/health` location targets `routes/portal.py`'s `/health` route. `client_max_body_size 25M` mirrors `MAX_UPLOAD_MB` in `config.py`; `proxy_read_timeout` mirrors `GUNICORN_TIMEOUT`. |
| `deploy/gunicorn.conf.py` | Gunicorn worker settings: bind address, `worker_class` (`gthread`, not `sync` — each worker gets a thread pool via `threads`, default 4, so a request blocked on Anthropic/disk I/O doesn't block everything else in that worker; safe since `BHiveParser`, its Anthropic clients, `RequirementsRegistry`, and `GovernanceLog` are all constructed fresh per request with no shared mutable state), worker count, `timeout` (150s, sized to the classify + consistency-check budget), `worker_tmp_dir` (defaults to `/dev/shm` — workers heartbeat via a temp file, and tmpfs avoids a slow-disk false-positive "worker timed out" kill), request-based worker recycling (`max_requests`/`max_requests_jitter`). | Loaded via `gunicorn -c deploy/gunicorn.conf.py wsgi:app`; `bind` must match what `deploy/nginx.conf`'s `upstream` proxies to; `timeout` must stay in sync with `deploy/nginx.conf`'s `proxy_read_timeout`. |
| `deploy/gunicorn.service` | systemd unit — runs gunicorn as the `archiosk` user/group, loads `.env` via `EnvironmentFile`, `Environment=PYTHONUNBUFFERED=1` (so a killed worker's last log lines aren't lost to buffering), `Type=simple` (gunicorn doesn't implement systemd's `sd_notify` protocol, so `Type=notify` would hang), restarts on failure, `TimeoutStopSec=45` (must exceed gunicorn's own 30s `graceful_timeout` so its graceful drain finishes before systemd SIGKILLs), applies sandboxing (`NoNewPrivileges`, `PrivateTmp`, `ProtectSystem`). | `ExecStart` points at `deploy/gunicorn.conf.py` and `wsgi:app`. Bumping `STATIC_VERSION` in `.env` requires `systemctl restart`, not `reload` — `EnvironmentFile` is only read at unit start. |

## 9. Development tools (not imported by the app)

| File | Purpose | Connects to |
|---|---|---|
| `tools/dependency_fit.py` | Standalone CLI (argparse, no Flask import) — checks a proposed library/tool/pattern against constraints this project has actually and deliberately established: no client-side build step, flat-JSON storage (SQLite was explicitly proposed and rejected), `gthread` not async workers, no new required cloud dependency, no background-worker infra, Python-native. Rule-based PASS/WARN/FAIL report; exit code 1 if anything FAILs. | Standalone — reads no app config, imports nothing from `services/`/`routes/`. Run directly: `python tools/dependency_fit.py --name ... [flags]`. |
| `tools/diagnostic_report.py` | (CLAUDE-DIAGNOSTIC-BRIDGE-01, new) `list` / `show` / `complete` / `preview` / `email` for captured diagnostics. A CLI on purpose: the bridge is a PULL, because ARCHIOSK cannot call a development agent. `complete` writes conclusions and touches neither the repository nor the server; `--commit-status` is a STATEMENT ABOUT work done elsewhere under its own authorization, never a means of causing any. Uses the real configuration rather than "testing", or it would silently read an in-memory database and report an empty list as though nothing had been captured. | Drives `services/diagnostic_report.py`. |
| `tools/create_credentials.py` | CLI to create/update a `User` row (username/password/role/email), including `--suspend`/`--activate` (CLAUDE-P27-B, sets `User.is_active`) and `--keep-password` (update role/email without touching the password). Provisioning is CLI-only — no self-registration route exists. | Imports `app.create_app`, `models.User`/`db`. Run directly: `python tools/create_credentials.py --username ... [flags]`. |
| `models.py` (`VerificationAccessToken`), `services/verification_access.py`, `tools/manage_verification_access.py`, `routes/portal.py` (`verification_access_login`/`verification_access_end`) — CLAUDE-LIVE-VERIFICATION-ACCOUNT-MECHANISM-01 | A sanctioned, auditable, fully ephemeral identity for bounded live-browser verification work, replacing repeated ad hoc "create a throwaway admin account" attempts that kept hitting the sandbox's own credential-creation restrictions. Mirrors `PasswordResetToken`'s already-shipped shape closely: `VerificationAccessToken` (single-use, expiring, only `token_hash` ever persisted). `services/verification_access.py` — `create_verification_access`/`revoke_verification_access` (CLI-only entry points, never called from a web route — account provisioning in this codebase has always been maintainer-CLI-only), `get_valid_verification_token`/`consume_verification_token`, `is_verification_session()` (a fixed-username check — no new column on `users`). `tools/manage_verification_access.py` — `create`/`revoke` subcommands, run by a human with real server access, prints a one-time login link, never a username/password. `GET /verification-access/<token>` (public, rate-limited, mirrors `reset_password`'s own "safe because the token is unguessable/single-use/short-lived" shape) establishes the session; `POST /verification-access/end` (`admin_required` + `is_verification_session()` check) self-revokes — 403s for a real admin, never a general "delete any account" capability. The account itself owns/participates in nothing by default (`ProjectWorkspace.owner` is a free-text string, structurally unlinked to `models.User` — confirmed by direct read of `services/case_workspace.py`), and `is_admin()` has zero `operating_environment` coupling, so the same session reaches both `CLIENT_OWNER` and `DESIGN_BUILDER_PROPONENT` projects identically to any other admin session. | Badge (`templates/_app_menu.html`, `.workspace-verification-session-badge`/`--attention-amber`, deliberately distinct from the Developer Mode badge's `--machine-blue`) driven by `app.py`'s `inject_globals` (`verification_session` context var). Tested by `tests/test_live_verification_account_mechanism_01.py` (service-layer + full HTTP round trip + both operating environments + no-backdoor/self-revoke-scope proofs). |
| `tools/backup_data.py` | (CLAUDE-P27-B) Bundles the SQLite DB file and the whole `REGISTRY_STORE_PATH` tree into one timestamped `.tar.gz` under `backups/` (git-ignored). Read-only with respect to source data. Resolves paths the same way `config.py` does (not hardcoded), so it honors a real `DATABASE_URL`/`REGISTRY_STORE_PATH` override. | Run directly: `python tools/backup_data.py [--output-dir ...]`. Pairs with `tools/restore_data.py`. |
| `tools/restore_data.py` | (CLAUDE-P27-B) Extracts a `tools/backup_data.py` archive into an explicit `--target-dir` — refuses a non-empty target (a restore silently overwriting live, newer data would itself be a data-loss incident) and has no default pointing at `instance/`. | Run directly: `python tools/restore_data.py --archive ... --target-dir ...`. |
| `tools/derive_theme_palettes.py` | (CLAUDE-P40-VW8-QA, Approved Theme Set) Derives a dark-appearance surface/border ramp for a new theme base color by LUMINANCE-matching each structural step to the corresponding step in the reference Black ramp (`#000000`-based), rather than reusing raw HSL lightness across hues — required once saturation varies (a fully saturated green reaches a given luminance at a much lower raw lightness than blue, at the same saturation). Produced the exact hex values in `static/css/tokens.css`'s `--dark-*`/`--tint-*`/`--forest-*` families; re-run (or extend `THEMES`) for any future dark appearance addition instead of hand-picking values. | Standalone — reads/writes nothing, just prints the derived palette and a pass/fail contrast report. Run directly: `python tools/derive_theme_palettes.py`. Also imported by `tests/test_p40vw8qa_approved_theme_set.py` to pin `tokens.css`'s live values against what it derives. |
| `tools/write_dev_session_status.py` | (CLAUDE-CA1D-LIVE-BRIDGE-01) Write side of the read-only Claude/agent session-state bridge — observation only, no reverse/control channel anywhere in this file. Invoked once per confirmed Claude Code hook event (`.claude/settings.json`), stdlib-only. Explicit event→state mapping narrowed to the evidence-confirmed vocabulary (`active`/`waiting_for_input`/`unknown`) a live hook-payload experiment actually verified — `SubagentStop` deliberately preserves whatever state already exists rather than forcing either value (an unrelated background subagent finishing must never falsely un-idle an already-waiting session). Never parses hook stdin at all, so `tool_input`/`prompt`/`last_assistant_message`/notification text can never reach the persisted file — content-free by construction, not by redaction. Atomic temp-file + `os.replace()` write to `instance/dev_session_status.json` (git-ignored). | Run only via `.claude/settings.json`'s hook wiring, never imported by the app. Its pure functions (`resolve_state`/`build_payload`/`atomic_write`/`read_prior_state_from_file`) are unit-tested directly by `tests/test_ca1d_live_bridge_01.py`. |
| `.claude/settings.json` | (CLAUDE-CA1D-LIVE-BRIDGE-01) Project-level, git-tracked hook configuration — wires exactly the 8 confirmed-relevant Claude Code hook events (`UserPromptSubmit`/`PreToolUse`/`PostToolUse`/`PostToolUseFailure`/`SubagentStart`/`SubagentStop`/`Stop`/`Notification`) to `tools/write_dev_session_status.py`. No other hook events, no unrelated hook behavior. Machine-specific absolute paths (this deployment's venv + repo location) — would need adjusting on a different machine, a disclosed limitation, not a hidden defect. | Distinct from `.claude/settings.local.json` (machine-local permissions only, untouched by this stage). |

## 10. Database migrations (CLAUDE-P27-B)

| File | Purpose | Connects to |
|---|---|---|
| `migrations/` (Flask-Migrate/Alembic scaffold: `alembic.ini`, `env.py`, `script.py.mako`, `README`, `versions/`) | Adopted for the **next** schema change onward — does not replace `app.py`'s existing hand-rolled `create_all()` + `_migrate_users_*` functions, which remain what actually creates/migrates the already-deployed schema at every boot. `versions/f8620fa70917_..._baseline_schema...py` is hand-written, not autogenerated (`create_all()` always runs first, even under the `flask db` CLI, so autogenerate never sees a diff) — verified to produce byte-identical schema to the real boot path. The already-deployed local database was `flask db stamp head`-ed to this revision, not migrated through it. Full rationale in `migrations/README`. **CLAUDE-LIVE-VERIFICATION-ACCOUNT-MECHANISM-01 added the second revision on this chain**: `versions/d67fbff1ba5e_verification_access_tokens.py` (`down_revision = 'f8620fa70917'`), also hand-written for the same autogenerate-never-sees-a-diff reason, generated via the real `flask db revision` skeleton then hand-filled to match `models.VerificationAccessToken` exactly. `tests/test_flask_migrate_baseline.py`'s own baseline-consistency test was generalized to walk the FULL revision chain (discovered by reading each file's `revision`/`down_revision` attributes, not a hardcoded file list) rather than one hardcoded file, so a future third revision needs no test change. That test's Path B deliberately stays on raw Alembic (`MigrationContext`/`Operations` against a bare SQLAlchemy engine) rather than `flask_migrate.upgrade()` against a second throwaway Flask app — the latter was tried first and corrupted later, unrelated tests in the same pytest process (`test_password_reset.py`, `test_auth_security_logging.py`) by re-registering `Migrate`/`db.init_app` on `models.py`'s shared, module-level `db` singleton. | `Migrate(app, db)` registered in `app.py`'s `_register_database`. |

---

## Request-flow reference

**Document ingestion (API):**
`POST /api/v1/documents/ingest` (`routes/api.py`) → `services.ingestion.ingest_upload` → `services.bhive_parser.BHiveParser.parse` → `services.requirements_registry.RequirementsRegistry.save` → JSON response.

**Document ingestion (HTML form):**
`POST /upload` (`routes/portal.py`) → same `ingest_upload` call → redirect straight into `workspace.show_workspace` (Case Workspace).

**Health check:**
External monitor → `deploy/nginx.conf`'s `/health` location → `routes/portal.py:health` → `services.ingestion.get_registry(...).list_ids()` (the only real runtime dependency check; does not call the Anthropic API).

**RFI export:**
`GET /api/v1/documents/<id>/rfi` (`routes/api.py`) → `services.requirements_registry.RequirementsRegistry.get` → `services.rfi_export.build_rfi_docx` → `.docx` file download (409 if the document has no flagged contradictions to export).

**Governance audit trail:**
Every ingestion (API or form) → `services.ingestion.ingest_upload` → after the registry save, `services.governance.GovernanceLog.append` records a `document_ingested` event (`actor`/`role` from the request, defaulting to `"anonymous"`/`"unspecified"`). Read via `GET /api/v1/documents/<id>/governance` or the dashboard's audit-trail table (`GovernanceLog.read`, newest first). Corrections append a new event with `predecessor_id` pointing at what they correct — the original event is never edited.

**Web UI login:**
`GET /upload` while unauthenticated → `@login_required` (`services/auth.py`) → redirect to `/login?next=<original path>` → `POST /login` with correct credentials → `services.auth.check_credentials` (`werkzeug.security.check_password_hash`) → `log_in()` sets the session → redirect to `next` (same-site paths only) if one was given, otherwise `/gateway` → the originally-requested page (or the gateway's action card(s)). `/logout` clears the session and redirects to `/`. **`routes/api.py` shares this same session login as of CLAUDE-P27-B** (corrected — this line previously claimed the opposite).
